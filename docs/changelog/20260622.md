# 2026-06-22 — 0 active errors; telemetry silent; main advanced to PR #891

## Prior context loaded

Read `docs/changelog/20260622.md` (earlier session). Key facts carried forward:
- Repo: `asomervell/probably`
- All 35 PostHog error tracking issues = `resolved`, 0 active
- App OTel telemetry: 0 logs/events for 36+ days — silent
- PostHog MCP server never connects — use REST API with `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID=88871`
- Auto-merge not enabled at repo level — cannot enable via API
- Fixed PRs to date: #3, #5, #9, #26, #57, #58, #92
- Open PRs: #163 (CI pipefail fix), #144 (Dependabot chi v5.2.4)

## Checks run (2026-06-22, session 2)

| Check | Result |
|---|---|
| Active error tracking issues | **0 active** (35 returned, all `status: resolved`) |
| Exception events (`$exception`, 7d) | **0** |
| App events (30d) | Only PostHog-internal: `$ai_trace_clusters` (4), `$ai_generation_clusters` (3) — no real app telemetry |
| Main HEAD | `96cd37b` — PR #891 merged (refactor captureTellerError) |
| Open PRs | #163 CI fix, #144 Dependabot chi, #193 stale docs (earlier session) |

## Reasoning

No active errors. No new app telemetry. Many refactoring PRs merged to main since last tracked point (`1cd136b` → `96cd37b`). These are all clean refactors extracted by automated sessions — no bugs introduced.

PRs #880–#891 are all refactoring/DRY work: extract helpers, remove duplication, consolidate scan rows. No actionable errors.

PR #193 (docs from earlier today) is stale — base moved, can be closed.

## Shipped this session

- Updated memory log: `docs/changelog/20260622.md`

## What was left behind (unchanged)

- **App telemetry silent**: 0 OTel logs/events reaching PostHog for 36+ days. PR #163 (CI pipefail fix) pending owner merge — likely root cause.
- **PR #163** (CI pipefail fix): open, not merged. Owner action required.
- **PR #144** (Dependabot chi v5.2.4): open, not merged. Owner action required.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Auto-merge not enabled at repo level — cannot enable via API.

## Next run checklist

1. **READ THIS FILE FIRST** — `asomervell/probably`, last updated 2026-06-22 session 2.
2. `git fetch origin main && git log --oneline origin/main -6`. Last known real code HEAD: `96cd37b` (PR #891, 2026-06-22).
3. PostHog: `error_tracking/issues/?status=active` — expect 0 truly active.
4. PostHog HogQL: `SELECT event, count() as cnt FROM events WHERE timestamp >= now() - INTERVAL 30 DAY GROUP BY event ORDER BY cnt DESC LIMIT 20` — check if new app telemetry started.
5. PostHog HogQL: `SELECT count() FROM events WHERE event='$exception' AND timestamp >= now() - INTERVAL 7 DAY` — expect 0.
6. If 0 active errors AND 0 new code issues, write changelog and stop. **Do NOT manufacture work.**
7. PostHog assignee API: HTTP 500 — do not attempt to assign issues programmatically.
8. Auto-merge not enabled — do not attempt via API.
9. No CI workflows on this repo — do not chase phantom CI failures.
10. All 35 PostHog error-tracking issues are `resolved`. Not actionable.
11. Close stale docs PRs from earlier sessions before creating a new one.
12. PostHog credentials: `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID=88871` available in env; use direct REST API (PostHog MCP server never connects).
13. Fixed PRs to date: #3, #5, #9, #26, #57, #58, #92.
14. Open PRs to monitor: #163 (CI fix — important), #144 (Dependabot).
