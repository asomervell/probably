# 2026-06-22 — 0 active errors; app telemetry still silent (36+ days)

## Prior context loaded

Read `docs/changelog/20260621-session3.md`. Key facts carried forward:
- Repo: `asomervell/probably`, last real code HEAD: `1cd136b` — PR #92 merged (background context fix, 2026-06-17)
- All 35 PostHog error tracking issues = `resolved`, 0 active
- App OTel telemetry: 0 logs/events for 35+ days — silent
- PostHog assignee API: HTTP 500 — skip programmatic assignment
- Auto-merge not enabled at repo level — cannot enable via API
- Fixed PRs to date: #3, #5, #9, #26, #57, #58, #92
- PR #163 (CI pipefail fix) and PR #144 (chi v5.2.4 Dependabot bump) remain open

## Checks run (2026-06-22)

| Check | Result |
|---|---|
| Active error tracking issues | **0 active** (35 returned, all `status: resolved`) |
| Exception events (`$exception`, 7d) | **0** |
| Log rows (any level, 7d) | **0** |
| App events (30d) | Only PostHog-internal: `$ai_trace_clusters` (5), `$ai_generation_clusters` (4) — no real app telemetry |
| Main HEAD | `c34b18f` — docs changelog only; last real code: `1cd136b` (2026-06-17) |

## Reasoning

No active errors. No new app telemetry. No new code commits since 2026-06-17. Situation unchanged.

4 stale duplicate docs PRs from yesterday's sessions (#181, #182, #183, #184) were closed as housekeeping.

Do NOT manufacture work.

## Shipped this session

- Closed stale docs PRs #181, #182, #183, #184
- Memory log: `docs/changelog/20260622.md`

## What was left behind (unchanged)

- **App telemetry silent**: 0 OTel logs/events reaching PostHog for 36+ days. Root cause: PR #163 (CI pipefail fix) is open but not merged — production deploys are broken, so the app running in production is stale. Owner must merge PR #163.
- **PR #163**: CI pipefail fix (open, not draft) — fixes `grep` exit-1 under `pipefail` in semver/release-notes actions. Blocking all production deploys.
- **PR #144**: Dependabot chi v5.2.4 bump (open) — minor security/maintenance.
- **Operational noise**: Akahu connections with expired OAuth tokens — users must reconnect via app UI.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Auto-merge not enabled at repo level — cannot enable via API.

## Next run checklist

1. **READ THIS FILE FIRST** — `asomervell/probably`, last updated 2026-06-22.
2. `git fetch origin main && git log --oneline origin/main -6`. Last known real code HEAD: `1cd136b` (2026-06-17).
3. PostHog: `error_tracking/issues/?status=active&limit=100` — filter for truly active (status != resolved). Expect 0.
4. PostHog HogQL: `SELECT event, count() as cnt FROM events WHERE timestamp >= now() - INTERVAL 30 DAY GROUP BY event ORDER BY cnt DESC LIMIT 20` — check if new app telemetry started.
5. PostHog HogQL: `SELECT level, count() FROM logs WHERE timestamp >= now() - INTERVAL 7 DAY GROUP BY level` — expect 0.
6. PostHog HogQL: `SELECT count() FROM events WHERE event='$exception' AND timestamp >= now() - INTERVAL 7 DAY` — expect 0.
7. If 0 active errors AND 0 new code issues, write changelog and stop. **Do NOT manufacture work.**
8. Close any stale duplicate docs PRs before creating a new one.
9. PostHog assignee API: HTTP 500 — do not attempt to assign issues programmatically.
10. Auto-merge not enabled — do not attempt via API.
11. No CI workflows on this repo — do not chase phantom CI failures.
12. Fixed PRs: #3, #5, #9, #26, #57, #58, #92. Do not reinvestigate.
13. All 35 PostHog error-tracking issues are `resolved`. Not actionable.
14. PostHog credentials: `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID=88871` available in env; use direct REST API (PostHog MCP server never connects).
15. PR #163 still needs owner to merge — key blocker for restoring production deploys and telemetry.
