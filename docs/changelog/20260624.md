# 2026-06-24 — 0 active errors; telemetry still silent

## Prior context loaded

Read `docs/changelog/20260623-session3.md`. Key facts carried forward:
- Repo: `asomervell/probably`, last real code commit: `1cd136b` (2026-06-17, PR #92)
- All 35 PostHog error tracking issues = `resolved`, 0 active
- App OTel telemetry: 0 logs/events for 37+ days — silent
- PostHog MCP server never connects — use REST API with `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID` from env
- Auto-merge not enabled at repo level — cannot enable via API
- Fixed PRs to date: #3, #5, #9, #26, #57, #58, #92
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically

## Checks run (2026-06-24, sessions 1 + 2)

| Check | Result |
|---|---|
| Main HEAD | `0a94b03` — docs changelog only; last real code: `1cd136b` (2026-06-17) |
| Active error tracking issues | **0 active** (35 total, all `status: resolved`) |
| Exception events (`$exception`, 7d) | **0** |
| Log rows (any level, 7d) | **0** |
| App events (7d) | No new real app telemetry |
| Open PRs | #163 (CI pipefail fix), #144 (Dependabot chi/v5) |

## Historical errors (all resolved — for reference only)

Two distinct error types appear in PostHog history (35 total issues, all resolved):

### 1. `orchestrator error [parse_error]: LLM API error 400 — function_response.name empty`
- **Root cause**: Gemini API rejects `GenerateContentRequest` when a `function_response` part has an empty `name` field. Occurred when tool call results were sent without matching the original tool call name.
- **Status**: resolved — fixed in prior sessions (PR #92 addressed orchestrator tool-call handling).

### 2. `traces export: Too many AI spans (N > 100)`
- **Root cause**: PostHog OTEL ingest rejects batches with more than 100 AI spans. Long AI conversations generated 141–223 spans per export request.
- **Status**: resolved — fixed in prior sessions (span batching / chunking added).

## Reasoning

No active errors. No new app telemetry. No new code commits since 2026-06-17. The app is silent because PR #163 (CI pipefail fix) is not merged, so deployments never succeed.

Do NOT manufacture work — all errors are resolved, all fixes have been shipped.

Closed stale PR #246 (earlier session today, superseded by this log).

## Shipped this session

- Memory log: `docs/changelog/20260624.md` (updated, supersedes session 1 copy on branch `claude/clever-knuth-prpjt6`)

## What was left behind (unchanged)

- **App telemetry silent**: 0 OTel logs/events reaching PostHog for 38+ days. Root cause is PR #163 not merged.
- **PR #163** (CI pipefail fix, `|| true` on tag-grep pipelines) — open, not merged. Owner needs to review/merge.
- **PR #144** (Dependabot chi/v5 5.2.3→5.2.4) — open, not merged. Owner needs to approve.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Auto-merge not enabled at repo level — cannot enable via API.
- Fixed PRs: #3, #5, #9, #26, #57, #58, #92.

## Next run checklist

1. **READ THIS FILE FIRST** — `asomervell/probably`, last updated 2026-06-24 (session 2).
2. `git fetch origin main && git log --oneline origin/main -6`. Last known real code HEAD: `1cd136b` (2026-06-17).
3. PostHog: `error_tracking/issues/?status=active&limit=10` — expect all `resolved`. Count field = 35 (total historical). All individual `status` values = `resolved` = no active work.
4. PostHog HogQL: `SELECT event, count() FROM events WHERE timestamp >= now() - INTERVAL 7 DAY GROUP BY event ORDER BY count() DESC LIMIT 20` — check for new telemetry.
5. PostHog HogQL: `SELECT level, count() FROM logs WHERE timestamp >= now() - INTERVAL 7 DAY GROUP BY level` — expect 0.
6. PostHog HogQL: `SELECT count() FROM events WHERE event='$exception' AND timestamp >= now() - INTERVAL 7 DAY` — expect 0.
7. If 0 active errors AND 0 new code issues, write changelog and stop. **Do NOT manufacture work.**
8. PostHog assignee API: HTTP 500 — do not attempt to assign issues programmatically.
9. Auto-merge not enabled — do not attempt via API.
10. No CI workflows on this repo — do not chase phantom CI failures.
11. Fixed PRs: #3, #5, #9, #26, #57, #58, #92. Do not reinvestigate.
12. All 35 PostHog error-tracking issues are `resolved`. Not actionable.
13. Close any stale duplicate docs PRs before creating a new one.
14. PostHog credentials: `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID` available directly in env; use REST API (PostHog MCP server never connects).
15. PR #163 is the critical open item — still needs owner review/merge. PR #144 (Dependabot) also open.
16. The `count` field in `/error_tracking/issues/?status=active` returns the **total historical count** (35), not the active count. Check each item's `status` field individually.
