# 2026-06-24 — 0 active errors; telemetry still silent

## Prior context loaded

Read `docs/changelog/20260623-session3.md`. Key facts carried forward:
- Repo: `asomervell/probably`, last real code commit: `1cd136b` (2026-06-17, PR #92)
- All 35 PostHog error tracking issues = `resolved`, 0 active
- App OTel telemetry: 0 logs/events for 37+ days — silent
- PostHog MCP server never connects — use REST API with `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID` from env
- Auto-merge not enabled at repo level — cannot enable via API
- Fixed PRs to date: #3, #5, #9, #26, #57, #58, #92
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically

## Checks run (multiple sessions, 2026-06-24)

| Check | Result |
|---|---|
| Main HEAD | `0a94b03` — docs changelog only; last real code: `1cd136b` (2026-06-17) |
| Active error tracking issues | **0 active** (35 total historical issues, all `status: resolved`, `occurrences: None`) |
| Exception events (`$exception`, 7d) | **0** |
| Log rows (any level, 7d) | **0** |
| App events (30d) | `$ai_trace_clusters` (2), `$ai_generation_clusters` (2) — no real app telemetry, unchanged |
| Open PRs | #250 (earlier session docs, closed as stale), #163 (CI pipefail fix), #144 (Dependabot chi/v5) |

**PostHog API note**: querying `status=active` returns all 35 issues (filter is ignored); all have `status: resolved` in response body. This is consistent across all prior sessions.

## Reasoning

No active errors. No new app telemetry. No new code commits since 2026-06-17 (7 days ago). Situation is unchanged for 8+ consecutive days. Do NOT manufacture work.

Multiple sessions ran today (sessions 1, 2, 3) — all found the same state. PR #250 (session 2 docs) closed as stale and superseded by this final run.

## Shipped this session

- Memory log updated: `docs/changelog/20260624.md` (this file, superseding session 1+2 versions)
- Closed stale PR #250 (session 2 docs, same state, superseded)

## What was left behind (unchanged)

- **App telemetry silent**: 0 OTel logs/events reaching PostHog for 38+ days. Production binary likely missing OTel exporter config or CI deploy broken (PR #163 not yet merged). Not actionable without owner action.
- **PR #163** (CI pipefail fix, `|| true` on tag-grep pipelines) — open, not merged. This is the likely root cause of silent deployments. Owner needs to review/merge.
- **PR #144** (Dependabot chi/v5 5.2.3→5.2.4) — open, not merged. Owner needs to approve.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Auto-merge not enabled at repo level — cannot enable via API.
- Fixed PRs: #3, #5, #9, #26, #57, #58, #92.

## Next run checklist

1. **READ THIS FILE FIRST** — `asomervell/probably`, last updated 2026-06-24.
2. `git fetch origin main && git log --oneline origin/main -6`. Last known real code HEAD: `1cd136b` (2026-06-17).
3. PostHog: `error_tracking/issues/?status=active&limit=10` — expect all `resolved` (API ignores status filter; all 35 issues are historical, not active).
4. PostHog HogQL: `SELECT event, count() as cnt FROM events WHERE timestamp >= now() - INTERVAL 30 DAY GROUP BY event ORDER BY cnt DESC LIMIT 20` — check if new app telemetry started.
5. PostHog HogQL: `SELECT level, count() FROM logs WHERE timestamp >= now() - INTERVAL 7 DAY GROUP BY level` — expect 0.
6. PostHog HogQL: `SELECT count() FROM events WHERE event='$exception' AND timestamp >= now() - INTERVAL 7 DAY` — expect 0.
7. If 0 active errors AND 0 new code issues, write changelog and stop. **Do NOT manufacture work.**
8. PostHog assignee API: HTTP 500 — do not attempt to assign issues programmatically.
9. Auto-merge not enabled — do not attempt via API.
10. No CI workflows on this repo — do not chase phantom CI failures.
11. Fixed PRs: #3, #5, #9, #26, #57, #58, #92. Do not reinvestigate.
12. All PostHog error-tracking issues are `resolved`. Not actionable.
13. Close any stale duplicate docs PRs before creating a new one.
14. PostHog credentials: `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID` available directly in env; use REST API (PostHog MCP server never connects).
15. PostHog API ignores `status=active` filter — all 35 results always come back as `resolved`. This is expected; do not re-investigate.
