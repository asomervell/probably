# 2026-06-24 — 0 active errors; telemetry still silent

## Prior context loaded

Read `docs/changelog/20260623.md`. Key facts carried forward:
- Repo: `asomervell/probably`, last real code commit: `1cd136b` (2026-06-17, PR #92)
- All 35 PostHog error tracking issues = `resolved`, 0 active
- App OTel telemetry: 0 logs/events for 38+ days — silent (likely due to CI deploy failure)
- PostHog MCP server never connects — use REST API with `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID=88871`
- Auto-merge not enabled at repo level — cannot enable via API
- Fixed PRs to date: #3, #5, #9, #26, #57, #58, #92
- Open CI fix PR #163 (`fix(ci): add || true to semver/release-notes pipelines`) — root cause of silent deployments

## Checks run (2026-06-24)

| Check | Result |
|---|---|
| Git HEAD | `d6bc2f2` — docs only; last real code `1cd136b` (2026-06-17) |
| Active error tracking issues | **0 active** (35 returned, all `status: resolved`) |
| Exception events (`$exception`, 7d) | **0** |
| Log rows (any level, 7d) | **0** |
| App events (7d HogQL) | **0** returned — no new app telemetry |
| Open PRs | #163 (CI fix, open since 2026-06-21), #144 (dependabot chi bump) |

## Reasoning

The two classes of errors visible in PostHog (all from 2026-04-25, environment=development) were:

1. **Too many AI spans** — OTEL exporter sending 100+ spans per batch to PostHog's `/i/v0/ai/otel` endpoint which has a 100-span limit. **Already fixed** in `internal/observability/otel.go` with `WithMaxExportBatchSize(50), WithMaxQueueSize(75)`.

2. **Gemini thought_signature / function_response.name empty** — Gemini API rejecting function calls with missing thought signatures or empty response names. **Already fixed** across multiple commits via: `thinkingConfig()` disabling thinking when tools active, `thoughtSignaturesConsistent()`, `sanitizeFunctionResponseNames()`, `annotateToolResponse()`, `resolveCategorizeToolName()`.

Both are resolved. No new errors in 7 days. No production telemetry because CI deploy pipeline was failing (PR #163 fixes this, awaiting owner merge).

## Shipped this session

- Closed stale duplicate docs PR #240 (earlier same-day session)
- Memory log: `docs/changelog/20260624.md`

## What was left behind (unchanged)

- **App telemetry silent**: 0 OTel logs/events reaching PostHog for 38+ days. Root cause identified: CI semver pipeline fails on repos without semver tags (`pipefail` + grep exit 1). PR #163 is the fix — awaiting owner merge.
- PR #163 open since 2026-06-21, not yet merged by owner.
- PR #144 (dependabot chi 5.2.4) open, not yet merged.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Auto-merge not enabled at repo level — cannot enable via API.

## Next run checklist

1. **READ THIS FILE FIRST** — `asomervell/probably`, last updated 2026-06-24.
2. `git fetch origin main && git log --oneline origin/main -6`. Last known real code HEAD: `1cd136b` (2026-06-17).
3. PostHog: `error_tracking/issues/?status=active` — expect 0 truly active (35 returned, all `resolved`).
4. PostHog HogQL: `SELECT event, count() as cnt FROM events WHERE timestamp >= now() - INTERVAL 7 DAY GROUP BY event ORDER BY cnt DESC LIMIT 20` — check if new app telemetry started.
5. PostHog HogQL: `SELECT level, count() FROM logs WHERE timestamp >= now() - INTERVAL 7 DAY GROUP BY level` — expect 0.
6. PostHog HogQL: `SELECT count() FROM events WHERE event='$exception' AND timestamp >= now() - INTERVAL 7 DAY` — expect 0.
7. If 0 active errors AND 0 new code issues, write changelog and stop. **Do NOT manufacture work.**
8. PostHog assignee API: HTTP 500 — do not attempt to assign issues programmatically.
9. Auto-merge not enabled — do not attempt via API.
10. No CI workflows triggered yet (deploy broken) — do not chase phantom CI failures.
11. Fixed PRs: #3, #5, #9, #26, #57, #58, #92. Do not reinvestigate.
12. All 35 PostHog error-tracking issues are `resolved`. Not actionable.
13. Close any stale duplicate docs PRs before creating a new one.
14. PostHog credentials: `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID=88871` available in env; use direct REST API (PostHog MCP server never connects).
15. PR #163 (CI semver fix) still open — check if merged; if not, note it.
16. PR #144 (chi 5.2.4 dependabot) still open — check if merged.
