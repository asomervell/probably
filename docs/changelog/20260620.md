# 2026-06-20 — 0 active errors; app telemetry silent (31+ days)

## Prior context loaded

Read `docs/changelog/20260619.md`. Key facts carried forward:
- Repo: `asomervell/probably`, working branch `claude/clever-knuth-xp1uyu`
- Last real code commit: `1cd136b` — PR #92 merged (background context fix, 2026-06-17)
- All 35 PostHog error tracking issues = `resolved`, 0 active
- App OTel telemetry: 0 logs/events for 30+ days — silent
- PostHog assignee API: HTTP 500 — skip programmatic assignment
- Auto-merge not enabled at repo level — cannot enable via API
- Fixed PRs to date: #3, #5, #9, #26, #57, #58, #92

## Checks run (2026-06-20, session 1)

| Check | Result |
|---|---|
| Active error tracking issues | **0 active** (35 total, all `resolved`, all from 2026-04-25) |
| Error/warn/fatal logs (7d) | **0** |
| Exception events (`$exception`, 7d) | **0** |
| Any app events (30d) | **0** — only PostHog-internal synthetic events (`$ai_trace_clusters`, `$ai_generation_clusters`) |
| New code commits on main since 2026-06-17 | **None** — last real code: `1cd136b`; HEAD `c87c14b` is a docs changelog commit |

## Reasoning

No active errors. No new app telemetry. No new code. Nothing to plan, fix, or ship.

## Shipped this session

- Memory log: `docs/changelog/20260620.md`

## What was left behind (unchanged)

- **App telemetry silent**: 0 OTel logs/events reaching PostHog for 31+ days. Likely the production binary lacks OTel exporter configuration. Not actionable from this session.
- **Operational noise**: Akahu connections with expired OAuth tokens — users must reconnect via app UI.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Auto-merge not enabled at repo level — cannot enable via API.
- Fixed PRs: #3, #5, #9, #26, #57, #58, #92.

---

## Session 2 (2026-06-20, branch `claude/clever-knuth-mw39r2`)

### Prior context loaded

Read session 1 findings above. All facts carried forward. Last known code HEAD: `1cd136b`. Docs HEAD: `429bc95`.

### Checks run

| Check | Result |
|---|---|
| Active error tracking issues | **0 active** (35 total, all `resolved`, all from 2026-04-25) |
| Exception events (`$exception`, 7d) | **0** |
| Log rows (any level, 7d) | **0** |
| App events (30d) | **0** — only PostHog-internal AI cluster events |
| Main HEAD | `429bc95` — docs changelog only, no new code since `1cd136b` |

### Reasoning

Same state as every prior run since 2026-06-17. No errors, no telemetry, no new code. Nothing to fix or ship.

Housekeeping: closed stale PRs #134 and #135 (docs-only, from 2026-06-19 sessions with stale base).

### Shipped

- Stale PRs #134 and #135 closed.
- Memory log appended: `docs/changelog/20260620.md`.

---

## Session 3 (2026-06-20, branch `claude/clever-knuth-7a4259`)

### Prior context loaded

Read sessions 1 & 2 above. All facts carried forward. Last known real code HEAD: `1cd136b`; docs HEAD: `85e3dd2`.

### Checks run

| Check | Result |
|---|---|
| Active error tracking issues | **0 active** (35 total, all `resolved`) |
| Exception events (`$exception`, 7d) | **0** |
| Log rows (any level, 7d) | **0** |
| App events (30d) | **0** — only PostHog-internal AI cluster events |
| Main HEAD | `85e3dd2` — docs changelog only, no new code since `1cd136b` |

### Reasoning

No errors. No telemetry. No new code. Nothing to plan, fix, or ship.

### Shipped

- Memory log appended: `docs/changelog/20260620.md`.

---

---

## Session 4 (2026-06-20, branch `claude/clever-knuth-ri0yld`)

### Prior context loaded

Read sessions 1–3 above. All facts carried forward. Last known real code HEAD: `1cd136b`; docs HEAD: `de3a313`.

### Checks run

| Check | Result |
|---|---|
| Active error tracking issues | **0 active** (35 total, all `resolved`) |
| Exception events (`$exception`, 7d) | **0** |
| Log rows (any level, 7d) | **0** |
| App events (30d) | **2 types, 5 each** — only `$ai_trace_clusters` and `$ai_generation_clusters` (PostHog-internal) |
| Main HEAD | `de3a313` — docs changelog only, no new code since `1cd136b` |

### Reasoning

No active errors. No app telemetry. No new code. Nothing to plan, fix, or ship.

### Shipped

- Memory log appended: `docs/changelog/20260620.md`.

---

## Next run checklist

1. **READ THIS FILE FIRST** — `asomervell/probably`, last updated 2026-06-20 session 4.
2. `git fetch origin main && git log --oneline origin/main -6`. Last known real code HEAD: `1cd136b`; docs HEAD: `de3a313`.
3. PostHog: `error_tracking/issues/?status=active` — expect 0 truly active.
4. PostHog HogQL: `SELECT event, count() as cnt FROM events WHERE timestamp >= now() - INTERVAL 30 DAY GROUP BY event ORDER BY cnt DESC LIMIT 20` — check if any new app telemetry started flowing.
5. PostHog HogQL: `SELECT level, count() FROM logs WHERE timestamp >= now() - INTERVAL 7 DAY GROUP BY level` — expect 0.
6. PostHog HogQL: `SELECT count() FROM events WHERE event='$exception' AND timestamp >= now() - INTERVAL 7 DAY` — expect 0.
7. If telemetry starts flowing, look for new `resource_fingerprint`, investigate any `partial_failure` pattern.
8. If 0 active errors AND 0 new code issues, write changelog and stop. **Do NOT manufacture work.**
9. PostHog assignee API: HTTP 500 — do not attempt to assign issues programmatically.
10. Auto-merge not enabled — do not attempt via API.
11. No CI workflows on this repo — do not chase phantom CI failures.
12. Fixed PRs: #3, #5, #9, #26, #57, #58, #92. Do not reinvestigate.
13. All 35 PostHog error-tracking issues are `resolved`. Not actionable.
14. Close any stale duplicate docs PRs before creating a new one.
15. PostHog credentials: `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID=88871` available in env; use direct REST API (PostHog MCP server never connects).
