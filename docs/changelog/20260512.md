# Session log — 2026-05-12

## What I read on startup

Read `docs/changelog/20260511.md` (Runs 1–4). Prior context summary:

- PostHog: zero active error tracking issues every run. All resolved issues come from `personal-cfo` binary (`cfo-cloud` service) — a separate binary. Nothing actionable in `probably`.
- Previous runs shipped PRs addressing: gemini_live Info→Debug, swallowed monthly-stats/upload errors, teller per-account debug downgrades, llm/executor swallowed tag write errors, nil panic on executor reload, Render errors in entities/akahu.
- Commits through HAM-4821 landed since last session (log swallowed pre-regeneration insight delete error).
- Left-behind items: personal-cfo binary (separate), POST-based Teller Connect handoff (Teller SDK limitation), `containsLastFour` dynamic regexps (runtime-dependent, cannot pre-compile), `splitTransaction` real accounting (deferred).

## Run 1 — 2026-05-12

### What I reasoned on

**PostHog audit — error tracking (active, last 30d):** zero active issues ✓ (consistent with all prior runs).

**PostHog audit — logs:**
- Only service: `cfo-cloud` (the `personal-cfo` binary — separate codebase, not `probably`).
- Zero error/warn/fatal log entries in 30 days ✓.
- ~70k info-level entries in 30 days — all from `personal-cfo`.
- Recurring info-level patterns visible in logs:
  - `provider sync complete ... status=partial_failure connections=10 errors=4` — fires every hourly sync. The `errors=4` means 4 bank connections consistently failing (non-disconnect errors). These are from `personal-cfo` binary — cannot be fixed in this repo.
  - `enrichment complete ... updated=0 skipped=2498` — enrichment skips all records every run. Also `personal-cfo`.
- **Conclusion**: no actionable signal from PostHog for the `probably` repo. Same outcome as all prior runs.

**Proactive code audit findings:**

Ran a deep audit across all `internal/` and `cmd/` files for: swallowed errors, nil pointer risks, slog.Info-in-loop, missing `rows.Err()`, dead code.

Findings:

1. **`internal/handlers/teller.go:393`** — `slog.InfoContext(r.Context(), "Syncing enrollment", ...)` inside the `for _, enrollmentID := range enrollmentIDs` loop. Fires on every enrollment being synced. Same class as HAM-4746 (which downgraded other teller per-operation logs); this line was missed. Should be `slog.DebugContext`. **(HAM-4822, Medium)**

2. **`internal/orchestrator/consensus.go:136`** — `slog.InfoContext(ctx, "consensus calling model", ...)` inside goroutine fan-out loop (`for _, model := range modelsToUse { go func(m) { slog.Info... } }`). Fires once per model per consensus categorisation run. With 3+ models and frequent categorisation, this generates high log volume at Info level. Should be `slog.DebugContext`. **(HAM-4822, Medium)**

3. **`internal/models/account.go:101` `IsFixedAsset()`** — exported method, zero callers anywhere in the codebase. Dead code. **(HAM-4823, Low)**

4. **`internal/models/entity.go:213` `IsFinancialInstitution()`** — exported method, zero callers anywhere in the codebase. Dead code. **(HAM-4823, Low)**

No new swallowed-error paths, nil pointer risks, or missing `rows.Err()` checks found. All remaining `_ =` patterns (best-effort enrichment, ResponseWriter writes, gorilla sessions `New()`, tx.Rollback defers) are intentional.

Other audit agent flags that were **not** actionable:
- `extraction/dedupe.go` WarnContext/DebugContext in loop — WarnContext fires only on DB error (appropriate), DebugContext is already at Debug (fine). Info summary logs are outside the loop.
- `handlers/api_rules.go:333` WarnContext in loop — fires only when `LoadTags` fails (appropriate error signal).
- `orchestrator/simple.go:179,196` WarnContext — fires only on error conditions (appropriate).
- `orchestrator/supervisor.go:102,114` DebugContext + WarnContext — Debug is fine; Warn fires only on worker failure (appropriate).
- `handlers/akahu.go:547` InfoContext inside account loop — fires only on `IsConnectionDisconnectedError` (rare revocation event); acceptable at Info.

### What I fixed and shipped

**HAM-4822** (Medium) → Done ✓. **HAM-4823** (Low) → Done ✓. Both assigned to Andrew Somervell.

**PR #409** (`claude/clever-knuth-tR3L5`, squash-merged SHA `dacd70c7`):

| File | Change |
|---|---|
| `internal/handlers/teller.go` | Line 393: `slog.InfoContext` → `slog.DebugContext` for "Syncing enrollment" |
| `internal/orchestrator/consensus.go` | Line 136: `slog.InfoContext` → `slog.DebugContext` for "consensus calling model" |
| `internal/models/account.go` | Remove dead `IsFixedAsset()` method |
| `internal/models/entity.go` | Remove dead `IsFinancialInstitution()` method |

Net: 4 files, +2 -12 lines. Build clean. CI passing. Merged directly.

Issues: #407 (HAM-4822), #408 (HAM-4823) — both closed as completed.

### What was left behind

- **`personal-cfo`** — `cfo-cloud` PostHog signals always from that separate binary; nothing to do in `probably`.
- **`errors=4` in cfo-cloud sync** — 4 bank connections failing every hourly sync; from `personal-cfo` binary, not fixable here.
- **POST-based Teller Connect handoff** — access token still passed as GET query param; Teller SDK limitation, deferred again.
- **`containsLastFour` in `transfer_matcher.go`** — compiles 4 dynamic regexps per call (depend on runtime `lastFour` value, cannot pre-compile). Acceptable.
- **`splitTransaction` real accounting** — implementation only adds tags, not separate expense entries. Deferred.

## Run 2 — 2026-05-12

### What I reasoned on

**PostHog audit (status=all, last 30d):** 14 resolved issues, zero active. All 14 are from the `cfo-cloud` / `personal-cfo` binary — confirmed by stack frame paths (`/app/internal/cloud/server.go`, old module `github.com/asomervell/personal-cfo`). Error types: `SyncAccountPullError` (106×), `ProviderPullError` (100×), `ProviderSyncError` (92×), `CLICommandError` (39+), `TellerWebhookTransactionBackfillRecordError` (12×), `ProviderTransactionStoreError` (6×), `TellerWebhookTransactionAggregationError` (5×), `ProviderBalancePullError` (5×), `TellerConnectSessionError` (2×). All come from the pre-rename architecture (`internal/cloud/server.go`) which no longer exists. Nothing actionable in `probably`.

**Proactive audit (via Explore agent):**
- 92 `_ =` patterns checked — all intentional (ResponseWriter writes, defer rollbacks, best-effort enrichment).
- All `rows.Next()` loops audited — all have `rows.Err()` checks.
- No zero-caller exported functions found in new areas.
- **One genuine finding:** `handlers/teller.go:414` — `slog.InfoContext` "Successfully synced enrollment" inside the `TellerSyncMulti` for-loop. Redundant given line 418 summary log. → Demoted to `slog.DebugContext`.
- **One false positive from agent:** `handlers/akahu.go:547` — correctly logged at Info (fires only on `IsConnectionDisconnectedError`, a rare revocation event). No change.

### What I fixed and shipped

**HAM-4824** (Low) → Done ✓. Assigned to Andrew Somervell.

**PR #412** (squash-merged SHA `47125f76`):

| File | Change |
|---|---|
| `internal/handlers/teller.go` | Line 414: `slog.InfoContext` → `slog.DebugContext` for "Successfully synced enrollment" |

Net: 1 file, +1 -1 lines. Build clean. CI passing. Merged directly.

Issue: #411 (HAM-4824) — closed as completed.

### What was left behind

- **`personal-cfo`** — `cfo-cloud` PostHog signals always from that separate binary; nothing to do in `probably`.
- **POST-based Teller Connect handoff** — access token still passed as GET query param; Teller SDK limitation, deferred.
- **`containsLastFour` in `transfer_matcher.go`** — compiles 4 dynamic regexps per call (runtime-dependent values, cannot pre-compile). Acceptable.
- **`splitTransaction` real accounting** — implementation only adds tags, not separate expense entries. Deferred.

## Run 3 — 2026-05-12

### What I reasoned on

**PostHog audit — error tracking (active, last 30d):** zero active issues ✓ (consistent with all prior runs).

**PostHog audit — logs:**
- Only service: `cfo-cloud` (personal-cfo binary, separate codebase). Zero error/warn/fatal entries in 30 days ✓.
- Info logs are all HTTP request access logs from cfo-cloud — nothing actionable in `probably`.

**Proactive code audit (Explore agent, very thorough):**
- Checked all `slog.Info`/`InfoContext` inside `for` loops across `internal/` and `cmd/`.
- Four genuine medium-severity findings, all same class: per-iteration Info logs that should be Debug.
- No new swallowed errors, missing `rows.Err()`, nil pointer risks, or dead exported functions found.

Findings:

1. **`internal/insights/providers/chain.go:55`** — `"attempting report generation"` inside `for _, provider := range c.providers`. Fires per provider per call. **(HAM-4822)**
2. **`internal/insights/providers/chain.go:78`** — `"attempting transaction analysis"` inside same loop pattern. **(HAM-4822)**
3. **`internal/insights/worker.go:145`** — `"generating report"` inside `for _, p := range pending`. Fires per pending report per tick. **(HAM-4822)**
4. **`internal/processing/worker.go:1575`** — `"Skipping low-confidence pattern for one-time business"` inside pattern detection loop. Fires per skipped pattern. **(HAM-4822)**

### What I fixed and shipped

**HAM-4822** (Medium) → Done ✓. Assigned to Andrew Somervell.

**PR #418** (squash-merged SHA `0faf37a4`):

| File | Change |
|---|---|
| `internal/insights/providers/chain.go` | Lines 55, 78: `slog.InfoContext` → `slog.DebugContext` |
| `internal/insights/worker.go` | Line 145: `slog.InfoContext` → `slog.DebugContext` |
| `internal/processing/worker.go` | Line 1575: `slog.InfoContext` → `slog.DebugContext` |

Net: 3 files, +4 -4 lines. Build clean. No CI checks. Merged directly.

Issue: HAM-4822 — closed as completed.

### What was left behind

- **`personal-cfo`** — `cfo-cloud` PostHog signals always from that separate binary; nothing to do in `probably`.
- **POST-based Teller Connect handoff** — access token still passed as GET query param; Teller SDK limitation, deferred.
- **`containsLastFour` in `transfer_matcher.go`** — compiles 4 dynamic regexps per call (runtime-dependent values, cannot pre-compile). Acceptable.
- **`splitTransaction` real accounting** — implementation only adds tags, not separate expense entries. Deferred.

## Run 4 — 2026-05-12

### What I reasoned on

**PostHog audit — error tracking (active, last 30d):** zero active issues ✓ (consistent with all prior runs).

**PostHog audit — logs:**
- `cfo-cloud` service: zero error/warn/fatal log entries in 30 days ✓.
- All signals remain from the `personal-cfo` binary (separate codebase). Nothing actionable in `probably`.

**Note on git log vs changelog gap:** Commits since Run 3's changelog (#419): `f069575` (HAM-4823 dead code — PR #420) was an extra fix merged after Run 3's log was written. PRs #415 and #417 (billing/auth and enrichment dead code) were from a session between Run 2 and Run 3 not captured in the log. This run is therefore labelled Run 4.

**Proactive code audit findings:**

1. **`internal/handlers/teller.go` — `syncSingleEnrollment` per-step Info logs**: Lines 487, 525, 527, 544, 557 each fire once per enrollment per sync run. With ~10 enrollments per hourly sync this is 50 unnecessary Info lines/hour. Same class as HAM-4822/4824. **(HAM-4832, Medium)**

2. **`internal/handlers/teller.go:1527` — webhook sync Info log**: `"synced transactions for account"` inside `handleTransactionWebhook` fires per Teller push event. Per-event trace detail → Debug. **(HAM-4832, Medium)**

3. **`internal/views/shadcn/shadcn.go` — dead HTMX helpers**: 16 zero-caller exported functions (`Hx`, `HxPut`, `HxPatch`, `HxDelete`, `HxConfirm`, `HxDisable`, `HxBoost`, `HxVals`, `HxHeaders`, `HxSelect`, `HxSelectOOB`, `HxPreserve`, `HxExt`, `HxDialog`, `HxModal`, `HxCloseModal`). **(HAM-4833, Low)**

4. **`internal/views/shadcn/shadcn.go` — dead data/aria/portal/icon helpers**: `DataDisabled`, `DataSelected`, `AriaRequired`, `ModalPortal`, `ToastPortal`, `IconXl`, `IconChevronUp`, `IconMinus`, `IconCircle`, `IconClock`, `IconCopy`, `IconEye`, `IconEyeOff` — all zero callers. **(HAM-4833, Low)**

5. **`internal/views/shadcn/empty.go` — dead Empty state variants and icons**: `EmptyCard`, `EmptyNoItems`, `EmptyError`, `EmptyPermission`, `EmptyOffline`, `EmptyMaintenance`, `EmptyComingSoon` plus their exclusively-dependent icons (`IconLock`, `IconWifiOff`, `IconTool`) and free-standing dead icons (`IconFolder`, `IconFile`, `IconImage`, `IconShoppingCart`, `IconBell`, `IconMessageSquare`). **(HAM-4833, Low)**

Care taken to preserve live code: `IconLg` (used by `IconSparkles` and `IconUsers` which have 6 and 1 callers), `IconSparkles`, `IconUsers`, `IconInbox` (used by `EmptyNoData` which has 13 callers), `EmptyNoData`, `EmptyNoResults`, `Empty`.

### What I fixed and shipped

**HAM-4832** (Medium) → Done ✓. **HAM-4833** (Low) → Done ✓. Both assigned to Andrew Somervell.

**PR #421** (squash-merged SHA `2de9beb3`):

| File | Change |
|---|---|
| `internal/handlers/teller.go` | Lines 487, 525, 527, 544, 557, 1527: `slog.InfoContext` → `slog.DebugContext` |
| `internal/views/shadcn/shadcn.go` | Remove 29 dead exported functions (HTMX helpers, data/aria, portals, icons) |
| `internal/views/shadcn/empty.go` | Remove 7 dead Empty state functions + 12 dead icons |

Net: 3 files, +6 -280 lines. Build clean. Tests passing. Merged directly.

Issues: #HAM-4832, #HAM-4833 — both closed as completed.

### What was left behind

- **`personal-cfo`** — all PostHog signals from that separate binary; nothing to do in `probably`.
- **POST-based Teller Connect handoff** — access token still passed as GET query param; Teller SDK limitation, deferred.
- **`containsLastFour` in `transfer_matcher.go`** — compiles 4 dynamic regexps per call (runtime-dependent values, cannot pre-compile). Acceptable.
- **`splitTransaction` real accounting** — implementation only adds tags, not separate expense entries. Deferred.
- **Remaining `slog.Info` in `teller.go`** — "No accounts found for enrollment" (483, error early-exit, correct at Info), "total transactions synced for enrollment" (589, enrollment-level summary, correct at Info), "teller-sync-multi completed" (418, request-level summary, correct at Info). All appropriate.

## Run 5 — 2026-05-12

### What I read on startup

Read `docs/changelog/20260512.md` Runs 1–4. Also saw HAM-4834 was committed after Run 4's log (chain.go success logs demoted to Debug). All prior findings fully landed.

### What I reasoned on

**PostHog audit — error tracking (active, last 30d):** zero active issues ✓. All 14 resolved issues confirmed from `cfo-cloud` / `personal-cfo` binary — paths reference `github.com/asomervell/personal-cfo/internal/cloud/server.go`, which does not exist in the current `probably` codebase. Not actionable here.

**PostHog audit — logs:** `cfo-cloud` service shows zero log ranges in last 30 days (service no longer running / renamed). No actionable signal.

**Proactive code audit (Explore agent — full read of sync + teller handler + observability):**

Found one genuine DRY violation not caught in previous runs:

- **`handlers/teller.go` — `handleEnrollmentDisconnected` vs `clearEnrollmentCredentials`** (HAM-4835, Medium): Both functions implemented identical credential-clearing logic (~25 lines each): `GetByConnectionID`, zero out four credential fields, `accounts.Update` per account. The webhook path (`handleEnrollmentDisconnected`) had `observability.CaptureFailure` calls; the user-initiated path (`clearEnrollmentCredentials`) silently logged errors only. A field addition to the clearing logic would have to be made in two places.

Other Explore agent findings reviewed and ruled out:
- Webhook handler returning 200 before sync processing — intentional per Teller spec (quick ACK). Not a bug.
- Timestamp updated on sync error — explicitly commented as intentional ("helps diagnose issues"). Not a bug.
- Provider init failures in `service.go` — intentional graceful degradation (slog.Error explains each). Not a bug.
- `orchestrator/api.go` Info logs — per-request (not per-iteration), appropriate at Info for AI call tracing.
- `handlers/akahu.go:547` — fires only on `IsConnectionDisconnectedError` (rare revocation event), acceptable at Info.

### What I fixed and shipped

**HAM-4835** (Medium) → Done ✓. Assigned to Andrew Somervell.

**PR #424** (squash-merged SHA `5979329f`):

| File | Change |
|---|---|
| `internal/handlers/teller.go` | `clearEnrollmentCredentials`: add `CaptureFailure` on lookup and per-account update errors |
| `internal/handlers/teller.go` | `handleEnrollmentDisconnected`: collapse ~25 lines to parse payload + delegate to `clearEnrollmentCredentials` |

Net: 1 file, +13 -37 lines. Build clean. CI passed. Merged directly.

Issue: HAM-4835 — closed as completed.

### What was left behind

- **`personal-cfo`** — all PostHog signals from that separate binary; nothing to do in `probably`.
- **POST-based Teller Connect handoff** — access token still passed as GET query param; Teller SDK limitation, deferred.
- **`containsLastFour` in `transfer_matcher.go`** — compiles 4 dynamic regexps per call (runtime-dependent values, cannot pre-compile). Acceptable.
- **`splitTransaction` real accounting** — implementation only adds tags, not separate expense entries. Deferred.

## Run 6 — 2026-05-12

### What I read on startup

Read `docs/changelog/20260512.md` Runs 1–5. All prior findings fully landed through HAM-4835 (DRY refactor of handleEnrollmentDisconnected). Checklist noted: "full sweep done" after Run 5.

### What I reasoned on

**PostHog audit — error tracking (status=all, last 30d):** zero active issues ✓. All 14 resolved issues confirmed from `cfo-cloud` / `personal-cfo` binary — stack frames reference `github.com/asomervell/personal-cfo/internal/cloud/server.go`, which does not exist in the current `probably` codebase. Not actionable here. (Same conclusion as all prior runs.)

**PostHog audit — logs:** `cfo-cloud` service shows zero log entries in last 30 days. No actionable signal.

**Proactive code audit (Explore agent — full read of all internal/ and cmd/):**

Three potential findings flagged; two confirmed false positives:

1. **`handlers/akahu.go:547`** — "Clearing revoked credentials for account" inside for loop — PREVIOUSLY RULED OUT in Run 5: fires only on `IsConnectionDisconnectedError` (rare revocation), acceptable at Info. Skip.
2. **`handlers/plaid.go:239`** — "plaid-webhook: cleared credentials for revoked item" inside for loop — same pattern as akahu.go:547. Fires only on rare Plaid revocation event. Acceptable at Info. Skip.
3. **`insights/providers/chain.go:21`** — "insights provider configured" inside for loop — fires ONCE at server startup (not per-request). Acceptable at Info. Skip.

Two genuine findings:

4. **`cmd/util/main.go` — dead `fast-path` command** (HAM-4836, Low): `runFastPath` printed "command has been removed" but was still registered in `printUsage()` and the switch. Classic dead code.
5. **`internal/processing/worker.go` lines 1494, 1503, 1538, 1561, 1645 — per-entity pattern worker Info logs** (HAM-4837, Medium): Five `slog.InfoContext` calls in `claimAndProcessEntityPatterns` / `processEntityPatternDetection` fire once per entity per processing cycle (5 parallel workers × N entities). Same class as HAM-4822/4832/4834. Had not been caught by previous runs because they were in per-entity worker functions rather than visible for-loop bodies.

### What I fixed and shipped

**HAM-4836** (Low) → Done ✓. **HAM-4837** (Medium) → Done ✓. Both assigned to Andrew Somervell.

**PR #426** (squash-merged SHA `3f1d7c77`):

| File | Change |
|---|---|
| `cmd/util/main.go` | Remove `fast-path` from printUsage, switch, and delete `runFastPath` function |
| `internal/processing/worker.go` | Lines 1494, 1503, 1538, 1561, 1645: `slog.InfoContext` → `slog.DebugContext` |

Net: 2 files, +5 -17 lines. Build clean. Tests passing. Merged directly.

Issues: HAM-4836, HAM-4837 — both closed as completed.

### What was left behind

- **`personal-cfo`** — all PostHog signals from that separate binary; nothing to do in `probably`.
- **POST-based Teller Connect handoff** — access token still passed as GET query param; Teller SDK limitation, deferred.
- **`containsLastFour` in `transfer_matcher.go`** — compiles 4 dynamic regexps per call (runtime-dependent values, cannot pre-compile). Acceptable.
- **`splitTransaction` real accounting** — implementation only adds tags, not separate expense entries. Deferred.
- **`cmd/util` hidden commands** (`check-tables`, `fix-insights-tables`, `list-ledgers`) — not in help, duplicate migration logic, but useful for manual debugging/recovery. Left as is.

## Run 7 — 2026-05-12

### What I read on startup

Read `docs/changelog/20260512.md` Runs 1–6. Also noted git log shows HAM-4840 (DeleteAndResync refactor) and HAM-4841 (remove dead IsReadOnlyTool/IsWriteTool, DRY HashAPIKey) committed after Run 6's log — both from a separate session, fully landed.

### What I reasoned on

**PostHog audit — error tracking (active, last 30d):** zero active issues ✓. Consistent with all prior runs.

**PostHog audit — logs:** `cfo-cloud` service shows zero error/warn/fatal log entries in 30 days ✓. Only service present. Nothing actionable in `probably`.

**Proactive code audit (Explore agent — full internal/ and cmd/ sweep):**

Explore agent returned 6 candidates; 3 ruled out as false positives:

- **`internal/sync/worker.go:179,193`** — "sync interrupted by shutdown" inside nested loops — fires exactly ONCE per graceful shutdown (returns immediately after log). Not excessive. False positive.
- **`internal/models/entity.go:881`** — `_ = pool.QueryRow(...).Scan(&e.ID)` post-upsert ID back-population — clearly best-effort after confirmed successful upsert. False positive.
- **`internal/insights/aggregator.go:232`** originally flagged as low-risk swallowed error — confirmed actionable (add WarnContext).

Three genuine findings:

1. **`internal/insights/worker.go:155`** — `slog.InfoContext(ctx, "report generated")` inside `for _, p := range pending`. Fires per pending report per tick. Same class as HAM-4822/4832/4834/4837. **(HAM-4842, Medium)**

2. **`internal/views/layouts/icons.go`** — 9 dead exported icon functions with zero callers: `IconHome`, `IconFilter`, `IconTrendingUp`, `IconTrendingDown`, `IconArrowUpRight`, `IconArrowDownLeft`, `IconTransfer`, `IconMenu`, `IconMapPin`. Same class as HAM-4833. **(HAM-4843, Low)**

3. **`internal/chat/store.go:213`** and **`internal/insights/aggregator.go:232`** — `_ = ...` on best-effort operations (thread timestamp update, prior-period snapshot). Failures are invisible; WarnContext preserves non-fatal semantics while making issues observable. **(HAM-4844, Low)**

### What I fixed and shipped

**HAM-4842** (Medium) → Done ✓. **HAM-4843** (Low) → Done ✓. **HAM-4844** (Low) → Done ✓. All assigned to Andrew Somervell.

**PR #431** (squash-merged SHA `6c7d0cc8`):

| File | Change |
|---|---|
| `internal/insights/worker.go` | Line 155: `slog.InfoContext` → `slog.DebugContext` for "report generated" |
| `internal/views/layouts/icons.go` | Remove 9 dead exported icon functions |
| `internal/chat/store.go` | `_ = UpdateThreadTimestamp` → explicit WarnContext on error; add `log/slog` import |
| `internal/insights/aggregator.go` | `_, _ = BuildSnapshot` → explicit WarnContext on prior-period failure; add `log/slog` import |

Net: 4 files, +12 -39 lines. Build clean. CI passing. Merged directly.

Issues: HAM-4842, HAM-4843, HAM-4844 — all closed as completed.

### What was left behind

- **`personal-cfo`** — all PostHog signals from that separate binary; nothing to do in `probably`.
- **POST-based Teller Connect handoff** — access token still passed as GET query param; Teller SDK limitation, deferred.
- **`containsLastFour` in `transfer_matcher.go`** — compiles 4 dynamic regexps per call (runtime-dependent values, cannot pre-compile). Acceptable.
- **`splitTransaction` real accounting** — implementation only adds tags, not separate expense entries. Deferred.
- **`cmd/util` hidden commands** (`check-tables`, `fix-insights-tables`, `list-ledgers`) — not in help, kept for manual debugging/recovery.
- **`internal/sync/worker.go` shutdown logs** — confirmed appropriate at Info (fires once on graceful shutdown, not per-iteration).

## Checklist for next run

- [ ] Query PostHog (status=active, last 7d) — expect zero active issues
- [ ] Verify any Teller/Sync issues reference personal-cfo (all confirmed to date)
- [ ] Consider POST-based Teller Connect handoff (low priority, Teller SDK limitation)
- [ ] Info-in-loop sweep: all known per-loop Info logs now fixed; focus on new packages if added
