# Session log — 2026-05-16

## What I read on startup

Read `docs/changelog/20260515.md` (Runs 1–4). Key context:

- Zero active PostHog errors across all 4 prior runs today.
- Single known signal: `partial_failure errors=4` every hourly sync from production binary at `3811458b...`.
- Root cause fixed (PRs #462 and #478) but production not yet redeployed.
- Deferred: POST-based Teller Connect handoff, `containsLastFour` regexps, `splitTransaction` accounting.
- Suppressed PostHog issue `ProviderBalancePullError` (019e05c7) had not yet been marked resolved.

---

## Run 1 — 2026-05-16

### What I reasoned on

**PostHog audit — error tracking (status=all, last 90d):**

14 total issues, all `resolved` or `suppressed`. Only one non-resolved issue was `ProviderBalancePullError` (019e05c7, suppressed), with 4 occurrences (last seen 2026-05-13). Root cause: HTTP 401 from Teller not detected as disconnect. Fix shipped in PR #462 (`1c84911`). The issue was left `suppressed` after the fix; marked it **resolved** in PostHog this session.

**PostHog audit — logs (cfo-cloud, last 2d):**

`partial_failure connections=10 transactions=67-68 errors=4` on every hourly sync. Service version `3811458b...` confirmed in all entries — production not yet deployed. Pattern unchanged from all prior runs.

No new log patterns. No panics. Enrichment: `status=success failed=0 skipped=2526` — healthy steady state.

**Deferred item investigation:**

1. **`containsLastFour` regexps** (`internal/sync/transfer_matcher.go:449`) — function compiles 4 regex patterns on every call. The patterns include dynamic `lastFour` (4 digits), preventing pure static pre-compilation. This is a minor performance inefficiency in the transfer-matching hot path, not a correctness bug and not a PostHog error source. Not actioned.

2. **`splitTransaction` accounting** (`internal/llm/executor.go:918`) — MVP implementation that applies tags but does not create proper split accounting entries. There is an explicit code comment documenting this limitation. Not a bug causing PostHog errors. Not actioned.

3. **TODOs in `plaid.go` and `voice.go`** — unimplemented feature stubs (`Handle transaction removal`, `Fetch income data`, `Fetch asset report`, `Generate TTS audio`). All confirmed as intentional stubs; none relate to PostHog errors. Not actioned.

**Branch state:**

HEAD at `bfda204` — identical to `origin/main`. Clean working tree. No new code to ship.

### What I fixed and shipped

**PostHog error tracking cleanup:**

- Marked `ProviderBalancePullError` (019e05c7) as **resolved** in PostHog error tracking. The suppression was a manual workaround applied when the root cause was unknown. The fix (`IsConnectionDisconnected()` returning true on HTTP 401) has been on `main` since `1c84911` (PR #462). Resolving the PostHog issue closes the loop.

No code changes this session. No new Linear issues or PRs created — no new distinct errors to address.

### What was left behind

- **Production deployment still pending** — `cfo-cloud` / `probably-worker` still run `3811458b...`. `errors=4 partial_failure` will persist every hour until Cloud Run is redeployed. Not a code task.
- **`containsLastFour` regexp pre-compilation** — minor perf improvement, not causing errors. Could DRY by caching compiled patterns per lastFour if profiling shows it matters.
- **`splitTransaction` full accounting** — intentionally deferred MVP; requires `UpdateWithEntries` API to create multiple expense entries. Out of scope for PostHog error audit.
- **Deferred features** — POST-based Teller Connect handoff, Plaid income/asset stubs, TTS audio. All intentional unimplemented stubs.

---

## Run 2 — 2026-05-16

### What I read on startup

Read Run 1 above. Key context: stop condition defined — if `service.version` in PostHog logs is still `3811458b...`, skip further analysis.

### What I reasoned on

**Stop condition check — `service.version`:**

Queried `cfo-cloud` logs (latest, last 1d). Every entry carries `service.version: 3811458b4373b7b8cf9f1662f86108232fd2e978`. Stop condition met — production not yet redeployed.

**PostHog error tracking (status=all, last 90d):**

14 issues, all `resolved`. Confirmed: zero active issues. No regressions. All issues confirmed resolved since Run 1 of this session.

**PostHog logs (cfo-cloud, last 30d):**

- Zero `warn`, `error`, or `fatal` log entries (queried explicitly — count = 0).
- Only `info` severity — HTTP access log pattern identical to all prior sessions.
- No panics, no new error patterns.

### What I fixed and shipped

Nothing. Stop condition met. No new distinct errors. No code changes, no new Linear issues, no new PRs.

### What was left behind

All items from Run 1 carry forward unchanged:
- Production deployment still pending (version `3811458b...`).
- `containsLastFour` regexp pre-compilation (minor perf, not a bug).
- `splitTransaction` full accounting (intentional MVP stub).
- Deferred feature stubs (Plaid income/asset, TTS, POST Teller Connect handoff).

---

## Stop condition for future runs

Until `service.version` in PostHog logs changes from `3811458b...`:
- PostHog will show `partial_failure errors=4` every hour — known, fixed in code, awaiting deploy.
- Error-level slog logs will be absent (old OTEL bridge maps all to INFO).
- No new distinct errors are expected.

**Check `service.version` first on next run. If unchanged → no new analysis needed.**

## Checklist for next run

- [ ] Check `service.version` in latest sync log — if `3811458b...`, production not deployed → skip further analysis
- [ ] If version changed: verify `status=success errors=0` in sync logs
- [ ] If version changed: verify error-level slog logs now visible in PostHog
- [ ] If version changed: confirm all PostHog error tracking issues remain resolved

---

## Run 3 — 2026-05-16

### What I read on startup

Read Runs 1–2 above. Key context: stop condition (`service.version == 3811458b...`) still met; no new PostHog errors expected. Prior runs confirmed zero active errors, zero error-level logs.

### What I reasoned on

**Stop condition check:**

Service version still `3811458b4373b7b8cf9f1662f86108232fd2e978` — production not yet redeployed. Stop condition met.

**PostHog error tracking (status=all, last 90d):**

14 issues, all `resolved`. Same as Runs 1–2. Zero active errors.

**PostHog logs (cfo-cloud, last 2d):**

Only `info` severity. No errors, no panics, no new patterns. Identical to prior runs.

**Static code review — float-to-cents consistency:**

Scanned for remaining `int64(float64(x) * factor)` patterns after the `bbdcda0` fix. Found a DRY violation in `internal/handlers/patterns.go`:

- Two identical frequency→monthly-cents `switch` statements (lines 83–96 and 150–163)
- Both used `int64(float64(x) * 4.33)` / `int64(float64(x) * 2.17)` — truncates without `math.Round`
- Inconsistent with `math.Round` pattern established across all sync providers by `bbdcda0`

No other truncation issues found in stored-value paths. The remaining `int64(...)` uses in `demo/data.go` and `pulse/spending_pace.go` are projections/display-only and acceptable.

### What I fixed and shipped

**HAM-4993 — DRY: extract `toMonthlyCents` + `math.Round` in `patterns.go`**

- Extracted `toMonthlyCents(amountCents int64, frequency string) int64` helper
- Replaced both switch sites with single call to helper
- Weekly: ×4.333 with `math.Round`; biweekly: ×2.167 with `math.Round`
- Net: 22 insertions, 29 deletions (-7 net)
- Linear: HAM-4993 (assigned to Andrew, marked Done)
- PR: [#492](https://github.com/asomervell/probably/pull/492) — merged to main (`07b11aff`)

### What was left behind

All items from Runs 1–2 carry forward unchanged:
- **Production deployment still pending** — version `3811458b...`. `partial_failure errors=4` will persist until Cloud Run is redeployed.
- **`containsLastFour` regexp pre-compilation** — minor perf improvement, not causing errors.
- **`splitTransaction` full accounting** — intentional MVP stub.
- **Deferred feature stubs** — Plaid income/asset, TTS audio, POST Teller Connect handoff.

### Stop condition update

Stop condition unchanged: `service.version == 3811458b...` → skip further PostHog analysis. Only code-quality findings remain actionable.

---

## Run 4 — 2026-05-16

### What I read on startup

Read Runs 1–3 above. Key context:
- Stop condition (`service.version == 3811458b...`) still met as of Run 3; no new PostHog errors expected.
- HAM-4993 (toMonthlyCents helper) merged in Run 3.
- HAM-4994 (containsLastFour combined regexp) merged between Run 3 log commit and this run.
- Remaining deferred: production deploy, splitTransaction MVP stub, feature stubs.

### What I reasoned on

**PostHog error tracking (status=all, last 30d):**

14 total issues, all `resolved`. Values are redacted (financial data). No active errors. Stop condition reconfirmed: `service.version: 3811458b4373b7b8cf9f1662f86108232fd2e978` still in every log entry as of 16:09 UTC.

**PostHog logs (cfo-cloud, last 7d):**

Zero error/warn entries. Only info-level HTTP access and enrichment logs. No panics, no new patterns.

**Static code scan:**

Explored sync package for DRY violations. Found `GetOrCreateExpenseAccount` + `GetOrCreateIncomeAccount` repeated as a 4-line block across 4 sites in 3 provider files (teller.go ×2, akahu.go ×1, plaid_sync.go ×1). All three services use `*models.AccountStore` — extractable to a package-level helper in `service.go`.

Also found: `parseTellerError` / `parseAkahuError` are structurally similar but return provider-specific types with different JSON shapes — not safely extractable without generics and not worth the complexity. The cross-provider constructor patterns and account-lookup 3-step fallbacks are intentionally provider-specific. No further clean DRY opportunities identified.

### What I fixed and shipped

**HAM-4995 — DRY: extract `getContraAccounts` helper in `service.go`**

- Added `getContraAccounts(ctx, store, ledgerID) (*Account, *Account, error)` to `service.go`
- Replaced 4 call sites (teller.go ×2, akahu.go ×1, plaid_sync.go ×1) with single 2-line call
- Removed redundant `slog.ErrorContext` calls inside `DeleteAndResync` (error propagates to caller; other sites don't log either)
- Net: 17 insertions, 29 deletions (−12 net)
- Linear: HAM-4995 (assigned to Andrew, marked Done)
- PR: [#495](https://github.com/asomervell/probably/pull/495) — merged to main (`6c75eded`)

### What was left behind

- **Production deployment still pending** — version `3811458b...`. `partial_failure errors=4` will persist every hour until Cloud Run is redeployed. Not a code task.
- **`splitTransaction` full accounting** — intentional MVP stub; requires multi-entry API. Out of scope for PostHog audit.
- **Deferred feature stubs** — Plaid income/asset, TTS audio, POST Teller Connect handoff. All intentional.
- **Provider-specific patterns** — `parseTellerError`/`parseAkahuError`, 3-step account-lookup fallback, constructor initialization — intentionally per-provider; cross-provider DRY would require interface complexity not justified by 3 call sites.

---

## Run 5 — 2026-05-16

### What I read on startup

Read Runs 1–4 above. Key context:
- Stop condition (`service.version == 3811458b...`) still met as of Run 4.
- HAM-4993/4994/4995 all merged (toMonthlyCents, containsLastFour regexp, getContraAccounts).
- Remaining deferred: production deploy, splitTransaction MVP stub, feature stubs.

### What I reasoned on

**Stop condition check:**

`service.version` still `3811458b4373b7b8cf9f1662f86108232fd2e978` in production logs (19:02 UTC). Stop condition met.

**PostHog error tracking (status=all, last 30d):**

14 issues, all `resolved`. Identical to Runs 1–4. Zero active errors.

**PostHog logs (cfo-cloud, last 7d):**

Zero `warn`/`error`/`fatal` entries. Only info-level HTTP access logs. No panics, no new patterns.

**Static code scan:**

Reviewed `internal/observability/posthog.go`. Found: `CaptureError` hardcodes `Type: "Error"` for every PostHog exception, meaning all 36 call sites produce identically-titled issues in the error tracker — impossible to distinguish `TellerAPIError` from `PgError` from `OpError`. Historical errors in PostHog had named types (`ProviderPullError`, `SyncAccountPullError`, etc.) from the pre-rewrite codebase; the current code lost that behaviour.

Fix: add `errorTypeName(err error) string` using `reflect.TypeOf` to check package paths. Skips standard-library wrapper packages (`fmt`, `errors`) and returns the innermost meaningful type name. Reviewed in `/simplify` — refactored from initial string-matching approach (`"*fmt.wrapError"`) to reflect-based package-path check, which is robust across Go versions.

### What I fixed and shipped

**HAM-4996 — fix: `CaptureError` concrete error type name in PostHog**

- Added `errorTypeName(err error) string` to `internal/observability/posthog.go`
- Uses `reflect.TypeOf(err).Elem().PkgPath()` to skip `fmt`/`errors` wrappers
- Falls back to `"Error"` for bare `errors.New` strings
- One-line change at call site: `Type: errorTypeName(err)`
- Net: 25 insertions, 1 deletion
- Linear: HAM-4996 (assigned to Andrew, marked Done)
- PR: [#498](https://github.com/asomervell/probably/pull/498) — merged to main (`5ac43952`)

### What was left behind

- **Production deployment still pending** — version `3811458b...`. `partial_failure errors=4` will persist every hour until Cloud Run is redeployed. Not a code task.
- **`splitTransaction` full accounting** — intentional MVP stub.
- **Deferred feature stubs** — Plaid income/asset, TTS audio, POST Teller Connect handoff. All intentional.
- **Provider-specific patterns** — intentionally per-provider, not worth abstracting.

## Checklist for next run

- [ ] Check `service.version` in latest sync log — if `3811458b...`, production not deployed → skip PostHog analysis
- [ ] If version changed: verify `status=success errors=0` in sync logs
- [ ] If version changed: verify error-level slog logs now visible in PostHog with typed names (not "Error")
- [ ] If version changed: confirm all PostHog error tracking issues remain resolved

---

## Run 6 — 2026-05-16

### What I read on startup

Read Runs 1–5 above. Key context:
- Stop condition (`service.version == 3811458b...`) still met as of Run 5.
- HAM-4993/4994/4995/4996 all merged (toMonthlyCents, containsLastFour, getContraAccounts, CaptureError type name).
- Remaining deferred: production deploy, splitTransaction MVP stub, feature stubs.

### What I reasoned on

**Stop condition check:**

`service.version: 3811458b4373b7b8cf9f1662f86108232fd2e978` in every cfo-cloud log entry as of 20:03 UTC. Stop condition met.

**PostHog error tracking (status=all, last 30d):**

14 issues, all `resolved`. Identical to Runs 1–5. Zero active errors.

**PostHog logs (cfo-cloud, last 7d):**

Zero `warn`/`error`/`fatal` entries. Only info-level HTTP access logs. No panics. `partial_failure` count = 49 entries over 2 days (expected ~48 for hourly sync — unchanged pattern).

**Static code scan (Explore agent):**

Five findings surfaced. Two clear wins selected:

1. **TellerLinks dead code** — `TellerLinks` struct with 5 fields embedded in 5 Teller response types. `grep -r "\.Links\."` returns zero hits across the entire codebase — the struct is deserialized from JSON but never read. Dead code.

2. **respondJSON not used consistently** — `internal/handlers/passkeys.go` had 14 inline occurrences of `w.Header().Set("Content-Type", "application/json") + optional w.WriteHeader(status) + _ = json.NewEncoder(w).Encode(data)`. Same pattern in `categorize.go` (1) and `statements_upload.go` (1). The `respondJSON(w, status, data)` helper in `api.go:29` is used everywhere else; these three files bypass it. DRY violation.

Three other findings were not actioned:
- HTTP response decoding boilerplate across Teller/Akahu — previously assessed in Run 4 as provider-specific; generic approach would require generics with complexity not justified by 3 call sites.
- Error parsing pattern across providers — same assessment as Run 4.
- Inconsistent error accumulation (Teller vs Akahu) — behavioral difference, not a DRY issue; not causing errors.

### What I fixed and shipped

**HAM-4997 — refactor: remove unused TellerLinks struct and fields**

- Removed `TellerLinks` struct definition from `internal/sync/teller.go`
- Removed `Links TellerLinks` field from 5 structs: `TellerAccount`, `TellerBalance`, `TellerAccountDetails`, `TellerIdentity`, `TellerTransaction`
- Net: 12 deletions, 0 insertions
- Linear: HAM-4997 (assigned to Andrew, marked Done)

**HAM-4998 — refactor: use respondJSON helper consistently in passkeys, categorize, statements_upload**

- Replaced 14 inline JSON encoding patterns in `passkeys.go` with `respondJSON(w, status, data)` calls
- Same for 1 call in `categorize.go` and 1 in `statements_upload.go`
- Removed now-unused `"encoding/json"` import from `statements_upload.go`
- Net: 53 deletions, 22 insertions (−31 net for this part)

Both fixes combined in a single commit (`389b2cf`) and PR [#500](https://github.com/asomervell/probably/pull/500), squash-merged to main at `4596893f`.

### What was left behind

- **Production deployment still pending** — version `3811458b...`. `partial_failure errors=4` will persist every hour until Cloud Run is redeployed. Not a code task.
- **`splitTransaction` full accounting** — intentional MVP stub; requires multi-entry API.
- **Deferred feature stubs** — Plaid income/asset, TTS audio, POST Teller Connect handoff. All intentional.
- **Provider-specific boilerplate** — HTTP response decoding and error parsing across Teller/Akahu/Plaid. Assessed as not worth abstracting (generic approach adds complexity, only 3 call sites).
- **Inconsistent error accumulation** — Teller accumulates errors (errors.Join), Akahu fails fast. Behavioral difference, not a bug.

---

## Run 7 — 2026-05-16

### What I read on startup

Read Runs 1–6 above. Key context:
- Stop condition (`service.version == 3811458b...`) still met as of Run 6.
- HAM-4993/4994/4995/4996/4997/4998/4999/5000 all merged across prior runs.
- Remaining deferred: production deploy, splitTransaction MVP stub, feature stubs, provider-specific boilerplate.

### What I reasoned on

**Stop condition check:**

`service.version: 3811458b4373b7b8cf9f1662f86108232fd2e978` in latest cfo-cloud logs (22:05 UTC). Stop condition met.

**PostHog error tracking (status=all, last 90d):**

14 issues, all `resolved`. Identical to Runs 1–6. Zero active errors.

**PostHog logs (cfo-cloud, last 7d):**

Zero `warn`/`error`/`fatal` entries. Only info-level HTTP access logs. No panics. Pattern unchanged.

**Static code scan (Explore agent):**

Two actionable findings:

1. **Dead code** — 8 exported constants in `internal/models/account.go` (`AssetSubtypeRealEstate/Vehicle/Equipment/Collectible/Other`, `DepreciationMethodNone/StraightLine/DecliningBalance`) — zero references anywhere in the codebase. Confirmed with `grep -r`.

2. **DRY violation** — `SyncTransactions` methods on `PlaidProvider` and `AkahuProvider` were identical 12-line bodies differing only in the impl field name. Both also **redundantly** set `account.LastSyncedAt` after calling the impl — but all three underlying impls (`teller.go:888`, `akahu.go:705`, `plaid_sync.go:787`) already set it internally. Teller's wrapper (correctly) does not duplicate the write. Fix: extract `syncTransactionsImpl(ctx, account, impl)` into `types.go`; all three providers become one-liners. Remove redundant `time.Now()`/`LastSyncedAt` from Plaid and Akahu wrappers. Remove now-unused `"time"` imports.

### What I fixed and shipped

**HAM-5001 — dead code: remove unused constants from models/account.go**

- Removed `AssetSubtype*` (5 constants) and `DepreciationMethod*` (3 constants) from `internal/models/account.go`.
- Net: 15 deletions.
- Linear: HAM-5001 (assigned to Andrew, marked Done).

**HAM-5002 — DRY: extract syncTransactionsImpl helper + remove redundant LastSyncedAt writes**

- Added `syncTransactionsImpl(ctx, account, impl)` to `internal/sync/providers/types.go`.
- Updated all three providers: Plaid and Akahu become single-line delegates; Teller adds `syncTellerFields(account)` pre-call.
- Removed redundant `time.Now()`/`LastSyncedAt` writes from Plaid and Akahu wrappers.
- Removed unused `"time"` imports from `plaid.go` and `akahu.go`.
- Net: +14 / −36 lines.
- Linear: HAM-5002 (assigned to Andrew, marked Done).

Both fixes combined in PR [#502](https://github.com/asomervell/probably/pull/502) — squash-merged to main at `d2bd5ff`.

### What was left behind

- **Production deployment still pending** — version `3811458b...`. `partial_failure errors=4` will persist every hour until Cloud Run is redeployed. Not a code task.
- **`splitTransaction` full accounting** — intentional MVP stub; requires multi-entry API.
- **Deferred feature stubs** — Plaid income/asset, TTS audio, POST Teller Connect handoff. All intentional.
- **Provider-specific boilerplate** — HTTP response decoding and error parsing across Teller/Akahu/Plaid. Assessed across multiple runs as not worth abstracting (only 3 call sites, generic approach adds complexity).
- **Inconsistent error accumulation** — Teller accumulates errors (errors.Join), Akahu fails fast. Behavioral difference, not a bug.

## Checklist for next run

- [ ] Check `service.version` in latest sync log — if `3811458b...`, production not deployed → skip PostHog analysis
- [ ] If version changed: verify `status=success errors=0` in sync logs
- [ ] If version changed: verify error-level slog logs now visible in PostHog with typed names (not "Error")
- [ ] If version changed: confirm all PostHog error tracking issues remain resolved
