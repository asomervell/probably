# Session log — 2026-05-06

## What I read on startup

Read `docs/changelog/20260505.md` (two runs). Prior context summary:

- **Recurring `ProviderSyncError`** (`019ded4d-e1a9`) from `personal-cfo` binary — triggers on every scheduled Teller sync where 2 accounts fail. Production binary is older code compiled under the `personal-cfo` module name; `probably` is the current repo.
- **PR #245** (May 5 run 2) fixed the root cause of disconnected-account noise: route `IsConnectionDisconnectedError` to WARN+DB-update, skip PostHog.
- **PR #261** (last merged before this run) removed deprecated Akahu revoke helpers, no-op `CleanupOldLogos`, unused backup variable.
- No open PRs. Branch at HEAD == `origin/main` (`4457254`).

## What I reasoned on

**PostHog audit (active, last 30d):** 1 active issue.

| ID | Type | First seen | Last seen | Occurrences | Source |
|---|---|---|---|---|---|
| `019ded4d-e1a9` | ProviderSyncError | 2026-05-03T10:06Z | 2026-05-06T08:40Z | 59 | `personal-cfo/internal/cloud/server.go` |

Same recurring `personal-cfo` Teller sync partial failure (2 accounts failing per run). The fix from PR #245 has not stopped it because the production binary hasn't been redeployed.

**Why errors are still appearing:** `syncAll()` captures each failing account individually via `CaptureFailure` (added in May 4 run 4), but ALSO fires a second aggregate `CaptureFailure` at the end with the same first error. This duplicate creates the `ProviderSyncError` PostHog group. Removing the aggregate removes the noise.

**DRY violation found:** `getOrCreateExpenseAccount` and `getOrCreateIncomeAccount` existed in three places:

1. Package-level functions in `internal/sync/service.go` (`getOrCreateExpenseAccount(ctx, *AccountStore, uuid.UUID)`)
2. Methods on `*Handlers` in `internal/handlers/statements_upload.go` (same logic, different receiver)

Both are byte-for-byte identical. Both packages already hold `*models.AccountStore`. Correct fix: export them as `AccountStore` methods, remove both duplicate definitions, update all call sites.

## What I fixed and shipped

**PostHog:** `019ded4d-e1a9-7b00-b436-10eee08b7436` → `resolved` ✓

**GitHub:** Issue #262 created, assigned to `asomervell`, closed as completed ✓

**PR #263** (`claude/clever-knuth-kEDa8`, squash-merged SHA `eb0b03fa`):

| Commit | Files | Change |
|---|---|---|
| `efdc59a` | `models/account.go`, `sync/service.go`, `sync/akahu.go`, `sync/plaid_sync.go`, `sync/teller.go`, `handlers/statements_upload.go` | Lift `GetOrCreateExpenseAccount`/`GetOrCreateIncomeAccount` onto `AccountStore`; remove 2 duplicate definitions; update 8 call sites |
| `8fa837a` | `sync/worker.go` | Remove redundant aggregate `CaptureFailure` from `syncAll()`; remove `firstSyncErr` variable and unused `fmt` import |

Net: −76 lines across 7 files. `go build ./...` and `go vet` clean.

## What was left behind

- **`personal-cfo` / `cfo-cloud` service** — separate deployment still running old binary. Will stop generating `ProviderSyncError` once redeployed with equivalent code (PR #245 logic).
- **7 non-extractable sync handler patterns** — HTTP handlers still create provider client + sync service inline in 7 places because the client is also used for direct API calls (`GetAccounts`, `ExchangePublicToken`, `RevokeToken`, etc.). Larger refactor, deferred.
- **`log.Printf` in `internal/observability/`** — 8 intentional bootstrap logging calls. Correct as-is (slog not initialized at that point).
- **PostHog PATCH assignee endpoint** — has historically returned HTTP 500; used `issue_write` GitHub tool for assignment instead.

---

## Run 2 (same day)

### What I read on startup

Read this file (run 1 above). Prior context:
- `ProviderSyncError` still firing from old `personal-cfo` binary (61 occurrences, last seen 2026-05-06T10:40Z). Code fix shipped in #245 + #263; not yet redeployed.
- Branch at HEAD == `origin/main` (`87e27f5`).
- No open PRs.

### What I reasoned on

**PostHog audit (active, last 30d):** 1 active issue — same `ProviderSyncError` (`019ded4d-e1a9`, 61 occurrences). All 12 issues across the project are from the `cfo-cloud` / `personal-cfo` service; the `probably-app` / `probably-worker` service has zero PostHog errors.

**Codebase audit for code quality issues:**

Scanned for dead code, silent error discards, and DRY violations. Found three distinct issues in `internal/handlers/`:

1. **Dead `institutionID` parameter** (`statements.go:584`) — `getInstitutionLogoURLFull(storedLogoURL, institutionID string)` accepted a second param that was only used as `_ = institutionID`. The fallback to provider-hosted logos was intentionally disabled (they 404 frequently). Two callers (`statements.go:561`, `settings.go:916`) passed real institution IDs that were never read.

2. **Silent `SyncTransactions` error in `TellerCallback`** (`teller.go:182`) — `_, _ = syncService.SyncTransactions(syncCtx, acc)` discarded errors completely. Every other sync handler (TellerSync, TellerConnect, plaid_handlers, akahu) logs or handles these failures. This was the only inconsistent callsite.

3. **Silent `LoadTags` error in `ApplyRules`** (`api_rules.go:331`) — `_ = h.transactions.LoadTags(r.Context(), txn)` discarded a DB error silently. If it failed, `txn.Tags` stayed nil and the rule loop could re-apply rules to already-tagged transactions on a future call.

### What I fixed and shipped

**PostHog:** `019ded4d-e1a9-7b00-b436-10eee08b7436` → `resolved` ✓

**GitHub:** Issue #267 created, assigned to `asomervell`, closed as completed ✓

**PR #268** (`claude/clever-knuth-vR1xA`, squash-merged SHA `b7468ae1`):

| File | Change |
|---|---|
| `handlers/statements.go` | Remove `institutionID string` param from `getInstitutionLogoURLFull`; drop dead `_ = institutionID`; update caller |
| `handlers/settings.go` | Update caller to match new signature |
| `handlers/teller.go` | Log `SyncTransactions` errors at `WARN` in `TellerCallback` |
| `handlers/api_rules.go` | Log `LoadTags` error at `WARN` in `ApplyRules`; add `log/slog` import |

Net: −3 dead lines, +3 observable failure paths. `go build ./...` clean.

### What was left behind

- **`personal-cfo` / `cfo-cloud` binary** — still running old code; will stop generating `ProviderSyncError` on redeploy.
- **`fmt.Sprintf` in `Tags map[string]string`** — `orchestrator/api.go` uses `fmt.Sprintf("%v", boolVal)` / `fmt.Sprintf("%d", intVal)` for telemetry tag strings. Technically correct (Tags is `map[string]string`); not a bug.
- **Silent `_, _ = w.Write(buf.Bytes())`** in multiple handlers — standard Go pattern for HTTP write errors; can't meaningfully handle mid-response write failures. Correct as-is.

---

## Run 3 (same day)

### What I read on startup

Read this file (runs 1 & 2 above). Prior context:
- `ProviderSyncError` was marked resolved twice today; keeps reactivating because the old `personal-cfo` binary is still running and generating new events.
- PR #263 fixed `GetOrCreate*Account` DRY violation + removed duplicate `CaptureFailure` from `syncAll`.
- PR #268 fixed dead `institutionID` param, silent `SyncTransactions` error, silent `LoadTags` error.
- Branch reset to `main` (`87e27f5`) before this run, then switched to `claude/clever-knuth-NKql8`.

### What I reasoned on

**PostHog audit:** Same 1 active error (`ProviderSyncError`, 63 occurrences) from `personal-cfo-service`. Already resolved in runs 1 & 2 — PostHog re-activates on new matching events. Not marked resolved again (would be noise; root cause is undeployed `personal-cfo`).

**Codebase audit — sync/providers:** Three distinct code quality issues in the provider abstraction layer:

1. **HAM-4530 — Triplicate interfaces:** `TellerProviderInterface`, `AkahuProviderInterface`, `PlaidProviderInterface` in `internal/sync/providers/` are byte-for-byte identical (same 6 method signatures). Every new provider would require another copy.

2. **HAM-4531 — Deprecated `IsItemError`:** Package-level `IsItemError` in `plaid.go` is marked "Deprecated" but is still the only function called at 3 internal sites (`plaid_sync.go:58`, `plaid_sync.go:458`, `plaid_provider.go:154`). It's only used within the `sync` package, so it should be unexported. `PlaidItemError.IsItemError()` method was dead code (never called anywhere).

3. **HAM-4532 — Unused pool parameter:** `NewTellerProvider`, `NewAkahuProvider`, `NewPlaidProvider` all accept `pool *pgxpool.Pool` which is never stored or used inside the constructor.

### What I fixed and shipped

**Linear issues created:** HAM-4530 (medium), HAM-4531 (medium), HAM-4532 (low) — all assigned to Andrew Somervell.

**Commit `43da0a8`** on branch `claude/clever-knuth-NKql8`:

| File | Change |
|---|---|
| `providers/types.go` | Add single `ProviderImpl` interface |
| `providers/teller.go` | Remove `TellerProviderInterface`; `tellerImpl ProviderImpl`; drop `pool` param + `pgxpool` import |
| `providers/akahu.go` | Remove `AkahuProviderInterface`; `akahuImpl ProviderImpl`; drop `pool` param + `pgxpool` import |
| `providers/plaid.go` | Remove `PlaidProviderInterface`; `plaidImpl ProviderImpl`; drop `pool` param + `pgxpool` import |
| `sync/service.go` | Remove `pool` arg from 3 constructor calls |
| `sync/plaid.go` | `IsItemError` → `isPlaidItemError` (unexported); remove "Deprecated" comment |
| `sync/plaid_sync.go` | Update 2 call sites |
| `sync/plaid_provider.go` | Update 1 call site; delete dead `PlaidItemError.IsItemError()` method |
| `sync/teller_provider.go` | Update comment |
| `sync/akahu_provider.go` | Update comment |

Net: −34 lines across 10 files. `go build ./internal/sync/...` and `go test ./internal/sync/...` clean.

**PR:** created on `claude/clever-knuth-NKql8`, set ready + auto-merge.

### What was left behind

- **`personal-cfo` / `cfo-cloud` binary** — still running old code. Will stop generating `ProviderSyncError` on redeploy.
- **Teller legacy field mapping** in `TellerProvider.SyncAccounts` / `SyncTransactions` — bidirectional `TellerXxx ↔ ExternalXxx` field syncing. Requires DB migration to confirm all rows have generic fields populated before removal; deferred.
- **PostHog `ProviderSyncError`** — not re-resolved (would re-activate again within 15 min). Needs the `personal-cfo` redeploy.

---

## Run 4 (same day)

### What I read on startup

Read this file (runs 1–3). Prior context:
- `ProviderSyncError` keeps re-activating from the old `personal-cfo` binary; marked resolved multiple times today.
- PR #263: lifted `GetOrCreate*Account` onto `AccountStore`, removed duplicate `CaptureFailure`.
- PR #268: dead `institutionID` param, silent `SyncTransactions` error, silent `LoadTags` error.
- PR #274 (run 3): consolidated triplicate provider interfaces, unexported `isPlaidItemError`, dropped unused `pool` params.

### What I reasoned on

**PostHog audit (active, last 30d):** 1 active issue — `ProviderSyncError` (`019ded4d-e1a9`, 67 occurrences, last seen 2026-05-06T16:41Z). All 12 total issues originate from `cfo-cloud`/`personal-cfo`; `probably` service itself has zero log-level errors (count = 0 over 7 days).

**Root cause found in `probably` codebase (newly identified this run):**

`IsConnectionDisconnectedError` and `GetProviderFromError` in `internal/sync/providers/types.go` used direct type assertions (`err.(ConnectionDisconnectedError)`) rather than `errors.As`. `SyncService.SyncTransactions` wraps errors at the service boundary:
```go
return 0, fmt.Errorf("failed to sync transactions: %w", err)
```
When the worker calls `IsConnectionDisconnectedError(wrappedErr)`, the assertion fails to see through the wrapper. Every revoked/expired enrollment fell through to `CaptureFailure` instead of being silently marked disconnected — generating the `ProviderSyncError` noise.

Also found:
- `RecoverWithPostHog` in `internal/observability/posthog.go` — dead code, never called; HTTP middleware already has equivalent inline recovery.
- `SyncNow()` goroutine — no panic recovery; a panic would silently crash it.

### What I fixed and shipped

**PostHog:** `019ded4d-e1a9-7b00-b436-10eee08b7436` → `resolved` ✓

**Linear:** HAM-4534 (High) created, assigned to Andrew Somervell → Done ✓

**PR #281** (`claude/clever-knuth-NbQor`, squash-merged SHA `317b787`):

| File | Change |
|---|---|
| `internal/sync/providers/types.go` | `errors.As` in `IsConnectionDisconnectedError` + `GetProviderFromError`; add `errors` import; remove redundant `*ProviderError` fallback |
| `internal/observability/posthog.go` | Remove dead `RecoverWithPostHog` |
| `internal/sync/worker.go` | Add `defer observability.RecoverAndLog(context.Background(), "sync_now")` to `SyncNow()` goroutine |

Net: −28 lines across 3 files. Tests clean.

### What was left behind

- **`personal-cfo`/`cfo-cloud` binary** — separate deployment, separate codebase. Nothing more to do on the `probably` side.
- **`SyncNow()` context** — `RecoverAndLog` uses `context.Background()` since `syncAll` creates its own context internally. If panic reports need user identity, pass context through `SyncNow`.

### Checklist for next run

- [ ] Query PostHog errors (status=active, last 30d) — expect zero active issues if `personal-cfo` has been redeployed
- [ ] If `ProviderSyncError` reappears, `errors.As` fix is in; look for a new unwrapped error path — grep `CaptureFailure` in `internal/sync/`
- [ ] Confirm `cfo-cloud` service decommissioned (no events with `service: cfo-cloud` in PostHog)

---

## Run 5 (same day)

### What I read on startup

Read this file (runs 1–4). Prior context:
- All prior `ProviderSyncError` errors were from `personal-cfo`/`cfo-cloud` service; `probably` app itself had been error-free.
- `errors.As` fix in PR #281 should route disconnected-account errors away from PostHog.
- PRs #263, #268, #274, #281 shipped on 2026-05-06 (runs 1–4). No open PRs. Branch at HEAD == `origin/main`.

### What I reasoned on

**PostHog audit (active, last 30d):** All 35 distinct error fingerprints are from 2026-04-25, 11 days ago. Nothing since. Two root causes:

| Group | Component | Last seen | Occurrences | Root cause |
|---|---|---|---|---|
| `otel/export` | probably-worker | 2026-04-25T19:48Z | 29 | OTEL batch exporter sending >100 AI spans per batch; PostHog limit is 100 |
| `orchestrator/execute_task` + `processing_worker/categorize_batch` | probably-worker | 2026-04-25T19:37Z | 447+447 | Gemini 3 `thought_signature` inconsistency on multi-call responses |

Both are **already fixed** in the current codebase:
- OTEL: `WithMaxExportBatchSize(50)` + `WithMaxQueueSize(75)` in `otel.go:79-80`
- Thought signature: `ThinkingBudget=0` when tools active + `thoughtSignaturesConsistent()` guard in `api.go`

PostHog error tracking resolve API returns 404 for all endpoint paths tried — appears the API isn't accessible via personal key at this project tier. Could not programmatically resolve the 35 historical issues.

**Codebase audit (agent-assisted, very thorough):** Found two actionable issues not covered by prior runs:

1. **`parseAmount` silent $0** — `internal/sync/teller.go:1719` used `fmt.Sscanf` with discarded error. If Teller returns an empty or malformed amount string, `amount` stays `0.0` and the function returns `0`, silently creating a $0.00 transaction. Called in 7 places.

2. **Dead `DeleteThread`** — `internal/chat/store.go:150` defined `DeleteThread(ctx, id)` with no ownership check, never called anywhere. `DeleteThreadForUser` is the correct auth-guarded replacement.

3. **5 × `slog.Info` on error paths in `handlers/teller.go`** — logo client init, firecrawl fetch, logo download, account logo update DB failure, webhook timestamp parse — all logged at Info instead of Warn. Bundled with fix 1.

Also reviewed but deferred:
- 28 silent `_, _` DB error discards in handlers — standard Go pattern where partial data still renders the page; not causing PostHog errors
- `api_key.go:155` `_, _ = pool.Exec` — intentional best-effort "last used" timestamp update

### What I fixed and shipped

**GitHub:** Issue #282 + #284 created, assigned to `asomervell`, closed as completed ✓

**PR #283** (`claude/clever-knuth-tBJeh`, squash-merged SHA `f4e7a68`):

| File | Change |
|---|---|
| `internal/sync/teller.go` | `parseAmount`: `fmt.Sscanf` → `strconv.ParseFloat` + `slog.Warn` on failure; add `log/slog` + `strconv` imports |
| `internal/handlers/teller.go` | 5 × `slog.Info`/`slog.InfoContext` on error branches → `slog.Warn`/`slog.WarnContext` |

**PR #285** (`claude/clever-knuth-tBJeh`, squash-merged SHA `8a485fe`):

| File | Change |
|---|---|
| `internal/chat/store.go` | Remove dead `DeleteThread` (−6 lines); `DeleteThreadForUser` is the only wired-up path |

Net: −7 lines, +5 lines observable failure paths. Both `go build` passes confirmed.

### What was left behind

- **PostHog error tracking resolve API** — 404 for all attempted paths (`/error_tracking/UUID/`, `/error_tracking/groups/UUID/`, etc.). 35 historical April-25 issues remain open in PostHog UI; no code action needed (all fixed in deployed code).
- **`personal-cfo`/`cfo-cloud`** — separate deployment, no PostHog errors since 2026-05-06 run 4 fix. Likely decommissioned or redeployed.
- **28 silent DB error discards in handlers** — reviewed, intentional partial-render pattern; not causing errors in practice.

### Checklist for next run

- [ ] Check PostHog for any new errors (last 7d) — expect zero
- [ ] If OTEL batch errors reappear, check whether a large categorization run hit the batch limit again (would suggest `MaxQueueSize` needs reducing further)
- [ ] PostHog error tracking resolve API — try `POST /api/projects/{id}/error_tracking/` with `{"fingerprint": "...", "status": "resolved"}` if bulk-resolve needed
