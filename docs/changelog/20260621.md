# 2026-06-21 — 0 active errors; app telemetry silent (35+ days)

## Prior context loaded

Read `docs/changelog/20260620.md` (sessions 1–7). Key facts carried forward:
- Repo: `asomervell/probably`, working branch `claude/clever-knuth-1ozxpl`
- Last real code commit: `1cd136b` — PR #92 merged (background context fix, 2026-06-17)
- All 35 PostHog error tracking issues = `resolved`, 0 active
- App OTel telemetry: 0 logs/events for 34+ days — silent
- PostHog assignee API: HTTP 500 — skip programmatic assignment
- Auto-merge not enabled at repo level — cannot enable via API
- Fixed PRs to date: #3, #5, #9, #26, #57, #58, #92
- PostHog MCP server never connects — use REST API directly (`POSTHOG_PROJECT_ID=88871`)

## Checks run (2026-06-21)

| Check | Result |
|---|---|
| Active error tracking issues | **0 active** (20 returned, all `status: resolved`) |
| Events (7d) | **0** |
| Log rows (7d) | **0** |
| Main HEAD | `f48a80a` — docs-only; last real code: `1cd136b` (2026-06-17) |

## Reasoning

Same state as every prior run since 2026-06-17. PostHog shows:
- 0 active errors or exceptions
- All historical issues (all from 2026-04-25) are `resolved`
- App telemetry completely silent (no OTel events or logs for 35+ days)
- No new code on main

Nothing to plan, fix, or ship.

## Shipped this session

- Memory log: `docs/changelog/20260621.md`

## What was left behind (unchanged)

- **App telemetry silent**: 0 OTel logs/events reaching PostHog for 35+ days. Production binary likely missing OTel exporter config. Not actionable from here.
- **Operational**: 5 Akahu connections with expired OAuth tokens — users must reconnect via app UI.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Auto-merge not enabled — owner must enable in GitHub Settings → General → Pull Requests.
- Fixed PRs: #3, #5, #9, #26, #57, #58, #92.

---

## Next run checklist

1. **READ THIS FILE FIRST** — `asomervell/probably`, last updated 2026-06-21.
2. `git fetch origin main && git log --oneline origin/main -6`. Last known real code HEAD: `1cd136b` (2026-06-17).
3. PostHog REST (POSTHOG_PROJECT_ID=88871): `error_tracking/issues/?status=active&limit=20` — expect 0 truly active.
4. PostHog HogQL: `SELECT event, count() FROM events WHERE timestamp >= now() - INTERVAL 7 DAY GROUP BY event ORDER BY count() DESC LIMIT 20` — expect 0.
5. PostHog HogQL: `SELECT severity_text, count() FROM logs WHERE timestamp >= now() - INTERVAL 7 DAY GROUP BY severity_text` — expect 0.
6. If telemetry starts flowing, look for new `resource_fingerprint`, investigate any `partial_failure` pattern.
7. If 0 active errors AND 0 new code → write changelog, stop. **Do NOT manufacture work.**
8. PostHog assignee API: HTTP 500 — do not attempt to assign issues programmatically.
9. Auto-merge not enabled — do not attempt via API.
10. No CI workflows on this repo — do not chase phantom CI failures.
11. Fixed PRs: #3, #5, #9, #26, #57, #58, #92. Do not reinvestigate.
12. All 35 PostHog error-tracking issues are `resolved`. Not actionable.
13. Close any stale duplicate docs PRs before creating a new one.
14. PostHog credentials: `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID=88871` available in env; PostHog MCP server never connects — use REST API.
