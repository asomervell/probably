# 2026-06-21 — Confirmed CI pipefail fix in PR #163; 0 active PostHog errors

## Prior context loaded

Read `docs/changelog/20260620.md` (sessions 1–7). Key facts carried forward:
- Repo: `asomervell/probably`; this session on branch `claude/clever-knuth-on2csl`
- Last real code commit: `1cd136b` — PR #92 merged (background context fix, 2026-06-17)
- All 35 PostHog error tracking issues = `resolved`, 0 active
- App OTel telemetry: 0 logs/events for 35+ days — silent
- PostHog assignee API: HTTP 500 — skip programmatic assignment
- Auto-merge not enabled at repo level — cannot enable via API
- Fixed PRs to date: #3, #5, #9, #26, #57, #58, #92
- PostHog MCP server never connects — use REST API directly (`POSTHOG_PROJECT_ID=88871`)

## Checks run (2026-06-21, this session)

| Check | Result |
|---|---|
| Active error tracking issues | **0 active** (20 returned, all `status: resolved`, all from 2026-04-25) |
| Events (7d) | **0** real app events |
| Events (30d) | Only PostHog-internal: `$ai_trace_clusters` (5), `$ai_generation_clusters` (4) |
| Log rows (7d) | **0** |
| Main HEAD | `f48a80a` — docs-only commits since `1cd136b` (2026-06-17) |
| GitHub issues | **0 open** |
| Open PRs | #163 (CI pipefail fix, non-draft), #164 (stale docs), #144 (Dependabot chi bump) |

## Reasoning

PostHog: 0 active errors, 0 real logs, 0 exceptions. Identical state to all prior sessions since 2026-06-17.

**PR #163** (branch `claude/clever-knuth-1ozxpl`, opened earlier today) is the only real pending work. It fixes a `bash -eo pipefail` + `grep-no-match` bug in the CI actions that caused every push to `main` to silently fail at the semver step — explaining 35+ days of silent app telemetry:

- `.github/actions/semver/action.yml`: `LAST_TAG=$(... | head -1)` → `LAST_TAG=$(... | head -1 || true)`
- `.github/actions/release-notes/action.yml`: `PREV_TAG=$(... | head -1)` → `PREV_TAG=$(... | head -1 || true)`

I verified the main branch still has the unfixed lines. PR #163's fix is correct.

**PR #164** (stale docs-only from an earlier session today) — closed.

Attempted to enable auto-merge on PR #163 — repo setting not enabled, owner must enable in GitHub Settings → General → Pull Requests → Allow auto-merge.

## Shipped this session

| Action | Detail |
|---|---|
| PR #164 closed | Stale docs-only, superseded by PR #163 |
| Verified PR #163 | CI pipefail fix correct; still pending merge |
| Memory log | `docs/changelog/20260621.md` (this file) |

## What was left behind

- **App telemetry**: Still silent. Will start flowing once PR #163 merges and CI deploys successfully.
- **Auto-merge**: Not enabled at repo level — owner action required in GitHub Settings.
- **PR #163**: Open, non-draft, correct fix. Needs owner merge.
- **PR #144**: Dependabot chi/v5 5.2.3→5.2.4 — left for user review.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Fixed PRs: #3, #5, #9, #26, #57, #58, #92 + CI fix in PR #163.

---

## Next run checklist

1. **READ THIS FILE FIRST** — `asomervell/probably`, last updated 2026-06-21.
2. `git fetch origin main && git log --oneline origin/main -6`. If PR #163 merged, last real code will be the CI fix.
3. PostHog REST (`POSTHOG_PROJECT_ID=88871`): `error_tracking/issues/?status=active&limit=20` — expect 0 truly active.
4. PostHog HogQL: `SELECT event, count() FROM events WHERE timestamp >= now() - INTERVAL 7 DAY GROUP BY event ORDER BY count() DESC LIMIT 20` — **check if telemetry starts flowing after CI fix merges**.
5. PostHog HogQL: `SELECT severity_text, count() FROM logs WHERE timestamp >= now() - INTERVAL 7 DAY GROUP BY severity_text` — check for new log lines.
6. If telemetry starts: look for new `resource_fingerprint`, check error patterns, note Akahu expired-token warnings.
7. Check CI workflow run history if telemetry still silent after PR #163 merges.
8. If 0 active errors AND 0 new code → write changelog and stop. **Do NOT manufacture work.**
9. PostHog assignee API: HTTP 500 — do not attempt to assign issues programmatically.
10. Auto-merge not enabled — do not attempt via API; note to user.
11. PR #163 is the CI fix — if still open, remind user to merge.
12. All 35 PostHog error-tracking issues are `resolved`. Not actionable.
13. Close any stale duplicate docs PRs before creating a new one.
14. PostHog credentials: `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID=88871` in env; PostHog MCP server never connects.
