# 2026-06-21 — 0 active errors; app telemetry silent (35+ days)

## Prior context loaded

Read `docs/changelog/20260620.md`. Key facts carried forward:
- Repo: `asomervell/probably`, working branch pattern: `claude/clever-knuth-*`
- Last real code commit: `1cd136b` — PR #92 merged (background context fix, 2026-06-17)
- All 35 PostHog error tracking issues = `resolved`, 0 active
- App OTel telemetry: 0 logs/events for 34+ days — silent
- PostHog assignee API: HTTP 500 — skip programmatic assignment
- Auto-merge not enabled at repo level — cannot enable via API
- Fixed PRs to date: #3, #5, #9, #26, #57, #58, #92

## Checks run (2026-06-21)

| Check | Result |
|---|---|
| Active error tracking issues | **0 active** (10 sampled, all `status: resolved`, all from 2026-04-25) |
| Exception events (`$exception`, 7d) | **0** |
| All app events (30d) | Only PostHog-internal: `$ai_trace_clusters` (5), `$ai_generation_clusters` (4) — no real app telemetry |
| Main HEAD | `f48a80a` — docs changelog only; last real code: `1cd136b` (2026-06-17) |

## Reasoning

No active errors. No new exceptions. App OTel telemetry silent for 35+ days (production binary likely missing OTel exporter config — not fixable from here without deployment access). No new code on main since 2026-06-17. Nothing to plan, fix, or ship.

## Shipped this session

- Memory log: `docs/changelog/20260621.md`

## What was left behind (unchanged)

- **App telemetry silent**: 0 OTel logs/events reaching PostHog for 35+ days. Production binary likely lacks OTel exporter configuration. Not actionable from this session.
- **Operational noise**: Akahu connections with expired OAuth tokens — users must reconnect via app UI.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Auto-merge not enabled at repo level — cannot enable via API.
- Fixed PRs: #3, #5, #9, #26, #57, #58, #92.
- All 35 PostHog error-tracking issues are `resolved`. Not actionable.

## Next run checklist

1. **READ THIS FILE FIRST** — `asomervell/probably`, last updated 2026-06-21.
2. `git fetch origin main && git log --oneline origin/main -6`. Last known real code HEAD: `1cd136b` (2026-06-17).
3. PostHog HogQL: `SELECT count() FROM events WHERE event='$exception' AND timestamp >= now() - INTERVAL 7 DAY` — expect 0.
4. PostHog: `error_tracking/issues/?status=active` — expect all `resolved`.
5. PostHog HogQL: `SELECT event, count() FROM events WHERE timestamp >= now() - INTERVAL 30 DAY GROUP BY event ORDER BY cnt DESC LIMIT 20` — check for new app telemetry.
6. If telemetry starts flowing: look for `resource_fingerprint`, investigate `partial_failure` patterns.
7. If 0 active errors AND 0 new code issues — write changelog and stop. **Do NOT manufacture work.**
8. PostHog assignee API: HTTP 500 — do not attempt programmatic assignment.
9. Auto-merge not enabled — do not attempt via API.
10. No CI workflows on this repo — do not chase phantom CI failures.
11. Fixed PRs: #3, #5, #9, #26, #57, #58, #92. Do not reinvestigate.
12. Close any stale duplicate docs PRs before creating a new one.
13. PostHog credentials: `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID=88871` available in env; use direct REST API (PostHog MCP server never connects in these sessions).
