# 2026-06-21 — 0 active errors; app telemetry silent (35+ days)

## Prior context loaded

Read `docs/changelog/20260620.md` (7 sessions). Key facts carried forward:
- Repo: `asomervell/probably`, working branch `claude/clever-knuth-kofenq`
- Last real code commit: `1cd136b` — PR #92 merged (2026-06-17)
- All 35 PostHog error tracking issues = `resolved` (all from 2026-04-25)
- App OTel telemetry: 0 logs/events for 35+ days — silent
- PostHog assignee API: HTTP 500 — skip programmatic assignment
- Auto-merge not enabled at repo level — cannot enable via API
- Fixed PRs to date: #3, #5, #9, #26, #57, #58, #92
- PostHog MCP server never connects — use REST API directly

## Checks run (2026-06-21)

| Check | Result |
|---|---|
| Active error tracking issues | **0 truly active** — 20 returned, all `status: resolved`, all from 2026-04-25 |
| Exception events (`$exception`, 7d) | **0** |
| Log rows (any level, 7d) | **0** |
| App events (30d) | Only PostHog-internal: `$ai_trace_clusters` (5), `$ai_generation_clusters` (5) — no real app telemetry |
| Main HEAD | `f48a80a` — docs changelog only; last real code: `1cd136b` (2026-06-17) |

## Reasoning

Identical state to all prior sessions since 2026-06-17:
- 0 active errors in PostHog
- 0 exception events
- 0 log lines
- App OTel telemetry silent for 35+ days
- No new code on main

Nothing to plan, fix, or ship. No issues to create. No PRs to raise.

## Shipped this session

- Memory log: `docs/changelog/20260621.md`

## What was left behind (unchanged)

- **App telemetry silent**: 0 OTel logs/events reaching PostHog for 35+ days. Production likely missing OTel exporter config. Not actionable from this session.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Auto-merge not enabled at repo level — cannot enable via API.
- Fixed PRs: #3, #5, #9, #26, #57, #58, #92.

---

## Next run checklist

1. **READ THIS FILE FIRST** — `asomervell/probably`, last updated 2026-06-21.
2. `git fetch origin main && git log --oneline origin/main -6`. Last known real code HEAD: `1cd136b` (2026-06-17).
3. PostHog: `error_tracking/issues/?status=active` — expect 0 truly active.
4. PostHog HogQL: `SELECT event, count() as cnt FROM events WHERE timestamp >= now() - INTERVAL 30 DAY GROUP BY event ORDER BY cnt DESC LIMIT 20` — check if any new app telemetry started flowing.
5. PostHog HogQL: `SELECT level, count() FROM logs WHERE timestamp >= now() - INTERVAL 7 DAY GROUP BY level` — expect 0.
6. PostHog HogQL: `SELECT count() FROM events WHERE event='$exception' AND timestamp >= now() - INTERVAL 7 DAY` — expect 0.
7. If telemetry starts flowing, look for new `resource_fingerprint`, investigate any `partial_failure` pattern.
8. If 0 active errors AND 0 new code issues, write changelog and stop. **Do NOT manufacture work.**
9. PostHog assignee API: HTTP 500 — do not attempt to assign issues programmatically.
10. Auto-merge not enabled — do not attempt via API.
11. No CI workflows on this repo — do not chase phantom CI failures.
12. Fixed PRs: #3, #5, #9, #26, #57, #58, #92. Do not reinvestigate.
13. All 35 PostHog error-tracking issues are `resolved`. Not actionable.
14. Close any stale duplicate docs PRs before creating a new one.
15. PostHog credentials: `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID=88871` available in env; use direct REST API (PostHog MCP server never connects).
