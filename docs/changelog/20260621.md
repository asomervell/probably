# 2026-06-21 — 0 active errors; app telemetry silent (35+ days)

## Prior context loaded

Read `docs/changelog/20260620.md` (7 sessions). Key facts carried forward:
- Repo: `asomervell/probably`, working branch: session-specific `claude/clever-knuth-*`
- Last real code commit: `1cd136b` — PR #92 merged (background context fix, 2026-06-17)
- All 35 PostHog error tracking issues = `resolved`, 0 active (all from 2026-04-25)
- App OTel telemetry: 0 logs/events for 35+ days — silent
- PostHog assignee API: HTTP 500 — skip programmatic assignment
- Auto-merge not enabled at repo level — cannot enable via API
- Fixed PRs to date: #3, #5, #9, #26, #57, #58, #92
- PostHog credentials available in environment (no `op` CLI needed)

## Checks run (2026-06-21, branch `claude/clever-knuth-g7vrhs`)

| Check | Result |
|---|---|
| Active error tracking issues | **0 truly active** — 35 returned, all `status: resolved`, all from 2026-04-25 |
| Exception events (`$exception`, 7d) | **0** |
| Log rows (any level, 7d) | **0** |
| App events (30d) | Only PostHog-internal: `$ai_trace_clusters` (5), `$ai_generation_clusters` (4) — no real app telemetry |
| Main HEAD | `f48a80a` — docs changelog only; last real code: `1cd136b` (2026-06-17) |

## Reasoning

Identical state to all prior sessions since 2026-06-17. PostHog shows:
- No new errors or exceptions in 7 days
- All 35 historical issues (all from 2026-04-25) are `resolved`
- App telemetry silent 35+ days (production likely missing OTel config — not fixable from here)
- No new code on main

Nothing to plan, fix, or ship. Do NOT manufacture work.

## Shipped this session

- Memory log: `docs/changelog/20260621.md`

## What was left behind (unchanged)

- **App telemetry silent**: 0 OTel logs/events reaching PostHog for 35+ days. Likely the production binary lacks OTel exporter configuration. Not actionable from this session.
- **Operational noise**: Akahu connections with expired OAuth tokens — users must reconnect via app UI.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Auto-merge not enabled at repo level — cannot enable via API.
- Fixed PRs: #3, #5, #9, #26, #57, #58, #92.

---

## Next run checklist

1. **READ THIS FILE FIRST** — `asomervell/probably`, last updated 2026-06-21.
2. `git fetch origin main && git log --oneline origin/main -6`. Last known real code HEAD: `1cd136b` (2026-06-17).
3. PostHog credentials: `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID=88871` available in env (no `make env` / `op` needed).
4. PostHog: `error_tracking/issues/?status=active` — expect 0 truly active.
5. PostHog HogQL: `SELECT event, count() as cnt FROM events WHERE timestamp >= now() - INTERVAL 30 DAY GROUP BY event ORDER BY cnt DESC LIMIT 20` — check if any new app telemetry started flowing.
6. PostHog HogQL: `SELECT level, count() FROM logs WHERE timestamp >= now() - INTERVAL 7 DAY GROUP BY level` — expect 0.
7. PostHog HogQL: `SELECT count() FROM events WHERE event='$exception' AND timestamp >= now() - INTERVAL 7 DAY` — expect 0.
8. If telemetry starts flowing, look for new `resource_fingerprint`, investigate any `partial_failure` pattern.
9. If 0 active errors AND 0 new code issues, write changelog and stop. **Do NOT manufacture work.**
10. PostHog assignee API: HTTP 500 — do not attempt to assign issues programmatically.
11. Auto-merge not enabled — do not attempt via API.
12. No CI workflows on this repo — do not chase phantom CI failures.
13. Fixed PRs: #3, #5, #9, #26, #57, #58, #92. Do not reinvestigate.
14. All 35 PostHog error-tracking issues are `resolved`. Not actionable.
15. Close any stale duplicate docs PRs before creating a new one.
