# 2026-06-21 — 0 active errors; app telemetry silent (35+ days)

## Prior context loaded

Read `docs/changelog/20260620.md`. Key facts carried forward:
- Repo: `asomervell/probably`, main HEAD: `f48a80a` (docs-only)
- Last real code commit: `1cd136b` — PR #92 merged (background context fix, 2026-06-17)
- All 35 PostHog error tracking issues = `resolved`, 0 active
- App OTel telemetry: 0 logs/events for 35+ days — silent
- PostHog assignee API: HTTP 500 — skip programmatic assignment
- Auto-merge not enabled at repo level — cannot enable via API
- Fixed PRs to date: #3, #5, #9, #26, #57, #58, #92

## Checks run (2026-06-21)

| Check | Result |
|---|---|
| Active error tracking issues | **0 active** (20 returned, all `status: resolved`, all from 2026-04-25) |
| Exception events (`$exception`, 7d) | **0** |
| Log rows (any level, 7d) | **0** |
| App events (30d) | Only PostHog-internal: `$ai_trace_clusters` (5), `$ai_generation_clusters` (4) — no real app telemetry |
| Main HEAD | `f48a80a` — docs changelog only; last real code: `1cd136b` (2026-06-17) |

## Reasoning

No active errors. No new app telemetry. No new code. Nothing to plan, fix, or ship.

## Shipped this session

- Memory log: `docs/changelog/20260621.md`

## What was left behind (unchanged)

- **App telemetry silent**: 0 OTel logs/events reaching PostHog for 35+ days. Production binary likely missing OTel exporter config. Not actionable from this session.
- **Operational noise**: Akahu connections with expired OAuth tokens — users must reconnect via app UI.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Auto-merge not enabled at repo level — cannot enable via API.
- Fixed PRs: #3, #5, #9, #26, #57, #58, #92.

## Next run checklist

1. **READ THIS FILE FIRST** — `asomervell/probably`, last updated 2026-06-21.
2. `git fetch origin main && git log --oneline origin/main -6`. Last known real code HEAD: `1cd136b` (2026-06-17).
3. PostHog: `error_tracking/issues/?status=active` — expect 0 truly active.
4. PostHog HogQL: `SELECT event, count() as cnt FROM events WHERE timestamp >= now() - INTERVAL 30 DAY GROUP BY event ORDER BY cnt DESC LIMIT 20` — check if new app telemetry started.
5. PostHog HogQL: `SELECT level, count() FROM logs WHERE timestamp >= now() - INTERVAL 7 DAY GROUP BY level` — expect 0.
6. PostHog HogQL: `SELECT count() FROM events WHERE event='$exception' AND timestamp >= now() - INTERVAL 7 DAY` — expect 0.
7. If 0 active errors AND 0 new code issues, write changelog and stop. **Do NOT manufacture work.**
8. PostHog assignee API: HTTP 500 — do not attempt to assign issues programmatically.
9. Auto-merge not enabled — do not attempt via API.
10. No CI workflows on this repo — do not chase phantom CI failures.
11. Fixed PRs: #3, #5, #9, #26, #57, #58, #92. Do not reinvestigate.
12. All 35 PostHog error-tracking issues are `resolved`. Not actionable.
13. Close any stale duplicate docs PRs before creating a new one.
14. PostHog credentials: `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID=88871` available in env; use direct REST API (PostHog MCP server never connects).

---

## Session 2 (2026-06-21, branch `claude/clever-knuth-u9cj1j`)

### Prior context loaded

Read session 1 above. All facts carried forward. Last known real code HEAD: `1cd136b` (2026-06-17); docs HEAD: `a5762bc`.

### Checks run

| Check | Result |
|---|---|
| Active error tracking issues | **0 truly active** — 20 returned, all `status: resolved`, all from 2026-04-25 |
| Exception events (`$exception`, 7d) | **0** |
| Log rows (any level, 7d) | **0** |
| App events (30d) | Only PostHog-internal: `$ai_trace_clusters` (5), `$ai_generation_clusters` (4) — no real app telemetry |
| Main HEAD | `a5762bc` — docs changelog only; last real code: `1cd136b` (2026-06-17) |
| Open GitHub issues | **0** |
| PostHog MCP server | Did not connect — used REST API directly |

### Reasoning

Identical state to all prior sessions since 2026-06-17: 0 active errors, 0 exceptions, 0 log rows. App OTel telemetry silent for 36+ days. No new code on main. PR #163 (CI pipefail fix) still open and valid — owner merge needed to unblock deploys.

### Housekeeping

Closed stale duplicate docs PRs: #166, #167, #168, #170, #171.
Attempted auto-merge on PR #163 — failed: repo auto-merge not enabled at repo level (owner must enable in Settings → General → Pull Requests → Allow auto-merge).

### Shipped

- Stale PRs #166, #167, #168, #170, #171 closed.
- Memory log appended: `docs/changelog/20260621.md` (this entry).

### What was left behind

- **PR #163** open: CI pipefail fix (`|| true` on `LAST_TAG`/`PREV_TAG` grep pipelines). Merging this will unblock all future deploys and restore PostHog telemetry.
- **PR #144** open: Dependabot chi v5.2.3 → v5.2.4 bump.
- App telemetry still silent — will only recover once PR #163 lands and a deploy succeeds.
- Auto-merge not enabled at repo level — owner must enable in Settings.

## Next run checklist (updated)

1. **READ THIS FILE FIRST** — last updated 2026-06-21 session 2.
2. Check if PR #163 has been merged. If yes, verify PostHog telemetry starts flowing.
3. `git fetch origin main && git log --oneline origin/main -6`. Last real code HEAD: `1cd136b`.
4. PostHog REST API checks (credentials in env): active issues, exceptions (7d), logs (7d), events (30d).
5. If telemetry starts flowing, look for new errors and act on them.
6. If still silent and PR #163 unmerged — write log, stop. Do NOT manufacture work.
7. PostHog assignee API: HTTP 500 — do not attempt.
8. Auto-merge not enabled — do not attempt via API.
9. PostHog MCP server never connects — use REST API directly.
