# Session log — 2026-05-01

## Context from previous runs

### 2026-04-27 (6 runs)
Fixed all known PostHog errors (PRs #114–#124, #126):
- Error A: Gemini `function_response.name: Name cannot be empty`
- Error B: Gemini `Function call is missing a thought_signature`
- Error C: OTEL `Too many AI spans`
- Error D: Plaid handlers using `log.Printf`, bypassing OTEL
- Error E: Google `API_KEY_INVALID` misclassified as `[parse_error]`

### 2026-04-28 (6 runs)
Defensive hardening (PR #139): added `WithMaxQueueSize(75)` to OTEL batcher; added `thoughtSignaturesConsistent()` guard for Gemini raw parts. All 711k historical error entries confirmed bounded to the April 25 burst, pre-fix.

### 2026-04-29 (5 runs) + 2026-04-30 (7 runs)
All runs: zero new active issues, zero post-April-26 errors or warnings. System confirmed clean across all signals (error tracking, app logs, worker logs, browser logs). 12 consecutive clean runs total.

## What I reasoned on (this run — 2026-05-01)

Read `docs/changelog/20260430.md` (7 runs) and `docs/changelog/20260429.md` (5 runs) before querying.

Queried PostHog across all signals:

| Source | Count |
|---|---|
| Error tracking issues (active, -7d) | 0 |
| Error tracking issues (all, -90d) | 35, all `resolved`, all `first_seen: 2026-04-25` |
| Error/fatal logs (-7d, all services) | 711,275 — entirely the April 25 burst, pre-fix |
| Error/fatal logs (-2d) | 0 |

The 35 resolved issues break down as:
- **`function_response.name: Name cannot be empty`** (~20 issues) — fixed: `sanitizeFunctionResponseNames` + tool-name propagation in `simple.go`
- **`Function call is missing a thought_signature`** (~2 issues) — fixed: `ThinkingBudget=0` on tool turns + `thoughtSignaturesConsistent` guard in `api.go`
- **`Too many AI spans: N exceeds limit of 100`** (~13 issues) — fixed: `WithMaxExportBatchSize(50)` + `WithMaxQueueSize(75)` in `otel.go`

All 711k error log entries remain bounded to the April 25 development burst. Zero new signals since April 26. System fully steady-state.

Services checked: `probably-app`, `probably-worker`, `posthog-browser-logs`.

## What I fixed and shipped (first run)

Nothing to fix. PostHog is clean. No PRs created.

## What was left behind (first run)

Same deferred items as all prior runs — none are actionable from PostHog signals:
- **Production project** (id 398540) — `ingested_event: false`; no traffic to check.
- **PostHog PATCH assignee bug** — server-side; manual UI assignment still the only path.
- **`syncCtx := context.Background()` in Plaid sync** — accepted low-priority, no PostHog signal.
- **`log.Printf` calls** — 8 remain in `internal/observability/` (intentional bootstrap logging, correct as-is).

---

## Session log — 2026-05-01 (second run)

### What I reasoned on

Read `docs/changelog/20260501.md` (first run) and prior days before querying. Prior context: 35 resolved issues all from April 25 burst, zero new signals since April 26.

Queried PostHog this run:

| Source | Count |
|---|---|
| Error tracking issues (active) | 0 |
| Error tracking issues (all, -90d) | 35, all `resolved`, all `first_seen: 2026-04-25` |
| Error/fatal logs since 2026-04-26 | 0 |
| Warn logs since 2026-04-26 | 0 |
| Warn logs (-7d) | 1,244 — all `http request` HTTP 401 on `/api/v1/check`, all from 2026-04-25 |

The 1,244 warn logs were new in scope (prior run hadn't sampled them) but confirmed to be pre-April-26 historical noise. However, they revealed an actionable **code issue**: `access_log.go` logged all HTTP 4xx responses at `slog.LevelWarn`. Client errors (4xx) are expected server behaviour — logging them at Warn pollutes the warning channel and will recur every development session.

**Distinct issue found: HTTP 4xx logged at Warn in access middleware**
- File: `internal/observability/access_log.go`, lines 48–53
- Root cause: `status >= 400 → LevelWarn` branch treated all client errors as server warnings
- Fix: removed the 4xx branch; 4xx now falls through to the default `LevelInfo`

### What I fixed and shipped

**PR #180** — `observability: log HTTP 4xx at Info, not Warn`
- Branch: `claude/clever-knuth-TqRch`
- 1 file changed, 2 lines deleted (`internal/observability/access_log.go`)
- Merged via squash to main: commit `f6f000883af280928dc04f2c67bbe8255ec388c2`
- Assigned to: `asomervell`
- Status: merged ✓

### What was left behind

All prior deferred items remain (no new PostHog signal for any of them):
- **Production project** (id 398540) — `ingested_event: false`; no traffic to check.
- **PostHog PATCH assignee bug** — server-side; manual UI assignment still the only path.
- **`syncCtx := context.Background()` in Plaid sync** — accepted low-priority, no PostHog signal.
- **`log.Printf` calls** — 8 remain in `internal/observability/` (intentional bootstrap logging, correct as-is).

---

## Session log — 2026-05-01 (third run)

### What I reasoned on

Read `docs/changelog/20260501.md` (first + second runs) before querying. Prior context: 35 resolved error tracking issues all from April 25 burst; PR #180 (HTTP 4xx at Info) merged in the second run.

Queried PostHog across all signals:

| Source | Count |
|---|---|
| Error tracking issues (active) | 0 |
| Error tracking issues (all, -90d) | 35, all `resolved`, all `first_seen: 2026-04-25` |
| Error/fatal logs (-24h) | 0 |
| Error/fatal logs since 2026-04-26 | 0 |
| Warn logs (-24h) | 0 |

Checked services: `probably-app`, `probably-worker`, `posthog-browser-logs`.

The 711k error log entries remain entirely bounded to the April 25 development burst, pre-fix. All error types in that burst (`function_response.name empty`, `thought_signature missing`, `Too many AI spans`) are confirmed resolved across all prior runs.

### What I fixed and shipped

Nothing to fix. PostHog is fully clean. No new PRs created.

### What was left behind

Same deferred items as all prior runs — none are actionable from PostHog signals:
- **Production project** (id 398540) — `ingested_event: false`; no traffic to check.
- **PostHog PATCH assignee bug** — server-side; manual UI assignment still the only path.
- **`syncCtx := context.Background()` in Plaid sync** — accepted low-priority, no PostHog signal.
- **`log.Printf` calls** — 8 remain in `internal/observability/` (intentional bootstrap logging, correct as-is).

---

## Session log — 2026-05-01 (fourth run)

### What I reasoned on

Read `docs/changelog/20260501.md` (runs 1–3) before querying. Prior context: 35 resolved issues all from April 25 burst; PR #180 (HTTP 4xx at Info) merged in run 2; runs 1 and 3 found nothing new.

Queried PostHog across all signals:

| Source | Count |
|---|---|
| Error tracking issues (active) | 0 |
| Error tracking issues (all, -90d) | 35, all `resolved`, all `last_seen: 2026-04-25` |
| Error/fatal/warn logs since 2026-04-26 | 0 |
| Error/fatal logs sample (-7d, 100 entries) | All 100 from 2026-04-25 — historical burst only |

Services checked: `probably-app`, `probably-worker`, `posthog-browser-logs`.

System is fully steady-state. No new signals.

### What I fixed and shipped

Nothing to fix. PostHog is clean. No PRs created.

### What was left behind

Same deferred items as all prior runs — none are actionable from PostHog signals (fourth run):
- **Production project** (id 398540) — `ingested_event: false`; no traffic to check.
- **PostHog PATCH assignee bug** — server-side; manual UI assignment still the only path.
- **`syncCtx := context.Background()` in Plaid sync** — accepted low-priority, no PostHog signal.
- **`log.Printf` calls** — 8 remain in `internal/observability/` (intentional bootstrap logging, correct as-is).

---

## Session log — 2026-05-01 (fifth run)

### What I reasoned on

Read `docs/changelog/20260501.md` (runs 1–4) before querying. Prior context: 35 resolved issues all from April 25 burst; PR #180 (HTTP 4xx at Info) merged in run 2; runs 1, 3, and 4 found nothing new.

Queried PostHog:

| Source | Count |
|---|---|
| Error tracking issues (active, -30d) | 0 |
| Error tracking issues (all, -90d) | 35, all `resolved`, all `first_seen: 2026-04-25` |

The 35 issues break down into 3 error types, all fixed before April 26 and confirmed in current code:
- `function_response.name: Name cannot be empty` (~20 issues) — fixed: `sanitizeFunctionResponseNames` + multi-level fallback in `convertMessagesToVertexContents` (`api.go`)
- `Function call is missing a thought_signature` (~2 issues) — fixed: `ThinkingBudget=0` on tool turns + `thoughtSignaturesConsistent` guard (`api.go`)
- `Too many AI spans: N exceeds limit of 100` (~13 issues) — fixed: `WithMaxExportBatchSize(50)` + `WithMaxQueueSize(75)` (`otel.go`)

Code is clean post dead-code removal (PR #176, ~1650 lines removed). No new signals since April 26.

### What I fixed and shipped

Nothing to fix. PostHog is clean. No PRs created.

### What was left behind

Same deferred items as all prior runs — none are actionable from PostHog signals (fifth run):
- **Production project** (id 398540) — `ingested_event: false`; no traffic to check.
- **PostHog PATCH assignee bug** — server-side; manual UI assignment still the only path.
- **`syncCtx := context.Background()` in Plaid sync** — accepted low-priority, no PostHog signal.
- **`log.Printf` calls** — 8 remain in `internal/observability/` (intentional bootstrap logging, correct as-is).

---

## Session log — 2026-05-01 (sixth run)

### What I reasoned on

Read `docs/changelog/20260501.md` (runs 1–5) before querying. Prior context: 35 resolved issues all from April 25 burst; PR #180 (HTTP 4xx at Info) merged in run 2; runs 1, 3, 4, and 5 found nothing new.

Queried PostHog across all signals:

| Source | Count |
|---|---|
| Error tracking issues (active) | 0 |
| Error tracking issues (all, -90d) | 35, all `resolved`, all `first_seen: 2026-04-25` |
| Error/fatal/warn logs since 2026-04-26 | 0 |
| Error/fatal/warn logs (-24h) | 0 |
| Error/fatal/warn logs (-7d) | 712,519 — entirely the April 25 historical burst |

Services checked: `probably-app`, `probably-worker`, `posthog-browser-logs`.

Zero new signals since April 26. System fully steady-state.

### What I fixed and shipped

Nothing to fix. PostHog is fully clean. No PRs created.

### What was left behind

Same deferred items as all prior runs — none are actionable from PostHog signals (sixth run):
- **Production project** (id 398540) — `ingested_event: false`; no traffic to check.
- **PostHog PATCH assignee bug** — server-side; manual UI assignment still the only path.
- **`syncCtx := context.Background()` in Plaid sync** — accepted low-priority, no PostHog signal.
- **`log.Printf` calls** — 8 remain in `internal/observability/` (intentional bootstrap logging, correct as-is).
