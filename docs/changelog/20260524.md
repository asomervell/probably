# 2026-05-24 — PostHog audit (Run 2) + stale PR cleanup

## Context read from previous session

Previous session (20260524.md Run 1) shipped `c61b170` (PR #614):
- Removed dead fields (`accountOwners`, `tags`, `rules`) from `PlaidSyncService` and `TellerSyncService`
- Removed `SyncStats.AccountName` (set but never read)
- Removed `syncErrorDetail` INFO workaround loop from `worker.go`
- Created GitHub issue #615 (`provider_sync_completed errors=4`) assigned to @asomervell
- Production still on `3811458b`; fix `ba2387f` not yet deployed

## PostHog state today

| Check | Result |
|---|---|
| Active error tracking issues | **0** |
| Error/warn/fatal logs (7d) | **0** (OTEL maps all levels to INFO — checked at INFO level too) |
| `provider_sync_completed errors=4` | Still present — production still on `3811458b` |
| "failed" keyword logs (7d) | 1176 — all `enrichment complete ... failed=0` (healthy) |
| "panic" keyword logs (7d) | 0 |
| Service version in production | `3811458b4373b7b8cf9f1662f86108232fd2e978` — UNCHANGED |

**Stop condition holds.** No new errors surfaced. All 168 "error" keyword log hits are the known `provider sync complete errors=4` pattern.

## What was done this session

### Stale doc-only PR cleanup

Closed 9 stale draft PRs (#598, #600–#612) that were doc-only audit logs from previous sessions. All had already been superseded by merged work or subsequent sessions.

| PR | Branch | Status → |
|---|---|---|
| #598 | claude/clever-knuth-0WcVX | closed |
| #600 | claude/clever-knuth-e0YQH | closed |
| #601 | claude/clever-knuth-VMzwy | closed |
| #603 | claude/clever-knuth-yUP9F | closed |
| #604 | claude/clever-knuth-XjKbD | closed |
| #606 | claude/clever-knuth-0r2zZ | closed |
| #609 | claude/clever-knuth-bzL5j | closed |
| #610 | claude/clever-knuth-Jpeqd | closed |
| #611 | claude/clever-knuth-Qxcbl | closed |
| #612 | claude/clever-knuth-jKFFt | closed |

## What was left behind (Run 2)

- **`errors=4` root cause**: fix is at HEAD (`ba2387f` — `noCredentialsError` + `login_required` exclusion). Issue #615 open, assigned to @asomervell. Clears on next production deploy.
- **Production deploy lag**: `service.version: 3811458b` unchanged for weeks. No action possible from this repo.
- **OTEL severity mapping**: PostHog shows all slog levels at INFO (`severity_number: 9`). PostHog-side display issue, not fixable here.

---

# 2026-05-24 — Run 3: orchestrator DRY sweep

## PostHog state (Run 3)

| Check | Result |
|---|---|
| Active error tracking issues | **0** |
| `provider_sync_completed errors=4` | Still present — production still on `3811458b` |
| Service version | `3811458b` — UNCHANGED |

**Stop condition holds.** No new errors. Same 168 "error" log hits — all the known hourly sync pattern.

## What was done this session (Run 3)

### Stale doc-only PR cleanup

Closed 11 stale doc-only PRs: #306, #321, #337, #381, #406, #435, #452, #453, #485, #618, #619.

### Code shipped — PR #624 → merged `09e6da2`

**GitHub Issue #623** (closed/completed) — three findings from Explore sweep of orchestrator:

| # | Finding | Location | Fix |
|---|---|---|---|
| 1 | Identical 18-line markdown→JSON stripping block in 3 files | `api.go`, `supervisor.go`, `simple.go` | Extracted `extractJSONFromMarkdown()` in `api.go`; replaced all 3 callsites |
| 2 | `VisionInput` exported struct, zero callers | `task.go:115` | Removed |
| 3 | `inferCategorizeToolName` 5-line wrapper called once, pure delegation | `simple.go:325` | Inlined into `resolveCategorizeToolName`; removed wrapper |

Net: **-42 lines**. All tests pass.

### Reasoning

- `extractJSONFromMarkdown`: byte-for-byte identical block in 3 places — any future bug fix must be applied 3 times. Helper removes that risk.
- `VisionInput`: comment says "represents input for vision/multimodal tasks" — planned work that never landed. Removing avoids confusion.
- `inferCategorizeToolName`: inlining 3-line body into single caller is strictly cleaner.

### Explore findings not actioned

| Finding | Reason |
|---|---|
| `GroupLabel` field in `backup.go` never populated from DB | Needs DB schema check; low risk (empty field in export) |
| Silent `_ = readJSON(...)` for backwards-compat backup fields | Intentional — silencing errors on missing-file reads is correct for v1.x compat |

### Note: Linear deprecated

Linear `/sse` transport is past its 2026-04-08 removal date — all `save_issue` calls fail. Issues tracked via GitHub issues instead.

## What was left behind (Run 3)

- **`errors=4`**: issue #615 open, fix at HEAD awaiting deploy.
- **Production deploy lag**: `3811458b` unchanged. No action possible here.
- **OTEL severity mapping**: all slog levels appear as INFO in PostHog. PostHog-side issue.
- **`GroupLabel` in backup.go**: never populated from DB query. Consider investigating what column it maps to.

## Next run checklist

1. Check if `service.version` in PostHog has changed from `3811458b`.
2. If deployed: confirm `provider_sync_completed` shows `errors=0`.
3. If errors=0: close issue #615.
4. Consider investigating `GroupLabel` field in `internal/backup/backup.go`.

---

# 2026-05-24 — Run 4: PostHog audit + dead code / DRY sweep

## Context read from previous sessions

- Run 3 shipped `09e6da2` (PR #624): orchestrator DRY sweep — `extractJSONFromMarkdown`, `VisionInput`, `inferCategorizeToolName`.
- `GroupLabel` in backup.go was merged as `499da79` (PR #626/#627) — already fixed.
- Issue #615 (`provider_sync_completed errors=4`) open, fix at HEAD (`ba2387f`), production still on `3811458b`.

## PostHog state (Run 4)

| Check | Result |
|---|---|
| Active error tracking issues | **0** |
| Error/warn/fatal logs (7d) | **0** (OTEL normalises all slog levels to INFO; confirmed again) |
| `provider_sync_completed errors=4` | Still present — every hourly sync, `connections=10 errors=4` |
| Service version in production | `3811458b` — **STILL UNCHANGED** |
| Per-account error detail in logs | Not visible — `slog.ErrorContext` appears only as INFO, body "account sync failed" not searchable without knowing exact text |
| New error patterns | None found |

**Stop condition holds.** No new errors. Same hourly `partial_failure errors=4` pattern, fix awaiting production deploy.

## What was done this session (Run 4)

### Issues created

| # | Title | Status |
|---|---|---|
| #628 | Delete dead `IsFeatureEnabled` export | Created → closed (fixed) |
| #629 | Extract `htmxRedirect` helper — DRY 21 occurrences | Created → closed (fixed) |

### Code shipped — PR #630 → merged `514c7e8`

**Two findings from code sweep during audit:**

| # | Finding | Location | Fix |
|---|---|---|---|
| 1 | `IsFeatureEnabled` exported function, zero callers | `observability/posthog.go:305` | Deleted (21 lines) |
| 2 | 5-line HTMX redirect pattern copied 24× across 7 handler files | `teller.go` ×10, `entities.go` ×4, `plaid_handlers.go` ×4, `akahu.go` ×2, `transfers.go` ×2, `categorize.go` ×2 | Added `htmxRedirect(w, r, url)` helper in `handlers.go`; replaced all 24 occurrences |

Net: **-143 lines**. All tests pass.

One `HX-Redirect` in `entities.go` intentionally left — HTMX target (`/entities`) differs from `http.Redirect` target (`/entities?deleted=...`).

### Reasoning

- `IsFeatureEnabled`: wrapped PostHog SDK feature flag eval, never called anywhere. Dead since introduction — removing it prevents confusing future readers into thinking feature flags are used server-side.
- `htmxRedirect`: identical 5-line block scattered across 7 files — any future change (e.g. status code, header name) requires 24 coordinated edits. Centralising removes that risk.

## What was left behind (Run 4)

- **`errors=4`**: issue #615 open, fix at HEAD (`ba2387f`) awaiting production deploy. `service.version: 3811458b` unchanged.
- **Production deploy lag**: external server, no action possible from this repo.
- **OTEL severity mapping**: PostHog shows all slog levels as INFO (`severity_number: 9`). PostHog-side issue, not fixable here.
- **Dependabot**: 1 high + 2 moderate vulnerabilities on default branch (noted in previous sessions, not investigated).

## Next run checklist

1. Check if `service.version` in PostHog has changed from `3811458b`.
2. If deployed: search PostHog `provider_sync_completed` events for `errors=0` — close issue #615 if confirmed.
3. If still `3811458b`: stop condition holds, write log, exit.
4. Dependabot alerts — consider reviewing the 1 high severity vulnerability.

---

# 2026-05-24 — Run 5: PostHog audit + stale PR cleanup

## Context read from previous sessions

- Run 4 shipped `514c7e8` (PR #630): `htmxRedirect` helper, deleted `IsFeatureEnabled`.
- `d7f5260` (PR #633) also merged since Run 4: dead snapshot exports removed, timezone bug fixed, DRY string join in insights/billing.
- Issue #615 (`provider_sync_completed errors=4`) open, fix at HEAD (`ba2387f`), production still on `3811458b`.

## PostHog state (Run 5)

| Check | Result |
|---|---|
| Active error tracking issues | **0** |
| Error/warn/fatal logs (7d) | **0** (OTEL maps all slog levels to INFO — confirmed again) |
| `provider_sync_completed errors=4` | Still present — every hourly sync, `connections=10 errors=4` |
| Service version in production | `3811458b` — **STILL UNCHANGED** |
| "sync error detail" logs | None — new per-account logging not visible (fix not deployed) |
| Panic logs | 0 |

**Stop condition holds.** No new errors. Same hourly `partial_failure errors=4` pattern, fix at HEAD awaiting production deploy.

## What was done this session (Run 5)

### Stale PR cleanup

Closed 8 open PRs whose changes were already in current HEAD (superseded via other commits):

| PR | Title | Reason closed |
|---|---|---|
| #622 | docs/changelog: 20260524 Run 1 | Doc-only, stale draft from earlier sub-run |
| #456 | HAM-4918/4919: remove dead Entity predicates + ValidateTransaction | `IsBusiness` etc. already absent from `entity.go` |
| #437 | HAM-4850/4851: remove dead detector functions + DRY enrollment | `DetectPatternWithLLM` etc. already absent from codebase |
| #428 | HAM-4839: remove dead GetStatementPath | `GetStatementPath` already absent from storage |
| #383 | HAM-4715: log swallowed UpdateStatus errors | `setUploadStatus` already in `statements_upload.go` |
| #359 | HAM-4689: Guard scheduled sync against concurrent manual sync | `SyncNow` no longer exists; guard irrelevant |
| #358 | HAM-4687: Fix parseAmount truncation | `math.Round` already used in all sync files |
| #348 | plaid_verify unit tests | `plaid_verify_test.go` already in main (merged via `0611aa2`) |

### No new code shipped

PostHog shows zero actionable errors beyond the known `errors=4` pattern already tracked in issue #615.

## What was left behind (Run 5)

- **`errors=4`**: issue #615 open, fix at HEAD (`ba2387f`) awaiting production deploy. `service.version: 3811458b` unchanged.
- **Production deploy lag**: external server, no action possible from this repo.
- **OTEL severity mapping**: PostHog shows all slog levels as INFO (`severity_number: 9`). PostHog-side issue, not fixable here.
- **Dependabot**: 1 high + 2 moderate vulnerabilities on default branch. Not investigated.

## Next run checklist

1. Check if `service.version` in PostHog has changed from `3811458b`.
2. If deployed: confirm `provider_sync_completed` shows `errors=0` — close issue #615 if confirmed.
3. If still `3811458b`: stop condition holds, write log, exit.
4. Consider reviewing Dependabot high-severity alert.

---

# 2026-05-24 — Run 6: PostHog audit + dead code sweep

## Context read from previous sessions

- Run 5 closed 8 stale PRs; no new code shipped.
- Run 4 shipped `514c7e8` (PR #630): `htmxRedirect` helper, deleted `IsFeatureEnabled`.
- PR #637 merged by @asomervell since Run 5: balance reconciliation error capture + ProviderImpl wrapper elimination.
- Issue #615 (`provider_sync_completed errors=4`) open, fix at HEAD, production still on `3811458b`.

## PostHog state (Run 6)

| Check | Result |
|---|---|
| Active error tracking issues | **0** |
| Error/warn/fatal logs (7d) | **0** (OTEL normalises all slog levels to INFO — confirmed again) |
| `provider_sync_completed errors=4` | Still present — every hourly sync |
| Service version in production | `3811458b` — **STILL UNCHANGED** |
| Panics | 0 |
| `enrichment complete failed=N` | `failed=0` on all runs — enrichment healthy |

**Stop condition holds.** No new errors in PostHog. Proceeded with code sweep.

## What was done this session (Run 6)

### Code sweep findings

Explore sweep across all `internal/` packages identified three issues:

| # | Finding | Location | Confidence |
|---|---|---|---|
| 1 | `SystemPrompt()` exported, zero callers | `internal/llm/prompts.go:10` | HIGH |
| 2 | Identical 5-line provider chain construction in 2 files | `reporter.go:28-33`, `analyzer.go:23-28` | HIGH |
| 3 | `NewVertexProvider(apiKey, projectID, location, model)` — `projectID`/`location` explicitly unused | `internal/insights/providers/grok.go:127` | HIGH |

### Issues created

| # | Title | Assignee |
|---|---|---|
| #638 | Delete dead `SystemPrompt()` from `internal/llm/prompts.go` | @asomervell |
| #639 | DRY provider chain construction + remove dead `NewVertexProvider` params | @asomervell |

### Code shipped — PR #640 → merged `a9e1158`

All three findings fixed in one commit `6c80d53`:

| Fix | Detail |
|---|---|
| Delete `llm.SystemPrompt()` | 100-line function, zero callers; `SystemPromptWithTools` is the live path |
| Extract `buildDefaultProviderChain(cfg)` in `insights` package | Single helper replaces duplicated 5-line block in `reporter.go` and `analyzer.go` |
| Remove dead `projectID`/`location` params from `NewVertexProvider` | Updated 2 callers |

Net: **-118 lines**. Build and insights tests pass.

### Reasoning

- `SystemPrompt()`: searching for all callers showed only the definition site. The function was replaced by `SystemPromptWithTools` at some point; the original was never cleaned up.
- Provider chain duplication: exact byte-for-byte copy in two files — any new provider or reorder requires editing both. The `buildDefaultProviderChain` helper costs zero abstraction and removes the risk.
- `NewVertexProvider` dead params: the comment literally said "unused in OpenAI-compatible mode". Two callers passed dead config values. Simplest safe change.

### Also noted (not actioned)

- PR #637 (merged by @asomervell) does two things: adds `observability.CaptureFailure` for balance reconciliation errors (so they'll appear in PostHog after next deploy), and eliminates thin `ProviderImpl` wrapper files (-163 lines). Clean work, no follow-up needed.

## What was left behind (Run 6)

- **`errors=4`**: issue #615 open, fix at HEAD (`ba2387f`) awaiting production deploy. `service.version: 3811458b` unchanged.
- **Balance reconciliation errors**: PR #637 added `CaptureFailure` calls — after next deploy, check PostHog error tracking for new `ProviderBalancePullError` issues. May indicate real failures needing fixing.
- **Production deploy lag**: external server, no action possible from this repo.
- **OTEL severity mapping**: PostHog shows all slog levels as INFO. PostHog-side issue, not fixable here.
- **Dependabot**: 1 high + 2 moderate vulnerabilities on default branch. Not investigated.

## Next run checklist

1. Check if `service.version` in PostHog has changed from `3811458b`.
2. If deployed: check error tracking for new `ProviderBalancePullError` issues (from PR #637's `CaptureFailure` calls).
3. If deployed: confirm `provider_sync_completed` shows `errors=0` — close issue #615 if confirmed.
4. If still `3811458b`: stop condition holds, write log, exit.
5. Consider reviewing Dependabot high-severity alert.
