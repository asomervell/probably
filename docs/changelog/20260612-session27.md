# 2026-06-12 — Session 27: PostHog deep-dive (no new errors, no new code)

## Prior context loaded

Read 20260612-session26.md (session 26). Key facts carried forward:
- 0 active errors, 0 warn/error/fatal logs. All logs `info`.
- Service fingerprint `406132732374446774` — PRs through #1066 merged but not deployed.
- `partial_failure errors=5` = 5 Akahu connections with expired OAuth tokens. Not a code bug.
- PostHog assignee API: HTTP 500 — skip programmatic assignment.
- Linear SSE: deprecated — skip all Linear tool calls.
- Last real code commits: `8545e96` (PR #1063) and `7ad26c0` (PR #1062). Last changelog: `c321c28` (PR #1066, session 26).

## Reasoning

1. Read 20260612-session26.md — full prior context loaded. All checklist rules applied.
2. `git fetch origin main && git log --oneline origin/main -10`:
   - No new code commits since session 26. Latest is `c321c28` (PR #1066 — session 26 changelog).
3. PostHog checks (ran in parallel):
   - `query-error-tracking-issues-list` (all statuses, 30d, filterTestAccounts: false) → **0 active**. 9 historical, all `resolved`/`suppressed`, from old `personal-cfo` binary.
   - `logs-count` (error/fatal, -7d, cfo-cloud) → **0**.
   - `logs-count` (warn, -7d, cfo-cloud) → **0**.
   - `query-logs` (all severity, -1h, cfo-cloud, limit 20) → all `severity_text: info`, `severity_number: 9`. Service fingerprint `406132732374446774`. Unchanged.
4. Investigated WHY all PostHog logs are `severity_text: info` (requested by user prompt):
   - Deployed binary (`3f3ec2a`) uses `log.Printf` with `log.SetFlags(log.LstdFlags)` for sync summary — this is routed through `stdLogToSlogWriter` in `RedirectStdLogToSlog()`.
   - `stdLogToSlogWriter.Write()` does keyword matching: messages containing "error", "failed", etc. → `slog.Error`. However the sync summary body `"provider sync complete ... errors=5"` does contain "error" as substring; the old stdlog_bridge in the deployed binary may use a simpler "all-INFO" approach.
   - NEW code at HEAD uses `slog.ErrorContext` directly (line 226: "account sync failed"; line 262: summary when syncErrors>0) — these bypass stdLogToSlogWriter and reach the OTEL handler with correct severity.
   - Conclusion: severity mapping in new code is correct. The INFO-only pattern is an artifact of the deployed binary being old.
5. Investigated `stdlog_bridge.go` for false-positive keyword matching:
   - `strings.Contains(lower, "error")` would match "errors=5" or "error_details=[]". Theoretically broad.
   - In practice: the main app logs ("provider sync complete", "enrichment complete") use structured `slog.InfoContext`/`slog.ErrorContext`, NOT `log.Printf`. They never go through `stdLogToSlogWriter`.
   - The only `log.Printf` calls in production code are bootstrap lines (lines 65, 89, 96, 115 of `cmd/server/main.go`) — none produce "errors=N" style messages.
   - Conclusion: false-positive risk is theoretical, not real. No fix needed.
6. Per checklist rule 8: 0 active errors AND 0 new code commits → write changelog, stop. Do NOT manufacture work.

## Deep investigation: why `errors=5` persists

- 5 Akahu connections with expired/invalid OAuth tokens hit the `syncErrors` path (not `disconnected`).
- In the current HEAD code, `providers.IsConnectionDisconnectedError(err)` should catch 401/403 from Akahu → `disconnected++`.
- But the deployed binary (`3f3ec2a`) predates the `disconnected` tracking field entirely — explains why PostHog shows `errors=5` with no `disconnected` count.
- When the new binary deploys: if Akahu returns 401 for expired tokens, they will reclassify as `disconnected=5` → `status=success` (with disconnected accounts skipped). If Akahu returns something else, they'll still show as `errors=5`.
- Regardless of classification: user action required — reconnect the 5 Akahu accounts via OAuth in app UI. GitHub issue #1009.

## PostHog state (2026-06-12, session 27)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0 active** (9 historical, all resolved/suppressed from old binary) |
| Error/warn/fatal logs (7d, cfo-cloud) | **0** |
| Recent log pattern | `partial_failure errors=5` + `enrichment complete skipped=4461` — unchanged |
| Service fingerprint in logs | `406132732374446774` — still not deployed |
| New code commits since session 26 | None |

## Shipped this session

Nothing. No new errors, no new code issues.

## What was left behind (unchanged from session 26)

- Service fingerprint `406132732374446774` still running — PRs #859–#1066 merged but not deployed.
- **Operational**: 5 Akahu connections with expired/invalid tokens → reconnect via OAuth. Issue #1009 open.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically. Use PostHog web UI.
- Linear SSE: deprecated — skip all Linear tool calls.
- `SyncAccounts` x3 structural duplication — deferred indefinitely.
- 4 TODOs in handlers/plaid.go + handlers/voice.go — pre-existing, unrelated to errors.
- Credential-clearing DRY violation (akahu/teller/plaid_handlers) — intentionally different per provider.
- `processAccountsByConnectionID` extraction opportunity — deferred.
- `syncInstitutionLogos` duplication (sync/teller.go vs handlers/teller.go) — deferred (different init contexts).

## Next run checklist

1. **READ THIS FILE FIRST** before any PostHog queries.
2. `git fetch origin main && git log --oneline origin/main -5`. Last known code commits: `8545e96` (PR #1063) and `7ad26c0` (PR #1062). Last changelog: `c321c28` (PR #1066, session 26).
3. `query-error-tracking-issues-list` (active) → expect 0.
4. `logs-count` (warn/error/fatal, -7d, cfo-cloud) → expect 0.
5. `query-logs` (all severity, -1h, cfo-cloud, limit 5) → check `severity_text` and `resource_fingerprint`.
6. If `resource_fingerprint` != `406132732374446774` → new binary deployed. Check if `errors=5` drops (may become `disconnected=5` or stay as `errors=5` depending on Akahu error codes).
7. If new code commits → scan for DRY/simplify/bug issues, open PRs.
8. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
9. Do NOT attempt PostHog issue assignment — API returns HTTP 500. Use GitHub issue assignment instead.
10. Skip all Linear tool calls — SSE transport deprecated.
11. `partial_failure errors=5` investigated across 30+ sessions — do not re-investigate unless fingerprint changes.
12. All PostHog error type names are from old `personal-cfo` binary — do not re-investigate.
13. Fixed PRs: #910/#911, #916/#917, #926, #934, #942, #958, #959, #973, #982, #990, #993, #999, #1000, #1002, #1003, #1010, #1013, #1018, #1019, #1026, #1037, #1038, #1062, #1063 — do not reinvestigate.
14. `SyncAccounts` x3 duplication — deferred indefinitely. Do not re-flag.
15. 4 TODOs in handlers/plaid.go + handlers/voice.go — pre-existing, unrelated to errors. Do not re-flag.
16. `dollarsToCents` in sync_helpers.go and handlers/api.go — intentional duplication across packages (avoids circular imports). Do not flag.
17. `handleBalanceReconciliationErr` is canonical balance error handler — do not duplicate.
18. Per-row CaptureFailure pattern fixed in Teller, Plaid, Akahu — do not reintroduce.
19. `captureAkahuError` and `captureTellerError` wrap `captureProviderError` — do not expand.
20. `LastSyncedAt` duplicate assignment in PlaidSyncAccounts — fixed in PR #1019. Do not re-flag.
21. `"error", err.Error()` anti-pattern in processing/worker.go — fixed in PR #1026. Do not re-flag.
22. `CaptureEvent` at sync/worker.go:227 — intentional (sync failures as analytics events). Do not change.
23. tx.Rollback defer in deleteTransactionsForAccount — idiomatic pgx no-op after commit. Not a bug.
24. WordPress scanner bot GETs (`/wp-json/...`) — normal internet noise. Not a bug, not worth filtering.
25. `enrichment complete skipped=N failed=0` — normal (no new transactions to enrich). Not a bug.
26. `context.Background()` in voice.go streaming session (line 199) — intentional: session outlives HTTP request lifetime. Do not flag.
27. Credential-clearing patterns (akahu.go, teller.go, plaid_handlers.go) — intentionally different per provider. Not a DRY violation. Do not re-flag.
28. `processAccountsByConnectionID` extraction opportunity — deferred. Do not re-flag.
29. `AgentOptions.Description` silently dropped — fixed in PR #1038. Do not re-flag.
30. `syncInstitutionLogos` duplication (sync/teller.go vs handlers/teller.go) — deferred (different init contexts). Do not re-flag.
31. `SyncAccounts` first-path not using `applyTellerCredentials` — intentional (conditional preserve vs. overwrite). Not a bug.
32. `disconnected=0` in production events explained by old binary (sha `3f3ec2a...`) not including `disconnected` field — do not re-investigate.
33. Session numbering drift corrected in session 18 (PR #1054).
34. All 9 PostHog error-tracking issues re-verified multiple times: `resolved`/`suppressed`, from `internal/cloud/server.go` in old `personal-cfo` binary. Every equivalent code path in `probably` is correctly handled. No new PRs warranted.
35. `LoadEntity`/`SetEntityID` usage — normal independent call sites, not duplicate logic. Do not re-flag.
36. DRY/simplify pass done (session 23) + follow-up PRs #1062/#1063 shipped: remaining deferred items unchanged. Do not repeat full DRY pass unless new code is added.
37. `reportTellerStoreErrors` extracted in PR #1062 — do not re-flag duplicate error reporting in teller.go.
38. `Account.TellerToken()` added in PR #1063 — do not re-flag dual-field token lookup in handlers/teller.go.
39. Session 25: no new errors, no new commits. State fully unchanged from session 24.
40. Session 26: no new errors, no new commits. State fully unchanged from session 25.
41. Session 27: no new errors, no new commits. Investigated severity mapping in depth — INFO-only is artifact of old binary. New code properly uses slog.ErrorContext. stdlog_bridge.go keyword false-positive risk is theoretical only (main logs use structured slog, not log.Printf). Do not re-investigate these topics.
