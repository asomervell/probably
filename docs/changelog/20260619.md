# 2026-06-19 — Session: No active errors; PostHog events absent since 2026-05-27

## Prior context loaded

Read `20260617.md`. Key facts carried forward:
- Repo: `asomervell/probably`, working branch `claude/clever-knuth-rh8jpq`
- Main HEAD before this session: `1cd136b` (PR #92 merged — `fix(handlers): run enrollment.disconnected webhook in background context`)
- Last code change: `1cd136b` (PR #92)
- 0 active PostHog errors for 50+ consecutive sessions
- `partial_failure errors=5` = 5 Akahu connections with expired OAuth tokens — operational
- Service fingerprint `406132732374446774` — old binary
- PostHog assignee API: HTTP 500 — skip programmatic assignment
- Auto-merge not enabled on repo — cannot enable via API
- PR #92 was awaiting merge as of 2026-06-17

## Reasoning

1. Fetched `origin/main` → HEAD `1cd136b` — **PR #92 merged!** (enrollment.disconnected webhook background context fix).
2. PostHog checks via direct API (MCP OAuth unavailable; used `POSTHOG_PERSONAL_API_KEY` from env):
   - `error_tracking/issues/?status=active` (HogQL) → **20 results, all `resolved`, all `count=0`** — no actionable errors.
   - `log entry` events, all severities, last 7d → **0 results**.
   - All events, last 7d → **0 results**.
   - Most recent events in PostHog across all time → `$ai_trace_clusters` / `$ai_generation_clusters` on **2026-05-27** (3 weeks ago).
3. **PostHog telemetry gap**: No application events since 2026-05-27. Either the service is not currently running in production, or the PostHog integration stopped ingesting. No errors to investigate because no traffic is being observed.
4. Listed open PRs → **PR #115** (stale docs PR from 2026-06-18 about OAuth blocking, draft) — **closed as superseded**.
5. No active errors, no new code issues, no logs → writing memory log only.

## PostHog state (2026-06-19)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0 active** (20 total, all resolved, count=0) |
| Error/warn/fatal logs (7d) | **0** |
| All log entries (7d) | **0** |
| Most recent any event | `$ai_trace_clusters` — 2026-05-27 (3 weeks ago) |
| Service fingerprint | Unknown — no recent logs to inspect |
| New code commits since session 2026-06-17 | **PR #92 merged** (`fix(handlers): enrollment.disconnected background context`) |

## Shipped this session

### PR #115 closed
- Stale draft docs PR from 2026-06-18 (OAuth-blocked session) — closed as superseded.

## What was left behind (unchanged)

- **PostHog telemetry gap**: No app events since 2026-05-27. If service is deployed, check PostHog SDK config / `POSTHOG_DISABLE_WEB` env in production. If service is not deployed, nothing to check.
- **Operational**: 5 Akahu connections with expired/invalid tokens → reconnect via OAuth in app UI.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Auto-merge not enabled — owner must enable in GitHub Settings → General → Pull Requests.
- No CI workflows on this repo.

## Next run checklist

1. **READ THIS FILE FIRST** — this is `asomervell/probably`, 2026-06-19.
2. `git fetch origin main && git log --oneline origin/main -6`. Last known main HEAD: `1cd136b` (PR #92).
3. PostHog check via direct API (MCP OAuth often unavailable in scheduled runs):
   - HogQL: `SELECT event, count() FROM events WHERE timestamp > now() - INTERVAL 7 DAY GROUP BY event ORDER BY count() DESC LIMIT 10`
   - If still 0 results → telemetry gap persists; nothing to investigate.
   - If results exist → check `log entry` events for `severity_text IN ('warn','error','fatal')`.
4. `error_tracking/issues/?status=active` → expect 0 active.
5. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
6. PostHog assignee API: HTTP 500 — do not attempt to assign issues programmatically.
7. Skip all Linear tool calls — SSE transport deprecated.
8. `partial_failure errors=5` = Akahu expired tokens (5 connections). Operational. Do not re-investigate unless fingerprint changes.
9. Auto-merge not enabled — do not attempt via API.
10. No CI workflows on this repo — do not chase phantom CI failures.
11. Fixed PRs to date: #3, #5, #9, #26, #57, #58, #92.
12. All PostHog error-tracking issues are `resolved` from old binary. Not actionable.
13. Close any stale duplicate docs PRs.
