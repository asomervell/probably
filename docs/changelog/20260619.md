# 2026-06-19 — Sessions: 0 active errors; app telemetry silent (30+ days)

## Prior context loaded

Read prior `20260619.md` (written by first session today). Key facts carried forward:
- Repo: `asomervell/probably`, working branch `claude/clever-knuth-4jmj8o`
- Last code commit on main: `fb2bbf9` (docs changelog)
- Last real code change: `1cd136b` — PR #92 merged (background context fix)
- All 35 PostHog error tracking issues = `resolved`, 0 active
- App OTel telemetry: 0 logs for 30+ days — silent
- PostHog assignee API: HTTP 500 — skip programmatic assignment
- Auto-merge not enabled at repo level — cannot enable via API
- Fixed PRs to date: #3, #5, #9, #26, #57, #58, #92

## Reasoning this session

1. PostHog checks (2026-06-19):
   - Error tracking issues (`status=active`) → 35 returned, **all `resolved`**, 0 truly active
   - `SELECT level, count() FROM logs WHERE timestamp >= now() - 7d GROUP BY level` → **0 rows**
   - `SELECT count() FROM events WHERE event='$exception' AND timestamp >= now() - 7d` → **0**
   - `SELECT event, count() FROM events WHERE timestamp >= now() - 30d GROUP BY event` → only PostHog-internal AI cluster events (`$ai_trace_clusters`, `$ai_generation_clusters`), no app telemetry
2. `git fetch origin main` → HEAD still `fb2bbf9` — no new code commits since last session
3. Found open draft PR #122 from earlier session today (branch `claude/clever-knuth-szez1f`) — same findings, stale → **closed as superseded**
4. 0 active errors, 0 new code issues → writing memory log only, no code changes

## PostHog state (2026-06-19)

| Check | Result |
|---|---|
| Active error tracking issues | **0 active** (35 total, all `resolved`) |
| Error/warn/fatal logs (7d) | **0** |
| Exception events (`$exception`, 7d) | **0** |
| Any app events (30d) | **0** — only PostHog-internal synthetic events |
| New code commits on main since 2026-06-17 | **None** — HEAD = `fb2bbf9` |

## Shipped this session

- PR #122 closed (stale earlier-session duplicate, branch `claude/clever-knuth-szez1f`)
- Memory log updated (`docs/changelog/20260619.md`)

## What was left behind (unchanged)

- **App telemetry silent**: 0 OTel logs/events reaching PostHog for 30+ days. Likely the deployed binary doesn't have OTel exporter config applied in production. Not actionable from this session — requires deployment or infra verification.
- **Operational noise**: 5 Akahu connections with expired/invalid OAuth tokens → users must reconnect via app UI.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Auto-merge not enabled at repo level — cannot enable via API.
- Fixed PRs to date: #3, #5, #9, #26, #57, #58, #92.

## Session 2 (later run, same day 2026-06-19)

Same checks re-run. No change whatsoever.

| Check | Result |
|---|---|
| Active error tracking issues | **0 active** (35 total, all `resolved`, all from 2026-04-25) |
| Error/warn/fatal logs (7d) | **0** |
| Exception events (`$exception`, 7d) | **0** |
| Any app events (30d) | **0** — only PostHog-internal synthetic events (`$ai_trace_clusters`, `$ai_generation_clusters`) |
| New code commits on main since session 1 | **None** — HEAD = `04d13da` (docs commit from session 1) |
| PostHog MCP server | Did not connect — used REST API directly |

No code changes made. No PRs opened. No notifications sent (nothing actionable).

---

## Session 3 (another run, same day 2026-06-19)

Same state. No change.

| Check | Result |
|---|---|
| Active error tracking issues | **0 active** (35 total, all `resolved`, all from 2026-04-25) |
| Exception events (`$exception`, 7d) | **0** |
| Any app events (30d) | **0** — only PostHog-internal synthetic events |
| Main HEAD | `8dd6c88` — docs changelog only, no code changes |

No code changes made. No PRs opened. No notification sent (nothing actionable).

---

## Next run checklist

1. **READ THIS FILE FIRST** — `asomervell/probably`, last updated 2026-06-19.
2. `git fetch origin main && git log --oneline origin/main -6`. Last known code HEAD: `8dd6c88`.
3. PostHog: query `error_tracking/issues/?status=active` → expect 0 truly active (all resolved).
4. PostHog: `SELECT level, count() FROM logs WHERE timestamp >= now() - 7d GROUP BY level` → expect 0.
5. PostHog: `SELECT count() FROM events WHERE event='$exception' AND timestamp >= now() - 7d` → expect 0.
6. PostHog: `SELECT event, count() FROM events WHERE timestamp >= now() - 30d GROUP BY event ORDER BY cnt DESC LIMIT 20` → check for new app events (telemetry resuming?).
7. If telemetry starts flowing → look for new `resource_fingerprint`, investigate `partial_failure errors=5` pattern.
8. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
9. PostHog assignee API: HTTP 500 — do not attempt to assign issues programmatically.
10. Auto-merge not enabled — do not attempt via API.
11. No CI workflows on this repo — do not chase phantom CI failures.
12. Fixed PRs: #3, #5, #9, #26, #57, #58, #92. Do not reinvestigate these.
13. All 35 PostHog error-tracking issues are `resolved`. Not actionable.
14. Close any stale duplicate docs PRs before creating a new one.
15. PostHog credentials: `POSTHOG_PERSONAL_API_KEY` and `POSTHOG_PROJECT_ID=88871` available in env; use direct REST API if MCP server not connected.
