# Changelog — 2026-05-22

## Context from previous run (2026-05-21)

- Zero active PostHog error tracking issues confirmed.
- Deployed service is `cfo-cloud` version `3811458b` (module `github.com/asomervell/personal-cfo`), a separate binary from `probably`.
- `partial_failure errors=4` on every hourly sync — already fixed at HEAD via HAM-5121 (`sync: treat Teller 'unable to process' as disconnected`), awaiting production deploy.
- All three 2026-04-25 dev-session errors (GEMINI_EMPTY_FUNCTION_NAME, GEMINI_THOUGHT_SIGNATURE, OTEL_SPAN_LIMIT) confirmed fixed.

---

## Run 1 — session 01TxT3wVqoJvdUkM8AEynAbg

### What I reasoned on

**PostHog state (as of 2026-05-22 00:00 UTC):**

| Check | Result |
|---|---|
| Active error tracking issues | **0** |
| `$exception` events (last 7d) | 2 — `ProviderBalancePullError` from Plaid, 2026-05-18 (4 days old) |
| `$exception` events (last 30d) | 384 total, all from `cfo-cloud` (`github.com/asomervell/personal-cfo`) |
| `provider_sync_completed` (last 7d) | **168 events, ALL `partial_failure`, errors=4** |
| `provider_pull_completed` (last 7d) | 1009 (505 Plaid + 504 Teller) — all successful |
| `api_request_rate_limited` (481 events) | Security scanner probes (PHP/env sniffing) — rate-limiting working correctly |
| Error/fatal logs (last 30d) | **0** |

**`partial_failure errors=4` pattern:**
Every hourly sync for the past 7+ days reports exactly 4 errors, 0 disconnected, 10 connections. The 4 consistently failing accounts are linked to `ProviderBalancePullError` exceptions from Plaid (accounts `sha256:14402c2adf9aa163` and `sha256:92a5dbd0b6a209cd`) plus 2 others. This is from the deployed `cfo-cloud` binary. The fix (HAM-5121) is already at HEAD in `probably` — needs deploy.

**New bug found in `probably` codebase:**

In `plaid_sync.go:SyncTransactions`, `getOrCreatePlaidEntity` was called with `txn.TellerCategory = ""` (line 553) because the category extraction block (PFC → legacy → fallback) ran 10+ lines later (562–590). Every Plaid-imported entity was created with the `BusinessSubtypeRetailer` default, ignoring Plaid's Personal Finance Category data entirely.

**Other findings:**
- `api_request_rate_limited` events: all bot probes (PHP info pages, `.env` files) — rate limiting is working, no action needed.
- No OTEL log errors (0 error/fatal log entries).
- `provider_pull_completed` events have null status — successful pulls don't report errors, consistent with no issues in the pull path itself.

### What was fixed and shipped

**HAM-5176: Plaid entity category ordering bug**

- **File**: `internal/sync/plaid_sync.go`
- **Bug**: `getOrCreatePlaidEntity(ctx, ..., txn.TellerCategory)` called before category extracted — always passed `""`
- **Fix**: moved category extraction (PFC → legacy → `GetCategoryId` fallback) to before the entity creation call
- **Also**: removed two stale inline comments (`// Store Plaid ID in Teller field for now`, `// Reuse this method`)
- **PR**: [#584](https://github.com/asomervell/probably/pull/584) — merged to main (squash commit `1b77466`)
- **Linear**: [HAM-5176](https://linear.app/hamsterinc/issue/HAM-5176) — assigned to Andrew Somervell, marked Done

### What was left behind (carry-forward)

1. **`partial_failure errors=4`**: Fixed at HEAD (HAM-5121), blocked on production deploy of `probably`. The deployed `cfo-cloud` binary (`3811458b`) does not have this fix. No new code action needed in `probably`.

2. **`ProviderBalancePullError` (Plaid, 2 events 2026-05-18)**: From the `cfo-cloud` service (`personal-cfo` module), a separate deployed binary that this repo cannot modify. Not re-activating as a PostHog error tracking issue. No action in `probably`.

3. **`TellerTransactionID` used for Plaid IDs** (`plaid_sync.go:496`): Functionally correct (same DB column, same lookup method). Would require a schema migration + method rename to fix properly. Low priority — skipped.

4. **No `provider_sync_completed` PostHog event in `probably` sync worker**: The `cfo-cloud` emits this event with rich metadata. `probably`'s `worker.go` only logs via slog. Low-priority observability gap — skipped.

### Stop condition for next run

1. Check `provider_sync_completed` events — if `errors=0` and `status=success`, the HAM-5121 fix has deployed. ✓ Stop if clean.
2. If still `errors=4`: no new action, this is awaiting deploy.
3. Check `$exception` events (last 7d) — if new errors appear from `github.com/asomervell/probably`, diagnose and fix.
4. If new active error tracking issues appear: diagnose and fix.
5. Do NOT re-investigate `ProviderBalancePullError` — it's from the `cfo-cloud` binary and cannot be fixed here.

---

## Run 2 — session 01XGSAA45beQjLbG2hMyCiZo

### What I reasoned on

**PostHog state (as of 2026-05-22 12:14 UTC):**

| Check | Result |
|---|---|
| Active error tracking issues | **0** |
| `$exception` events (last 30d) | 384 total — all from `cfo-cloud` (`posthog-go` lib), last seen 2026-05-18. No change. |
| `provider_sync_completed` (last 7d) | Still `partial_failure`, `errors=4`, deployed version `3811458b`. HAM-5121 not yet deployed. |
| Error/fatal logs (cfo-cloud, last 7d) | **0** |
| New errors from `probably` | **None** |

**Stop condition evaluation:**
- `provider_sync_completed` still shows `errors=4` — HAM-5121 fix is at HEAD in `probably` but the deployed `cfo-cloud` binary (`3811458b`) does not include it. No new code action needed.
- No `$exception` events from `github.com/asomervell/probably`. No action.
- Zero active error tracking issues. No action.
- Zero error/fatal log lines. No action.

Stop condition holds. No errors to fix this run.

### What was fixed and shipped

Nothing — PostHog is clean. Stop condition held.

### What was left behind (carry-forward)

Same as Run 1:

1. **`partial_failure errors=4`**: HAM-5121 fix at HEAD, blocked on production deploy.
2. **`ProviderBalancePullError`**: `cfo-cloud`-only, cannot fix here. Do not re-investigate.
3. **`TellerTransactionID` used for Plaid IDs**: Low priority, requires schema migration. Skipped.
4. **No `provider_sync_completed` PostHog event in `probably` worker**: Low-priority observability gap. Skipped.

### Stop condition for next run

Same as Run 1 — unchanged.

---

## Run 3 — session 01XE74Bva6v9txa2NetaPDYz

### What I reasoned on

**PostHog state (as of 2026-05-22 UTC):**

| Check | Result |
|---|---|
| Active error tracking issues | **0** |
| Historical issues (30d) | 14, all `resolved` — all from `cfo-cloud` (personal-cfo) or local dev CLI |
| `$exception` events (last 7d) | 2 on 2026-05-18 — `ProviderBalancePullError` from `cfo-cloud`. No change from Run 2. |
| `cli_command_failed` (30d) | Spike 2026-05-03 to 2026-05-08 (local dev CLI), zero since 2026-05-09 |
| `provider_sync_completed` with errors>0 | Still ~15–24/day — HAM-5121 fix at HEAD, not yet deployed |
| Error/warn/fatal logs (30d) | **0** |

**Stop condition evaluation:**
- Zero active error tracking issues.
- No `$exception` events from `github.com/asomervell/probably`.
- Zero error/fatal/warn log entries.
- `provider_sync_completed errors=4` pattern unchanged — HAM-5121 fix at HEAD, blocked on production deploy.
- All 14 historical issues are resolved, all originating from `cfo-cloud` or dev CLI sessions — no action in `probably`.

Stop condition holds. No new errors to investigate or fix.

### What was fixed and shipped

Nothing — PostHog is clean. Stop condition held for third consecutive run today.

### What was left behind (carry-forward)

1. **`partial_failure errors=4`**: HAM-5121 fix at HEAD, blocked on production deploy of `probably`.
2. **`ProviderBalancePullError`**: `cfo-cloud`-only binary — cannot fix here. Do not re-investigate.
3. **`TellerTransactionID` used for Plaid IDs**: Low priority, requires schema migration. Skipped.
4. **No `provider_sync_completed` PostHog event in `probably` worker**: Low-priority observability gap. Skipped.

### Stop condition for next run

1. Check `provider_sync_completed` — if `errors=0` and `status=success`, HAM-5121 has deployed. ✓ Stop if clean.
2. If still `errors=4`: no new action, awaiting deploy.
3. Check `$exception` events (last 7d) — if new errors appear from `github.com/asomervell/probably`, diagnose and fix.
4. If new active error tracking issues appear: diagnose and fix.
5. Do NOT re-investigate `ProviderBalancePullError` — it's from the `cfo-cloud` binary.

---

## Run 4 — session 01PUrQgUEGpxJrbqZRaQUSxY

### What I reasoned on

**PostHog state (as of 2026-05-22 17:08 UTC):**

| Check | Result |
|---|---|
| Active error tracking issues | **0** |
| `$exception` events (last 7d) | 2 on 2026-05-18 — `ProviderBalancePullError` from `cfo-cloud`. No change from Run 3. |
| `cloud_http_request` errors (7d) | 37 total: all 404s from bot/scanner probes (`/`, `/robots.txt`, `/.env`, `/.git/config`, `/favicon.ico`). Not real user errors. |
| `provider_sync_completed` (last 7d) | Still `errors=4`, `partial_failure`, version `3811458b`. HAM-5121 fix at HEAD, not deployed. |
| Error/warning logs (7d) | **0** |
| Error-pattern events (30d) | `$exception`: 384 (all `cfo-cloud`, last 2026-05-18); `cli_command_failed`: 44 (last 2026-05-08, local dev CLI). No change. |

**Stop condition evaluation:**
- Zero active error tracking issues. No action.
- No `$exception` events from `github.com/asomervell/probably`. No action.
- Zero error/warning/fatal log entries. No action.
- `provider_sync_completed errors=4` pattern unchanged — HAM-5121 fix at HEAD, blocked on production deploy.
- HTTP errors are exclusively scanner bot 404s — rate limiting working correctly, no action needed.

Stop condition holds for the fourth consecutive run today. No errors to fix.

### What was fixed and shipped

Nothing — PostHog is clean. Stop condition held.

### What was left behind (carry-forward)

1. **`partial_failure errors=4`**: HAM-5121 fix at HEAD, blocked on production deploy.
2. **`ProviderBalancePullError`**: `cfo-cloud`-only binary — cannot fix here. Do not re-investigate.
3. **`TellerTransactionID` used for Plaid IDs**: Low priority, requires schema migration. Skipped.
4. **No `provider_sync_completed` PostHog event in `probably` worker**: Low-priority observability gap. Skipped.

### Stop condition for next run

1. Check `provider_sync_completed` — if `errors=0` and `status=success`, HAM-5121 has deployed. ✓ Stop if clean.
2. If still `errors=4`: no new action, awaiting deploy.
3. Check `$exception` events (last 7d) — if new errors appear from `github.com/asomervell/probably`, diagnose and fix.
4. If new active error tracking issues appear: diagnose and fix.
5. Do NOT re-investigate `ProviderBalancePullError` — it's from the `cfo-cloud` binary.
6. HTTP 404s on bot-probe paths (`/robots.txt`, `/.env`, etc.) are expected scanner noise — do not investigate.

---

## Run 5 — session 01BWjwoFHvm47kwwpkM1KBm3

### What I reasoned on

**PostHog state (as of 2026-05-22 19:10 UTC):**

| Check | Result |
|---|---|
| Active error tracking issues | **0** |
| `$exception` events (last 7d) | 2 on 2026-05-18 — `ProviderBalancePullError` from `cfo-cloud` (resolved). No change from Run 4. |
| Error/fail/exception events (last 7d) | Only `$exception` (2, last 2026-05-18). No new events. |
| `cfo-cloud` error/warn logs (last 7d) | **0** |
| `provider_sync_completed` (last 3d) | Still `errors=4`, version `3811458b`. HAM-5121 fix at HEAD, not yet deployed. |
| Recent codebase commits (since Run 4) | `c42f7e4` HAM-5179 (dead `ProviderError` removal), `8d513f4` HAM-5178 (PostHog stack trace extractor) — both from earlier today, already merged. |

**Stop condition evaluation:**
- Zero active error tracking issues. No action.
- No `$exception` events from `github.com/asomervell/probably`. No action.
- Zero error/warn log entries from `cfo-cloud`. No action.
- `provider_sync_completed errors=4` unchanged — HAM-5121 fix at HEAD, blocked on production deploy of `cfo-cloud`.
- No new code bugs identified in recent commits (HAM-5179 dead-code removal, HAM-5178 observability fix — both clean).

Stop condition holds for the fifth consecutive session on 2026-05-22. No errors to fix.

### What was fixed and shipped

Nothing — PostHog is clean. Stop condition held.

### What was left behind (carry-forward)

1. **`partial_failure errors=4`**: HAM-5121 fix at HEAD, blocked on production deploy of `cfo-cloud`.
2. **`ProviderBalancePullError`**: `cfo-cloud`-only binary — cannot fix here. Do not re-investigate.
3. **`TellerTransactionID` used for Plaid IDs**: Low priority, requires schema migration. Skipped.
4. **No `provider_sync_completed` PostHog event in `probably` worker**: Low-priority observability gap. Skipped.

### Stop condition for next run

1. Check `provider_sync_completed` — if `errors=0` and `status=success`, HAM-5121 has deployed. ✓ Stop if clean.
2. If still `errors=4`: no new action, awaiting deploy.
3. Check `$exception` events (last 7d) — if new errors appear from `github.com/asomervell/probably`, diagnose and fix.
4. If new active error tracking issues appear: diagnose and fix.
5. Do NOT re-investigate `ProviderBalancePullError` — it's from the `cfo-cloud` binary.
6. HTTP 404s on bot-probe paths are expected scanner noise — do not investigate.

---

## Run 6 — session 016WGj2ZDngaNWRJvybfrG8i

### What I reasoned on

**PostHog state (as of 2026-05-22 21:08 UTC):**

| Check | Result |
|---|---|
| Active error tracking issues | **0** |
| `$exception` events (last 7d) | 2 on 2026-05-18 — `ProviderBalancePullError` from `cfo-cloud`. No change from Run 5. |
| `provider_sync_completed` (last 3d) | Still `errors=4`, version `3811458b`. HAM-5121 fix at HEAD, not yet deployed. |
| Error/warn/fatal logs (cfo-cloud, last 7d) | **0** |
| New errors from `probably` | **None** |
| New commits since Run 5 | None — latest is `9e2b351` (Run 5 changelog entry). |

**Stop condition evaluation:**
- Zero active error tracking issues. No action.
- No `$exception` events from `github.com/asomervell/probably`. No action.
- Zero error/warn/fatal log entries from `cfo-cloud`. No action.
- `provider_sync_completed errors=4` pattern unchanged — HAM-5121 fix at HEAD, blocked on production deploy of `cfo-cloud`.
- No new code commits to review for bugs.

Stop condition holds for the sixth consecutive session on 2026-05-22. No errors to fix.

### What was fixed and shipped

Nothing — PostHog is clean. Stop condition held.

### What was left behind (carry-forward)

1. **`partial_failure errors=4`**: HAM-5121 fix at HEAD, blocked on production deploy of `cfo-cloud`.
2. **`ProviderBalancePullError`**: `cfo-cloud`-only binary — cannot fix here. Do not re-investigate.
3. **`TellerTransactionID` used for Plaid IDs**: Low priority, requires schema migration. Skipped.
4. **No `provider_sync_completed` PostHog event in `probably` worker**: Low-priority observability gap. Skipped.

### Stop condition for next run

1. Check `provider_sync_completed` — if `errors=0` and `status=success`, HAM-5121 has deployed. ✓ Stop if clean.
2. If still `errors=4`: no new action, awaiting deploy.
3. Check `$exception` events (last 7d) — if new errors appear from `github.com/asomervell/probably`, diagnose and fix.
4. If new active error tracking issues appear: diagnose and fix.
5. Do NOT re-investigate `ProviderBalancePullError` — it's from the `cfo-cloud` binary.
6. HTTP 404s on bot-probe paths are expected scanner noise — do not investigate.
