# 2026-05-31 — Session 12: Monitoring-only, clean state

## PostHog state (2026-05-31 session 12)

| Check | Result |
|---|---|
| Active error tracking issues (7d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (unchanged — PRs #750–#799 still not deployed) |
| Provider sync | `partial_failure connections=11 transactions=87 errors=5` (structural Akahu disconnects) |
| New commits | None since `68b6af6` (session 11 changelog) |
| All PostHog issues (30d) | 2 suppressed CLICommandError (local Mac CLI), 18 resolved (unchanged) |

## Reasoning (session 12)

Ran mandatory checklist per session 11 instructions:

1. ✅ Read existing changelog (sessions 1–11) — confirmed zero actionable errors across all prior sessions.
2. ✅ `query-error-tracking-issues-list` (all statuses, 30d, filterTestAccounts=false) → 0 active. 2 suppressed CLICommandError, 18 resolved (all from old `personal-cfo` module / local Mac CLI — different repo).
3. ✅ `logs-count` WITH `query` wrapper: `{"query": {"serviceNames": ["cfo-cloud"], "severityLevels": ["warn","error","fatal"], "dateRange": {"date_from": "-7d"}}}` → **0**.
4. ✅ `query-logs` (all levels, last 1h, cfo-cloud) → service.version = `3f3ec2a...` unchanged, `partial_failure connections=11 transactions=87 errors=5` structural.
5. ✅ `git log --oneline -5` → latest commit is `68b6af6` (session 11 changelog), no new code.

**No actionable errors.** All PostHog issues continue to be:
- 2 suppressed **CLICommandError**: from `personal-cfo` CLI at `/Users/andrew/code/personal-cfo/cmd/cfo/main.go` on a local Mac — different module (`github.com/asomervell/personal-cfo`), not fixable from `probably`.
- 18 resolved issues: from old `personal-cfo` server module — all already resolved in PostHog; not errors in the current `probably` codebase.
- PostHog assignee API: HTTP 500 on every attempt — cannot assign issues programmatically (confirmed across all sessions).

Zero active errors. No dead code (confirmed by exhaustive scans in sessions 1–7 on 2026-05-30). Monitoring-only.

## Changes shipped (session 12)

None.

## What was left behind (session 12)

- Service version `3f3ec2a...` still running (PRs #750–#799 merged but not yet deployed).
- Structural `errors=5` in provider sync: broken Akahu connections requiring re-auth — fixed in code (#780), awaiting deploy.
- Log severity not surfacing in PostHog: stdout collector strips slog WARN/ERROR → all land as info. Infrastructure-level, not addressable via source-code PRs.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear MCP SSE transport: past removal date 2026-04-08 — skip all Linear tool calls.

## Next run checklist

1. Switch to PostHog project 407550 (Personal CFO) — project 371628 returns 403.
2. `query-error-tracking-issues-list` (active, 7d) → expect 0.
3. `logs-count` WITH `query` wrapper: `{"query": {"serviceNames": ["cfo-cloud"], "severityLevels": ["warn","error","fatal"], "dateRange": {"date_from": "-7d"}}}` → expect 0.
4. `query-logs` (all levels, last 1h, cfo-cloud) → check `service.version` for new git hash.
5. `git log --oneline -5` → check for new commits.
6. Skip Linear tool calls — SSE transport is past its removal date.
7. **CRITICAL**: Always use `query` wrapper in `logs-count` calls — bare calls (no wrapper) return wrong counts.
8. If clean and no new commits: monitoring-only. Do NOT re-run full codebase scan (sessions 1–7 on 2026-05-30 + sessions 1–12 on 2026-05-31 all confirmed zero findings).

---

# 2026-05-31 — Session 11: Monitoring-only, clean state

## PostHog state (2026-05-31 session 11)

| Check | Result |
|---|---|
| Active error tracking issues (7d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (unchanged — all PRs #750–#799 still not deployed) |
| Provider sync | `partial_failure connections=11 transactions=87 errors=5` (structural Akahu disconnects) |
| New commits | None since `9a6482e` (session 10 changelog) |
| All PostHog issues (30d) | 2 suppressed CLICommandError (local Mac CLI), resolved issues unchanged |

## Reasoning (session 11)

Ran mandatory checklist per session 10 instructions:

1. ✅ `query-error-tracking-issues-list` (active, 30d, project 407550) → 0 active issues
2. ✅ `logs-count` (warn+error+fatal, 7d, cfo-cloud, correct `query` wrapper) → **0**
3. ✅ `query-logs` (all levels, last 1h, cfo-cloud) → `service.version = 3f3ec2a...` unchanged, `partial_failure connections=11 transactions=87 errors=5` structural
4. ✅ `git log --oneline -10` → latest commit is `9a6482e` (session 10 changelog), no new code

**Tool-usage finding:** `logs-count` called WITHOUT the required `query` wrapper object returned `8` for warn/error/fatal (7d). Calling it WITH `{"query": {"serviceNames": ..., "severityLevels": ..., "dateRange": ...}}` returned the correct `0`. Always use the `query` wrapper — bare calls return stale/wrong counts.

**No actionable errors.** Zero active PostHog issues. No new code. Monitoring-only.

Known standing issues (unchanged from sessions 1–10):
- **Structural `errors=5`**: 5 Akahu connections failing per sync cycle — broken tokens needing re-auth, not a code defect. Fix already shipped in #780 (Akahu 404 → disconnected), awaiting deploy.
- **Log severity**: stdout collector maps all slog levels → `severity_number=9` (info). Infrastructure-level, not addressable via source-code PRs.
- **2 suppressed CLICommandErrors**: local Mac `personal-cfo` CLI — different repo, nothing to do from `probably`.
- **Linear MCP SSE transport**: past removal date 2026-04-08 — skip all Linear tool calls.
- **PostHog assignee API**: HTTP 500 on every attempt — cannot assign issues programmatically.

## Changes shipped (session 11)

None.

## What was left behind (session 11)

- Service version `3f3ec2a...` still running (PRs #750–#799 merged but not yet deployed).
- Structural `errors=5` in provider sync: broken Akahu connections requiring re-auth — fixed in code (#780), awaiting deploy.
- Log severity not surfacing in PostHog: stdout collector strips slog WARN/ERROR → all land as info.

## Next run checklist

1. Switch to PostHog project 407550 (Personal CFO) — project 371628 returns 403.
2. `query-error-tracking-issues-list` (active, 7d) → expect 0.
3. `logs-count` WITH `query` wrapper: `{"query": {"serviceNames": ["cfo-cloud"], "severityLevels": ["warn","error","fatal"], "dateRange": {"date_from": "-7d"}}}` → expect 0.
4. `query-logs` (info, last 1h) → check `service.version` for new git hash.
5. `git log --oneline -5` → check for new commits.
6. Skip Linear tool calls — SSE transport is past its removal date.
7. **CRITICAL**: Always use `query` wrapper in `logs-count` calls — bare calls (no wrapper) return wrong counts.
8. If clean and no new commits: monitoring-only. Do NOT re-run full codebase scan.

---

# 2026-05-31 — Session 10: Monitoring-only, clean state

## PostHog state (2026-05-31 session 10)

| Check | Result |
|---|---|
| Active error tracking issues (7d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (unchanged — all PRs #750–#798 still not deployed) |
| Provider sync | `partial_failure connections=11 transactions=87 errors=5` (structural Akahu disconnects) |
| New commits | None since `6f6ea30` (session 9 changelog) |
| All PostHog issues (30d) | 2 suppressed CLICommandError (local Mac CLI), 17 resolved (unchanged) |

## Reasoning (session 10)

Ran mandatory checklist per session 9 instructions:

1. ✅ `query-error-tracking-issues-list` (active, 7d, project 407550) → 0 active issues
2. ✅ `logs-count` (warn+error+fatal, 7d, cfo-cloud) → 0
3. ✅ `query-logs` (info, last 1h, cfo-cloud) → `service.version = 3f3ec2a...` unchanged
4. ✅ `git log --oneline -5` → latest commit is `6f6ea30` (session 9 changelog), no new code
5. ✅ All PostHog issues (all statuses, 30d, filterTestAccounts=false) → 2 suppressed CLICommandError (local Mac CLI), 17 resolved from early May (unchanged)

**No actionable errors.** The user requested: "for each distinct error assign it to me, create a simple PR that addresses the issue." There are zero active PostHog errors — nothing to assign, no PR warranted.

Known standing issues (not code bugs, not actionable):
- **Structural `errors=5`**: 5 Akahu connections failing per sync cycle — broken tokens needing re-auth, not a code defect. Fix already shipped in #780 (Akahu 404 → disconnected), awaiting deploy.
- **Log severity**: stdout collector maps all slog levels → `severity_number=9` (info). Infrastructure-level, not addressable via source-code PRs.
- **2 suppressed CLICommandErrors**: local Mac `personal-cfo` CLI at `/Users/andrew/code/personal-cfo/cmd/cfo/main.go` — different repo, nothing to do from `probably`.
- **Linear MCP SSE transport**: past removal date 2026-04-08 — skip all Linear tool calls.
- **PostHog assignee API**: HTTP 500 on every attempt — cannot assign issues programmatically.

## Changes shipped (session 10)

None.

## What was left behind (session 10)

- Service version `3f3ec2a...` still running (PRs #750–#798 merged but not yet deployed).
- Structural `errors=5` in provider sync: broken Akahu connections requiring re-auth — fixed in code (#780), awaiting deploy.
- Log severity not surfacing in PostHog: stdout collector strips slog WARN/ERROR → all land as info.

## Next run checklist

1. Switch to PostHog project 407550 (Personal CFO) — project 371628 returns 403.
2. `query-error-tracking-issues-list` (active, 7d) → expect 0.
3. `logs-count` → `{"query": {"serviceNames": ["cfo-cloud"], "severityLevels": ["warn","error","fatal"], "dateRange": {"date_from": "-7d"}}}` → expect 0.
4. `query-logs` (info, last 1h) → check `service.version` for new git hash.
5. `git log --oneline -5` → check for new commits.
6. Skip Linear tool calls — SSE transport is past its removal date.
7. If clean and no new commits: monitoring-only. Do NOT re-run full codebase scan.

---

# 2026-05-31 — Session 9: Monitoring-only, clean state

## PostHog state (2026-05-31 session 9)

| Check | Result |
|---|---|
| Active error tracking issues (7d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (unchanged — all PRs #750–#795 still not deployed) |
| Provider sync | `partial_failure connections=11 transactions=87 errors=5` (structural Akahu disconnects) |
| New commits | None since `68664b1` (session 8 changelog) |
| All PostHog issues (30d) | 2 suppressed CLICommandError (local Mac CLI), 18 resolved (unchanged) |

## Reasoning (session 9)

Ran mandatory checklist per session 8 instructions:

1. ✅ `query-error-tracking-issues-list` (active, 7d, Personal CFO project 407550) → 0 active issues
2. ✅ `logs-count` (warn+error+fatal, 7d, cfo-cloud) → 0
3. ✅ `query-logs` (info, last 1h, cfo-cloud) → `service.version = 3f3ec2a...` unchanged, `partial_failure connections=11 transactions=87 errors=5` structural
4. ✅ `git log --oneline -10` → latest commit is `68664b1` (session 8 changelog), no new code
5. ✅ All PostHog issues (all statuses, 30d, filterTestAccounts=false) → 2 suppressed CLICommandError (local Mac CLI), 18 resolved from early May (unchanged)

**Note:** Initial query hit project 371628 with HTTP 403. Switched to Personal CFO project (407550) — correct project for cfo-cloud service.

All PostHog issues remain exactly as documented in sessions 1–8. No actionable errors. No new code. Monitoring-only.

## Changes shipped (session 9)

None.

## What was left behind (session 9)

Same as sessions 1–8:
- Service version `3f3ec2a...` still running (PRs #750–#795 merged but not yet deployed).
- Structural `errors=5` in provider sync: broken Akahu connections requiring re-auth — fixed in code (#780), awaiting deploy.
- Log severity not surfacing in PostHog: stdout collector maps all slog levels → `severity_number=9` (info). Infrastructure config issue.
- Linear MCP SSE transport past removal date — all calls rejected. Skip.
- PostHog assignee API: HTTP 500 on every attempt — cannot assign issues programmatically.
- PostHog project context: default project (371628) returns 403; must always use Personal CFO project (407550).

## Next run checklist

1. Switch to PostHog project 407550 (Personal CFO) — project 371628 returns 403.
2. `query-error-tracking-issues-list` (active, 7d) → expect 0.
3. `logs-count` → `{"query": {"serviceNames": ["cfo-cloud"], "severityLevels": ["warn","error","fatal"], "dateRange": {"date_from": "-7d"}}}` → expect 0.
4. `query-logs` (info, last 1h) → check `service.version` for new git hash.
5. `git log --oneline -5` → check for new commits.
6. Skip Linear tool calls — SSE transport is past its removal date.
7. If clean and no new commits: monitoring-only. Do NOT re-run full codebase scan.

---

# 2026-05-31 — Session 8: Monitoring-only, clean state

## PostHog state (2026-05-31 session 8)

| Check | Result |
|---|---|
| Active error tracking issues (7d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (unchanged — all PRs #750–#795 still not deployed) |
| New commits | None since `847a59e` (session 7 changelog) |
| All PostHog issues (30d) | 2 suppressed CLICommandError (local Mac CLI), 18 resolved (all from early May, unchanged) |

## Reasoning (session 8)

Ran mandatory checklist per session 7 instructions:

1. ✅ `query-error-tracking-issues-list` (active, all statuses, 30d, filterTestAccounts=false) → 0 active. Only suppressed/resolved from early May.
2. ✅ `logs-count` (warn+error+fatal, 30d, cfo-cloud) → 0
3. ✅ `query-logs` (info, last 1h, cfo-cloud) → `service.version = 3f3ec2a...` unchanged
4. ✅ `git log --oneline -5` → latest commit is `847a59e` (session 7 changelog), no new code

All PostHog issues remain exactly as documented in sessions 1–7:
- 2 suppressed **CLICommandError** from local Mac `personal-cfo` CLI (`/Users/andrew/code/personal-cfo/...`) — not fixable from `probably` repo.
- 18 resolved issues from early May all belong to the old `personal-cfo` module (`github.com/asomervell/personal-cfo/internal/cloud/server.go`). These are a separate codebase. All resolved in PostHog.
- No errors from `github.com/asomervell/probably` module.

No actionable errors. Monitoring-only session.

## Changes shipped (session 8)

None.

## What was left behind (session 8)

Same as sessions 1–7:
- Service version `3f3ec2a...` still running (PRs #750–#795 merged but not yet deployed).
- Structural `errors=5` in provider sync: broken Akahu connections requiring re-auth — fixed in code (#780), awaiting deploy.
- Log severity not surfacing in PostHog: stdout collector maps all slog levels → `severity_number=9` (info). Infrastructure config issue.
- Linear MCP SSE transport past removal date — all calls rejected. Skip.
- PostHog assignee API: HTTP 500 on every attempt — cannot assign issues programmatically.

## Next run checklist

1. `query-error-tracking-issues-list` (active, 7d) → expect 0.
2. `logs-count` → `{"query": {"serviceNames": ["cfo-cloud"], "severityLevels": ["warn","error","fatal"], "dateRange": {"date_from": "-7d"}}}` → expect 0.
3. `query-logs` (info, last 1h) → check `service.version` for new git hash.
4. `git log --oneline -5` → check for new commits.
5. Skip Linear tool calls — SSE transport is past its removal date.
6. If clean and no new commits: monitoring-only. Do NOT re-run full codebase scan.

---

# 2026-05-31 — Session 7: Monitoring-only, clean state

## PostHog state (2026-05-31 session 7)

| Check | Result |
|---|---|
| Active error tracking issues (7d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (unchanged — all PRs #750–#793 still not deployed) |
| Provider sync | `partial_failure connections=11 transactions=86 errors=5` (structural Akahu disconnects) |
| New commits | None since `082e936` (session 6 changelog) |
| All PostHog issues (30d) | 2 suppressed CLICommandError (local Mac CLI), 18 resolved (unchanged) |

## Reasoning (session 7)

1. ✅ `query-error-tracking-issues-list` (active, 7d) → 0
2. ✅ `logs-count` (warn+error+fatal, 7d, cfo-cloud) → 0
3. ✅ `query-logs` (info, last 1h, cfo-cloud) → `service.version = 3f3ec2a...` unchanged
4. ✅ `git log --oneline -10` → latest commit is `082e936` (session 6 changelog), no new code
5. ✅ All PostHog issues (30d, filterTestAccounts=false) → only suppressed CLICommandErrors and resolved issues from early May

Full checklist ran. No actionable errors.

- **3 PostHog suppression rules** (ProviderSyncError, SyncAccountPullError, ProviderPullError — from old codebase naming, created May 7) — still present, not relevant to current code.
- **`errors=5` pattern**: Each sync cycle 5 Akahu connections fail (non-transient, non-disconnect). Root cause confirmed as broken Akahu tokens needing re-auth. Fix already shipped in commit `8ddc680` (treats Akahu 404 as disconnected), awaiting deployment.
- **Log severity architecture**: stdout collector → all slog levels arrive as `severity_number=9` (info) in PostHog. No WARN/ERROR severity visible. Infrastructure-level issue, not code.
- **Linear MCP**: SSE transport deprecated past removal date — all calls rejected.
- **PostHog issue assignee API**: HTTP 500 on every attempt — cannot programmatically assign issues.

No code changes warranted. Monitoring-only.

## Changes shipped (session 7)

None.

## What was left behind (session 7)

- Same as session 6: suppressed CLICommandErrors from local Mac CLI; resolved issues from early May all handled.
- Service version `3f3ec2a...` still running (PRs #750–#793 merged but not yet deployed).
- Structural `errors=5` in provider sync: broken Akahu connections requiring re-auth — fixed in code (#780), awaiting deploy.
- Log severity not surfacing in PostHog: stdout collector strips slog WARN/ERROR → all land as info.

## Next run checklist

1. `query-error-tracking-issues-list` (active, 7d) → expect 0.
2. `logs-count` → `{"query": {"serviceNames": ["cfo-cloud"], "severityLevels": ["warn","error","fatal"], "dateRange": {"date_from": "-7d"}}}` → expect 0.
3. `query-logs` (info, last 1h) → check `service.version` for new git hash.
4. `git log --oneline -5` → check for new commits.
5. Skip Linear tool calls — SSE transport is past its removal date.
6. If clean and no new commits: monitoring-only. Do NOT re-run full codebase scan (session 7 on 2026-05-30 + sessions 1-7 on 2026-05-31 all confirmed zero findings).

---

## Context from previous sessions (read this first on next run)

- **Session 7 (2026-05-31)**: Monitoring-only — full checklist ran; 0 active PostHog errors, 0 warn/error/fatal logs, service version `3f3ec2a...` still unchanged. No new commits. No actionable issues.
- **Session 6 (2026-05-31)**: Monitoring-only — full checklist ran; 0 active PostHog errors, 0 warn/error/fatal logs, service version `3f3ec2a...` still unchanged. No new commits. No actionable issues.
- **Session 5 (2026-05-31)**: Monitoring-only — deep PostHog investigation; logs arrive via stdout collector (not OTLP bridge); all severity mapped to info. No code bugs found.
- **Session 4 (2026-05-31)**: Monitoring-only — full triage confirmed zero issues.
- **Session 3 (2026-05-31)**: Monitoring-only — full triage confirmed zero issues.
- **Session 2 (2026-05-31)**: Monitoring-only — full triage confirmed zero issues.
- **Session 1 (2026-05-31)**: Monitoring-only — full codebase scan found zero findings.
- **Session 7 (2026-05-30)**: Monitoring-only — full codebase scan found zero findings.
- **Session 6 (2026-05-30)**: PR #786 — pass request `ctx` to `buildLogoStore` in `plaid_sync.go`; remove dead `storageCtx` alias.
- **Session 5 (2026-05-30)**: PR #783 — extract `readHTTPErrorBody` helper; DRY up sync provider error parsing.
- **Session 4 (2026-05-30)**: PR #774 — remove dead `ProcessBatch` method.
- **Session 3 (2026-05-30)**: PR #771 — bubble sort → `slices.SortFunc` in `recurring/detector.go`.
- **Session 2 (2026-05-30)**: PRs #765, #766, #767 — dead-code sweep.
- **Session 1 (2026-05-30)**: PR #753 — CaptureError → CaptureFailure in LLM handler.
- **Session 9 (2026-05-29)**: PR #750 — syncErrors++ guard; IsAkahuTransientError; fetchTransactionsPage.
- **Linear MCP**: SSE transport deprecated (past removal date 2026-04-08) — skip all Linear tool calls.
- **PostHog assignee API**: HTTP 500 on every attempt — cannot assign issues programmatically.

## PostHog state (2026-05-31 session 6)

| Check | Result |
|---|---|
| Active error tracking issues (7d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (unchanged — all PRs #750–#793 still not deployed) |
| Provider sync | `partial_failure connections=11 transactions=85 errors=5` (structural Akahu disconnects) |
| New commits | None since `74ecc40` (session 5 changelog) |
| All PostHog issues (30d) | 2 suppressed CLICommandError (local CLI), all others resolved |

## Reasoning (session 6)

1. ✅ `query-error-tracking-issues-list` (active, 7d) → 0
2. ✅ `logs-count` (warn+error+fatal, 7d, cfo-cloud) → 0
3. ✅ `query-logs` (info, last 1h, cfo-cloud) → `service.version = 3f3ec2a...` unchanged
4. ✅ `git log --oneline -10` → latest commit is `74ecc40` (session 5 changelog), no new code
5. ✅ All PostHog issues (30d, filterTestAccounts=false) → only suppressed CLICommandErrors (local Mac CLI, not addressable from `probably`) and resolved issues from early May

No actionable errors. Monitoring-only session.

## Changes shipped (session 6)

None.

## What was left behind (session 6)

- Same as session 5: suppressed CLICommandErrors from local Mac CLI; resolved issues from early May all handled.
- Service version `3f3ec2a...` still running (PRs #750–#793 merged but not yet deployed).
- Structural `errors=5` in provider sync: broken Akahu connections requiring re-auth — not a code bug.
- Log severity not surfacing in PostHog: stdout collector strips slog WARN/ERROR → all land as info.

## Next run checklist

1. `query-error-tracking-issues-list` (active, 7d) → expect 0.
2. `logs-count` → `{"query": {"serviceNames": ["cfo-cloud"], "severityLevels": ["warn","error","fatal"], "dateRange": {"date_from": "-7d"}}}` → expect 0.
3. `query-logs` (info, last 1h) → check `service.version` for new git hash.
4. `git log --oneline -5` → check for new commits.
5. Skip Linear tool calls — SSE transport is past its removal date.
6. If clean and no new commits: monitoring-only. Do NOT re-run full codebase scan (session 7 on 2026-05-30 + sessions 1-6 on 2026-05-31 all confirmed zero findings).

---

## PostHog state (2026-05-31 session 5)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| Total info logs (30d, cfo-cloud) | 142,756 |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (unchanged — all PRs #750–#791 still not deployed) |
| Provider sync | `partial_failure connections=11 transactions=85 errors=5` (structural Akahu disconnects) |
| New commits | None since `0630f12` (session 4 changelog) |

## Reasoning

Ran checklist plus deeper investigation this session:

1. ✅ `query-error-tracking-issues-list` (active, 30d) → 0 results
2. ✅ `logs-count` (warn+error+fatal, 7d, cfo-cloud) → 0
3. ✅ `query-logs` (info, last 1h, cfo-cloud) → `service.version = 3f3ec2a...` unchanged
4. ✅ `git log --oneline -10` → no new commits since session 4 changelog (branch matches origin/main at `0630f12`)

**New finding — log pipeline architecture:**
All PostHog log entries carry `instrumentation_scope: cfo-cloud@` with empty `attributes`. This proves logs arrive via a **stdout log collector** (infrastructure-level), NOT via the direct OTLP bridge (`otelslog.NewHandler("probably", ...)` in `otel.go`). Consequences:
- All slog structured attrs (trigger, status, connections, etc.) are packed into the body text as key=value, not as OTel attributes — observability dashboards/filters on those fields won't work via PostHog logs API.
- slog level (WARN/ERROR) is stripped; collector sends everything as `severity_number: 9` (info). This explains the long-standing "0 warn/error/fatal" count — WARN/ERROR calls never reach PostHog with the right severity.
- The `otelslog.NewHandler("probably", ...)` OTLP path may be active in parallel but its logs do not appear in PostHog (different instrumentation_scope and/or deduplicated away).

**Not a code bug to fix today:** The OTLP bridge in `otel.go` may be working but overridden by the collector. Fixing severity mapping would require either configuring the stdout collector to parse slog's text format, or bypassing the collector entirely. This is infrastructure-level, not addressable via source-code PRs.

**`errors=5` confirmed structural:** Log search for "account sync failed" returned 0 results (not reaching PostHog). The 5 connections are structural broken Akahu accounts needing re-authentication — not a code defect.

## Changes shipped

None. Monitoring-only session.

## What was left behind

- **Linear MCP transport**: Deprecated/dead — skip Linear tool calls on next run.
- **PostHog assignee API**: HTTP 500 on every attempt — cannot assign issues programmatically.
- **CLICommandError (019e086e, 019decf8)**: Suppressed, local Mac `personal-cfo` CLI — not fixable from `probably`.
- **Old binary `3f3ec2a...`**: Still deployed. All PRs #750–#791 merged but not running.
- **`errors=5` in provider sync**: Structural broken Akahu connections — not a code bug.
- **Log severity not surfacing in PostHog**: Stdout collector strips slog WARN/ERROR → all land as info. Infrastructure config change needed, not a code fix.

## Next run checklist

1. `query-error-tracking-issues-list` (active, 7d) → expect 0.
2. `logs-count` → `{"query": {"serviceNames": ["cfo-cloud"], "severityLevels": ["warn","error","fatal"], "dateRange": {"date_from": "-7d"}}}` → expect 0.
3. `query-logs` (info, last 1h) → check `service.version` for new git hash (expect `0630f12` until redeployed).
4. `git log --oneline -5` → check for new commits.
5. Skip Linear tool calls — SSE transport is past its removal date.
6. If PostHog and logs are clean and no new commits: monitoring-only. Full codebase scan NOT needed — session 7 on 2026-05-30 confirmed zero findings; session 5 on 2026-05-31 confirmed log pipeline architecture.

---

# 2026-05-31 — Session 4: Monitoring-only, clean state
