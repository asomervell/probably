# Session log — 2026-05-11

## What I read on startup

Read `docs/changelog/20260509.md` (run 1). Prior context summary:

- PostHog: zero active issues every run. All resolved issues in the 30-day window come from the `personal-cfo` binary (`/app/internal/cloud/server.go`, `/Users/andrew/code/personal-cfo/...`) — a separate binary. Nothing actionable in `probably`.
- Previous runs shipped PRs #328–#361 covering: slog context, dead types, WebSocket CSRF, Plaid JWT, Teller credential clearing, sync/worker slog, MCP distinct_id, Plaid http timeout, sync worker provider guard, Teller sync error capture, redundant ledger fetch removal, SyncLogf dead wrapper, webhook+multi-sync PostHog capture, orchestrator DRY, dead code removal (detector/IsSpendableByDefault/FindMatchingAliases), DB column fix, regexp pre-compilation (HAM-4689 #361).
- Left-behind items from 20260509: `api_patterns.go` dead `_ = recurring.NewDetector`, POST-based Teller Connect GET param, `containsLastFour` dynamic regexps.

## Run 1 — 2026-05-11

### What I reasoned on

**PostHog audit (active, last 7d):** zero active issues ✓  
**PostHog audit (all-status, last 30d):** 14 issues, all `resolved`, all from `personal-cfo` binary — same pattern as all prior runs. Two new entries since 20260509: `ProviderBalancePullError` (2026-05-08, 2 occurrences, resolved) and `CLICommandError` (2026-05-08, arm64 source, resolved). Both personal-cfo, nothing to do.

**Recent commits since last run (after `3ac62fd3`):**
- `HAM-4694` cmd/server: slog post-init
- `HAM-4695` handlers: remove APIPatternsDetect noop loop ✓ (checklist item resolved)
- `HAM-4696` orchestrator: remove redundant SetTag/AddBreadcrumb dead logging
- `HAM-4697/4698` processing/worker + sync/teller: rows.Err() checks
- `HAM-4699` db: rows.Err() in api_patterns, insight, suggestions
- `HAM-4700` sync/teller: rows.Err() after Opening Balance scan
- `HAM-4701` cmd/util: rows.Err() + swallowed scan errors
- `HAM-4704` models: rows.Err() in UnlinkTransferPair
- `HAM-4705` sync/akahu: remove blank-identifier assignments
- `HAM-4706` handlers: APIPatternsDetect now calls QueueForPatternDetection ✓ (checklist item resolved)
- `HAM-4707` processing/worker: log markTransactionsFailed DB errors
- `HAM-4708/4709` sync,worker: log swallowed errors; decouple enrichment-step
- `HAM-4710/4711` cmd/server,worker,observability: slog post-init + log swallowed writes
- `HAM-4712/4713` Remove dead code: TransactionStore methods, containsString, custom itoa
- `HAM-4714` handlers/voice: downgrade per-chunk slog.Info to slog.Debug ✓ (previously deferred)
- `HAM-4716/4717` processing/worker,models: fix enrichment step gating; replace itoa wrapper (#384)
- `HAM-4744` processing/worker: log swallowed UpdateCategorizationStatus errors
- `HAM-4746` handlers/teller: downgrade verbose per-operation Info logs to Debug
- `HAM-4747` handlers/teller: capture enrollment-disconnected credential-clear failures to PostHog
- `HAM-4748` handlers/teller: capture connect/link sync failures to PostHog

**Proactive code audit findings:**

`internal/voice/gemini_live.go` streaming receive goroutine (`StartStreamingRecognition`): five `slog.InfoContext` calls at lines 200, 204, 207, 213, 219 — all inside the `for { stream.Recv() ... }` loop, firing on every Speech-to-Text packet. Same class of bug as HAM-4714 (handlers/voice.go per-chunk logs), just in the underlying streaming session implementation. No logic issues; the lifecycle logs (line 171 "config sent", 186 "starting to receive", 195 "EOF") are one-shot and correctly remain at Info.

No new dead code, swallowed errors, or rows.Err() gaps found — recent commits addressed the prior backlog comprehensively.

`api_patterns.go` `APIPatternsDetect` dead assignment — fully resolved in HAM-4695/4706 ✓.

### What I fixed and shipped

**HAM-4783** (Medium) → Done ✓. Assigned to Andrew Somervell.

**PR #389** (`claude/clever-knuth-KVF69`, squash-merged SHA `f218a7ee`):

| File | Change |
|---|---|
| `internal/voice/gemini_live.go` | Lines 200, 204, 207, 213, 219: `slog.InfoContext` → `slog.DebugContext` inside streaming receive loop |

Net: 5-line change, 1 file. Build clean.

### What was left behind

- **`personal-cfo`** — all PostHog entries from that separate binary; nothing to do in `probably`.
- **POST-based Teller Connect handoff** — access token still passed as GET query param; Teller SDK pattern, deferred again.
- **`containsLastFour` in `transfer_matcher.go`** — compiles 4 dynamic regexps per call (depend on runtime `lastFour` value, cannot pre-compile). Acceptable.

## Checklist for next run

- [ ] Query PostHog (status=active, last 7d) — expect zero active issues
- [ ] If `personal-cfo` errors reappear they are always from that separate binary — skip
- [ ] Consider POST-based Teller Connect handoff (access token in URL query param — Teller SDK limitation, low priority)

---

## Run 2 — 2026-05-11

### What I read on startup

Read `docs/changelog/20260511.md` (Run 1). Prior context: zero active PostHog issues every run. All resolved issues from `personal-cfo` binary. Recent commits HAM-4783 through HAM-4788 addressed: gemini_live Info→Debug, swallowed monthly-stats/upload errors, teller loop/per-account debug downgrades.

### What I reasoned on

**PostHog audit (active, last 7d):** zero active issues ✓

**PostHog audit (all-status, last 30d):** 14 issues, all `resolved`. Investigated whether any originated from `probably`:
- `TellerWebhookTransactionBackfillRecordError` (019defc3) — function field confirms `github.com/asomervell/personal-cfo/internal/cloud.(*Server).backfillTellerWebhookTransactions` → personal-cfo.
- `TellerConnectSessionError` (019df478) — personal-cfo (same cloud server pattern).
- `CLICommandError` entries — all personal-cfo sources (`/Users/andrew/code/personal-cfo/`, arm64 homebrew path).
- `SyncAccountPullError`, `ProviderPullError`, `ProviderSyncError`, `ProviderTransactionStoreError`, etc. — all personal-cfo.
- Log service discovery: only `cfo-cloud` appears in PostHog logs — no `probably` service emitting logs. Confirms no signal from this repo.

**Proactive code audit:**
- `internal/auth/webauthn.go` lines 264, 281: `session, _ = s.sessionStore.New(...)` — intentional gorilla/sessions fallback pattern; `New()` rarely errors, `Save()` error is propagated via return. OK.
- `internal/handlers/transactions.go` lines 237–278: multiple `_ = hdl.X.LoadEntries/LoadTags/LoadEntity/SumAmounts/List/GetPending(...)` — all best-effort enrichment for template rendering; partial results are handled gracefully. Not a bug.
- No new dead code, swallowed-errors, or rows.Err() gaps found. Recent commits addressed the backlog.

### What I fixed and shipped

Nothing. Zero active issues, no actionable code defects found.

### What was left behind

- **`personal-cfo`** — all PostHog entries from that separate binary; nothing to do in `probably`.
- **POST-based Teller Connect handoff** — access token still passed as GET query param; Teller SDK pattern, deferred again.
- **`containsLastFour` in `transfer_matcher.go`** — compiles 4 dynamic regexps per call (depend on runtime `lastFour` value, cannot pre-compile). Acceptable.

## Run 3 — 2026-05-11

### What I read on startup

Read `docs/changelog/20260511.md` (Runs 1 & 2). Prior context: zero active PostHog issues every run. All resolved issues from `personal-cfo` binary. Recent PRs: #389 (gemini_live Info→Debug), #392 (monthly-stats/upload errors), #394 (teller per-account debug downgrades).

### What I reasoned on

**PostHog audit (active, last 7d):** zero active issues ✓

**PostHog audit (all-status, last 30d):** 14 issues, all `resolved`, all same set as Run 2 — nothing new since 2026-05-09. Confirmed `cfo-cloud` is the only service in logs; zero error/fatal log entries from `probably`.

**Proactive code audit findings:**

`internal/llm/executor.go` — two LLM tool handlers silently swallowed write errors:
- `createJournalEntry` (line 892): `_ = e.tags.AddTagToTransaction(...)` — if tagging fails after the journal entry is created, the AI sees a success response but tags are missing.
- `splitTransaction` (lines 1017–1019): `_ = e.tags.CategorizeTransaction(...)` and `_ = e.tags.AddTagToTransaction(...)` — split categorisation failures were completely invisible.

No new rows.Err() gaps, no missing slog.Info-in-loop issues, no dead code found. Remaining `_ =` patterns (LoadEntries, LoadTags, best-effort enrichments, session.New) are all intentional.

### What I fixed and shipped

**HAM-4789** (Medium) → Done ✓. Assigned to Andrew Somervell.

**PR #395** (`claude/clever-knuth-CSjAj`, squash-merged SHA `80fe3653`):

| File | Change |
|---|---|
| `internal/llm/executor.go` | Add `log/slog` import; replace 3× `_ =` with `if err :=` + `slog.WarnContext` in `createJournalEntry` and `splitTransaction` |

Net: 1 file, +10 -3 lines. Build clean. CI passing. Merged directly (checks already green).

### What was left behind

- **`personal-cfo`** — all PostHog entries from that separate binary; nothing to do in `probably`.
- **POST-based Teller Connect handoff** — access token still passed as GET query param; Teller SDK pattern, deferred.
- **`containsLastFour` in `transfer_matcher.go`** — compiles 4 dynamic regexps per call. Acceptable.
- **`splitTransaction` MVP comment** — the split implementation only adds tags, not separate expense entries. Real split accounting deferred (would need `UpdateWithEntries`).

## Checklist for next run

- [ ] Query PostHog (status=active, last 7d) — expect zero active issues
- [ ] Verify `function` field on any Teller/Sync issues to confirm personal-cfo (all confirmed personal-cfo to date)
- [ ] Consider POST-based Teller Connect handoff (access token in URL query param — Teller SDK limitation, low priority)

---

## Run 4 — 2026-05-11

### What I read on startup

Read `docs/changelog/20260511.md` (Runs 1–3). Prior context: zero active PostHog issues every run. All resolved issues from `personal-cfo` binary. Recent PRs: #389 (gemini_live Info→Debug), #392 (monthly-stats/upload errors), #394 (teller per-account debug downgrades), #395 (llm/executor swallowed tag write errors). Commits HAM-4790–4796 landed since Run 3.

### What I reasoned on

**PostHog audit (active, last 7d):** zero active issues ✓

**PostHog audit (all-status, last 90d):** 14 issues, all `resolved`. No new entries since Run 3. All stack traces reference `github.com/asomervell/personal-cfo/internal/cloud.*` — separate binary, nothing to do in `probably`. Logs service: only `cfo-cloud` present; zero error/warn entries in last 30 days; 62,727 info/debug entries in 90 days.

**Recent commits since Run 3:**
- HAM-4790: handlers/plaid,billing,akahu: downgrade per-account loop Info→Debug
- HAM-4792: processing/worker,sync: log swallowed entity-update and sync-cleanup errors
- HAM-4793: processing/worker: downgrade per-row Info→Debug in pattern inheritance loops
- HAM-4794: processing/worker: log swallowed pattern-detection status update errors
- HAM-4795/4796: downgrade noisy Info→Debug in mcp/server.go and handlers/voice.go

**Proactive code audit findings:**

`internal/llm/executor.go` — four post-write `GetByID` reloads discarded the error with `txn, _ = ...`, reassigning the variable to `nil` on transient DB failure. `transactionToResponse()` dereferences `txn.ID` unconditionally → **nil pointer panic** (HAM-4797, High). Functions affected: `linkTransfer` (txn1+txn2), `unlinkTransfer` (txn), `createJournalEntry` (txn), `splitTransaction` (txn). Previously Run 3 fixed the *tag write* silencing in these same functions but missed the reload guards.

`internal/handlers/entities.go` + `handlers/akahu.go` — four `_ = *.Render(...)` calls inconsistent with the established `if err := *.Render(...); err != nil { http.Error(..., 500) }` pattern used everywhere else. Silent failures produce blank/partial 200 responses with no log entry (HAM-4798, Medium). Affected: `EntitiesList`, `EntitiesShow`, `EntitiesMerge` (entities.go), not-configured early return (akahu.go).

No new Info-in-loop issues. No new dead code. Remaining `_ =` patterns (LoadEntries, LoadTags, best-effort enrichments, gorilla/sessions, tx.Rollback defers, json.Encode to ResponseWriter) are all intentional.

### What I fixed and shipped

**HAM-4797** (High) → Done ✓. **HAM-4798** (Medium) → Done ✓. Both assigned to Andrew Somervell.

**PR #402** (`claude/clever-knuth-wa5YR`, squash-merged SHA `46d7dca1`):

| File | Change |
|---|---|
| `internal/llm/executor.go` | 4 post-write reloads: `txn, _ = GetByID(...)` → `if reloaded, err := ...; err == nil { txn = reloaded }` |
| `internal/handlers/entities.go` | 3× `_ = pageNode.Render(w)` → `if err := ...; err != nil { http.Error(..., 500) }` |
| `internal/handlers/akahu.go` | 1× `_ = page.Render(&buf)` → same pattern with early return |

Net: 3 files, +32 -13 lines. Build clean. CI passed. Merged directly.

### What was left behind

- **`personal-cfo`** — all PostHog entries from that separate binary; nothing to do in `probably`.
- **POST-based Teller Connect handoff** — access token still passed as GET query param; Teller SDK pattern, deferred.
- **`containsLastFour` in `transfer_matcher.go`** — compiles 4 dynamic regexps per call. Acceptable.
- **`splitTransaction` real accounting** — implementation only adds tags, not separate expense entries. Deferred.

## Checklist for next run

- [ ] Query PostHog (status=active, last 7d) — expect zero active issues
- [ ] Verify `function` field on any Teller/Sync issues to confirm personal-cfo (all confirmed to date)
- [ ] Consider POST-based Teller Connect handoff (low priority)
