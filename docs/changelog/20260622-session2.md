# 2026-06-22 session 2 — 0 active errors; telemetry silent (36+ days)

## Prior context loaded

Read `docs/changelog/20260622.md`. Key facts carried forward:
- Repo: `asomervell/probably`, last real code commit: `1cd136b` (2026-06-17, PR #92)
- All 35 PostHog error tracking issues = `resolved`, 0 active
- App OTel telemetry: 0 logs/events for 35+ days — silent
- PostHog MCP server never connects — use REST API with `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID=88871`
- Auto-merge not enabled at repo level — cannot enable via API
- Fixed PRs to date: #3, #5, #9, #26, #57, #58, #92

## Checks run (2026-06-22 session 2)

| Check | Result |
|---|---|
| Active error tracking issues | **0 active** (20 returned, all `status: resolved`) |
| Exception events (`$exception`, last 7d) | **0** |
| Log rows (any level, last 7d) | **0** |
| PostHog `$exception` events (all time) | 100 total, all from 2026-04-25 |
| Main HEAD | `a4bdd5b` — docs changelog only; last real code: `1cd136b` (2026-06-17) |

## What I reasoned on

User instruction was: look at PostHog errors, plan fixes, create PRs, mark resolved.

Investigated PostHog thoroughly:

**Two distinct error types found (all from 2026-04-25):**

1. **Gemini thought_signature missing** (96 occurrences) — `comp=orchestrator op=execute_task`, `comp=processing_worker op=categorize_batch`. Error: `LLM API error 400: Function call is missing a thought_signature in functionCall parts`. Model: `google/gemini-3.1-flash-lite-preview`. Root cause: Gemini thinking models require thought signatures to be preserved across multi-turn tool-calling conversations; when the stored function call was reconstructed without its original thought signature, the API rejected the next request.

   **Already fixed** by prior sessions: `simple.go` has `allowToolCalls=false` for Google non-Vertex path; `thoughtSignaturesConsistent()` guard preserves `RawVertexParts`; `thinkingConfig()` sets `ThinkingBudget=0` when tool history present.

2. **OTEL too many AI spans** (4 occurrences) — `comp=otel op=export`. Error: `Too many AI spans: 246 exceeds limit of 100`. Root cause: PostHog's `/i/v0/ai/otel` endpoint rejects batches with >100 AI spans.

   **Already fixed**: `otel.go` uses `MaxExportBatchSize(50)` and `MaxQueueSize(75)`.

**Conclusion**: All errors are from 2026-04-25 and have been fixed in prior sessions. The code already contains the necessary workarounds and guards. Creating new PRs or issues for already-resolved errors would be manufacturing work.

## Shipped this session

- Memory log: `docs/changelog/20260622-session2.md`

## What was left behind (unchanged)

- **App telemetry silent**: 0 OTel logs/events reaching PostHog for 36+ days. Production binary likely missing OTel exporter config. Not actionable from this session.
- **Operational noise**: Akahu connections with expired OAuth tokens — users must reconnect via app UI.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Auto-merge not enabled at repo level — cannot enable via API.
- Fixed PRs: #3, #5, #9, #26, #57, #58, #92.

## Next run checklist

1. **READ THIS FILE FIRST** — `asomervell/probably`, last updated 2026-06-22 session 2.
2. `git fetch origin main && git log --oneline origin/main -6`. Last known real code HEAD: `1cd136b` (2026-06-17).
3. PostHog REST: `GET /api/projects/88871/error_tracking/issues/?status=active` — expect 0 truly active.
4. PostHog `$exception` events: last 7d — expect 0.
5. If 0 active errors AND 0 new code issues, write changelog and stop. **Do NOT manufacture work.**
6. All 35 PostHog error-tracking issues are `resolved`. Do not reinvestigate 2026-04-25 errors.
7. PostHog assignee API: HTTP 500 — do not attempt to assign issues programmatically.
8. Auto-merge not enabled — do not attempt via API.
9. No CI workflows on this repo — do not chase phantom CI failures.
10. Fixed PRs: #3, #5, #9, #26, #57, #58, #92. Do not reinvestigate.
11. Close any stale duplicate docs PRs before creating a new one.
12. PostHog credentials: `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID=88871` available in env; use direct REST API (PostHog MCP server never connects).
