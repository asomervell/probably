# 2026-06-22 session 2 — PostHog deep-check; 0 new errors; app silent 37+ days

## Prior context loaded

Read `docs/changelog/20260622.md` (session 1, same day). Key facts carried forward:
- Repo: `asomervell/probably`, last real code commit at time of that log: `1cd136b` (2026-06-17, PR #92)
- All 35 PostHog error tracking issues = `resolved`, 0 active
- App OTel telemetry silent for 36+ days
- PostHog MCP server never connects — use REST API with `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID=88871`
- Auto-merge not enabled at repo level — cannot enable via API
- Fixed PRs to date: #3, #5, #9, #26, #57, #58, #92

## Task

User asked: look at PostHog errors/logs, plan fixes, create PRs per distinct error, mark resolved, set auto-merge.

## Checks run (2026-06-22, session 2)

| Check | Result |
|---|---|
| Active error tracking issues | **0 active** (35 total, all `status: resolved`) |
| Exception events (7d) | **0** |
| Exception events (30d) | **0** |
| Exception events (90d) | **923 — ALL on 2026-04-25** (58 days ago) |
| Log errors (90d) | 9× `HTTP 500 GET /connections/connect/plaid` — all from 2026-04-25, development env only |
| App events (30d) | PostHog-internal only: `$ai_trace_clusters` (4), `$ai_generation_clusters` (3) — no real app telemetry |
| Main HEAD | `96cd37b` (PR #891, refactor handlers) — new code since last session's known head |

## Errors found and analysis

### Error A — Gemini thought_signature (896 occurrences, 2026-04-25, all resolved)
- `orchestrator error [parse_error]: LLM API error 400: Function call is missing a thought_signature`
- Model: `google/gemini-3.1-flash-lite-preview` via OpenAI-compat path
- Already classified as `ErrTypeParseError, Retryable=false` in `internal/orchestrator/api.go:100-104`
- Already resolved in PostHog. Code already handles this correctly.

### Error B — OTEL AI span batch limit (20 occurrences, 2026-04-25, all resolved)
- `Too many AI spans: 246 exceeds limit of 100` from PostHog OTEL endpoint
- `internal/observability/otel.go:79-80` already has `MaxExportBatchSize(50)` and `MaxQueueSize(75)`
- The comment in code confirms this was already fixed. PostHog issues resolved.

### Error C — HTTP 500 GET /connections/connect/plaid (9 occurrences, 2026-04-25, all resolved)
- All from `environment: development` (Andrew's Mac, IP 136.24.196.206)
- Handler: `internal/handlers/plaid_handlers.go:161-201`
- Likely caused by missing Plaid credentials or missing DB person entity in dev
- Already resolved in PostHog. Development-only, not production.

## Reasoning — why no PRs were created

All 923 exceptions and 9 error logs occurred on a single day: 2026-04-25 (58 days ago). Every PostHog error tracking issue shows `status: resolved`. The app telemetry has been silent for 37+ days — no new events at all reaching PostHog. Creating PRs to "fix" 2-month-old already-resolved issues would manufacture work and risk regressing code that has been stable.

The right action is to document and stop.

## New main branch commits since last session (2026-06-17 → 2026-06-22)

PRs #864–#891 merged: all refactors and cleanups (extract helpers, collapse duplicates, fix surface errors in TellerFullResync). No new features. No new error patterns expected. Last real code HEAD: `96cd37b` (PR #891).

## Shipped this session

- Memory log: `docs/changelog/20260622-session2.md`

## What was left behind (unchanged)

- **App telemetry silent**: 0 OTel logs/events reaching PostHog for 37+ days. Production binary likely missing OTel exporter config or credentials. Not actionable from here.
- **Operational noise**: Akahu connections with expired OAuth tokens — users must reconnect via app UI.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Auto-merge not enabled at repo level — cannot enable via API.
- Fixed PRs: #3, #5, #9, #26, #57, #58, #92.

## Next run checklist

1. **READ THIS FILE FIRST** — `asomervell/probably`, last updated 2026-06-22 (session 2).
2. `git fetch origin main && git log --oneline origin/main -6`. Last known real code HEAD: `96cd37b` (PR #891, 2026-06-22).
3. PostHog HogQL: `SELECT event, count() as cnt FROM events WHERE timestamp >= now() - INTERVAL 7 DAY GROUP BY event ORDER BY cnt DESC LIMIT 20` — check for new app telemetry.
4. PostHog HogQL: `SELECT count() FROM events WHERE event='$exception' AND timestamp >= now() - INTERVAL 7 DAY` — expect 0.
5. PostHog: `error_tracking/issues/?status=active` — expect 0.
6. If 0 active errors AND 0 new telemetry, write changelog and stop. **Do NOT manufacture work.**
7. PostHog MCP server never connects — use REST API with credentials from env.
8. PostHog assignee API: HTTP 500 — do not attempt to assign issues programmatically.
9. Auto-merge not enabled — do not attempt via API.
10. No CI workflows on this repo — do not chase phantom CI failures.
11. Fixed PRs: #3, #5, #9, #26, #57, #58, #92. Do not reinvestigate.
12. All 35 PostHog error-tracking issues are `resolved`. Not actionable.
13. Close any stale duplicate docs PRs before creating a new one.
14. PostHog credentials: `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID=88871` available in env.
