# Session log — 2026-05-09

## What I read on startup

Read `docs/changelog/20260508.md` (runs 1–6). Prior context summary:

- All PostHog errors originate in the old `personal-cfo` binary (`internal/cloud/server.go`), not in `probably`. Every issue has status `resolved`.
- Runs 2–6 shipped PRs #328–#341 covering: slog context, dead types, WebSocket CSRF, Plaid JWT verification (+tests in HAM-4683), Teller credential clearing, Teller log bugs, sync/worker slog, processing/worker slog, MCP distinct_id, Plaid http client timeout, sync worker provider guard, Teller sync error capture, redundant ledger fetch removal, SyncLogf dead wrapper, webhook+multi-sync PostHog capture, orchestrator DRY, dead code (detector/IsSpendableByDefault/FindMatchingAliases), and DB column fix for transfer pair restore.
- Deferred items: `plaid_verify.go` test (done in HAM-4683 ✓), POST-based Teller Connect handoff (access token in GET param, Teller SDK pattern, still deferred).

## Run 1 — 2026-05-09

### What I reasoned on

**PostHog audit (active, last 7d):** zero active issues ✓  
**PostHog audit (all-status, last 30d):** 14 issues, all `resolved`, all from `github.com/asomervell/personal-cfo/internal/cloud.(*Server).*` — separate binary, nothing actionable in `probably`. Same population as runs 3 and 5 of 20260508.

**Recent commits since last run (after `2bbcf88`):**
- `a2ca6df` HAM-4681: processing/worker feedWork() slog (left-behind from 20260508 run 6 checklist ✓)
- `54ee694` HAM-4680: remove SyncLogf dead wrapper
- `b6d1d16` HAM-4679: capture non-disconnect sync errors to PostHog
- `e86a565` HAM-4677: redundant ledger fetch
- `0e174c4` HAM-4676/HAM-4161: sync worker provider guard
- `0c37932` HAM-4675: Plaid http client timeout
- `8227787` HAM-4673/4674: MCP distinct_id + dead schema param
- `0397321` HAM-4672: processing/worker slog
- `783e6c1` HAM-4683: plaid_verify tests + slog
- `83703b4` HAM-4684/4685: webhook + multi-sync PostHog capture
- `c90a831` HAM-4686: orchestrator DRY
- `1864dc7` HAM-4688: DB column fix for transfer pair restore
- `3b72adc` #360: dead code removal (detector, IsSpendableByDefault, FindMatchingAliases)

**Proactive code audit findings:**
- `sync/transfer_matcher.go` `scoreMatch`: 13 income-pattern strings + 6 transfer-pattern strings declared as local `[]string` inside `scoreMatch`, matched via `regexp.MatchString(pattern, s)` — each compiles a regexp on every call. `scoreMatch` is a hot path called for every candidate pair during sync.
- `handlers/chat.go` `extractSimilarityTarget`: 4 `regexp.MustCompile` calls inside the function body, compiled on every chat message. (The adjacent `similarityPatterns` var is already correctly pre-compiled.)
- `chat/validator.go` `normalizeSQL` + `onlyWhitelistedTables`: 5 `regexp.MustCompile` calls compiled on every SQL validation request.

All three are the same class of bug: static patterns that should be package-level vars. No new security bugs found. `LoadEntries`/`LoadTransactions` swallowed errors are intentional best-effort enrichment. `clearSessionData session.Save` swallowed error is intentional (best-effort cleanup). OAuth `api_key_plaintext` in DB is intentional by design (ChatGPT integration requirement).

### What I fixed and shipped

**HAM-4691** (Medium) → Done ✓. Assigned to Andrew Somervell.

**PR #361** (`claude/ham-4689-precompile-regexps`, squash-merged SHA `3ac62fd3`):

| File | Change |
|---|---|
| `sync/transfer_matcher.go` | Add package-level `incomeRegexps []*regexp.Regexp` (13 items) and `transferRegexps []*regexp.Regexp` (6 items); replace local slice + `regexp.MatchString` loops with pre-compiled `.MatchString` |
| `handlers/chat.go` | Hoist `extractSimilarityTarget`'s 4 patterns to `extractSimilarityTargetPatterns` package-level var |
| `chat/validator.go` | Hoist 5 patterns from `normalizeSQL`/`onlyWhitelistedTables` to package-level `re*` vars |

Net: −2 lines across 3 files. Build and unit tests clean.

### What was left behind

- **`personal-cfo`** — all PostHog entries from that separate binary; nothing to do in `probably`.
- **POST-based Teller Connect handoff** — access token still passed as GET query param; Teller SDK pattern, noted for future hardening.
- **`containsLastFour` in `transfer_matcher.go`** — compiles 4 dynamic regexps per call (depend on `lastFour` value, so can't be pre-compiled at package level). Acceptable; called once per candidate, not in a tight inner loop.
- **`api_patterns.go`** — `APIPatternsDetect` creates `_ = recurring.NewDetector(h.db.Pool)` (dead assignment, linter-visible). Noted for next run.
- **`oauth/token_store.go`** — uses direct `==`/`!=` comparison for `pgx.ErrNoRows` rather than `errors.Is`; acceptable since pgx does not wrap this error. Noted for review.
- **Feature TODOs** (`voice/gemini_live.go:255`, `handlers/voice.go:736`, `handlers/plaid.go:116,200,215`) — feature gaps, out of scope.

## Checklist for next run

- [ ] Query PostHog (status=active, last 7d) — expect zero active issues
- [ ] If `personal-cfo` errors reappear they are always from that separate binary — skip
- [ ] Investigate `api_patterns.go` `APIPatternsDetect`: `_ = recurring.NewDetector(h.db.Pool)` is dead code — should either use the detector or remove it
- [ ] Consider POST-based Teller Connect handoff (access token in URL query param)
