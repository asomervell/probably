# 2026-05-30 — Session 7: Monitoring-only, clean state

## Context from previous sessions (read this first on next run)

- **Session 6 (2026-05-30)**: PR #786 — pass request `ctx` to `buildLogoStore` in `plaid_sync.go`; remove dead `storageCtx` alias.
- **Session 5 (2026-05-30)**: PR #783 — extract `readHTTPErrorBody` helper; DRY up sync provider error parsing.
- **Session 4 (2026-05-30)**: PR #774 — remove dead `ProcessBatch` method.
- **Session 3 (2026-05-30)**: PR #771 — bubble sort → `slices.SortFunc` in `recurring/detector.go`.
- **Session 2 (2026-05-30)**: PRs #765, #766, #767 — dead-code sweep.
- **Session 1 (2026-05-30)**: PR #753 — CaptureError → CaptureFailure in LLM handler.
- **Session 9 (2026-05-29)**: PR #750 — syncErrors++ guard; IsAkahuTransientError; fetchTransactionsPage.
- **Linear MCP**: SSE transport deprecated (past removal date 2026-04-08) — skip all Linear tool calls.
- **PostHog assignee API**: HTTP 500 on every attempt — cannot assign issues programmatically.

## PostHog state (2026-05-30 session 7)

| Check | Result |
|---|---|
| Active error tracking issues (7d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| All issues (30d, all statuses) | **20** — 2 suppressed CLICommandError, 18 resolved (unchanged) |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (old binary — PRs #750–#786 still not deployed) |
| Provider sync | `partial_failure connections=11 transactions=89 errors=5` (old binary) |
| New commits since session 6 | None — branch is in sync with `origin/main` at `0e8e93a` |

## Reasoning

PostHog clean. Ran full Explore-agent scan across all remaining unswept packages: `chat/`, `mcp/`, `auth/`, `billing/`, `orchestrator/`, `pulse/`, `categorize/`, `insights/`, `handlers/`, `db/`, `models/`. Agent verified 130 tool uses across 388s — **zero high-confidence dead code or DRY violations found**. All exported functions and types have confirmed callers. Previous sessions have cleaned the codebase thoroughly.

## Changes shipped

None. Monitoring-only session.

## What was left behind

- **Linear MCP transport**: Deprecated/dead — skip Linear tool calls on next run.
- **PostHog assignee API**: Still HTTP 500.
- **CLICommandError (019e086e, 019decf8)**: Suppressed, local Mac `personal-cfo` CLI — not fixable from `probably`.
- **Old binary `3f3ec2a...`**: Still deployed. PRs #750–#786 merged but not running.
- **`errors=5` in provider sync**: Structural broken Akahu connections — not a code bug.

## Next run checklist

1. `query-error-tracking-issues-list` (active, 7d) → expect 0.
2. `logs-count` → `{"query": {"serviceNames": ["cfo-cloud"], "severityLevels": ["warn","error","fatal"], "dateRange": {"date_from": "-7d"}}}` → expect 0.
3. `query-logs` (info, last 1h) → check `service.version` for new git hash (expect `0e8e93a` or later once redeployed).
4. `git log --oneline -5` → check for new commits since session 7.
5. Skip Linear tool calls — SSE transport is past its removal date and will reject.
6. If PostHog and logs are clean and no new commits: note monitoring-only and write brief changelog. Don't re-run the full codebase scan (session 7 confirmed zero findings across all packages).

---

# 2026-05-30 — Session 6: Fix context propagation in Plaid logo sync (PR #786)

## Context from previous sessions (read this first on next run)

- **Session 5 (2026-05-30)**: PR #783 — extract `readHTTPErrorBody` helper; DRY up sync provider error parsing.
- **Session 4 (2026-05-30)**: PR #774 — remove dead `ProcessBatch` method.
- **Session 3 (2026-05-30)**: PR #771 — bubble sort → `slices.SortFunc` in `recurring/detector.go`.
- **Session 2 (2026-05-30)**: PRs #765, #766, #767 — dead-code sweep.
- **Session 1 (2026-05-30)**: PR #753 — CaptureError → CaptureFailure in LLM handler.
- **Session 9 (2026-05-29)**: PR #750 — syncErrors++ guard; IsAkahuTransientError; fetchTransactionsPage.
- **Linear MCP**: SSE transport deprecated (past removal date 2026-04-08) — all Linear tool calls rejected.
- **PostHog assignee API**: HTTP 500 on every attempt — cannot assign issues programmatically.

## PostHog state (2026-05-30 session 6)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| All issues (30d, all statuses) | **20** — 2 suppressed CLICommandError, 18 resolved (unchanged) |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (old binary — PRs #750–#783 still not deployed) |
| Provider sync | `partial_failure connections=11 transactions=89 errors=5` (old binary; Akahu 404 fix #780 will reduce once deployed) |
| New commits since session 5 | None before this session (last was `6eeb188` changelog PR) |

## Reasoning

PostHog clean. Ran Explore-agent scan across `internal/sync/`, `internal/handlers/`, `internal/models/`, `internal/db/`, `cmd/`. Six findings evaluated:

1. **`context.Background()` in `buildLogoStore` calls (plaid_sync.go:954-955, 1005)** — genuine context-propagation bug: storage client init ignores request cancellation/timeout. Fixed.
2. **DRY violation: account-logo-update loop** — 4-line loop duplicated in `syncPlaidInstitutionLogo` and `syncPlaidInstitutionLogoFromWebsite`. Only superficially identical (different log messages); extracting would add complexity for minimal gain. Skipped.
3. **`syncAndPersistAccountMeta` thin wrapper** — called twice, legitimate factoring of 3 operations. Not dead. Skipped.
4. **`findAccountByAccountNumber` O(n) fetch** — loads all ledger accounts then filters in memory. No DB method for filtering by `account_number_masked`; adding one would require DB layer changes. Per-ledger account count typically <20. Skipped.
5. **`filterAccountsNeedingLogo` inlining** — called twice; inlining would be anti-DRY. Skipped.
6. **`storageCtx` alias variable** — dead variable removed as side effect of fix #1.

## Changes shipped

| PR | Issue | Files | Change |
|---|---|---|---|
| #786 (squash `640c2e2`) | #785 | `internal/sync/plaid_sync.go` | Pass `ctx` to `buildLogoStore`; remove dead `storageCtx` alias; net -1 line |

Issue #785 closed as completed. PR #786 squash-merged.

## What was left behind

- **Linear MCP transport**: Deprecated/dead — skip Linear tool calls on next run.
- **PostHog assignee API**: Still HTTP 500.
- **CLICommandError (019e086e, 019decf8)**: Suppressed, local Mac `personal-cfo` CLI — not fixable from `probably`.
- **Old binary `3f3ec2a...`**: Still deployed. PRs #750–#786 merged but not running.
- **`errors=5` in provider sync**: Structural broken Akahu connections — not a code bug.
- **Stale monitoring PRs** (#699, #705, #717, #724, #726, #729, #731, #736, #760, #769): Open from earlier sessions; base branches stale. No action needed.
- **`findAccountByAccountNumber` O(n)**: Low priority — small data set; would need new DB query method.

## Next run checklist

1. `query-error-tracking-issues-list` (active, 7d) → expect 0.
2. `logs-count` → `{"query": {"serviceNames": ["cfo-cloud"], "severityLevels": ["warn","error","fatal"], "dateRange": {"date_from": "-7d"}}}` → expect 0.
3. `query-logs` (info, last 1h) → check `service.version` for new git hash (expect `640c2e2` or later once redeployed).
4. `git log --oneline -5` → check for new commits since session 6.
5. Skip Linear tool calls — SSE transport is past its removal date and will reject.
6. **`findAccountByAccountNumber`**: If adding a DB query method for `account_number_masked` lookup is desired, that would be a clean targeted improvement.

---

# 2026-05-30 — Session 5: DRY readHTTPErrorBody in sync providers (PR #783)

## Context from previous sessions (read this first on next run)

- **Session 4 (2026-05-30)**: PR #774 — remove dead `ProcessBatch` method from `processing/worker.go`.
- **Session 3 (2026-05-30)**: PR #771 — bubble sort → `slices.SortFunc` in `recurring/detector.go`.
- **Session 2 (2026-05-30)**: PRs #765, #766, #767 — dead-code sweep (GetLogoURL var, getCurrentUser wrapper, unexport Aggregator helpers).
- **Session 1 (2026-05-30)**: PR #753 — CaptureError → CaptureFailure in LLM handler.
- **Session 9 (2026-05-29)**: PR #750 — syncErrors++ moved after transient-error guard; IsAkahuTransientError; fetchTransactionsPage extracted.
- **PostHog assignee API**: HTTP 500 on every attempt — cannot assign issues programmatically (all sessions).

## PostHog state (2026-05-30 session 5)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| All issues (30d, all statuses) | **20** — 2 suppressed CLICommandError, 18 resolved (unchanged) |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (old binary — PRs #750..#780 not yet deployed) |
| Provider sync | `partial_failure connections=11 transactions=87 errors=5` (old binary; Akahu 404 fix #780 will reduce errors once deployed) |
| New commits since session 4 | `ae99241` (#778 Plaid fix), `6522b68` (#779/#780 enrichment cleanup), `8ddc680` (Akahu 404 fix) — all merged to main |

## Reasoning

PostHog clean. Read session 4 checklist; confirmed origin/main updated to `8ddc680` (three PRs merged since session 4). Ran full dead-code scan (Explore agent) across sync package and all recently changed files — no dead code found in any of them. All recent commits clean.

**Only actionable finding:** `parseAkahuError` (akahu.go:62-83) and `parseTellerError` (teller.go:78-100) share an identical 4-line block for reading an HTTP error body with a warn log on failure. This was the "sync provider error-handling DRY violation" deferred since session 4 (and earlier). The body-reading block is the only truly verbatim-duplicated code between the two — the JSON parsing structs differ and must remain separate.

**Ruled out:**
- `IsAkahuTransientError` / `IsTellerTransientError` unification: each is 4 lines and called independently in 6 handler call sites (`handlers/teller.go`) — abstracting would add an interface with no net benefit.
- Entity creation pattern unification (`findOrCreateEntityFromAkahu` / `getOrCreatePlaidEntity`): different field shapes, different enrichment paths — genuine provider divergence.
- Credential sync patterns: same reasoning.

## Changes shipped

| PR | Issue | Files | Change |
|---|---|---|---|
| #783 (squash `4f8a75f`) | #782 | `internal/sync/retry.go`, `akahu.go`, `teller.go` | Extract `readHTTPErrorBody` helper; remove `io` import from both provider files; net -6 lines |

Issue #782 closed as completed. PR #783 squash-merged.

## What was left behind

- **PostHog assignee API**: Still HTTP 500 — cannot assign issues programmatically.
- **CLICommandError (019e086e, 019decf8)**: Suppressed, local Mac `personal-cfo` CLI — not fixable from `probably`.
- **Old binary `3f3ec2a...`**: Still deployed. PRs #750–#783 merged but not yet running.
- **`errors=5` in provider sync**: 5 structurally broken Akahu connections. Will resolve to ≤5 once #780 (Akahu 404 fix) deploys. Remainder are genuine disconnected accounts.
- **Stale monitoring-only PRs**: PRs #699, #705, #717, #724, #726, #729, #731, #736, #760, #769 are open draft/non-draft PRs from earlier monitoring-only sessions. Their base branches are stale (main has moved far ahead). No action needed — they'll auto-close if the repo has stale-PR policies, or can be closed manually.

## Next run checklist

1. `query-error-tracking-issues-list` (active, 7d) → expect 0.
2. `logs-count` with `query` wrapper → `{"query": {"serviceNames": ["cfo-cloud"], "severityLevels": ["warn","error","fatal"], "dateRange": {"date_from": "-7d"}}}` → expect 0.
3. `query-logs` (info, last 1h) → check `service.version` for new git hash (expect `143b08f` or later once redeployed).
4. `git log --oneline -5` → check for new commits since session 5.
5. Once new binary deployed: check `provider_sync_completed` — `errors` should drop from 5; `status` should be `success` if all 5 were Akahu 404 accounts.
6. Stale monitoring PRs (#699, #705, #717, #724, #726, #729, #731, #736, #760, #769): consider closing them if they're cluttering the PR list.

---

# 2026-05-30 — Session 4: Remove dead ProcessBatch method (PR #774)

## Context from previous sessions (read this first on next run)

- **Session 3 (2026-05-30)**: PR #771 — bubble sort → `slices.SortFunc` in `recurring/detector.go`.
- **Session 2 (2026-05-30)**: PRs #765, #766, #767 — dead-code sweep (GetLogoURL var, getCurrentUser wrapper, unexport Aggregator helpers).
- **Session 1 (2026-05-30)**: PR #753 — CaptureError → CaptureFailure in LLM handler.
- **Session 9 (2026-05-29)**: PR #750 — syncErrors++ moved after transient-error guard; IsAkahuTransientError; fetchTransactionsPage extracted.
- **PostHog assignee API**: HTTP 500 on every attempt — cannot assign issues programmatically (all sessions).

## PostHog state (2026-05-30 session 4)

| Check | Result |
|---|---|
| Active error tracking issues (7d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** (confirmed with correct `query` wrapper) |
| All issues (30d, all statuses) | **20** — 2 suppressed CLICommandError, 18 resolved (unchanged) |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (old binary — PRs #750..#771 not deployed yet) |
| Provider sync | `partial_failure connections=11 transactions=85 errors=5` (old binary; after deploy: expect `errors=0 transient_errors=5`) |
| New commits since session 3 | None before this session |

**Note:** Earlier in this session `logs-count` appeared to return 12 warn/error/fatal — this was a schema error (parameters not wrapped in `query` object). With correct call: **0**.

## Reasoning

PostHog clean. Ran codebase scan via Explore agent across all packages. Evaluated 8 findings:

- **`getString` duplication** (`orchestrator/simple.go` + `llm/handler.go`): False positive — both are package-private helpers in separate packages. `llm` already imports `orchestrator`, so deduplication would require exporting a 5-line helper. Not worth it.
- **`ProcessBatch` dead method** (`processing/worker.go:1225`): Confirmed dead — exported, never called from `cmd/`, handlers, or tests. Clear removal.
- **Sync wrappers** (`SyncAccountsWithToken`/`SyncTransactionsForAccount`): NOT dead — these satisfy the `ProviderImpl` interface and are called from `providers/akahu.go`, `providers/teller.go`, `providers/plaid.go`. Scan finding was incorrect.
- **`getOrCreateEntity` wrapper**: Private 2-line wrapper with 2 callers in the same file. Not worth touching.
- **CLI utility commands**: Intentional admin tools — not dead code.
- **Error handling duplication across sync providers**: Medium-risk (~100 lines), would require interface redesign. Deferred.
- **Interface abstraction design**: Too architectural. Deferred.
- **Numeric helpers**: Too scattered and low-value for the effort.

## Changes shipped

| PR | Issue | Files | Change |
|---|---|---|---|
| #774 (6e6903c) | #773 | `internal/processing/worker.go` | Remove dead `ProcessBatch` exported method; net -18 lines |

PR assigned to asomervell, squash-merged, issue #773 closed as completed.

## What was left behind

- **PostHog assignee API**: Still HTTP 500 — cannot assign PostHog issues programmatically.
- **CLICommandError (019e086e, 019decf8)**: Suppressed, local Mac `personal-cfo` CLI — not fixable from `probably`.
- **Old binary `3f3ec2a...`**: Still deployed. PRs #750, #753, #765–#767, #771, #774 merged but not yet running.
- **`errors=5` in `provider_sync_completed`**: Structural broken connections — not a code bug. Will disappear after redeploy.
- **Error handling duplication across sync providers**: `AkahuAPIError`/`TellerAPIError`/`PlaidItemError` + parse helpers are ~100 lines of parallel code. Medium-risk — deferred.

## Next run checklist

1. `query-error-tracking-issues-list` (active, 7d) → expect 0.
2. `logs-count` with `query` wrapper → `{"query": {"serviceNames": ["cfo-cloud"], "severityLevels": ["warn","error","fatal"], "dateRange": {"date_from": "-7d"}}}` → expect 0.
3. `query-logs` (info, last 1h) → check `service.version` for new git hash (expect `6e6903c` or later once redeployed).
4. If new hash: check `provider_sync_completed` — `errors` should be 0, `transient_errors` non-zero.
5. `git log --oneline -5` → check for new commits since session 4 (2026-05-30).
6. Consider tackling sync provider error-handling DRY violation if nothing live to triage.

---

# 2026-05-30 — Session 3: bubble sort → slices.SortFunc (PR #771)

## Context from previous sessions (read this first on next run)

- **Session 2 (2026-05-30)**: PRs #765, #766, #767 — dead-code sweep (GetLogoURL var, getCurrentUser wrapper, unexport Aggregator helpers).
- **Session 1 (2026-05-30)**: PR #753 — CaptureError → CaptureFailure in LLM handler.
- **Session 9 (2026-05-29)**: PR #750 — syncErrors++ moved after transient-error guard; IsAkahuTransientError; fetchTransactionsPage extracted.
- **PostHog assignee API**: HTTP 500 on every attempt — cannot assign issues programmatically (all sessions).

## PostHog state (2026-05-30 session 3)

| Check | Result |
|---|---|
| Active error tracking issues (7d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| All issues (30d, all statuses) | **20** — 2 suppressed CLICommandError, 18 resolved (unchanged) |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (old binary — PRs #750, #753, #765–#767 not deployed yet) |
| Provider sync | `partial_failure connections=11 transactions=85 errors=5` (old binary; after deploy: expect `errors=0 transient_errors=5`) |
| New commits since session 2 | None — latest `24d20cf` (session 2 changelog) |

## Reasoning

PostHog clean (0 active errors, 0 warn/error/fatal). No new commits since session 2. Ran a fresh codebase scan across unswept packages (`orchestrator/`, `billing/`, `mcp/`, `auth/`, `chat/`, `recurring/`, `categorize/`).

**Scan findings evaluated:**
- Consensus silent error drop (`consensus.go:158`) — intentional graceful degradation; already warns on partial consensus. No change.
- `GetVertexClient()` exported — has an external caller in `handlers/voice.go:154`. Not dead. No change.
- `json.Marshal` in chat executor — scan false positive, grep returned no matches.
- Billing `CreateCheckoutSession` — already DRY (delegates to `WithTrial` with `trialDays=0`). No change.
- **Bubble sort in `recurring/detector.go:56–62`** — confirmed O(n²) hand-rolled sort. `slices.SortFunc` already used in `pulse/calculator.go:142`. Clear fix.

## Changes shipped

| PR | Issue | Files | Change |
|---|---|---|---|
| #771 (b8c96af) | #770 | `internal/recurring/detector.go` | Replace 6-line bubble sort with `slices.SortFunc`; add `"slices"` import; net -4 lines |

PR assigned to asomervell, squash-merged, issue #770 closed as completed.

## What was left behind

- **PostHog assignee API**: Still HTTP 500 — cannot assign PostHog issues programmatically.
- **CLICommandError (019e086e, 019decf8)**: Suppressed, local Mac `personal-cfo` CLI — not fixable from `probably`.
- **Old binary `3f3ec2a...`**: Still deployed. PRs #750, #753, #765–#767, #771 merged but not yet running.
- **`errors=5` in `provider_sync_completed`**: Structural broken connections — not a code bug. Will disappear after redeploy.
- **`sort.Slice` usages in handlers/insights/models**: These use the standard sort package which is fine (O(n log n)); not worth migrating unless there's a specific reason.

## Next run checklist

1. `query-error-tracking-issues-list` (active, 7d) → expect 0.
2. `logs-count` (warn+error+fatal, `cfo-cloud`, 7d) → expect 0.
3. `query-logs` (info, last 1h) → check `service.version` for new git hash (expect `b8c96af` or later once redeployed).
4. If new hash: check `provider_sync_completed` — `errors` should be 0, `transient_errors` non-zero.
5. `git log --oneline -5` → check for new commits since session 3 (2026-05-30).
6. Packages not yet scanned in depth: none remaining — all swept across sessions 1–9 (2026-05-27) through today.

---

# 2026-05-30 — Session 2: Dead-code sweep (PRs #765, #766, #767)

## Context from previous sessions

- **Session 1 (2026-05-30)**: PR #753 — replaced 5 `CaptureError` calls in `internal/llm/handler.go` with `CaptureFailure`/`FailureOptions` for consistent PostHog properties.
- **Session 9 (2026-05-29)**: PR #750 — `syncErrors++` moved after transient-error guard; `IsAkahuTransientError`; `fetchTransactionsPage` extracted.
- **Sessions 1–8 (2026-05-29)**: Monitoring-only.
- **2026-05-27**: Full dead-code sweep — all major error classes eliminated.
- **PostHog assignee API**: HTTP 500 on every attempt — cannot assign issues programmatically (unchanged across all sessions).

## PostHog state (2026-05-30 session 2)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (30d, cfo-cloud) | **0** |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (old binary — PRs #750, #753 not deployed yet) |
| Provider sync | `partial_failure connections=11 transactions=85 errors=5` (old binary; after deploy: `errors=0 transient_errors=5 status=success`) |

## Reasoning

PostHog was clean (0 active errors, 0 warn/error/fatal). Since there were no live errors to triage, ran a full dead-code/DRY scan via an Explore agent across `internal/`. The scan returned 11 findings; evaluated each for risk/benefit.

**Skipped (low benefit or risky):**
- Orchestrator type aliases (`LLMResult = processing.LLMResult` etc.) — used heavily within the package as local shorthands; removing just adds `processing.` prefix noise everywhere.
- Plaid webhook switches → table-driven — premature; no new webhook type in flight.
- Single-implementation interfaces (`PatternDetectorInterface`, `StrategyExecutor`) — may exist for test mocking; not worth touching without test context.
- Aggregator `reports`/`insights`/`permissions` struct fields — are initialized and used; false positive from the scan.

**Actioned (3 PRs):**

### PR #765 — Remove dead `var GetLogoURL` (`fix/remove-dead-logo-url-export`)
`handlers.go` had `var GetLogoURL = enrichment.GetLogoURL` at package level with a comment "// Import GetLogoURL for fallback". It was only referenced once, inside the private `getLogoURL()` method on the same file. Replaced with direct `enrichment.GetLogoURL(...)` call. Issue #762.

### PR #766 — Remove `getCurrentUser()` wrapper (`refactor/remove-get-current-user-wrapper`)
`getCurrentUser()` on `*Handlers` was a one-liner `return auth.CurrentUser(r)` with no added logic. ~50 call sites across 20 handler files used it. API handlers already called `auth.APICurrentUser()` directly — HTMX handlers were inconsistent. Removed the method; added `"github.com/asomervell/probably/internal/auth"` import to 20 files; replaced all call sites. Issue #763.

### PR #767 — Unexport aggregator helpers (`refactor/unexport-aggregator-methods`)
`GetRecentTransactions`, `GetMonthToDate`, `GetLastMonthSummary` on `*Aggregator` had zero external callers (confirmed by grep). All three are only called from `analyzer.go` in the same `insights` package. Renamed to lowercase; updated 3 call sites. Issue #764.

## Changes shipped

| PR | Issue | Files | Change |
|---|---|---|---|
| #765 (d9233fc) | #762 | `internal/handlers/handlers.go` | Remove `var GetLogoURL`, inline `enrichment.GetLogoURL` |
| #766 (9177ee6) | #763 | `internal/handlers/handlers.go` + 20 handler files | Remove `getCurrentUser()`, update ~50 call sites |
| #767 (9270d1b) | #764 | `internal/insights/aggregator.go`, `analyzer.go` | Unexport 3 Aggregator helper methods |

All PRs: assigned to asomervell, squash-merged, issues closed as completed.

## What was left behind

- **PostHog assignee API**: Still HTTP 500 — cannot assign PostHog issues programmatically.
- **CLICommandError (019e086e, 019decf8)**: Suppressed, local Mac `personal-cfo` CLI — not fixable from `probably`.
- **Old binary `3f3ec2a...`**: Still deployed. After redeploy: expect `errors=0 transient_errors=5 status=success` in `provider_sync_completed`.
- **Orchestrator type aliases**: Skipped — internal-only convenience, low value to remove.
- **Plaid webhook switch chain**: Skipped — premature abstraction.
