# Session log — 2026-05-13

## What I read on startup

Read `docs/changelog/20260512.md` Runs 1–7. Prior context summary:

- PostHog: zero active error tracking issues every run. All resolved issues come from `cfo-cloud` / `personal-cfo` binary (separate codebase, path `github.com/asomervell/personal-cfo/internal/cloud/server.go`). Nothing actionable in `probably`.
- All prior findings shipped through HAM-4844 (chat/store + insights/aggregator swallowed errors), HAM-4840 (DeleteAndResync refactor), HAM-4841 (dead IsReadOnlyTool/IsWriteTool, DRY HashAPIKey).
- Persistent left-behinds: POST-based Teller Connect handoff (Teller SDK limitation), `containsLastFour` dynamic regexps (runtime-dependent), `splitTransaction` real accounting (deferred), `cmd/util` hidden debug commands (intentional).

## Run 1 — 2026-05-13 (earlier session, Run 1)

### What I reasoned on

**PostHog audit — error tracking (status=active, last 30d):** zero active issues ✓ (consistent with all prior runs).

**PostHog audit — logs:** `cfo-cloud` service: zero error/warn/fatal log entries in 7 days ✓. No actionable signal from PostHog for the `probably` repo.

**Proactive code audit (Explore agent — very thorough sweep of all internal/ and cmd/):**

Three findings flagged as false positives (ruled out in prior runs):
- `handlers/plaid.go:239` — fires only on rare Plaid revocation event. Acceptable at Info. (Prior: Run 6)
- `handlers/akahu.go:547` — fires only on `IsConnectionDisconnectedError` (rare). Acceptable at Info. (Prior: Runs 5, 6, 7)
- `insights/providers/chain.go:21` — fires once at server startup only. Acceptable at Info. (Prior: Run 7)

One genuine finding (new):

**`internal/models/patterns.go` — dead `PatternStore` methods and `PatternExample` type** (HAM-4847, Low):

- `GetMonthlyTotal` (line 167) — aggregates recurring bill frequency into a monthly cost total; zero callers.
- `GetHighConfidencePatterns` (line 383) — DB query for LLM context examples; zero callers.
- `GetPatternsByPatternType` (line 456) — DB query scoped by pattern type; zero callers.
- `PatternExample` struct (line 371) — type exclusively referenced by the two dead query methods above; zero external callers. `recurring/prompts.go` defines its own independent `PatternExample` type and is unaffected.

### What I fixed and shipped

**HAM-4847** (Low) → Done ✓. Assigned to Andrew Somervell.

**PR #433** (squash-merged SHA `b3fdff70`):

| File | Change |
|---|---|
| `internal/models/patterns.go` | Remove `GetMonthlyTotal`, `GetHighConfidencePatterns`, `GetPatternsByPatternType`, and `PatternExample` struct |

Net: 1 file, -180 lines. Build clean. CI passing. Merged directly.

Issue: HAM-4847 — closed as Done.

### What was left behind

- **`personal-cfo`** — all PostHog signals from that separate binary; nothing to do in `probably`.
- **POST-based Teller Connect handoff** — access token still passed as GET query param; Teller SDK limitation, deferred.
- **`containsLastFour` in `transfer_matcher.go`** — compiles 4 dynamic regexps per call (runtime-dependent values, cannot pre-compile). Acceptable.
- **`splitTransaction` real accounting** — implementation only adds tags, not separate expense entries. Deferred.
- **`cmd/util` hidden commands** (`check-tables`, `fix-insights-tables`, `list-ledgers`) — not in help, kept for manual debugging/recovery.

---

## Run 2 — 2026-05-13 (this session)

### What I reasoned on

**PostHog audit — error tracking (status=active, last 30d):** zero active issues ✓.

**PostHog audit — logs (`cfo-cloud`, last 7d + 30d):** zero warn/error/fatal entries ✓. Consistent with all prior runs.

**Proactive code audit — Explore agent sweep of `internal/chat/`, `internal/llm/`, `internal/orchestrator/`, `internal/mcp/`, `cmd/mcp-server/`, `internal/billing/`, `internal/auth/`, `internal/pulse/`:**

Findings reviewed and verified by manual file reads:

**HAM-4854 — DRY: duplicate `formatDollars` in `internal/llm`**
- `internal/llm/prompts.go:186` `formatDollars` and `internal/llm/executor.go:1452` `formatCents` are byte-for-byte identical (same package). `formatDollars` had 2 callers; `formatCents` had 8.

**HAM-4855 — dead stub + duplicate helpers in `internal/chat`**
- `internal/chat/validator.go:300` `hasUnsafeSubqueries` always returns `false` with `_ string` parameter. Call site at line 172 can never error. Both stub and call removed.
- `internal/chat/prompt.go` re-implemented four helpers verbatim from `executor.go` (same package): `getInt64ForFormatting` = `getInt64`, `getUint64ForFormatting` = `getUint64`, `parseNumericStringForFormat` = `parseNumericString`, `extractNumericFromString` ≈ `extractNumericFromStructString`. Plus `pow10` which only served the removed functions. All removed; 5 call sites in `prompt.go` redirected to plain-named versions. Unused `math` import removed.

Agent-raised findings ruled out as false positives:
- `_ = e.transactions.LoadEntries/LoadTags` patterns throughout `llm/executor.go` — intentional best-effort enrichment; prior runs confirm this is acceptable.
- `json.NewEncoder(w).Encode(data)` with `_ =` in `llm/handler.go` — standard HTTP response path, consistent with project style.
- `_, _ = w.Write(...)` in `mcp/server.go` and `mcp/context.go` — HTTP error response writes; write failures here are unactionable.
- `orchestrator/simple.go` Google/Vertex tool-call guard — conditional logic is correct business logic, not dead code.

### What I fixed and shipped

**HAM-4854** (Low) → Done ✓. **HAM-4855** (Low) → Done ✓. Both assigned to Andrew Somervell.

**PR #439** (squash-merged SHA `4c3dc0b8`):

| File | Change |
|---|---|
| `internal/llm/prompts.go` | Remove `formatDollars`, redirect 2 callers to `formatCents` |
| `internal/chat/validator.go` | Remove `hasUnsafeSubqueries` stub + its `// Check 7` call site |
| `internal/chat/prompt.go` | Remove 4 duplicate helpers + `pow10`, remove unused `math` import, update 5 call sites |

Net: 3 files, -113 lines. Build clean. CI passed. Merged directly.

### What was left behind

- **`personal-cfo`** — all PostHog signals from that separate binary; nothing to do in `probably`.
- **POST-based Teller Connect handoff** — access token still passed as GET query param; Teller SDK limitation, deferred.
- **`containsLastFour` in `transfer_matcher.go`** — compiles 4 dynamic regexps per call (runtime-dependent values, cannot pre-compile). Acceptable.
- **`splitTransaction` real accounting** — implementation only adds tags, not separate expense entries. Deferred.
- **`cmd/util` hidden commands** (`check-tables`, `fix-insights-tables`, `list-ledgers`) — not in help, kept for manual debugging/recovery.
- **`_ = e.transactions.LoadEntries/LoadTags`** — best-effort enrichment pattern, intentional.

## Run 3 — 2026-05-13 (this session)

### What I read on startup

Read `docs/changelog/20260513.md` Runs 1–2. Noted PR #441 (HAM-4856/4857/4858, SHA `b058d47`) was committed between sessions — dropped `MatchAllForAccount` wrapper, removed 4 dead reporter methods, logged swallowed `io.ReadAll` errors in Akahu/Teller sync error parsers.

### What I reasoned on

**PostHog audit — error tracking (status=active, last 30d):** zero active issues ✓. Consistent with all prior runs.

**PostHog audit — logs (`cfo-cloud`, last 30d):** zero error/warn/fatal entries ✓. `cfo-cloud` is the only registered service. Nothing actionable in `probably`.

**Proactive code audit (Explore agent — very thorough sweep of `internal/processing/`, `internal/sync/`, `internal/recurring/`, `internal/enrichment/`, `internal/categorize/`, `internal/db/`, `internal/pulse/`):**

No genuine findings. All packages are clean:
- No `slog.InfoContext` calls inside per-item loops (all Info calls are batch-level, startup, or error-gated).
- No swallowed errors beyond the documented `LoadEntries`/`LoadTags` best-effort pattern.
- No exported functions or types with zero callers.

All known false positives confirmed skipped: `handlers/plaid.go:239`, `handlers/akahu.go:547`, `insights/providers/chain.go:21`, `LoadEntries/LoadTags _ =`, MCP `w.Write` discards.

### What was shipped

- No code changes required. System is clean.
- Appended this Run 3 entry to the changelog.

### What was left behind

- **`personal-cfo`** — all PostHog signals from that separate binary; nothing to do in `probably`.
- **POST-based Teller Connect handoff** — access token still passed as GET query param; Teller SDK limitation, deferred.
- **`containsLastFour` in `transfer_matcher.go`** — compiles 4 dynamic regexps per call (runtime-dependent values, cannot pre-compile). Acceptable.
- **`splitTransaction` real accounting** — implementation only adds tags, not separate expense entries. Deferred.
- **`cmd/util` hidden commands** (`check-tables`, `fix-insights-tables`, `list-ledgers`) — not in help, kept for manual debugging/recovery.

## Run 4 — 2026-05-13 (this session)

### What I read on startup

Read `docs/changelog/20260513.md` Runs 1–3. Prior context: system clean after Runs 1–3. No outstanding findings.

### What I reasoned on

**PostHog audit — error tracking (status=active, last 30d):**

One active issue found for the first time this session:

- **`ProviderBalancePullError`** (id `019e05c7-f3d7-7331-9963-ae72ba61545d`) — 3 occurrences over 5 days (May 8, 9, 13). Distinct ID: `personal-cfo-service`. Call site: `github.com/asomervell/personal-cfo/internal/cloud.(*Server).syncIncludedConnections`. `mechanism.handled: true`. Exception value redacted by PostHog (PII filtering). Zero presence in `probably` codebase (grep confirmed).

  **Decision:** handled error in the separate `personal-cfo` binary. Marked resolved in PostHog. No PR possible in this repo.

**PostHog audit — logs (`cfo-cloud`, last 30d):** zero warn/error/fatal ✓.

**Proactive code audit (manual + Explore agent — full sweep of all `internal/` and `cmd/`):**

Two genuine findings:

**HAM-4859 — DRY: duplicate `formatCents` in `llm` and `insights/providers`**
- `internal/llm/executor.go:1452` — definition + 8 call sites (also `llm/prompts.go` lines 171, 173)
- `internal/insights/providers/prompts.go:233` — definition + 18 call sites
- Both produce identical output: `"$X.XX"` / `"-$X.XX"` (no `+` prefix, unlike `models.FormatAmount`).
- `models/format.go` already exists as the shared location. `llm` already imports `models`; `insights/providers` does not.

**HAM-4860 — 4 dead store methods in `internal/models/`**
- `transaction.go:GetQueuedForCategorization` (27 lines) — single-ledger variant; all callers use `GetAllQueuedForCategorization`
- `entity.go:GetFinancialInstitutions` (6 lines) — thin filter wrapper, never integrated
- `passkey.go:DeleteByUserID` (4 lines) — bulk passkey deletion, no code path uses it
- `report.go:GetLatestByType` (13 lines) — convenience wrapper; callers use `GetByPeriod`/`ListByLedger` directly

All confirmed via grep — zero external callers.

### What I fixed and shipped

**HAM-4859** (Low) → Done ✓. **HAM-4860** (Low) → Done ✓. Both assigned to Andrew Somervell.

**PR #443** (squash-merged SHA `6f8cb7ad`):

| File | Change |
|---|---|
| `internal/models/format.go` | Add `FormatCents` |
| `internal/llm/executor.go` | Remove `formatCents` definition, redirect 8 callers |
| `internal/llm/prompts.go` | Redirect 2 callers, add `models` import |
| `internal/insights/providers/prompts.go` | Remove `formatCents` definition, redirect 18 callers, add `models` import |
| `internal/models/transaction.go` | Remove `GetQueuedForCategorization` |
| `internal/models/entity.go` | Remove `GetFinancialInstitutions` |
| `internal/models/passkey.go` | Remove `DeleteByUserID` |
| `internal/models/report.go` | Remove `GetLatestByType` |

Net: 8 files, +7 / -97 lines. Build clean. Tests passing. Merged directly.

PostHog `ProviderBalancePullError` issue → marked resolved (handled error in personal-cfo).

### What was left behind

- **`personal-cfo`** — all PostHog signals from that separate binary; nothing to do in `probably`.
- **POST-based Teller Connect handoff** — access token still passed as GET query param; Teller SDK limitation, deferred.
- **`containsLastFour` in `transfer_matcher.go`** — compiles 4 dynamic regexps per call (runtime-dependent values, cannot pre-compile). Acceptable.
- **`splitTransaction` real accounting** — implementation only adds tags, not separate expense entries. Deferred.
- **`cmd/util` hidden commands** (`check-tables`, `fix-insights-tables`, `list-ledgers`) — not in help, kept for manual debugging/recovery.
- **`_ = e.transactions.LoadEntries/LoadTags`** — best-effort enrichment pattern, intentional.

## Checklist for next run

- [ ] Query PostHog (status=active, last 30d) — `ProviderBalancePullError` was resolved; watch for recurrence
- [ ] `cfo-cloud` logs: zero error/warn/fatal — consistent pattern
- [ ] Explore agent sweep: models, handlers, insights, llm all swept clean this run
- [ ] Known false positives to skip: plaid.go:239, akahu.go:547, chain.go:21, LoadEntries/LoadTags `_ =` pattern, MCP/handler `w.Write` discards

---

## Run 5 — 2026-05-13 (this session)

### What I read on startup

Read `docs/changelog/20260513.md` Runs 1–4. Post-Run-4 commits confirmed:
- `b9c8f7c` — HAM-4861/4862: removed 7 dead methods from `insights/analyzer.go`, `insights/worker.go`, `models/insight.go`
- `9145706` — HAM-4863: removed duplicate `formatCents` in `handlers/entities.go` (PR #446)
- `ef6d779` — HAM-4864: removed 5 dead icon exports + broken `attrs` stub from `views/shadcn/shadcn.go`

### What I reasoned on

**PostHog audit — error tracking (status=active, last 30d):** zero active issues ✓.

**PostHog audit — logs (`cfo-cloud`, last 30d):** zero warn/error/fatal entries ✓. Consistent with all prior runs.

**Linear housekeeping:** HAM-4852 and HAM-4853 were both merged in PR #438 (`a1af26e`) but remained in "Todo" state in Linear. Verified fixes in code:
- `HAM-4852`: `cleanCompanyName` line 683 — parentheses added, operator precedence fixed ✓
- `HAM-4853`: `SeedDefaultTags` child creation — `slog.WarnContext` call present before `continue` ✓

Both marked Done in Linear.

**Proactive code audit (Explore agent — very thorough sweep of `internal/handlers/`, `internal/views/`, `cmd/server/`, `internal/insights/`):**

No genuine findings. All packages are clean:
- All exported handler helpers, view components, insight methods have active callers.
- Response builders (`respondJSON`, `respondError`) are properly centralized in `handlers/api.go`.
- Struct name duplicates across packages are intentional (separate API vs LLM response shapes).
- No always-false stubs or unimplemented placeholders found.

All known false positives confirmed skipped.

### What was shipped

- No code changes required. System is clean.
- HAM-4852 and HAM-4853 marked Done in Linear (status cleanup only).
- Appended this Run 5 entry to the changelog.

### What was left behind

- **`personal-cfo`** — all PostHog signals from that separate binary; nothing to do in `probably`.
- **POST-based Teller Connect handoff** — access token still passed as GET query param; Teller SDK limitation, deferred.
- **`containsLastFour` in `transfer_matcher.go`** — compiles 4 dynamic regexps per call (runtime-dependent values, cannot pre-compile). Acceptable.
- **`splitTransaction` real accounting** — implementation only adds tags, not separate expense entries. Deferred.
- **`cmd/util` hidden commands** (`check-tables`, `fix-insights-tables`, `list-ledgers`) — not in help, kept for manual debugging/recovery.
- **`_ = e.transactions.LoadEntries/LoadTags`** — best-effort enrichment pattern, intentional.
- **Open PRs #437, #435, #428, #383, #381, #359, #358, #348** — stale open PRs on old branches, all superseded by direct squash-merges to main. Not blocking anything.

---

## Run 6 — 2026-05-13 (this session)

### What I read on startup

Read `docs/changelog/20260513.md` Runs 1–5. Prior context: all internal packages swept in Runs 1–5. Packages swept: `models`, `chat`, `llm`, `orchestrator`, `mcp`, `billing`, `auth`, `pulse`, `processing`, `sync`, `recurring`, `enrichment`, `categorize`, `db`, `handlers`, `views`, `insights`, `cmd/server`, `cmd/mcp-server`. Post-Run-5 merges: HAM-4861–4864 (dead insights/model methods, duplicate formatCents in handlers, dead shadcn icons/attrs stub).

### What I reasoned on

**PostHog audit — error tracking (status=active, last 30d):** zero active issues ✓.

**PostHog audit — logs (`cfo-cloud`, last 30d):** zero warn/error/fatal entries ✓. Consistent with all prior runs.

**Proactive code audit (Explore agent — very thorough sweep of unswept packages: `backup`, `blog`, `config`, `demo`, `embedding`, `extraction`, `ledger`, `legal`, `oauth`, `observability`, `storage`, `voice`, `cmd/`):**

Two genuine findings:

**HAM-4865 — dead `embedding/similarity.go` and `similarity_test.go`** (Low):
- `CosineSimilarity` (only used by `TopKSimilar` below)
- `EuclideanDistance`, `DotProduct`, `Normalize`, `TopKSimilar`, `AverageEmbedding`, `Float32ArrayToFloat64`, `Float64ArrayToFloat32`, `SimilarityResult` type
- All external importers of the `embedding` package use only `embedding.Service`/`embedding.NewServiceFromConfig`. Zero callers outside the file.
- Deleted both files: 168 + 269 = -437 lines.

**HAM-4866 — dead blog/voice functions + duplicate `getEnv` in cmd/server** (Low):
- `blog/loader.go:37` `PostsDir()` — exported getter, zero callers.
- `blog/loader.go:221` `ClearCache()` — exported cache-reset, zero callers.
- `voice/gemini_live.go:254` `GenerateAudio()` — "not yet implemented" stub, zero callers.
- `cmd/server/main.go:374` `getEnv()` — verbatim duplicate of `internal/config/config.go:356`. Fixed by exporting `config.GetEnv` (thin wrapper) and redirecting the one call site.

All other packages (`backup`, `config`, `demo`, `extraction`, `ledger`, `legal`, `oauth`, `observability`, `storage`, `cmd/util`, `cmd/mcp-server`) clean — no dead exports, no stubs, no duplicates.

### What I fixed and shipped

**HAM-4865** (Low) → Done ✓. **HAM-4866** (Low) → Done ✓. Both assigned to Andrew Somervell.

**PR #449** (squash-merged SHA `b72d88a4`):

| File | Change |
|---|---|
| `internal/embedding/similarity.go` | Deleted (168 lines) |
| `internal/embedding/similarity_test.go` | Deleted (269 lines) |
| `internal/blog/loader.go` | Remove `PostsDir`, `ClearCache` |
| `internal/voice/gemini_live.go` | Remove `GenerateAudio` stub |
| `internal/config/config.go` | Add exported `GetEnv` wrapper |
| `cmd/server/main.go` | Use `config.GetEnv`, remove local `getEnv` |

Net: 6 files, +4 / -460 lines. Build clean. Merged directly.

### What was left behind

- **`personal-cfo`** — all PostHog signals from that separate binary; nothing to do in `probably`.
- **POST-based Teller Connect handoff** — access token still passed as GET query param; Teller SDK limitation, deferred.
- **`containsLastFour` in `transfer_matcher.go`** — compiles 4 dynamic regexps per call (runtime-dependent values, cannot pre-compile). Acceptable.
- **`splitTransaction` real accounting** — implementation only adds tags, not separate expense entries. Deferred.
- **`cmd/util` hidden commands** (`check-tables`, `fix-insights-tables`, `list-ledgers`) — not in help, kept for manual debugging/recovery.
- **`_ = e.transactions.LoadEntries/LoadTags`** — best-effort enrichment pattern, intentional.

## Checklist for next run

- [ ] Query PostHog (status=active, last 30d) — consistent zero; watch for recurrence
- [ ] `cfo-cloud` logs: zero error/warn/fatal — consistent pattern
- [ ] All packages now swept at least once. Next run: re-check any packages with recent commits for new dead code.
- [ ] Known false positives to skip: `plaid.go:239`, `akahu.go:547`, `chain.go:21`, `LoadEntries/LoadTags _ =` pattern, MCP/handler `w.Write` discards
