# Session log — 2026-05-08

## What I read on startup (run 1)

Read `docs/changelog/20260506.md` (5 runs). Prior context summary:

- `ProviderSyncError` / `ProviderBalancePullError` from `personal-cfo` binary — recurring, nothing actionable in `probably`.
- PRs #245–#327 shipped covering: DRY helpers, silent errors, dead provider API surface, errors.Is, slog context migration, bulk-transaction DRY, storage fixes, MCP slog fixes.
- Branch at HEAD == `origin/main`.

## Run 2 — 2026-05-08T17:xx

### What I reasoned on

**PostHog audit (active, last 7d):** 1 active issue.

| ID | Type | First seen | Last seen | Occurrences | Source |
|---|---|---|---|---|---|
| `019e086e-d093` | CLICommandError | 2026-05-08T16:32Z | 2026-05-08T16:32Z | 1 | `personal-cfo/cmd/cfo/main.go` |

Stack trace: `main.run → main.main → runtime.main → runtime.goexit`. From the `personal-cfo` CLI binary, distinct_id `personal-cfo-service`. Not actionable in `probably`.

**Code audit findings (HAM-4662):**
- `orchestrator/simple.go:179,196`: `slog.Warn` inside `executeCategorize(ctx, ...)` — ctx in scope but ignored.
- `orchestrator/api.go:911`: `slog.Debug` inside `callVertexAIChatToolsStreaming(ctx, ...)` — same.
- `extraction/dedupe.go`: `ValidateTransactions` had no `ctx` param (unlike adjacent `CheckDuplicates`), used context-free `slog.Debug`/`slog.Info`, and always returned `nil` for error (dead code path checked by 2 callers and 6 tests).

### What I fixed and shipped

**PostHog:** `019e086e-d093-7890-b5db-66cb4b4ddb21` (CLICommandError) → `resolved` ✓

**Linear:** HAM-4662 (Medium) → Done ✓. Assigned to Andrew Somervell.

**PR #328** (`claude/clever-knuth-WpTIG`, squash-merged SHA `183289a3`):

| File | Change |
|---|---|
| `orchestrator/simple.go` | `slog.Warn` → `slog.WarnContext(ctx, ...)` at 2 call sites |
| `orchestrator/api.go` | `slog.Debug` → `slog.DebugContext(ctx, ...)` |
| `extraction/dedupe.go` | Add `ctx context.Context` param; `slog.Debug/Info` → context variants; remove always-nil error return |
| `handlers/statements_upload.go` | Drop 2 dead `if err != nil` blocks after callers updated |
| `extraction/extraction_test.go` | Pass `context.Background()`; drop dead error checks |

Net: −38 lines across 5 files. All tests clean.

### What was left behind

- **`personal-cfo` CLI** — `CLICommandError` from that binary; separate codebase, nothing to do in `probably`.
- **`auth/renderer.go`, `observability/*.go`, `stdlog_bridge.go`** — remaining context-free slog calls are in adapter/init functions where no context is available; intentional.
- **`handlers/voice.go`, `handlers/chat.go`** — a few context-free slog calls in helper functions (`sendSSEEvent`, WebSocket loops) that don't accept context; not worth threading ctx through.

## Run 3 — 2026-05-08T18:xx

### What I reasoned on

**PostHog audit (all statuses, last 90d):** 14 issues, all resolved. All from `github.com/asomervell/personal-cfo/internal/cloud.(*Server).*` via distinct_id `personal-cfo-service` — separate binary. Nothing actionable in `probably`.

Most-impactful resolved issues (for reference):
| Type | Occurrences | Last seen | Stack |
|---|---|---|---|
| SyncAccountPullError | 106 | 2026-05-07 | `syncIncludedConnections → runScheduledSync` |
| ProviderPullError | 100 | 2026-05-07 | `runProviderPull → syncIncludedConnections` |
| ProviderSyncError | 92 | 2026-05-07 | `runScheduledSync → StartBackgroundSync` |
| TellerWebhookTransactionBackfillRecordError | 12 | 2026-05-03 | `backfillTellerWebhookTransactions` |

**Proactive code audit findings:**
- `handlers/voice.go:34-41`: `var upgrader` with `CheckOrigin: always true` — WebSocket CSRF vector (**HAM-4663**, High).
- `handlers/plaid.go:27-41`: `PlaidWebhook` only checks header existence, never verifies JWT signature — any caller with a non-empty `Plaid-Verification` header passes (**HAM-4664**, Urgent).

### What I fixed and shipped

**Linear:** HAM-4663 (High) → Done ✓. HAM-4664 (Urgent) → Done ✓. Both assigned to Andrew Somervell.

**PR #330** `claude/ham-4663-websocket-origin` — squash-merged SHA `f4058369`:
- `handlers/voice.go`: replaced `var upgrader` (CheckOrigin: always true) with `(hdl *Handlers).voiceUpgrader() *websocket.Upgrader` that validates `Origin` == `cfg.BaseURL`.

**PR #331** `claude/ham-4664-plaid-jwt` — squash-merged SHA `f193eabf`:
- `handlers/plaid_verify.go` (new): `fetchPlaidPublicKey` with 5-min in-memory JWKS cache, `jwkToRSA`, `verifyPlaidWebhookJWT` (RS256 sig + body SHA-256 hash check).
- `handlers/plaid.go`: replaced TODO stub with `hdl.verifyPlaidWebhookJWT(...)`, returns 401 on failure.

### What was left behind

- **`personal-cfo`** — all historical errors are in that separate binary; nothing to do in `probably`.
- **`handlers/plaid.go:119,203,218`** — TODOs for transaction removal, income data fetch, asset report fetch. These are feature gaps, not bugs; out of scope.
- **`voice/gemini_live.go:255`, `handlers/voice.go:728`** — TTS not yet implemented. Feature work, not a bug.
- **`handlers/voice.go` context-free slog** — WebSocket goroutines (`writePump`, `readPump`) don't carry context through; intentional.
- **`llm/handler.go` `sendSSEEvent`** — package-level helper has no context parameter; intentional (SSE flush helper).
- **`plaid_verify.go` JWKS cache** — package-level vars (shared across requests). Fine for a single-process server; would need revisiting for multi-instance deployments sharing no state.

## Run 4 — 2026-05-08T19:xx

### What I reasoned on

**PostHog audit (active, last 7d):** zero active issues. Warn/error/fatal logs: zero.
All-statuses last 1d: `CLICommandError` (resolved, 1 occ) + `ProviderBalancePullError` (resolved, 1 occ) — both from `personal-cfo` binary, nothing to do.

**Proactive code audit findings:**
- `handlers/api.go`: `APIListResponse[T any]` defined but never instantiated — callers use `APIPagination` directly (**HAM-4665**).
- `mcp/server.go`: `MCPRequest` defined but never used — the dispatcher uses `jsonRPCRequest` (**HAM-4665**).
- All remaining context-free `slog` calls are in adapter/init/constructor functions or WebSocket goroutines without ctx — all intentional, unchanged.
- `sync/service.go` `slog.Error` calls in `NewSyncService` constructor — no ctx available, intentional.
- `sync/teller.go:parseAmount` — context-free `slog.Warn`; threading ctx into a pure parsing helper adds complexity for no benefit, left alone.

### What I fixed and shipped

**Linear:** HAM-4665 (Low) → Done ✓. Assigned to Andrew Somervell.

**PR #333** (`claude/ham-4665-dead-types`, squash-merged SHA `0fd3d87d`):

| File | Change |
|---|---|
| `handlers/api.go` | Remove `APIListResponse[T any]` (5 lines) |
| `mcp/server.go` | Remove `MCPRequest` (5 lines) |

Net: −10 lines across 2 files. Build and unit tests clean.

### What was left behind

- **`personal-cfo`** — `CLICommandError` / `ProviderBalancePullError` from that separate binary; nothing to do in `probably`.
- **`plaid_verify.go` JWKS cache** — package-level vars, single-process fine; noted for multi-instance revisit if needed.
- **`sync/teller.go:parseAmount`** — context-free `slog.Warn`; intentionally left as pure helper.
- **Feature TODOs** (`voice/gemini_live.go:255`, `handlers/voice.go:736`, `handlers/plaid.go:116,200,215`) — feature gaps, out of scope.

## Run 5 — 2026-05-08T21:xx

### What I read on startup

Read this file (runs 1–4). Prior context summary: all PostHog errors originate in the old `personal-cfo` binary (`internal/cloud/server.go`), not in `probably`. Runs 2–4 shipped PRs #328–#333 covering slog context, dead types, WebSocket CSRF, Plaid JWT verification.

### What I reasoned on

**PostHog audit (active, last 7d):** zero active issues ✓
**PostHog audit (all statuses, last 90d):** 14 issues, all resolved, all from `personal-cfo-service`. Nothing newly actionable.

**Proactive code audit — `handlers/teller.go`:**

Found three categories of bugs not addressed in previous runs:

1. **Credential-clearing inconsistency** (HAM-4668, High): `clearEnrollmentCredentials` has an explicit invariant — do NOT clear `TellerAccountID`/`ExternalAccountID` (persistent IDs needed for reconnect matching). Two paths violated it:
   - `TellerDisconnect` (old line 1018): cleared `TellerAccountID` and was also missing `ConnectionID`/`AccessToken` clears.
   - `handleEnrollmentDisconnected` (old lines 1417, 1421): cleared both `ExternalAccountID` and `TellerAccountID`.
   Root cause: copy-paste from before `clearEnrollmentCredentials` was refactored to preserve persistent IDs. Maps directly to the `TellerConnectSessionError` / `SyncAccountPullError` patterns seen in PostHog — reconnect matching would silently fail.

2. **Wrong log level** (HAM-4669, Low): `TellerSyncMulti` completion was always logged at `Error` regardless of outcome, generating false error signals in PostHog on every call.

3. **Log message typos** (HAM-4669, Low): double space in `"Found  accounts for enrollment"` and `"count"` field key holding an account name string (should be `"name"`).

**Other files reviewed:** `sync/teller.go` — only remaining context-free slog call is `parseAmount` (pure parsing helper, intentional, previously noted). No new issues.

### What I fixed and shipped

**HAM-4668** (High) → Done ✓. Assigned to Andrew Somervell.

**PR #338** (`claude/ham-4668-teller-credential-clearing`, squash-merged SHA `b9713867`):

| File | Change |
|---|---|
| `handlers/teller.go` | `TellerDisconnect`: remove `TellerAccountID = ""`, add `ConnectionID = ""` + `AccessToken = ""`  |
| `handlers/teller.go` | `handleEnrollmentDisconnected`: remove `ExternalAccountID = ""` + `TellerAccountID = ""` |

Net: −5 lines, +6 lines across 2 hunks.

**HAM-4669** (Low) → Done ✓. Assigned to Andrew Somervell.

**PR #339** (`claude/ham-4669-teller-log-bugs`, squash-merged SHA `ef870dbe`):

| File | Change |
|---|---|
| `handlers/teller.go:385` | `slog.ErrorContext` → `slog.InfoContext` for sync-multi completion |
| `handlers/teller.go:459` | `"Found  accounts"` → `"Found accounts"` (double space) |
| `handlers/teller.go:497` | `"count", accountWithToken.Name` → `"name", accountWithToken.Name` |

Net: −3 lines, +3 lines (pure substitutions).

### What was left behind

- **`personal-cfo`** — all historical PostHog errors from that separate binary; nothing to do in `probably`.
- **`plaid_verify.go` JWKS cache** — package-level vars; single-process fine, noted for multi-instance revisit.
- **`sync/teller.go:parseAmount`** — context-free `slog.Warn`; pure parsing helper, intentional.
- **Access token in Teller Connect URL** (`TellerConnect`, `TellerLink` JS): token passed as GET query param; this is the standard Teller Connect SDK pattern. A POST-based handoff would be safer but requires JS rework; noted as a future hardening item, not a blocking bug.
- **Feature TODOs** (`voice/gemini_live.go:255`, `handlers/voice.go:736`, `handlers/plaid.go:116,200,215`) — feature gaps, out of scope.
- **`plaid_verify.go` test** — still not added; deferred again.

## Run 6 — 2026-05-08T22:xx

### What I read on startup

Read this file (runs 1–5). Prior context: all PostHog errors originate in `personal-cfo`; runs 2–5 shipped PRs #328–#339 covering slog context, dead types, WebSocket CSRF, Plaid JWT, Teller credential clearing, Teller log bugs.

### What I reasoned on

**PostHog audit (active, last 7d):** zero active issues ✓
**PostHog audit (all-status, last 1d):** 2 entries, both resolved, both `personal-cfo-service`. Nothing actionable.
**Logs (error/warn/fatal, last 24h):** zero.

**Proactive code audit — `sync/worker.go`:**

`syncAll()` creates a `context.Context` at the top (with `observability.WithDistinctID`) to carry the sync worker's distinct-id for PostHog and trace propagation. However, 9 slog calls inside the function body were context-free, silently dropping that context from all scheduled-sync log lines. Functions without a ctx param (`Start`, `Stop`, `run`, `SyncNow`) were correctly context-free and untouched.

No other actionable findings: `processing/worker.go` and `insights/worker.go` slog calls are in background goroutines/lifecycle methods without ctx — all exempt.

### What I fixed and shipped

**HAM-4670** (Low) → Done ✓. Assigned to Andrew Somervell.

**PR #341** (squash-merged SHA `2bbcf881`):

| File | Change |
|---|---|
| `internal/sync/worker.go` | 9× `slog.Info`/`slog.Debug` → `slog.InfoContext(ctx,...)`/`slog.DebugContext(ctx,...)` inside `syncAll()` |

Net: 9 substitutions, 0 net line change. Build and unit tests clean.

### What was left behind

- **`personal-cfo`** — all PostHog entries from separate binary; nothing to do in `probably`.
- **`plaid_verify.go` test** — JWKS verification path still has no dedicated test; deferred again.
- **POST-based Teller Connect handoff** — access token still passed as GET query param; Teller SDK pattern, noted for future hardening.
- **`processing/worker.go`** — slog calls in `feedWork()`/`run()` goroutines without ctx; intentional.

## Checklist for next run

- [ ] Query PostHog (status=active, last 7d) — expect zero active issues
- [ ] If `personal-cfo` errors reappear they are always from that separate binary — skip
- [ ] Consider adding a test for `plaid_verify.go` JWKS verification path
- [ ] Consider a POST-based Teller Connect handoff to avoid access token in URL query params
