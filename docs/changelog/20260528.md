# 2026-05-28 — Session 8: monitoring-only, clean state

## PostHog state (2026-05-28 session 8)

| Check | Result |
|---|---|
| Active error tracking issues | **0** |
| Warn/error/fatal logs (last 7d) | **0** |
| All PostHog issues (last 30d, all statuses) | 19 total — all `resolved` or `suppressed`, unchanged |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (old personal-cfo binary, unchanged) |
| Enrichment | Normal: `updated=0 skipped=4386 failed=0` |
| Provider sync | `status=partial_failure connections=11 transactions=79 errors=5` — 5 structural broken connections |
| CLICommandError (019e086e) | Suppressed, 117 occurrences — local Mac arm64 CLI, not fixable |
| CLICommandError (019decf8) | Suppressed, 41 occurrences — same local CLI |

**Stop condition holds.** No new active errors, no warn/error/fatal logs.

## Reasoning

Read session 7 changelog before acting. Ran full triage checklist:
1. `query-error-tracking-issues-list` (active, 30d) → 0 ✓
2. `logs-count` (warn+error+fatal, `cfo-cloud`, 7d) → 0 ✓
3. `query-error-tracking-issues-list` (all statuses, 30d) → 19 issues, identical set to session 7 ✓
4. `query-logs` (info, last 1h) → enrichment and provider sync running normally; `service.version` `3f3ec2a...` unchanged ✓
5. `git log --oneline -10` → most recent commit `f5febdc` (session 7 changelog) — no new code changes since session 7 ✓

Enrichment `updated=0 skipped=4386` is normal: all transactions already enriched.
Provider sync `errors=5 status=partial_failure` is structural: 5 permanently broken/expired provider connections, not a code bug.

## Shipped this session

Nothing — no active errors, stop condition met.

## What was left behind

- **PostHog assignee API**: HTTP 500 on every attempt — cannot assign issues to Andrew programmatically.
- **Linear MCP**: /sse transport past removal date 2026-04-08.
- **CLICommandError (019e086e, 019decf8)**: Suppressed, local Mac `personal-cfo` CLI — not fixable from `probably`.
- **`errors=5` in `provider_sync_completed`**: 5 structural broken provider connections — not a code bug.
- **Old binary still deployed**: `service.version` `3f3ec2a...` unchanged. Once `probably` is redeployed, `errors=5` should disappear.

## Next run checklist

1. `query-error-tracking-issues-list` (active, 30d) → expect 0.
2. `logs-count` (warn+error+fatal, `cfo-cloud`, 7d) → expect 0.
3. `query-logs` (info, last 1h) → check `service.version` for new `probably` git hash.
4. `git log --oneline -5` → check for new commits since session 8.
5. If no errors and no new commits: monitoring-only, stop condition met.

---

# 2026-05-28 — Session 7: monitoring-only, clean state

## PostHog state (2026-05-28 session 7)

| Check | Result |
|---|---|
| Active error tracking issues | **0** |
| Warn/error/fatal logs (last 7d) | **0** |
| All PostHog issues (last 30d, all statuses) | 19 total — all `resolved` or `suppressed`, unchanged |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (old personal-cfo binary, unchanged) |
| Enrichment | Normal: after_sync `updated=5 skipped=4381`, scheduled `updated=0 skipped=4386` |
| Provider sync | `status=partial_failure connections=11 transactions=79 errors=5` — 5 structural broken connections |
| Teller webhooks | Processing normally (multiple events at ~20:55 UTC) |
| CLICommandError (019e086e) | Suppressed, 117 occurrences — local Mac arm64 CLI, not fixable |
| CLICommandError (019decf8) | Suppressed, 41 occurrences — same local CLI |

**Stop condition holds.** No new active errors, no warn/error/fatal logs.

## Reasoning

Read session 6 changelog (top of this file) before acting. Ran full triage checklist:
1. `query-error-tracking-issues-list` (active, 30d) → 0 ✓
2. `logs-count` (warn+error+fatal, `cfo-cloud`, 7d) → 0 ✓
3. `query-error-tracking-issues-list` (all statuses, 30d) → 19 issues, identical set to session 6 ✓
4. `query-logs` (info, last 1h) → enrichment and Teller webhooks running normally; `service.version` `3f3ec2a...` unchanged ✓
5. `git log --oneline -10` → most recent commit `4ac139f` (session 6 changelog) — no new code changes since session 6 ✓

No new error types, no regressions. The `updated=5` in the after_sync enrichment trigger means 5 new Teller-webhookt transactions were picked up and enriched correctly — normal operation.

## Shipped this session

Nothing — no active errors, stop condition met.

## What was left behind

- **PostHog assignee API**: HTTP 500 on every attempt — cannot assign issues to Andrew programmatically.
- **Linear MCP**: /sse transport past removal date 2026-04-08.
- **CLICommandError (019e086e, 019decf8)**: Suppressed, local Mac `personal-cfo` CLI — not fixable from `probably`.
- **`errors=5` in `provider_sync_completed`**: 5 structural broken provider connections — not a code bug.
- **Old binary still deployed**: `service.version` `3f3ec2a...` unchanged. Once `probably` is redeployed, `errors=5` should disappear.

## Next run checklist

1. `query-error-tracking-issues-list` (active, 30d) → expect 0.
2. `logs-count` (warn+error+fatal, `cfo-cloud`, 7d) → expect 0.
3. `query-logs` (info, last 1h) → check `service.version` for new `probably` git hash.
4. `git log --oneline -5` → check for new commits since session 7.
5. If no errors and no new commits: monitoring-only, stop condition met.

---

# 2026-05-28 — Session 6: monitoring-only, clean state

## Context from previous sessions (1–18 on 2026-05-27, 1–5 on 2026-05-28)

- Sessions 1–9 (2026-05-27): Full codebase dead-code sweep. Fixes: Teller webhook goroutine timeout,
  write-only Config fields (22), Akahu unused types, MCP dead fields, Storage interface dead methods,
  recurring PatternResult types, LogoStore entityID param (11 call sites), TellerSyncService
  per-request allocation → cached at startup, balance reconciliation CaptureFailure noise for
  non-5xx TellerAPIErrors, getOrCreateOpeningBalanceAccount N+1 query.
- Session 10: All packages verified clean. Sweep complete.
- Sessions 11–18 (2026-05-27) and 1–5 (2026-05-28): Monitoring-only. No new errors. Stop condition held.
- PostHog assignee API: HTTP 500 on every attempt across all prior sessions — cannot assign programmatically.
- Linear MCP: /sse transport past removal date 2026-04-08. Tools visible in deferred list.
- `errors=5` in `provider_sync_completed`: structural (5 permanently broken/expired provider connections).
- Old binary: service.version `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (personal-cfo, not redeployed).
- Since session 5: Andrew pushed PR #720 (Teller log level fix + TellerLastSyncedAt → LastSyncedAt on error)
  and PR #722 (TellerLastSyncedAt alias removed entirely from Account struct, DeleteAndResync+FullResync fixed).

## PostHog state (2026-05-28 session 6)

| Check | Result |
|---|---|
| Active error tracking issues | **0** |
| Warn/error/fatal logs (last 30d) | **0** |
| All PostHog issues (last 30d, all statuses) | 19 total — all `resolved` or `suppressed`, unchanged |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (old personal-cfo binary, unchanged) |
| Enrichment | Normal: `updated=0 skipped=4380 failed=0` |
| Provider sync | `status=partial_failure connections=11 transactions=73 errors=5` — 5 structural broken connections |
| CLICommandError (019e086e) | Suppressed, 117 occurrences — local Mac arm64 CLI, not fixable |
| CLICommandError (019decf8) | Suppressed, 41 occurrences — same local CLI |

**Stop condition holds.** No new active errors, no warn/error/fatal logs.

## Reasoning

Read all prior session changelogs (20260527.md, 20260528.md sessions 1–5) before acting.

Full PostHog triage:
1. `query-error-tracking-issues-list` (active, 30d) → 0 ✓
2. `logs-count` (warn+error+fatal, `cfo-cloud`, 30d) → 0 ✓
3. `query-error-tracking-issues-list` (all statuses, 30d) → 19 issues, identical set to all prior sessions ✓
4. `query-logs` (info, last 7d) → enrichment and sync running normally; `service.version` `3f3ec2a...` unchanged ✓
5. `git log` → 2 new commits since session 5: PR #720 and PR #722 (by Andrew, not error-driven)

**PR #720** fixed Teller SyncTransactions to log transient errors at WARN (not ERROR) and disconnect
errors silently, and corrected the timestamp field from TellerLastSyncedAt → LastSyncedAt so
last_synced_at actually advances after a failed fetch.

**PR #722** removed TellerLastSyncedAt entirely — it was an alias never written by accounts.Update().
DeleteAndResync and FullResync now set LastSyncedAt correctly. Dead fallback in settings.go removed.
Same class of bug as #720. Net: -36 lines.

Neither PR introduced new errors. The `errors=5` in provider sync is unchanged (structural broken connections
predating the `probably` rewrite — expired/deactivated provider credentials, not a code bug).

`enrichment updated=0 skipped=4380` is normal: all 4380 transactions are already enriched.

## Shipped this session

Nothing — no active errors, stop condition met.

## What was left behind

- **PostHog assignee API**: HTTP 500 on every attempt — cannot assign issues to Andrew programmatically.
- **Linear MCP**: /sse transport past removal date 2026-04-08.
- **CLICommandError (019e086e, 019decf8)**: Suppressed, local Mac `personal-cfo` CLI — not fixable from `probably`.
- **`errors=5` in `provider_sync_completed`**: 5 structural broken provider connections — not a code bug.
- **Old binary still deployed**: `service.version` `3f3ec2a...` unchanged. Once `probably` is redeployed, `errors=5` should disappear.

## Next run checklist

1. `query-error-tracking-issues-list` (active, 30d) → expect 0.
2. `logs-count` (warn+error+fatal, `cfo-cloud`, 7d) → expect 0.
3. `query-logs` (info, last 1h) → check `service.version` for new `probably` git hash.
4. `git log --oneline -5` → check for new commits since last session.
5. If no errors and no new commits: monitoring-only, stop condition met.

---

# 2026-05-28 — Session 5: monitoring-only, clean state

## PostHog state (2026-05-28 session 5)

| Check | Result |
|---|---|
| Active error tracking issues | **0** |
| Warn/error/fatal logs (last 7d) | **0** |
| All PostHog issues (last 30d, all statuses) | 19 total — all `resolved` or `suppressed`, unchanged |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (old personal-cfo binary, unchanged) |
| Enrichment | Normal: `updated=0 skipped=4380 failed=0` |
| Provider sync | `status=partial_failure connections=11 transactions=73 errors=5` — 5 structural broken connections |
| CLICommandError (019e086e) | Suppressed, 117 occurrences — local Mac arm64 CLI, not fixable |
| CLICommandError (019decf8) | Suppressed, 41 occurrences — same local CLI |

**Stop condition holds.** No new active errors, no warn/error/fatal logs.

## Reasoning

Read sessions 1–4 changelogs before acting. Ran full triage checklist:
1. `query-error-tracking-issues-list` (active, 30d) → 0 ✓
2. `logs-count` (warn+error+fatal, `cfo-cloud`, 7d) → 0 ✓
3. `query-error-tracking-issues-list` (all statuses, 30d) → 19 issues, same set as sessions 1–4 ✓
4. `query-logs` (info, last 1h) → enrichment and sync running normally; `service.version` `3f3ec2a...` unchanged ✓

No new error types, no regressions. The two suppressed `CLICommandError` issues are from
`/Users/andrew/code/personal-cfo/cmd/cfo/main.go` — a local development CLI tool not present in
this repo. Error values are redacted by PostHog; errors are intentionally suppressed. Not fixable here.

All other resolved errors (`ProviderBalancePullError`, `TellerWebhookTransactionAggregationError`,
`ProviderTransactionStoreError`, `HTTPServerError`, `TellerConnectSessionError`, `ProviderSyncError`,
`ProviderPullError`, `SyncAccountPullError`, `TellerWebhookTransactionBackfillRecordError`) were
addressed in sessions 1–9 on 2026-05-27 via the Teller transient-error helper and related fixes.

PostHog assignee API: HTTP 500 on all prior attempts across sessions 1–4 and 1–18 on May 27 —
cannot assign issues programmatically. User must assign via PostHog web UI.

## Shipped this session

Nothing — no errors to fix, stop condition met.

## What was left behind

- **PostHog assignee API**: HTTP 500 on every attempt — cannot assign issues programmatically.
- **Linear MCP**: /sse transport past removal date 2026-04-08.
- **CLICommandError (019e086e, 019decf8)**: Suppressed, local Mac `personal-cfo` CLI — not fixable from `probably`.
- **`errors=5` in `provider_sync_completed`**: 5 structural broken provider connections — not a code bug.
- **Old binary still deployed**: `service.version` is still `3f3ec2a...` (personal-cfo). Watch for a new hash after redeployment.

## Next run checklist

1. `query-error-tracking-issues-list` (active, 30d) → expect 0.
2. `logs-count` (warn+error+fatal, `cfo-cloud`, 7d) → expect 0.
3. `query-logs` (info, last 1h) → check `service.version` for new `probably` git hash.
4. If no errors: monitoring-only, stop condition met.

---

# 2026-05-28 — Session 4: monitoring-only, clean state

## PostHog state (2026-05-28 session 4)

| Check | Result |
|---|---|
| Active error tracking issues | **0** |
| Warn/error/fatal logs (last 30d) | **0** count |
| All PostHog issues (last 30d, all statuses) | 19 total — all `resolved` or `suppressed`, unchanged |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (old personal-cfo binary, unchanged) |
| Enrichment | Normal: `updated=0 skipped=4376 failed=0` |
| Provider sync | `status=partial_failure connections=11 transactions=69 errors=5` — 5 structural broken connections |
| CLICommandError (019e086e) | Suppressed, 117 occurrences — local Mac arm64 CLI, not fixable |
| CLICommandError (019decf8) | Suppressed, 41 occurrences — same local CLI |
| Teller webhook | Successfully processed (1 transaction aggregated at 04:16 UTC) |

**Stop condition holds.** No new active errors, no queryable warn/error/fatal logs.

## Reasoning

Read sessions 1–3 changelogs before acting. Ran full triage checklist:
1. `query-error-tracking-issues-list` (active, 30d) → 0 ✓
2. `logs-count` (warn+error+fatal, `cfo-cloud`, 30d) → 0 ✓
3. `query-error-tracking-issues-list` (all statuses, 30d) → 19 issues, same set as sessions 1–3 ✓
4. `query-logs` (info, last 1h) → enrichment and sync running normally; Teller webhook processed 1 txn at 04:16 UTC; `service.version` unchanged ✓

PostHog assignee API: HTTP 500 on all prior attempts — did not retry this session (known broken).
No new error types, no regressions. Codebase fully swept since sessions 1–18 on 2026-05-27.

## Shipped this session

Nothing — no errors to fix, stop condition met.

## What was left behind

- **PostHog assignee API**: HTTP 500 on every attempt — cannot assign issues programmatically. Use PostHog web UI.
- **Linear MCP**: /sse transport past removal date 2026-04-08.
- **CLICommandError (019e086e, 019decf8)**: Suppressed, local Mac `personal-cfo` CLI — not fixable from `probably`.
- **`errors=5` in `provider_sync_completed`**: 5 structural broken connections — not a code bug.
- **Old binary still deployed**: `service.version` is still `3f3ec2a...` (personal-cfo). Watch for a new hash after redeployment.

## Next run checklist

1. `query-error-tracking-issues-list` (active, 30d) → expect 0.
2. `logs-count` (warn+error+fatal, `cfo-cloud`, 7d) → expect 0.
3. `query-logs` (info, last 1h) → check `service.version` for new `probably` git hash.
4. If no errors: monitoring-only, stop condition met.

---

# 2026-05-28 — Session 1: monitoring-only, clean state

## Context from previous sessions (1–18, all on 2026-05-27)

- Sessions 1–9: Full codebase dead-code sweep. Fixes shipped: Teller webhook goroutine timeout,
  write-only Config fields (22 fields), Akahu unused types, MCP dead fields, Storage interface dead
  methods (UploadFromReader/Delete/GetURL), recurring PatternResult types + Frequency/PatternType/BillType
  constants, LogoStore entityID param (11 call sites), TellerSyncService per-request allocation → cached
  at startup, balance reconciliation CaptureFailure noise for non-5xx TellerAPIErrors,
  getOrCreateOpeningBalanceAccount N+1 query → single targeted SELECT.
- Session 10: All packages verified clean. Sweep complete. No dead exports or unused symbols remain.
- Sessions 11–18: Monitoring-only. No new errors. Stop condition held every session.
- PostHog assignee API: HTTP 500 on every attempt across all 18 sessions — cannot assign programmatically.
- Linear MCP: /sse transport past removal date (2026-04-08). Tools visible in deferred list.
- `errors=5` in `provider_sync_completed`: structural broken provider connections — not a code bug.
- Old binary: service.version `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (personal-cfo), not a `probably` git hash.

## PostHog state (2026-05-28 session 1)

| Check | Result |
|---|---|
| Active error tracking issues | **0** |
| Warn/error/fatal logs (last 7d) | **0** |
| All PostHog issues (last 30d) | 19 total — all `resolved` or `suppressed`, unchanged |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (old personal-cfo binary, unchanged) |
| Enrichment | Normal: `updated=0 skipped=4376 failed=0` |
| Provider sync | Hourly, `errors=5 status=partial_failure` — 5 structural broken connections |
| CLICommandError (019e086e) | Suppressed, 117 occurrences — local Mac arm64 CLI, not fixable |
| CLICommandError (019decf8) | Suppressed, 41 occurrences — same local CLI |

**Stop condition holds.** No new active errors, no warn/error/fatal logs.

## Reasoning

Full PostHog triage per session 18 checklist:
1. `query-error-tracking-issues-list` (active, 30d) → 0 results ✓
2. `logs-count` for `cfo-cloud` with `severityLevels: ["warn","error","fatal"]` over 7d → count = 0 ✓
3. `query-error-tracking-issues-list` (all, 30d) → 19 issues, identical set to session 18 ✓
4. `query-logs` (info, last 1h) → enrichment and provider sync running normally; service.version `3f3ec2a...` unchanged ✓

No new error types, no regressions, no new dead code. Binary has not been redeployed.

## Shipped this session

Nothing — no errors to fix, no dead code found, stop condition met.

## What was left behind

- **PostHog assignee API**: HTTP 500 on every attempt — cannot assign issues to Andrew programmatically. Use PostHog web UI.
- **Linear MCP**: Transport /sse past removal date 2026-04-08. Tools visible in deferred list but creation untested.
- **CLICommandError (019e086e, 019decf8)**: Suppressed, local Mac `personal-cfo` CLI — not fixable from `probably`.
- **`errors=5` in `provider_sync_completed`**: Structural broken provider connections — not a code problem.
- **Old binary still deployed**: Once `probably` is redeployed, `errors=5` noise should disappear. Watch service.version for a new `probably` git hash.

## Next run checklist

1. Check for new active PostHog error tracking issues.
2. Check warn/error/fatal log count for `cfo-cloud` over last 7d.
3. Check service.version — watch for a new `probably` git hash (binary redeployed).
4. If no errors: monitoring-only. Codebase fully swept — no dead code remaining.

---

# 2026-05-28 — Session 3: monitoring-only, clean state

## PostHog state (2026-05-28 session 3)

| Check | Result |
|---|---|
| Active error tracking issues | **0** |
| Warn/error/fatal logs (last 7d) — count | 8 (known discrepancy; `query-logs` returns 0 results) |
| All PostHog issues (last 30d, all statuses) | 19 total — all `resolved` or `suppressed`, unchanged |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (old personal-cfo binary, unchanged) |
| Enrichment | Normal: `updated=0 skipped=4376 failed=0` |
| Provider sync | `status=partial_failure connections=11 transactions=69 errors=5` — 5 structural broken connections |
| CLICommandError (019e086e) | Suppressed, 117 occurrences — local Mac arm64 CLI, not fixable |
| CLICommandError (019decf8) | Suppressed, 41 occurrences — same local CLI |

**Stop condition holds.** No new active errors, no queryable warn/error/fatal logs.

## Reasoning

Read sessions 1 and 2 changelogs from `docs/changelog/20260528.md` before acting. Ran full triage:
1. `query-error-tracking-issues-list` (active, 30d) → 0 ✓
2. `logs-count` (warn+error+fatal, `cfo-cloud`, 7d) → 8; `query-logs` → 0 (known indexing discrepancy) ✓
3. `query-error-tracking-issues-list` (all statuses, 30d) → 19 issues, same set as sessions 1–2 ✓
4. `query-logs` (info, last 1h) → enrichment and sync running normally; `service.version` unchanged ✓

Most recent fix (#716, `c36e26e`) shipped yesterday by a prior session: `IsTellerTransientError` helper
applied consistently across all Teller call sites, preventing 5xx provider outages from being reported
to PostHog as application bugs. Directly closed the ProviderBalancePullError, TellerConnectSessionError,
ProviderPullError, SyncAccountPullError noise patterns that used to appear.

No new error types, no regressions, no dead code found.

## Shipped this session

Nothing — no errors to fix, stop condition met.

## What was left behind

- **PostHog assignee API**: HTTP 500 on every attempt — cannot assign issues programmatically.
- **Linear MCP**: /sse transport past removal date 2026-04-08.
- **CLICommandError (019e086e, 019decf8)**: Suppressed, local Mac `personal-cfo` CLI — not fixable from `probably`.
- **`errors=5` in `provider_sync_completed`**: 5 structural broken provider connections — not a code bug.
- **Old binary still deployed**: `service.version` is still `3f3ec2a...` (personal-cfo). Watch for a new hash after redeployment.

## Next run checklist

1. `query-error-tracking-issues-list` (active, 30d) → expect 0.
2. `logs-count` + `query-logs` (warn+error+fatal, `cfo-cloud`, 7d) → expect 0 queryable.
3. `query-logs` (info, last 1h) → check `service.version` for new `probably` git hash.
4. If no errors: monitoring-only, stop condition met.

---

# 2026-05-28 — Session 2: monitoring-only, clean state

## PostHog state (2026-05-28 session 2)

| Check | Result |
|---|---|
| Active error tracking issues | **0** |
| Warn/error/fatal logs (last 7d) — query | **0** queryable results |
| Warn/error/fatal logs (last 7d) — count | 9 (known discrepancy; `logs-count` stale vs `query-logs`, same gap as session 1) |
| All PostHog issues (last 30d, all statuses) | 19 total — all `resolved` or `suppressed`, unchanged |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` (old personal-cfo binary, unchanged) |
| Enrichment | Normal: `updated=0 skipped=4376 failed=0` |
| Provider sync | `status=partial_failure connections=11 transactions=69 errors=5` — 5 structural broken connections |
| CLICommandError (019e086e) | Suppressed, 117 occurrences — local Mac arm64 CLI, not fixable |
| CLICommandError (019decf8) | Suppressed, 41 occurrences — same local CLI |

**Stop condition holds.** No new active errors, no queryable warn/error/fatal logs.

## Reasoning

Ran the full session checklist:
1. `query-error-tracking-issues-list` (active, 30d) → 0 results ✓
2. `logs-count` (warn+error+fatal, `cfo-cloud`, 7d) → 9 — but `query-logs` returns 0 results; same known discrepancy as session 1.
3. `query-error-tracking-issues-list` (all statuses, 90d) → 19 issues, identical set to all previous sessions ✓
4. `query-logs` (info, last 7d) → enrichment and provider sync running normally; service.version `3f3ec2a...` unchanged ✓
5. Codebase grep → no new dead exports or error patterns found.

The `logs-count` / `query-logs` discrepancy (count=9, queryable=0) was already present in session 1 and is consistent with PostHog's eventual-consistency indexing lag or a stale count cache. Not a new signal.

## Shipped this session

Nothing — no errors to fix, no dead code found, stop condition met.

## What was left behind

- **PostHog assignee API**: HTTP 500 on every attempt — cannot assign issues programmatically. Use PostHog web UI.
- **Linear MCP**: Transport /sse past removal date 2026-04-08.
- **CLICommandError (019e086e, 019decf8)**: Suppressed, local Mac `personal-cfo` CLI — not fixable from `probably`.
- **`errors=5` in `provider_sync_completed`**: 5 structural broken connections — not a code bug.
- **Old binary still deployed**: Once `probably` is redeployed, `service.version` will show a `probably` git hash.

## Next run checklist

1. `query-error-tracking-issues-list` (active, 30d) → expect 0.
2. `logs-count` + `query-logs` (warn+error+fatal, `cfo-cloud`, 7d) → expect 0 queryable.
3. `query-logs` (info, last 1h) → check `service.version` for new `probably` git hash.
4. If no errors: monitoring-only, stop condition met.
