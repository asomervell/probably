# Session log — 2026-04-30

## Context from previous runs

### 2026-04-27 (6 runs)
Fixed all known PostHog errors (PRs #114–#124, #126):
- Error A: Gemini `function_response.name: Name cannot be empty`
- Error B: Gemini `Function call is missing a thought_signature`
- Error C: OTEL `Too many AI spans`
- Error D: Plaid handlers using `log.Printf`, bypassing OTEL
- Error E: Google `API_KEY_INVALID` misclassified as `[parse_error]`

### 2026-04-28
Defensive hardening (PR #139): added `WithMaxQueueSize(75)` to OTEL batcher; added `thoughtSignaturesConsistent()` guard for Gemini raw parts. All 711k historical error entries confirmed bounded to the April 25 burst, pre-fix.

### 2026-04-29 (5 runs)
All runs: zero new active issues, zero post-April-26 errors or warnings. System confirmed clean across all signals (error tracking, app logs, worker logs, browser logs).

## What I reasoned on (this run — 2026-04-30)

Read `docs/changelog/20260429.md` (all 5 runs) before querying.

Queried PostHog across all signals:

| Source | Count |
|---|---|
| Error tracking issues (active, -7d) | 0 |
| Error tracking issues (all) | 35, all `resolved`, all first_seen `2026-04-25` |
| Error/fatal logs (-2d) | 0 |
| Warn logs (-2d) | 0 |
| Error/fatal/warn logs (since 2026-04-26) | 0 |

The 35 resolved issues are the same pre-fix burst from April 25. No new issues have appeared. No log-level signals of any kind since April 26.

## What I fixed and shipped

Nothing to fix. PostHog is clean. No PRs created.

## What was left behind

- **Production project** (id 398540) — `ingested_event: false`; no traffic to check.
- **PostHog PATCH assignee bug** — server-side; manual UI assignment still the only path.
- **`syncCtx := context.Background()` in Plaid sync** — accepted low-priority, no PostHog signal.
- **`log.Printf` calls** — 8 remain in `internal/observability/` (intentional bootstrap logging, correct as-is).

---

## Session log — 2026-04-30 (second run)

### What I reasoned on

Read `docs/changelog/20260430.md` (first run, earlier today) and `docs/changelog/20260429.md` before querying.

All prior context: the 35 PostHog issues are pre-fix (April 25 burst), all resolved. Fixes shipped in PRs #114–#139 across April 27–28. System clean since April 26.

Queried all PostHog signals:

| Source | Count |
|---|---|
| Error tracking issues (active, -30d) | 0 |
| Error tracking issues (all, -90d) | 35, all `resolved`, all first_seen `2026-04-25` |
| Error/fatal/warn logs since 2026-04-26 | 0 |
| Error/fatal/warn logs (-2d) | 0 |

### What I fixed and shipped

Nothing to fix. PostHog is clean. No PRs created.

### What was left behind

Same as prior runs — no open items:
- **Production project** (id 398540) — `ingested_event: false`; no traffic to check.
- **PostHog PATCH assignee bug** — server-side; manual UI assignment still the only path.
- **`syncCtx := context.Background()` in Plaid sync** — accepted low-priority, no PostHog signal.
- **`log.Printf` calls** — 8 remain in `internal/observability/` (intentional bootstrap logging, correct as-is).

---

## Session log — 2026-04-30 (third run)

### What I reasoned on

Read `docs/changelog/20260430.md` (two prior runs today) before querying. All prior context held:
- 35 PostHog issues, all `resolved`, all first_seen `2026-04-25` (pre-fix burst).
- Fixes for Errors A–E shipped in PRs #114–#139 across April 27–28.
- System confirmed clean across all signals since April 26 in every prior run.

Queried PostHog error tracking:

| Source | Count |
|---|---|
| Error tracking issues (active, -7d) | 0 |
| Error tracking issues (all, -90d) | 35, all `resolved`, all first_seen `2026-04-25` |

Distinct error classes in the 35 resolved issues:
- **`function_response.name: Name cannot be empty`** (~20 issues) — fixed: `sanitizeFunctionResponseNames` + tool-name propagation in `simple.go`.
- **`Function call is missing a thought_signature`** (~2 issues) — fixed: `ThinkingBudget=0` on tool turns + `thoughtSignaturesConsistent` guard.
- **`Too many AI spans: N exceeds limit of 100`** (~13 issues) — fixed: `WithMaxExportBatchSize(50)` + `WithMaxQueueSize(75)` in OTEL batcher.

No new errors. No regressions.

### What I fixed and shipped

Nothing to fix. PostHog is clean. No PRs created.

### What was left behind

Same as prior runs — no open items.

---

## Session log — 2026-04-30 (fourth run)

### What I reasoned on

Read `docs/changelog/20260430.md` (three prior runs today) before querying. All prior context confirmed held:
- 35 PostHog issues, all `resolved`, all first_seen `2026-04-25` (pre-fix burst).
- Fixes for Errors A–E shipped in PRs #114–#139 across April 27–28.
- System clean across all signals since April 26 in all 8 prior runs (5 on April 29 + 3 on April 30).

Queried PostHog this run:

| Source | Count |
|---|---|
| Error tracking issues (active, -7d) | 0 |
| Error tracking issues (all statuses, -7d) | 35, all `resolved`, all first_seen `2026-04-25` |
| Error/fatal logs (-24h) | 0 |

No new errors. No regressions. System confirmed steady-state clean.

### What I fixed and shipped

Nothing to fix. PostHog is clean. No PRs created.

### What was left behind

Same as all prior runs — no open items:
- **Production project** (id 398540) — `ingested_event: false`; no traffic to check.
- **PostHog PATCH assignee bug** — server-side; manual UI assignment still the only path.
- **`syncCtx := context.Background()` in Plaid sync** — accepted low-priority, no PostHog signal.
- **`log.Printf` calls** — 8 remain in `internal/observability/` (intentional bootstrap logging, correct as-is).

---

## Session log — 2026-04-30 (fifth run)

### What I reasoned on

Read `docs/changelog/20260430.md` (four prior runs today) before querying. All prior context held:
- 35 PostHog issues, all `resolved`, all `first_seen: 2026-04-25` (pre-fix burst).
- Fixes for Errors A–E shipped in PRs #114–#139 across April 27–28.
- System confirmed clean across all signals since April 26 in all 9 prior runs (5 on April 29 + 4 on April 30).

Queried PostHog this run:

| Source | Count |
|---|---|
| Error tracking issues (active) | 0 |
| Error tracking issues (all) | 35, all `resolved`, all `first_seen: 2026-04-25` |
| Error/fatal/warn logs since 2026-04-26 | 0 |
| Error/fatal/warn logs (last 2 days) | 0 |

No new errors. No regressions. System confirmed steady-state clean.

### What I fixed and shipped

Nothing to fix. PostHog is clean. No PRs created.

### What was left behind

Same as all prior runs — no open items:
- **Production project** (id 398540) — `ingested_event: false`; no traffic to check.
- **PostHog PATCH assignee bug** — server-side; manual UI assignment still the only path.
- **`syncCtx := context.Background()` in Plaid sync** — accepted low-priority, no PostHog signal.
- **`log.Printf` calls** — 8 remain in `internal/observability/` (intentional bootstrap logging, correct as-is).

---

## Session log — 2026-04-30 (seventh run)

### What I reasoned on

Read `docs/changelog/20260430.md` (six prior runs today) before querying. All prior context held:
- 35 PostHog issues, all `resolved`, all `first_seen: 2026-04-25` (pre-fix burst).
- Fixes for Errors A–E shipped in PRs #114–#139 across April 27–28.
- System confirmed clean across all signals since April 26 in all 11 prior runs (5 on April 29 + 6 on April 30).

Queried PostHog this run:

| Source | Count |
|---|---|
| Error tracking issues (active, -7d) | 0 |
| Error tracking issues (all, -90d) | 35, all `resolved`, all `first_seen: 2026-04-25` |
| Error/fatal/warn logs (-2d) | 0 |

No new errors. No regressions. System confirmed steady-state clean.

### What I fixed and shipped

Nothing to fix. PostHog is clean. No PRs created.

### What was left behind

Same as all prior runs — no open items:
- **Production project** (id 398540) — `ingested_event: false`; no traffic to check.
- **PostHog PATCH assignee bug** — server-side; manual UI assignment still the only path.
- **`syncCtx := context.Background()` in Plaid sync** — accepted low-priority, no PostHog signal.
- **`log.Printf` calls** — 8 remain in `internal/observability/` (intentional bootstrap logging, correct as-is).

---

## Session log — 2026-04-30 (sixth run)

### What I reasoned on

Read `docs/changelog/20260430.md` (five prior runs today) before querying. All prior context held:
- 35 PostHog issues, all `resolved`, all `first_seen: 2026-04-25` (pre-fix burst).
- Fixes for Errors A–E shipped in PRs #114–#139 across April 27–28.
- System confirmed clean across all signals since April 26 in all 10 prior runs (5 on April 29 + 5 on April 30).

Queried PostHog this run:

| Source | Count |
|---|---|
| Error tracking issues (active, -30d) | 0 |
| Error tracking issues (all statuses, -90d) | 35, all `resolved`, all `first_seen: 2026-04-25` |
| Error/fatal/warn logs (-2d) | 0 |

No new errors. No regressions. System confirmed steady-state clean.

### What I fixed and shipped

Nothing to fix. PostHog is clean. No PRs created.

### What was left behind

Same as all prior runs — no open items:
- **Production project** (id 398540) — `ingested_event: false`; no traffic to check.
- **PostHog PATCH assignee bug** — server-side; manual UI assignment still the only path.
- **`syncCtx := context.Background()` in Plaid sync** — accepted low-priority, no PostHog signal.
- **`log.Printf` calls** — 8 remain in `internal/observability/` (intentional bootstrap logging, correct as-is).
