# 2026-06-06 — Session 1: PostHog clean sweep (confirmed clean)

## Prior context (from 20260605.md sessions 1–11)

- Sessions 1–8 on 2026-06-05: 0 active errors, system clean. No code shipped.
- Session 9: Three bugs fixed — `FindByLastFourAndInstitution` silent data corruption (PR #910), Teller `console.log` access-token leak + dead `otherErrors` slice + redundant nil guards (PR #911).
- Session 10: 0 active errors, no new code.
- Session 11: Two bugs fixed — `SpendingSummary.tsx` debug `console.log` leaking `window.openai` on every render (PR #916), `config.GetEnv`/`ENVIRONMENT` double-default mismatch + dead `GetEnv` exported wrapper (PR #917).
- Latest code PR on main: `c45bd20` (#917). Latest commit: `45ee677` (#918, session 11 memory log — doc only).
- Service version `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` — PRs #859–#917 merged but not yet deployed.
- Structural `partial_failure errors=5` (5 Akahu expired tokens, not code-fixable) — will drop once `3f3ec2a...` is deployed (PR #780 + #859 in undeployed build).
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE transport: deprecated — skip all Linear tool calls.

## Reasoning

1. Read 20260605.md (all 11 sessions) — full context loaded.
2. Fetched origin/main — latest commit `45ee677` (PR #918, session 11 memory log). No new code since PR #917.
3. All PostHog checks ran in parallel:
   - `query-error-tracking-issues-list` (active, 30d) → **0 active issues**.
   - `query-logs` (warn/error/fatal, 7d, cfo-cloud) → **0**.
   - `query-logs` (info, -1h, cfo-cloud) → service.version `3f3ec2a...` — **still not deployed**.
   - `logs-count` searchTerm "partial_failure" (-7d) → **168** — structural baseline unchanged.
4. Per checklist rule: 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.

## PostHog state (2026-06-06, session 1)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| `partial_failure` count (7d) | **168** — stable baseline |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` — **still not deployed** |
| Latest commit on origin/main | `45ee677` (PR #918, 2026-06-05) — only changelog docs since last code PR (#917) |

## Shipped this session

Nothing — system clean. No PRs created.

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#917 merged but not deployed.
- Structural `errors=5` (5 Akahu expired tokens) — will auto-resolve once `3f3ec2a...` is deployed.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.

---

# 2026-06-06 — Session 2: PostHog clean sweep (confirmed clean)

## Prior context (from session 1 above)

- Session 1 today: 0 active errors, 0 warn/error/fatal logs, partial_failure=168 stable, service `3f3ec2a...` not deployed.
- Latest code PR on main: `c45bd20` (#917). Latest commit: `55ac527` (#919, session 1 memory log — doc only).
- PostHog assignee API: HTTP 500 — do not attempt programmatic assignment.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read 20260606.md (session 1) — full prior context loaded.
2. `git fetch origin main` → `55ac527` (session 1 changelog doc, #919) — no new code since #917.
3. All PostHog checks ran in parallel:
   - `query-error-tracking-issues-list` (active, 30d) → **0 active issues**.
   - `logs-count` (warn/error/fatal, 7d, cfo-cloud) → **0**.
   - `query-logs` (info, -1h, cfo-cloud, limit 1) → service.version `3f3ec2a...` — **still not deployed**.
   - `logs-count` searchTerm "partial_failure" (-7d) → **168** — stable baseline unchanged.
4. Per checklist rule: 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.

## PostHog state (2026-06-06, session 2)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| `partial_failure` count (7d) | **168** — stable baseline |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` — **still not deployed** |
| Latest commit on origin/main | `55ac527` (PR #919, 2026-06-06) — only changelog docs since last code PR (#917) |

## Shipped this session

Nothing — system clean. No PRs created.

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#917 merged but not deployed.
- Structural `errors=5` (5 Akahu expired tokens) — will auto-resolve once `3f3ec2a...` is deployed.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.

## Next run checklist

1. Read 20260606.md (this file) + 20260605.md (session 11 summary) first. Do NOT re-investigate prior work.
2. PostHog MCP defaults to "Personal CFO" (id: 407550) — verify active project.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `logs-count` with `query` wrapper: serviceNames=["cfo-cloud"], severityLevels=["warn","error","fatal"], -7d → expect 0.
5. `query-logs` (info, -1h, cfo-cloud, limit 1, excludeAttributes: false) → check `resource_attributes.service.version` — if new hash, deployment happened.
6. If deployed: verify `partial_failure errors=5` drops to 0 (Akahu 404 → disconnect fix #780 + Teller 403 fix #859 deployed).
7. `logs-count` searchTerm "partial_failure" (-7d) → expect ~168 if undeployed; expect drop if deployed.
8. `git fetch origin main && git log --oneline origin/main -5` → check for new code commits (expect `45ee677` or newer).
9. If new code commits: scan changed files for DRY/simplify opportunities, open issues/PRs.
10. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
11. PostHog assignee API HTTP 500 — do not attempt programmatic assignment.
12. Linear SSE deprecated — skip all Linear tool calls.
13. Branch fix PRs from `origin/main`, not from `claude/clever-knuth-*`.
14. All PostHog error types (CLICommandError, ProviderPullError, etc.) are from old `personal-cfo` binary — NOT in current `probably` codebase.
15. `syncTellerFields` — intentional (1 call site), do not extract.
16. `FindByLastFourAndInstitution` + Teller handler bugs — **fixed in PRs #910/#911**, do not reinvestigate.
17. GCS writer `Close()` pattern — intentional (commits upload), do NOT add defer. Do not re-flag.
18. `SpendingSummary.tsx` debug logs + `config.GetEnv` override — **fixed in PRs #916/#917**, do not reinvestigate.
19. When calling `query-logs` to check service version, use `excludeAttributes: false` so `resource_attributes.service.version` is visible.

---

# 2026-06-06 — Session 3: PostHog clean sweep (confirmed clean)

## Prior context (from sessions 1–2 above)

- Sessions 1–2 today: 0 active errors, 0 warn/error/fatal logs, partial_failure=168 stable, service `3f3ec2a...` not deployed.
- Latest code PR on main: `c45bd20` (#917). Latest commit: `3c0e5d5` (#920, session 2 memory log — doc only).
- PostHog assignee API: HTTP 500 — do not attempt programmatic assignment.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read 20260606.md (sessions 1–2) — full prior context loaded.
2. `git fetch origin main` → `3c0e5d5` (session 2 changelog doc, #920) — no new code since #917.
3. All PostHog checks ran in parallel:
   - `query-error-tracking-issues-list` (active, 30d) → **0 active issues**.
   - `logs-count` (warn/error/fatal, 7d, cfo-cloud) → **0**.
   - `logs-count` searchTerm "partial_failure" (-7d) → **168** — stable baseline unchanged.
   - `query-logs` (info, -1h, cfo-cloud, limit 1, excludeAttributes: false) → service.version `3f3ec2a...` — **still not deployed**.
4. Per checklist rule: 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.

## PostHog state (2026-06-06, session 3)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| `partial_failure` count (7d) | **168** — stable baseline |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` — **still not deployed** |
| Latest commit on origin/main | `3c0e5d5` (PR #920, 2026-06-06) — only changelog docs since last code PR (#917) |

## Shipped this session

Nothing — system clean. No PRs created.

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#917 merged but not deployed.
- Structural `errors=5` (5 Akahu expired tokens) — will auto-resolve once `3f3ec2a...` is deployed.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.

## Next run checklist

1. Read 20260606.md (this file) first. Do NOT re-investigate prior work.
2. PostHog MCP defaults to "Personal CFO" (id: 407550) — verify active project.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `logs-count` with `query` wrapper: serviceNames=["cfo-cloud"], severityLevels=["warn","error","fatal"], -7d → expect 0.
5. `query-logs` (info, -1h, cfo-cloud, limit 1, excludeAttributes: false) → check `resource_attributes.service.version` — if new hash, deployment happened.
6. If deployed: verify `partial_failure errors=5` drops to 0 (Akahu 404 → disconnect fix #780 + Teller 403 fix #859 deployed).
7. `logs-count` searchTerm "partial_failure" (-7d) → expect ~168 if undeployed; expect drop if deployed.
8. `git fetch origin main && git log --oneline origin/main -5` → check for new code commits.
9. If new code commits: scan changed files for DRY/simplify opportunities, open issues/PRs.
10. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
11. PostHog assignee API HTTP 500 — do not attempt programmatic assignment.
12. Linear SSE deprecated — skip all Linear tool calls.
13. Branch fix PRs from `origin/main`, not from `claude/clever-knuth-*`.
14. All PostHog error types (CLICommandError, ProviderPullError, etc.) are from old `personal-cfo` binary — NOT in current `probably` codebase.
15. `syncTellerFields` — intentional (1 call site), do not extract.
16. `FindByLastFourAndInstitution` + Teller handler bugs — **fixed in PRs #910/#911**, do not reinvestigate.
17. GCS writer `Close()` pattern — intentional (commits upload), do NOT add defer. Do not re-flag.
18. `SpendingSummary.tsx` debug logs + `config.GetEnv` override — **fixed in PRs #916/#917**, do not reinvestigate.
19. When calling `query-logs` to check service version, use `excludeAttributes: false` so `resource_attributes.service.version` is visible.

---

# 2026-06-06 — Session 5: PostHog clean sweep (confirmed clean)

## Prior context (from sessions 1–4 above)

- Sessions 1–4 today: 0 active errors, 0 warn/error/fatal logs, partial_failure=168 stable, service `3f3ec2a...` not deployed.
- Latest code PR on main: `c45bd20` (#917). Latest commit: `d427b8c` (#922, session 4 memory log — doc only).
- PostHog assignee API: HTTP 500 — do not attempt programmatic assignment.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read 20260606.md (sessions 1–4) — full prior context loaded.
2. `git fetch origin main` → `d427b8c` (session 4 changelog doc, #922) — no new code since #917.
3. All PostHog checks ran in parallel:
   - `query-error-tracking-issues-list` (active, 30d) → **0 active issues**.
   - `logs-count` (warn/error/fatal, 7d, cfo-cloud) → **0**.
   - `logs-count` searchTerm "partial_failure" (-7d) → **168** — stable baseline unchanged.
   - `query-logs` (info, -1h, cfo-cloud, limit 1, excludeAttributes: false) → service.version `3f3ec2a...` — **still not deployed**.
4. Per checklist rule: 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.

## PostHog state (2026-06-06, session 5)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| `partial_failure` count (7d) | **168** — stable baseline |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` — **still not deployed** |
| Latest commit on origin/main | `d427b8c` (PR #922, 2026-06-06) — only changelog docs since last code PR (#917) |

## Shipped this session

Nothing — system clean. No PRs created.

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#917 merged but not deployed.
- Structural `errors=5` (5 Akahu expired tokens) — will auto-resolve once `3f3ec2a...` is deployed.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.

## Next run checklist

1. Read 20260606.md (this file) first. Do NOT re-investigate prior work.
2. PostHog MCP defaults to "Personal CFO" (id: 407550) — verify active project.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `logs-count` with `query` wrapper: serviceNames=["cfo-cloud"], severityLevels=["warn","error","fatal"], -7d → expect 0.
5. `query-logs` (info, -1h, cfo-cloud, limit 1, excludeAttributes: false) → check `resource_attributes.service.version` — if new hash, deployment happened.
6. If deployed: verify `partial_failure errors=5` drops to 0 (Akahu 404 → disconnect fix #780 + Teller 403 fix #859 deployed).
7. `logs-count` searchTerm "partial_failure" (-7d) → expect ~168 if undeployed; expect drop if deployed.
8. `git fetch origin main && git log --oneline origin/main -5` → check for new code commits.
9. If new code commits: scan changed files for DRY/simplify opportunities, open issues/PRs.
10. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
11. PostHog assignee API HTTP 500 — do not attempt programmatic assignment.
12. Linear SSE deprecated — skip all Linear tool calls.
13. Branch fix PRs from `origin/main`, not from `claude/clever-knuth-*`.
14. All PostHog error types (CLICommandError, ProviderPullError, etc.) are from old `personal-cfo` binary — NOT in current `probably` codebase.
15. `syncTellerFields` — intentional (1 call site), do not extract.
16. `FindByLastFourAndInstitution` + Teller handler bugs — **fixed in PRs #910/#911**, do not reinvestigate.
17. GCS writer `Close()` pattern — intentional (commits upload), do NOT add defer. Do not re-flag.
18. `SpendingSummary.tsx` debug logs + `config.GetEnv` override — **fixed in PRs #916/#917**, do not reinvestigate.
19. When calling `query-logs` to check service version, use `excludeAttributes: false` so `resource_attributes.service.version` is visible.

---

# 2026-06-06 — Session 4: PostHog clean sweep (confirmed clean)

## Prior context (from sessions 1–3 above)

- Sessions 1–3 today: 0 active errors, 0 warn/error/fatal logs, partial_failure=168 stable, service `3f3ec2a...` not deployed.
- Latest code PR on main: `c45bd20` (#917). Latest commit: `5046d29` (#921, session 3 memory log — doc only).
- PostHog assignee API: HTTP 500 — do not attempt programmatic assignment.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read 20260606.md (sessions 1–3) — full prior context loaded.
2. `git fetch origin main` → `5046d29` (session 3 changelog doc, #921) — no new code since #917.
3. All PostHog checks ran in parallel:
   - `query-error-tracking-issues-list` (active, 30d) → **0 active issues**.
   - `logs-count` (warn/error/fatal, 7d, cfo-cloud) → **0**.
   - `query-logs` (info, -1h, cfo-cloud, limit 1, excludeAttributes: false) → service.version `3f3ec2a...` — **still not deployed**.
   - `logs-count` searchTerm "partial_failure" (-7d) → **168** — stable baseline unchanged.
4. Per checklist rule: 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.

## PostHog state (2026-06-06, session 4)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| `partial_failure` count (7d) | **168** — stable baseline |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` — **still not deployed** |
| Latest commit on origin/main | `5046d29` (PR #921, 2026-06-06) — only changelog docs since last code PR (#917) |

## Shipped this session

Nothing — system clean. No PRs created.

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#917 merged but not deployed.
- Structural `errors=5` (5 Akahu expired tokens) — will auto-resolve once `3f3ec2a...` is deployed.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.

## Next run checklist

1. Read 20260606.md (this file) first. Do NOT re-investigate prior work.
2. PostHog MCP defaults to "Personal CFO" (id: 407550) — verify active project.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `logs-count` with `query` wrapper: serviceNames=["cfo-cloud"], severityLevels=["warn","error","fatal"], -7d → expect 0.
5. `query-logs` (info, -1h, cfo-cloud, limit 1, excludeAttributes: false) → check `resource_attributes.service.version` — if new hash, deployment happened.
6. If deployed: verify `partial_failure errors=5` drops to 0 (Akahu 404 → disconnect fix #780 + Teller 403 fix #859 deployed).
7. `logs-count` searchTerm "partial_failure" (-7d) → expect ~168 if undeployed; expect drop if deployed.
8. `git fetch origin main && git log --oneline origin/main -5` → check for new code commits.
9. If new code commits: scan changed files for DRY/simplify opportunities, open issues/PRs.
10. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
11. PostHog assignee API HTTP 500 — do not attempt programmatic assignment.
12. Linear SSE deprecated — skip all Linear tool calls.
13. Branch fix PRs from `origin/main`, not from `claude/clever-knuth-*`.
14. All PostHog error types (CLICommandError, ProviderPullError, etc.) are from old `personal-cfo` binary — NOT in current `probably` codebase.
15. `syncTellerFields` — intentional (1 call site), do not extract.
16. `FindByLastFourAndInstitution` + Teller handler bugs — **fixed in PRs #910/#911**, do not reinvestigate.
17. GCS writer `Close()` pattern — intentional (commits upload), do NOT add defer. Do not re-flag.
18. `SpendingSummary.tsx` debug logs + `config.GetEnv` override — **fixed in PRs #916/#917**, do not reinvestigate.
19. When calling `query-logs` to check service version, use `excludeAttributes: false` so `resource_attributes.service.version` is visible.

---

# 2026-06-06 — Session 6: Reconciliation error handling gaps fixed (PR #926)

## Prior context (from sessions 1–5 above)

- Sessions 1–5 today: 0 active errors, 0 warn/error/fatal logs, partial_failure=168 stable, service `3f3ec2a...` not deployed.
- Latest code PR on main before this session: `c45bd20` (#917).
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read 20260606.md (sessions 1–5) and 20260605.md (all 11 sessions) — full prior context loaded.
2. Ran standard PostHog checks: 0 active issues, 0 warn/error/fatal logs (7d), partial_failure=168, service `3f3ec2a...` not deployed.
3. User requested investigating all historical errors. Drilled into PostHog event details for resolved issues: `ProviderPullError` (100), `ProviderSyncError` (92), `SyncAccountPullError` (106), `ProviderBalancePullError` (10), `TellerWebhookTransactionAggregationError` (9). All stack traces confirmed from `internal/cloud/server.go` (old `personal-cfo` binary — path doesn't exist in current `probably` codebase).
4. Cross-referenced current codebase for analogous patterns. Found two genuine gaps in `internal/sync/teller.go`:
   - **`FullResync()` line 1203**: `reconcileOpeningBalance` errors silently swallowed — no `ConnectionDisconnectedError` propagation, no `TellerAPIError` classification, no `CaptureFailure`.
   - **`DeleteAndResync()` line 955**: had `TellerAPIError` check + `CaptureFailure`, but missing `ConnectionDisconnectedError` check.
   - Stale floating comment (lines 1224–1226) about `autoCategorize` (removed code).
5. `SyncTransactions()` has the correct three-way pattern; applied the same to both.
6. Confirmed `TellerFullResync` handler (PR #889) already has correct error classification at the handler layer — the gap was inside the service-layer functions.
7. Applied fixes, `go build ./internal/sync/...` passes. Created issue #925, PR #926, merged directly (all CI passed).

## PostHog state (2026-06-06, session 6)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| All issues (90d) | 19 — 2 suppressed CLICommandError (local Mac CLI, old binary), 17 resolved |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| `partial_failure` count (7d) | **168** — stable baseline |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` — **still not deployed** |
| Latest commit on origin/main after session | `00a2d01` (PR #926 squash merge) |

## Shipped this session

| PR | Issue | Change |
|---|---|---|
| #926 | #925 | `internal/sync/teller.go`: `FullResync()` reconcileOpeningBalance now follows the three-way pattern (disconnected → propagate, TellerAPIError → warn, else → warn + CaptureFailure). `DeleteAndResync()` adds missing `ConnectionDisconnectedError` check. Removes stale floating `autoCategorize` comment. |

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#926 merged but not deployed.
- Structural `errors=5` (5 Akahu expired tokens) — will auto-resolve once deployed.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.
- All 19 PostHog error tracking issues are from the old `personal-cfo` binary — cannot be resolved via the API.

## Next run checklist

1. Read 20260606.md (this file, all 6 sessions) first. Do NOT re-investigate prior work.
2. PostHog MCP defaults to "Personal CFO" (id: 407550) — verify active project.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `logs-count` with `query` wrapper: serviceNames=["cfo-cloud"], severityLevels=["warn","error","fatal"], -7d → expect 0.
5. `query-logs` (info, -1h, cfo-cloud, limit 1, excludeAttributes: false) → check `resource_attributes.service.version` — if new hash, deployment happened.
6. If deployed: verify `partial_failure errors=5` drops to 0 (Akahu 404 → disconnect fix #780 + Teller 403 fix #859 deployed).
7. `logs-count` searchTerm "partial_failure" (-7d) → expect ~168 if undeployed; expect drop if deployed.
8. `git fetch origin main && git log --oneline origin/main -5` → check for new code commits (expect `00a2d01` or newer).
9. If new code commits: scan changed files for DRY/simplify opportunities, open issues/PRs.
10. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
11. PostHog assignee API HTTP 500 — do not attempt programmatic assignment.
12. Linear SSE deprecated — skip all Linear tool calls.
13. Branch fix PRs from `origin/main`, not from `claude/clever-knuth-*`.
14. All PostHog error types (CLICommandError, ProviderPullError, etc.) are from old `personal-cfo` binary — NOT in current `probably` codebase.
15. `syncTellerFields` — intentional (1 call site), do not extract.
16. `FindByLastFourAndInstitution` + Teller handler bugs — **fixed in PRs #910/#911**, do not reinvestigate.
17. GCS writer `Close()` pattern — intentional (commits upload), do NOT add defer. Do not re-flag.
18. `SpendingSummary.tsx` debug logs + `config.GetEnv` override — **fixed in PRs #916/#917**, do not reinvestigate.
19. `FullResync()` + `DeleteAndResync()` reconciliation error handling — **fixed in PR #926**, do not reinvestigate.
20. When calling `query-logs` to check service version, use `excludeAttributes: false` so `resource_attributes.service.version` is visible.

---

# 2026-06-06 — Session 7: PostHog clean sweep (confirmed clean)

## Prior context (from sessions 1–6 above)

- Sessions 1–5 today: 0 active errors, 0 warn/error/fatal logs, partial_failure=168 stable, service `3f3ec2a...` not deployed.
- Session 6: Fixed `FullResync()` + `DeleteAndResync()` reconciliation error handling gaps (PR #926). Latest code commit: `00a2d01`.
- Latest memory log commit: `d5c0b52` (PR #927).
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read 20260606.md (all 6 sessions) — full prior context loaded.
2. `git fetch origin main` → `d5c0b52` (session 6 changelog doc, #927) — no new code since #926 (`00a2d01`).
3. All PostHog checks ran in parallel:
   - `query-error-tracking-issues-list` (active, 30d) → **0 active issues**.
   - `logs-count` (warn/error/fatal, 7d, cfo-cloud) → **0**.
   - `logs-count` searchTerm "partial_failure" (-7d) → **168** — stable baseline unchanged.
   - `query-logs` (info, -1h, cfo-cloud, limit 1, excludeAttributes: false) → service.version `3f3ec2a...` — **still not deployed**.
4. Per checklist rule: 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.

## PostHog state (2026-06-06, session 7)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| `partial_failure` count (7d) | **168** — stable baseline |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` — **still not deployed** |
| Latest commit on origin/main | `d5c0b52` (PR #927, 2026-06-06) — only changelog docs since last code PR (#926) |

## Shipped this session

Nothing — system clean. No PRs created.

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#926 merged but not deployed.
- Structural `errors=5` (5 Akahu expired tokens) — will auto-resolve once deployed.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.

## Next run checklist

1. Read 20260606.md (this file, all 7 sessions) first. Do NOT re-investigate prior work.
2. PostHog MCP defaults to "Personal CFO" (id: 407550) — verify active project.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `logs-count` with `query` wrapper: serviceNames=["cfo-cloud"], severityLevels=["warn","error","fatal"], -7d → expect 0.
5. `query-logs` (info, -1h, cfo-cloud, limit 1, excludeAttributes: false) → check `resource_attributes.service.version` — if new hash, deployment happened.
6. If deployed: verify `partial_failure errors=5` drops to 0 (Akahu 404 → disconnect fix #780 + Teller 403 fix #859 deployed).
7. `logs-count` searchTerm "partial_failure" (-7d) → expect ~168 if undeployed; expect drop if deployed.
8. `git fetch origin main && git log --oneline origin/main -5` → check for new code commits.
9. If new code commits: scan changed files for DRY/simplify opportunities, open issues/PRs.
10. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
11. PostHog assignee API HTTP 500 — do not attempt programmatic assignment.
12. Linear SSE deprecated — skip all Linear tool calls.
13. Branch fix PRs from `origin/main`, not from `claude/clever-knuth-*`.
14. All PostHog error types (CLICommandError, ProviderPullError, etc.) are from old `personal-cfo` binary — NOT in current `probably` codebase.
15. `syncTellerFields` — intentional (1 call site), do not extract.
16. `FindByLastFourAndInstitution` + Teller handler bugs — **fixed in PRs #910/#911**, do not reinvestigate.
17. GCS writer `Close()` pattern — intentional (commits upload), do NOT add defer. Do not re-flag.
18. `SpendingSummary.tsx` debug logs + `config.GetEnv` override — **fixed in PRs #916/#917**, do not reinvestigate.
19. `FullResync()` + `DeleteAndResync()` reconciliation error handling — **fixed in PR #926**, do not reinvestigate.
20. When calling `query-logs` to check service version, use `excludeAttributes: false` so `resource_attributes.service.version` is visible.

---

# 2026-06-06 — Session 8: PostHog clean sweep (confirmed clean)

## Prior context (from sessions 1–7 above)

- Sessions 1–5 today: 0 active errors, 0 warn/error/fatal logs, partial_failure=168 stable, service `3f3ec2a...` not deployed.
- Session 6: Fixed `FullResync()` + `DeleteAndResync()` reconciliation error handling gaps (PR #926). Latest code commit: `00a2d01`.
- Session 7: 0 active errors, no new code. Latest memory log: `40fe063` (PR #928).
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read 20260606.md (all 7 sessions) — full prior context loaded.
2. `git fetch origin main` → `6ed56b4` (PR #928 merge, session 7 changelog) — no new code since #926 (`00a2d01`).
3. All PostHog checks ran in parallel:
   - `query-error-tracking-issues-list` (active, 30d) → **0 active issues**.
   - `query-error-tracking-issues-list` (all, 90d) → 20 issues — all resolved or suppressed, all from old `personal-cfo` binary.
   - `logs-count` (warn/error/fatal, 7d, cfo-cloud) → **0**.
   - `logs-count` searchTerm "partial_failure" (-7d) → **168** — stable baseline unchanged.
   - `query-logs` (info, -1h, cfo-cloud, limit 1, excludeAttributes: false) → service.version `3f3ec2a...` — **still not deployed**.
4. Per checklist rule: 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.

## PostHog state (2026-06-06, session 8)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| All issues (90d) | 20 — 2 suppressed CLICommandError (local Mac CLI, old binary), 18 resolved |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| `partial_failure` count (7d) | **168** — stable baseline |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` — **still not deployed** |
| Latest commit on origin/main | `6ed56b4` (PR #928, 2026-06-06) — only changelog docs since last code PR (#926) |

## Shipped this session

Nothing — system clean. No PRs created.

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#926 merged but not deployed.
- Structural `errors=5` (5 Akahu expired tokens) — will auto-resolve once deployed.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.

## Next run checklist

1. Read 20260606.md (this file, all 9 sessions) first. Do NOT re-investigate prior work.
2. PostHog MCP defaults to "Personal CFO" (id: 407550) — verify active project.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `logs-count` with `query` wrapper: serviceNames=["cfo-cloud"], severityLevels=["warn","error","fatal"], -7d → expect 0.
5. `query-logs` (info, -1h, cfo-cloud, limit 1, excludeAttributes: false) → check `resource_attributes.service.version` — if new hash, deployment happened.
6. If deployed: verify `partial_failure errors=5` drops to 0 (Akahu 404 → disconnect fix #780 + Teller 403 fix #859 deployed).
7. `logs-count` searchTerm "partial_failure" (-7d) → expect ~168 if undeployed; expect drop if deployed.
8. `git fetch origin main && git log --oneline origin/main -5` → check for new code commits.
9. If new code commits: scan changed files for DRY/simplify opportunities, open issues/PRs.
10. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
11. PostHog assignee API HTTP 500 — do not attempt programmatic assignment.
12. Linear SSE deprecated — skip all Linear tool calls.
13. Branch fix PRs from `origin/main`, not from `claude/clever-knuth-*`.
14. All PostHog error types (CLICommandError, ProviderPullError, etc.) are from old `personal-cfo` binary — NOT in current `probably` codebase.
15. `syncTellerFields` — intentional (1 call site), do not extract.
16. `FindByLastFourAndInstitution` + Teller handler bugs — **fixed in PRs #910/#911**, do not reinvestigate.
17. GCS writer `Close()` pattern — intentional (commits upload), do NOT add defer. Do not re-flag.
18. `SpendingSummary.tsx` debug logs + `config.GetEnv` override — **fixed in PRs #916/#917**, do not reinvestigate.
19. `FullResync()` + `DeleteAndResync()` reconciliation error handling — **fixed in PR #926**, do not reinvestigate.
20. When calling `query-logs` to check service version, use `excludeAttributes: false` so `resource_attributes.service.version` is visible.

---

# 2026-06-06 — Session 9: PostHog clean sweep (confirmed clean)

## Prior context (from sessions 1–8 above)

- Sessions 1–5 today: 0 active errors, 0 warn/error/fatal logs, partial_failure=168 stable, service `3f3ec2a...` not deployed.
- Session 6: Fixed `FullResync()` + `DeleteAndResync()` reconciliation error handling gaps (PR #926). Latest code commit: `00a2d01`.
- Sessions 7–8: 0 active errors, no new code. Latest memory log: `83f542e` (PR #930, session 8).
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read 20260606.md (all 8 sessions) — full prior context loaded.
2. `git fetch origin main` → `83f542e` (PR #930, session 8 changelog doc) — no new code since #926 (`00a2d01`).
3. All PostHog checks ran in parallel:
   - `query-error-tracking-issues-list` (active, 30d) → **0 active issues**.
   - `logs-count` (warn/error/fatal, 7d, cfo-cloud) → **0**.
   - `logs-count` searchTerm "partial_failure" (-7d) → **168** — stable baseline unchanged.
   - `query-logs` (info, -1h, cfo-cloud, limit 1, excludeAttributes: false) → service.version `3f3ec2a...` — **still not deployed**.
4. Per checklist rule: 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.

## PostHog state (2026-06-06, session 9)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| `partial_failure` count (7d) | **168** — stable baseline |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` — **still not deployed** |
| Latest commit on origin/main | `83f542e` (PR #930, 2026-06-06) — only changelog docs since last code PR (#926) |

## Shipped this session

Nothing — system clean. No PRs created.

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#926 merged but not deployed.
- Structural `errors=5` (5 Akahu expired tokens) — will auto-resolve once deployed.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.

## Next run checklist

1. Read 20260606.md (this file, all 9 sessions) first. Do NOT re-investigate prior work.
2. PostHog MCP defaults to "Personal CFO" (id: 407550) — verify active project.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `logs-count` with `query` wrapper: serviceNames=["cfo-cloud"], severityLevels=["warn","error","fatal"], -7d → expect 0.
5. `query-logs` (info, -1h, cfo-cloud, limit 1, excludeAttributes: false) → check `resource_attributes.service.version` — if new hash, deployment happened.
6. If deployed: verify `partial_failure errors=5` drops to 0 (Akahu 404 → disconnect fix #780 + Teller 403 fix #859 deployed).
7. `logs-count` searchTerm "partial_failure" (-7d) → expect ~168 if undeployed; expect drop if deployed.
8. `git fetch origin main && git log --oneline origin/main -5` → check for new code commits.
9. If new code commits: scan changed files for DRY/simplify opportunities, open issues/PRs.
10. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
11. PostHog assignee API HTTP 500 — do not attempt programmatic assignment.
12. Linear SSE deprecated — skip all Linear tool calls.
13. Branch fix PRs from `origin/main`, not from `claude/clever-knuth-*`.
14. All PostHog error types (CLICommandError, ProviderPullError, etc.) are from old `personal-cfo` binary — NOT in current `probably` codebase.
15. `syncTellerFields` — intentional (1 call site), do not extract.
16. `FindByLastFourAndInstitution` + Teller handler bugs — **fixed in PRs #910/#911**, do not reinvestigate.
17. GCS writer `Close()` pattern — intentional (commits upload), do NOT add defer. Do not re-flag.
18. `SpendingSummary.tsx` debug logs + `config.GetEnv` override — **fixed in PRs #916/#917**, do not reinvestigate.
19. `FullResync()` + `DeleteAndResync()` reconciliation error handling — **fixed in PR #926**, do not reinvestigate.
20. When calling `query-logs` to check service version, use `excludeAttributes: false` so `resource_attributes.service.version` is visible.

---

# 2026-06-06 — Session 10: PostHog clean sweep (confirmed clean)

## Prior context (from sessions 1–9 above)

- Sessions 1–5 today: 0 active errors, 0 warn/error/fatal logs, partial_failure=168 stable, service `3f3ec2a...` not deployed.
- Session 6: Fixed `FullResync()` + `DeleteAndResync()` reconciliation error handling gaps (PR #926). Latest code commit: `00a2d01`.
- Sessions 7–9: 0 active errors, no new code. Latest memory log: `d35a8ae` (PR #931, session 9).
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read 20260606.md (all 9 sessions) — full prior context loaded.
2. `git fetch origin main` → `d35a8ae` (PR #931, session 9 changelog doc) — no new code since #926 (`00a2d01`).
3. All PostHog checks ran in parallel:
   - `query-error-tracking-issues-list` (active, 30d) → **0 active issues**.
   - `logs-count` (warn/error/fatal, 7d, cfo-cloud) → **0**.
   - `logs-count` searchTerm "partial_failure" (-7d) → **168** — stable baseline unchanged.
   - `query-logs` (info, -1h, cfo-cloud, limit 1, excludeAttributes: false) → service.version `3f3ec2a...` — **still not deployed**.
4. Per checklist rule: 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.

## PostHog state (2026-06-06, session 10)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| `partial_failure` count (7d) | **168** — stable baseline |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` — **still not deployed** |
| Latest commit on origin/main | `d35a8ae` (PR #931, 2026-06-06) — only changelog docs since last code PR (#926) |

## Shipped this session

Nothing — system clean. No PRs created.

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#926 merged but not deployed.
- Structural `errors=5` (5 Akahu expired tokens) — will auto-resolve once deployed.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.

## Next run checklist

1. Read 20260606.md (this file, all 10 sessions) first. Do NOT re-investigate prior work.
2. PostHog MCP defaults to "Personal CFO" (id: 407550) — verify active project.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `logs-count` with `query` wrapper: serviceNames=["cfo-cloud"], severityLevels=["warn","error","fatal"], -7d → expect 0.
5. `query-logs` (info, -1h, cfo-cloud, limit 1, excludeAttributes: false) → check `resource_attributes.service.version` — if new hash, deployment happened.
6. If deployed: verify `partial_failure errors=5` drops to 0 (Akahu 404 → disconnect fix #780 + Teller 403 fix #859 deployed).
7. `logs-count` searchTerm "partial_failure" (-7d) → expect ~168 if undeployed; expect drop if deployed.
8. `git fetch origin main && git log --oneline origin/main -5` → check for new code commits.
9. If new code commits: scan changed files for DRY/simplify opportunities, open issues/PRs.
10. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
11. PostHog assignee API HTTP 500 — do not attempt programmatic assignment.
12. Linear SSE deprecated — skip all Linear tool calls.
13. Branch fix PRs from `origin/main`, not from `claude/clever-knuth-*`.
14. All PostHog error types (CLICommandError, ProviderPullError, etc.) are from old `personal-cfo` binary — NOT in current `probably` codebase.
15. `syncTellerFields` — intentional (1 call site), do not extract.
16. `FindByLastFourAndInstitution` + Teller handler bugs — **fixed in PRs #910/#911**, do not reinvestigate.
17. GCS writer `Close()` pattern — intentional (commits upload), do NOT add defer. Do not re-flag.
18. `SpendingSummary.tsx` debug logs + `config.GetEnv` override — **fixed in PRs #916/#917**, do not reinvestigate.
19. `FullResync()` + `DeleteAndResync()` reconciliation error handling — **fixed in PR #926**, do not reinvestigate.
20. When calling `query-logs` to check service version, use `excludeAttributes: false` so `resource_attributes.service.version` is visible.

---

# 2026-06-06 — Session 12: PostHog clean sweep (confirmed clean)

## Prior context (from sessions 1–11 above)

- Sessions 1–5 today: 0 active errors, 0 warn/error/fatal logs, partial_failure=168 stable, service `3f3ec2a...` not deployed.
- Session 6: Fixed `FullResync()` + `DeleteAndResync()` reconciliation error handling (PR #926).
- Sessions 7–10: 0 active errors, no new code.
- Session 11: Fixed `clearAkahuConnectionCredentials` uuid.Nil bug + `finishAkahuConnect` DRY (PR #934). Latest code commit `7f50c28`. Latest memory log `488de4a` (PR #935).
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read 20260606.md (all 11 sessions) — full prior context loaded.
2. `git fetch origin main` → `488de4a` (session 11 changelog doc, PR #935) — no new code since #934 (`7f50c28`).
3. All PostHog checks ran in parallel:
   - `query-error-tracking-issues-list` (active, 30d) → **0 active issues**.
   - `logs-count` (warn/error/fatal, 7d, cfo-cloud) → returned 13 — **false positive**: all 13 are body-text matches on "errors=5" in info-level `partial_failure` log lines. `query-logs` with severity filter confirms all entries are `severity_text: info`.
   - `query-logs` (searchTerm "error", -7d, cfo-cloud) → 30 results, all `severity_text: info`, body `provider sync complete ... errors=5` — the stable Akahu baseline.
   - Service version `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` — **still not deployed**.
   - `partial_failure errors=5` pattern — unchanged hourly, structural baseline confirmed.
4. `logs-count` with severityLevels filter appears to match body text rather than severity field — do not treat non-zero count as actionable; always verify with `query-logs` to confirm `severity_text`.
5. Per checklist rule: 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.

## PostHog state (2026-06-06, session 12)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| All issues (90d) | 20 — 2 suppressed CLICommandError (local Mac CLI, old binary), 18 resolved |
| True warn/error/fatal logs (7d) | **0** — `logs-count` returns 13 but those are body-text false positives (all `severity_text: info`) |
| `partial_failure` count (7d) | stable baseline — 5 Akahu expired tokens, structural |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` — **still not deployed** |
| Latest commit on origin/main | `488de4a` (PR #935, 2026-06-06) — only changelog docs since last code PR (#934) |

## Shipped this session

Nothing — system clean. No PRs created.

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#934 merged but not deployed.
- Structural `errors=5` (5 Akahu expired tokens) — will auto-resolve once deployed.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.

## Next run checklist

1. Read 20260606.md (this file, all 12 sessions) first. Do NOT re-investigate prior work.
2. PostHog MCP defaults to "Personal CFO" (id: 407550) — verify active project.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. **Do NOT trust `logs-count` with severityLevels filter** — it matches body text, not severity field. Always verify with `query-logs` (check `severity_text` in results).
5. `query-logs` (info, -1h, cfo-cloud, limit 1, excludeAttributes: false) → check `resource_attributes.service.version`.
6. If deployed: verify `partial_failure errors=5` drops to 0 (Akahu 404 → disconnect fix #780 + Teller 403 fix #859 deployed).
7. `logs-count` searchTerm "partial_failure" (-7d) → expect ~168 if undeployed; expect drop if deployed.
8. `git fetch origin main && git log --oneline origin/main -5` → check for new code commits.
9. If new code commits: scan changed files for DRY/simplify opportunities, open issues/PRs.
10. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
11. PostHog assignee API HTTP 500 — do not attempt programmatic assignment.
12. Linear SSE deprecated — skip all Linear tool calls.
13. Branch fix PRs from `origin/main`, not from `claude/clever-knuth-*`.
14. All PostHog error types (CLICommandError, ProviderPullError, etc.) are from old `personal-cfo` binary — NOT in current `probably` codebase.
15. `syncTellerFields` — intentional (1 call site), do not extract.
16. `FindByLastFourAndInstitution` + Teller handler bugs — **fixed in PRs #910/#911**, do not reinvestigate.
17. GCS writer `Close()` pattern — intentional (commits upload), do NOT add defer. Do not re-flag.
18. `SpendingSummary.tsx` debug logs + `config.GetEnv` override — **fixed in PRs #916/#917**, do not reinvestigate.
19. `FullResync()` + `DeleteAndResync()` reconciliation error handling — **fixed in PR #926**, do not reinvestigate.
20. `clearAkahuConnectionCredentials` uuid.Nil bug + `finishAkahuConnect` DRY — **fixed in PR #934**, do not reinvestigate.
21. `handleAkahuTokenDelete` N+1 API pattern — intentional, do not flag.
22. When calling `query-logs` to check service version, use `excludeAttributes: false` so `resource_attributes.service.version` is visible.

---

# 2026-06-06 — Session 13: PostHog clean sweep (confirmed clean)

## Prior context (from sessions 1–12 above)

- Sessions 1–5 today: 0 active errors, 0 warn/error/fatal logs, partial_failure=168 stable, service `3f3ec2a...` not deployed.
- Session 6: Fixed `FullResync()` + `DeleteAndResync()` reconciliation error handling (PR #926).
- Sessions 7–10: 0 active errors, no new code.
- Session 11: Fixed `clearAkahuConnectionCredentials` uuid.Nil bug + `finishAkahuConnect` DRY (PR #934).
- Session 12: 0 active errors; discovered `logs-count` severityLevels filter matches body text (false positives) — must verify with `query-logs`. Latest memory log: `c7a1e6f` (PR #937).
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read 20260606.md (all 12 sessions) — full prior context loaded.
2. `git fetch origin main` → `6d0a468` (PR #937 merge, session 12 changelog doc) — PR #937 only changed `docs/changelog/20260606.md`. No new code since #934 (`7f50c28`).
3. All PostHog checks ran in parallel:
   - `query-error-tracking-issues-list` (active, 30d) → **0 active issues**.
   - `query-logs` (warn/error/fatal, -7d, cfo-cloud) → **0 results**.
   - `query-logs` (info, -1h, cfo-cloud, limit 1, excludeAttributes: false) → service.version `3f3ec2a...` — **still not deployed**.
4. Per checklist rule: 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.

## PostHog state (2026-06-06, session 13)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| Service version | `3f3ec2a69e2f608fc9d7c2ca5e5c8b7fe3a6296e` — **still not deployed** |
| Latest commit on origin/main | `6d0a468` (PR #937, 2026-06-06) — only changelog docs since last code PR (#934) |

## Shipped this session

Nothing — system clean. No PRs created.

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#934 merged but not deployed.
- Structural `errors=5` (5 Akahu expired tokens) — will auto-resolve once deployed.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.

## Next run checklist

1. Read 20260606.md (this file, all 13 sessions) first. Do NOT re-investigate prior work.
2. PostHog MCP defaults to "Personal CFO" (id: 407550) — verify active project.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. **Do NOT trust `logs-count` with severityLevels filter** — it matches body text, not severity field. Always verify with `query-logs` (check `severity_text` in results).
5. `query-logs` (warn/error/fatal, -7d, cfo-cloud) → expect 0.
6. `query-logs` (info, -1h, cfo-cloud, limit 1, excludeAttributes: false) → check `resource_attributes.service.version`.
7. If deployed: verify `partial_failure errors=5` drops to 0 (Akahu 404 → disconnect fix #780 + Teller 403 fix #859 deployed).
8. `git fetch origin main && git log --oneline origin/main -5` → check for new code commits (expect `6d0a468` or newer).
9. If new code commits: scan changed files for DRY/simplify opportunities, open issues/PRs.
10. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
11. PostHog assignee API HTTP 500 — do not attempt programmatic assignment.
12. Linear SSE deprecated — skip all Linear tool calls.
13. Branch fix PRs from `origin/main`, not from `claude/clever-knuth-*`.
14. All PostHog error types (CLICommandError, ProviderPullError, etc.) are from old `personal-cfo` binary — NOT in current `probably` codebase.
15. `syncTellerFields` — intentional (1 call site), do not extract.
16. `FindByLastFourAndInstitution` + Teller handler bugs — **fixed in PRs #910/#911**, do not reinvestigate.
17. GCS writer `Close()` pattern — intentional (commits upload), do NOT add defer. Do not re-flag.
18. `SpendingSummary.tsx` debug logs + `config.GetEnv` override — **fixed in PRs #916/#917**, do not reinvestigate.
19. `FullResync()` + `DeleteAndResync()` reconciliation error handling — **fixed in PR #926**, do not reinvestigate.
20. `clearAkahuConnectionCredentials` uuid.Nil bug + `finishAkahuConnect` DRY — **fixed in PR #934**, do not reinvestigate.
21. `handleAkahuTokenDelete` N+1 API pattern — intentional, do not flag.
22. When calling `query-logs` to check service version, use `excludeAttributes: false` so `resource_attributes.service.version` is visible.

---

# 2026-06-06 — Session 11: Akahu credential clearing bug + sync duplication fixed (PR #934)

## Prior context (from sessions 1–10 above)

- Sessions 1–5: 0 active errors, 0 warn/error/fatal logs, partial_failure=168 stable, service `3f3ec2a...` not deployed.
- Session 6: Fixed `FullResync()` + `DeleteAndResync()` reconciliation error handling (PR #926).
- Sessions 7–10: 0 active errors, no new code. Latest memory log: `713c58a` (PR #932, session 10).
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read 20260606.md (all 10 sessions) — full prior context loaded.
2. `git fetch origin main` → `713c58a` (session 10 changelog, #932) — no new code since #926.
3. PostHog checks: 0 active issues, 0 warn/error/fatal logs (7d, cfo-cloud). 19 total issues (all resolved/suppressed, all from old `personal-cfo` binary).
4. User requested full historical error investigation + fixes. All stack traces reference `github.com/asomervell/personal-cfo/internal/cloud/server.go` — not in current `probably` codebase.
5. Found two genuine issues in `internal/handlers/akahu.go`:
   - **Critical bug:** `clearAkahuConnectionCredentials` called `GetByLedgerID(ctx, uuid.Nil)` → queries `WHERE ledger_id = '00000000-...'` → returns 0 rows → credentials NEVER cleared on Akahu disconnect. Impact: stale tokens persist in DB after revocation. Teller equivalent correctly uses `GetByConnectionID`.
   - **DRY violation:** `akahuPersonalAppConnect` and `AkahuCallback` had ~20 identical lines: create sync service → sync accounts → loop syncing transactions → redirect to /accounts.
6. Applied both fixes. `go build ./... && go test ./internal/handlers/... ./internal/sync/...` pass.
7. Committed, pushed, PR #934 created, CI green, merged (squash) `7f50c28`.

## PostHog state (2026-06-06, session 11)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| All issues (90d) | 19 — 2 suppressed CLICommandError (local Mac CLI, old binary), 17 resolved |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| Service version | `3f3ec2a...` — **still not deployed** |
| Latest commit on origin/main after session | `7f50c28` (PR #934 squash merge) |

## Shipped this session

| PR | Change |
|---|---|
| #934 | `internal/handlers/akahu.go`: fix `clearAkahuConnectionCredentials` to use `GetByConnectionID` (was `GetByLedgerID(uuid.Nil)` — a no-op). Extract `finishAkahuConnect` to remove ~20 duplicate lines from `akahuPersonalAppConnect` and `AkahuCallback`. |

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#934 merged but not deployed.
- Structural `errors=5` (5 Akahu expired tokens) — will auto-resolve once deployed.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.
- `handleAkahuTokenDelete` N+1 API pattern — intentional, do not flag.

## Next run checklist

1. Read 20260606.md (this file, all 11 sessions) first. Do NOT re-investigate prior work.
2. PostHog MCP defaults to "Personal CFO" (id: 407550) — verify active project.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `logs-count` with `query` wrapper: serviceNames=["cfo-cloud"], severityLevels=["warn","error","fatal"], -7d → expect 0.
5. `query-logs` (info, -1h, cfo-cloud, limit 1, excludeAttributes: false) → check `resource_attributes.service.version`.
6. If deployed: verify `partial_failure errors=5` drops to 0.
7. `logs-count` searchTerm "partial_failure" (-7d) → expect ~168 if undeployed.
8. `git fetch origin main && git log --oneline origin/main -5` → expect `7f50c28` or newer.
9. If new code commits: scan changed files for DRY/simplify opportunities, open issues/PRs.
10. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
11. PostHog assignee API HTTP 500 — do not attempt programmatic assignment.
12. Linear SSE deprecated — skip all Linear tool calls.
13. Branch fix PRs from `origin/main`, not from `claude/clever-knuth-*`.
14. All PostHog error types (CLICommandError, ProviderPullError, etc.) are from old `personal-cfo` binary — NOT in current `probably` codebase.
15. `syncTellerFields` — intentional (1 call site), do not extract.
16. `FindByLastFourAndInstitution` + Teller handler bugs — **fixed in PRs #910/#911**, do not reinvestigate.
17. GCS writer `Close()` pattern — intentional (commits upload), do NOT add defer. Do not re-flag.
18. `SpendingSummary.tsx` debug logs + `config.GetEnv` override — **fixed in PRs #916/#917**, do not reinvestigate.
19. `FullResync()` + `DeleteAndResync()` reconciliation error handling — **fixed in PR #926**, do not reinvestigate.
20. `clearAkahuConnectionCredentials` uuid.Nil bug + `finishAkahuConnect` DRY — **fixed in PR #934**, do not reinvestigate.
21. `handleAkahuTokenDelete` N+1 API pattern — intentional, do not flag.
22. When calling `query-logs` to check service version, use `excludeAttributes: false` so `resource_attributes.service.version` is visible.
