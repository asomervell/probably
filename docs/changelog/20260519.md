# Changelog — 2026-05-19

## Context from previous runs (2026-05-18)

- Production `service.version` has been `3811458b4373b7b8cf9f1662f86108232fd2e978` since before 2026-05-14 — undeployed.
- PR #462 (merged 2026-05-14) fixes `ProviderBalancePullError` / `partial_failure errors=4` in `probably`'s Teller sync path — awaiting production deploy.
- `ProviderBalancePullError` (PostHog id `019e05c7-f3d7-7331-9963-ae72ba61545d`) is from the `personal-cfo` codebase (`internal/cloud/server.go`), not `probably`. Suppressed in PostHog; permanent fix requires a separate `personal-cfo` codebase change.
- Runs 1–8 on 2026-05-18 all confirmed stop condition holds. No new fixes shipped.

## Stop condition

1. Check `service.version` in latest sync log — if still `3811458b`, stop.
2. If version changed: verify `status=success errors=0` in sync logs.
3. Query PostHog `status=active` — expect zero issues.
4. Re-suppress `ProviderBalancePullError` if PostHog re-activates it.

---

## Run 1 — ~12:05 UTC

### What was confirmed

- Read 2026-05-18 changelog (Runs 1–8) before doing any analysis.
- Production `service.version` still `3811458b4373b7b8cf9f1662f86108232fd2e978` — **stop condition holds**.
- PostHog error tracking: `status=active, dateRange=-30d` → zero active issues (empty result).
- Latest logs (12:03:52 UTC): enrichment `status=success updated=0 skipped=2539 failed=0`. All entries at `severity_number: 9` (info). Zero warn/error/fatal entries in last 24 hours.
- `ProviderBalancePullError` (019e05c7): not re-activated.

### Reasoning

- Read 2026-05-18 session log first; stop condition (`service.version` = `3811458b`) confirmed immediately.
- Queried PostHog active issues, latest sync logs, and warn/error logs in parallel — all clear.
- Enrichment `skipped` count moved from 2534→2539 (five new transactions enriched since yesterday), consistent with normal operation.
- No new code analysis, issues, or PRs required.

### What was fixed and shipped

Nothing — stop condition holds. No new errors, no new active issues.

### Carry-forward (unchanged)

- `partial_failure errors=4`: awaiting production deploy of PR #462. Clears automatically on next deploy.
- OTEL severity mapping: all logs at `severity_number: 9`; fix at HEAD, clears on next deploy.
- `ProviderBalancePullError`: resolved in PostHog. Permanent fix requires `personal-cfo` codebase change.

### Stop condition for future runs

1. Check `service.version` in latest sync log — if still `3811458b`, stop.
2. If version changed: verify `status=success errors=0` in sync logs.
3. Query PostHog `status=active` — expect zero issues.
4. Re-suppress `ProviderBalancePullError` if PostHog re-activates it.

---

## Run 2 — ~14:15 UTC

### What was confirmed

- Read Run 1 notes above before analysis.
- Production `service.version` still `3811458b4373b7b8cf9f1662f86108232fd2e978` — **stop condition holds**.
- PostHog error tracking: `status=all, dateRange=-30d` → 14 issues, all `resolved`. Zero `active` issues.
- Latest sync logs (13:10:57 UTC): `status=partial_failure connections=10 transactions=68 errors=4` — same as Run 1.
- Error logs queried with `severityLevels=[error,fatal]` → count=0. Confirmed: OTEL severity bridge maps all Go logs to `severity_number:9` (info) in this deployed version. The `errors=4` messages are emitted at info level.
- Enrichment: `status=success updated=0 skipped=2539 failed=0` — no change.
- PostHog error type investigation: all 14 historical issues referenced `github.com/asomervell/personal-cfo/internal/cloud/server.go` — that's the old module name. Current repo is `github.com/asomervell/probably`. Errors predate the refactor and are permanently resolved.

### Reasoning

- Checked stop condition immediately: `3811458b` → stop.
- Investigated PostHog `status=all` to understand the 14 historical issues (ProviderPullError×2, SyncAccountPullError×2, ProviderSyncError, ProviderBalancePullError×2, TellerWebhookTransactionAggregationError, ProviderTransactionStoreError, TellerWebhookTransactionBackfillRecordError, TellerConnectSessionError, CLICommandError×3). All resolved; call stacks point to old `personal-cfo` module paths that no longer exist in `probably`.
- Confirmed `errors=4` is from 4 provider connections that persistently fail each sync cycle (likely disconnected/expired tokens for Akahu or Teller accounts) — fixed in PR #462 via the balance-pull skip logic, awaiting deploy.
- No new issues found. No new code to write.

### What was fixed and shipped

Nothing — stop condition holds.

### Carry-forward (unchanged)

- `partial_failure errors=4`: awaiting production deploy of PR #462.
- OTEL severity mapping broken in `3811458b`; fix at HEAD, clears on next deploy.
- `ProviderBalancePullError`: resolved in PostHog. Permanent fix is `personal-cfo` side.

### Stop condition for future runs

1. Check `service.version` in latest sync log — if still `3811458b`, stop immediately.
2. If version changed: verify `status=success errors=0` in sync log AND PostHog `status=active` = 0.
3. Re-suppress `ProviderBalancePullError` (019e05c7) if PostHog re-activates it.

---

## Run 4 — ~21:05 UTC

### What was confirmed

- Read Runs 1–3 notes above before analysis.
- Production `service.version` still `3811458b4373b7b8cf9f1662f86108232fd2e978` — **stop condition holds**.
- PostHog error tracking: `status=active, dateRange=-30d` → zero active issues (empty result).
- Latest sync log (20:10:56 UTC): `provider sync complete trigger=scheduled status=partial_failure connections=10 transactions=69 errors=4` — same pattern as all prior runs.
- Enrichment (20:10:56 UTC): `status=success updated=0 skipped=2540 failed=0` — unchanged.
- Warn/error/fatal log query for last 24h → zero results. All production logs at `severity_number: 9` (info) due to OTEL severity mapping bug in deployed version.
- `transactions=69` (up 1 from prior runs at 68) — normal operation, one new transaction processed.

### Reasoning

- Stop condition confirmed immediately on first log check.
- Zero active PostHog errors — nothing to investigate or fix.
- `partial_failure errors=4` is the same persistent 4 disconnected/expired provider connections awaiting PR #462 deploy. Not a new issue.
- Enrichment `skipped=2540` unchanged — consistent with no new enrichable transactions.
- No new code analysis, issues, or PRs required.

### What was fixed and shipped

Nothing — stop condition holds. No new errors.

### Carry-forward (unchanged)

- `partial_failure errors=4`: awaiting production deploy of PR #462. Clears automatically on next deploy.
- OTEL severity mapping broken in `3811458b`; fix at HEAD, clears on next deploy.
- `ProviderBalancePullError`: resolved in PostHog. Permanent fix requires `personal-cfo` codebase change.

### Stop condition for future runs

1. Check `service.version` in latest sync log — if still `3811458b`, stop immediately.
2. If version changed: verify `status=success errors=0` in sync log AND PostHog `status=active` = 0.
3. Re-suppress `ProviderBalancePullError` (019e05c7) if PostHog re-activates it.

---

## Run 3 — ~16:10 UTC

### What was confirmed

- Read Runs 1–2 notes above before analysis.
- Production `service.version` still `3811458b4373b7b8cf9f1662f86108232fd2e978` — **stop condition holds**.
- PostHog error tracking: `status=active, dateRange=-30d` → zero active issues.
- Latest log (15:59:50 UTC): `enrichment complete trigger=scheduled status=success updated=0 skipped=2540 failed=0`. All at `severity_number: 9` (info). No errors or warnings.
- `skipped` count: 2540 (up 1 from Run 2's 2539) — normal operation.

### Reasoning

- Stop condition (`service.version` = `3811458b`) confirmed on first check.
- Zero active PostHog error tracking issues — nothing to fix.
- Logs clean: no warn/error/fatal entries.
- No new code, issues, or PRs required.

### What was fixed and shipped

Nothing — stop condition holds. No new errors.

### Carry-forward (unchanged)

- `partial_failure errors=4`: awaiting production deploy of PR #462.
- OTEL severity mapping broken in `3811458b`; fix at HEAD, clears on next deploy.
- `ProviderBalancePullError`: resolved in PostHog. Permanent fix requires `personal-cfo` codebase change.

### Stop condition for future runs

1. Check `service.version` in latest sync log — if still `3811458b`, stop immediately.
2. If version changed: verify `status=success errors=0` in sync log AND PostHog `status=active` = 0.
3. Re-suppress `ProviderBalancePullError` (019e05c7) if PostHog re-activates it.
