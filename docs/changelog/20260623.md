# 2026-06-23 — 0 active errors; telemetry still silent (37+ days)

## Prior context loaded

Read `docs/changelog/20260622.md`. Key facts carried forward:
- Repo: `asomervell/probably`, last real code commit: `1cd136b` (2026-06-17, PR #92)
- All 35 PostHog error tracking issues = `resolved`, 0 active
- App OTel telemetry: 0 logs/events for 36+ days — silent
- PostHog MCP server never connects — use REST API with `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID=88871`
- Auto-merge not enabled at repo level — cannot enable via API
- Fixed PRs to date: #3, #5, #9, #26, #57, #58, #92

## Checks run (2026-06-23)

| Check | Result |
|---|---|
| Active error tracking issues | **0 active** (35 returned, all `status: resolved`) |
| Exception events (`$exception`, 7d) | **0** |
| Log rows (any level, 7d) | **0** |
| App events (30d) | Only PostHog-internal: `$ai_trace_clusters` (4), `$ai_generation_clusters` (3) — no real app telemetry |
| Main HEAD | `278ff8b` — docs changelog only; last real code: `1cd136b` (2026-06-17) |

## Reasoning

No active errors. No new app telemetry. No new code commits since 2026-06-17.

Multiple earlier sessions today (sessions from 00:02, 01:03) already ran identical checks and produced docs PRs #208 and #209. Closed those as stale duplicates along with #206 and #207 from yesterday. Only leaving PR #163 (CI pipefail fix) and #144 (Dependabot chi/v5 bump) open — these are pre-existing and not docs-only.

Attempted to enable auto-merge on PR #163 — repo-level setting still disabled, cannot enable via API.

## Shipped this session

- Closed stale docs PRs: #206, #207, #208, #209
- Attempted auto-merge on PR #163 (failed — repo setting)
- Memory log: `docs/changelog/20260623.md`

## What was left behind (unchanged)

- **App telemetry silent**: 0 OTel logs/events reaching PostHog for 37+ days. Production binary likely missing OTel exporter config or CI deploy pipeline broken (see PR #163). Not actionable from this session.
- **PR #163**: fix(ci) `|| true` on semver/release-notes pipelines — open since 2026-06-21, not merged. Auto-merge not enabled at repo level.
- **PR #144**: Dependabot chi/v5 5.2.3→5.2.4 — open since 2026-06-20, not merged.
- **Operational noise**: Akahu connections with expired OAuth tokens — users must reconnect via app UI.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Auto-merge not enabled at repo level — cannot enable via API.
- Fixed PRs: #3, #5, #9, #26, #57, #58, #92.

## Next run checklist

1. **READ THIS FILE FIRST** — `asomervell/probably`, last updated 2026-06-23.
2. `git fetch origin main && git log --oneline origin/main -6`. Last known real code HEAD: `1cd136b` (2026-06-17).
3. PostHog: `error_tracking/issues/?status=active&limit=100` — expect 0 truly active (all 35 resolved).
4. PostHog HogQL: `SELECT event, count() as cnt FROM events WHERE timestamp >= now() - INTERVAL 30 DAY GROUP BY event ORDER BY cnt DESC LIMIT 20` — check if new app telemetry started.
5. PostHog HogQL: `SELECT level, count() FROM logs WHERE timestamp >= now() - INTERVAL 7 DAY GROUP BY level` — expect 0.
6. PostHog HogQL: `SELECT count() FROM events WHERE event='$exception' AND timestamp >= now() - INTERVAL 7 DAY` — expect 0.
7. If 0 active errors AND 0 new code issues, write changelog and stop. **Do NOT manufacture work.**
8. PostHog assignee API: HTTP 500 — do not attempt to assign issues programmatically.
9. Auto-merge not enabled — cannot enable via API. Do not retry.
10. No CI workflows on this repo — do not chase phantom CI failures.
11. Fixed PRs: #3, #5, #9, #26, #57, #58, #92. Do not reinvestigate.
12. All 35 PostHog error-tracking issues are `resolved`. Not actionable.
13. **Close stale duplicate docs PRs before creating a new one.** Check for open docs-only PRs and close them first.
14. PostHog credentials: `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID=88871` available in env; use direct REST API (PostHog MCP server never connects).
15. PR #163 (CI pipefail fix) and PR #144 (chi/v5 bump) are pre-existing open PRs — do not recreate. Check if merged.
