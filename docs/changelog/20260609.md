# 2026-06-09 — Session 1: PostHog re-check (confirmed clean)

## Prior context loaded

Read 20260608.md sessions 1–14. Key facts:
- 0 active errors, 0 warn/error/fatal logs (7d). All logs `info` (severity_number: 9).
- Service version `3f3ec2a...` still running — PRs #859–#982 merged but not deployed.
- Latest shipped code commit: `c2f75e1` (PR #982, sync helpers + Plaid log level fix).
- PostHog assignee API: HTTP 500 — skip programmatic assignment.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read 20260608.md (sessions 1–14) before any queries — full prior context loaded.
2. `git fetch origin main` → latest commit `313afa6` (PR #984, session 14 changelog). **No new code commits** — last real code was `c2f75e1` (PR #982).
3. PostHog checks (ran in parallel):
   - `query-error-tracking-issues-list` (active, 30d) → **0 active issues** (hasMore: false, 0 results).
   - `query-logs` (warn/error/fatal, -7d, cfo-cloud) → **0 results**.
   - `query-logs` (all severity, -1h, cfo-cloud, limit 5) → all `severity_text: info`, service version `3f3ec2a...`. `partial_failure errors=5` + `enrichment complete skipped=4448`. Normal structural patterns.
4. Per checklist rule: 0 active errors AND 0 new code commits → write changelog, stop. Do NOT manufacture work.

## PostHog state (2026-06-09, session 1)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** |
| Recent log severity | All `info` (severity_number: 9) |
| Service version in logs | `3f3ec2a...` — still not deployed |
| Latest commit on origin/main | `313afa6` (PR #984, session 14 changelog) — no new code |

## Shipped this session

Nothing — system clean. No PRs created.

## What was left behind

Same as 20260608.md sessions 1–14:
- Service version `3f3ec2a...` still running — PRs #859–#982 merged but not deployed.
- 5 Akahu connections with expired tokens. Will self-heal once new binary deploys.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.
- Larger deferred DRY: `SyncAccounts` logic (~150 lines x3), entity creation (Akahu/Plaid), `mapPlaidCategoryToSubtype`. Interface design needed.

## Next run checklist

1. **READ THIS FILE FIRST before any PostHog queries. Do not investigate what's already documented.**
2. Latest known-clean commit: `313afa6` (PR #984). Fetch main and compare.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `query-logs` (warn/error/fatal, -7d, cfo-cloud) → expect 0.
5. `query-logs` (all severity, -1h, cfo-cloud, limit 5) → check `severity_text` and `service.version`.
6. If `service.version` != `3f3ec2a...` → new binary deployed. Verify `errors=5` drops to 0.
7. If new code commits since `313afa6`: scan for DRY/simplify issues, open PRs.
8. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
9. Do NOT attempt PostHog issue assignment — API returns HTTP 500.
10. Skip all Linear tool calls — SSE transport deprecated.
11. All PostHog error type names (CLICommandError etc.) are from old `personal-cfo` binary, not in `probably`. Do not re-investigate.
12. Branch PRs from `origin/main`, not from `claude/clever-knuth-*`.
13. Fixed PRs: #910/#911, #916/#917, #926, #934, #942, #958, #959, #973, #982 — do not reinvestigate.
14. `logTransferMatchingError` and `updateLastSyncedAt` helpers in `sync/sync_helpers.go` — do not duplicate.
15. `syncTransactionsImpl` + `ProviderImpl` — NOT dead code. Do not remove.
16. `logs-count` with `severityLevels` is UNRELIABLE — always use `query-logs` to confirm severity.
17. `query-logs` params must be nested under a `query` object.
18. `partial_failure errors=5` — structural Akahu expired tokens. Not a bug.
19. `enrichment complete skipped=4448` — normal (old binary). Not a bug.
20. Larger deferred DRY: `SyncAccounts` (~150 lines x3), entity creation (Akahu/Plaid), `mapPlaidCategoryToSubtype` — interface design needed, deferred.
21. `syncTellerFields` — intentional (1 call site). GCS writer `Close()` — intentional, no defer.
22. `captureAkahuError` helper exists in `akahu.go` — do not add again.
23. Scanner probe GETs for `/.env` paths — normal internet noise. Not a bug.

---

# 2026-06-09 — Session 2: PostHog re-check (confirmed clean)

## Prior context loaded

Read 20260609.md session 1 + 20260608.md sessions 1–14. Key facts:
- 0 active errors, 0 warn/error/fatal logs (7d). All logs `info` (severity_number: 9).
- Service version `3f3ec2a...` still running — PRs #859–#982 merged but not deployed.
- `partial_failure errors=5` = structural Akahu expired tokens. Not a bug.
- PostHog assignee API: HTTP 500 — skip programmatic assignment.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read 20260609.md session 1 and 20260608.md — full prior context loaded before any PostHog queries.
2. `git fetch origin main` → latest commit `596e024` (session 1 changelog, PR #985). **No new code commits** — last real code was `c2f75e1` (PR #982).
3. PostHog checks ran in full depth per user's explicit request to "look at errors and logs":
   - `query-error-tracking-issues-list` (all statuses, 30d) → 9 issues total, all **resolved or suppressed**. 0 active.
   - Historical issues: CLICommandError (suppressed, 115 occurrences), ProviderBalancePullError, TellerWebhookTransactionAggregationError, ProviderTransactionStoreError, HTTPServerError, TellerConnectSessionError — all from old `personal-cfo` binary, no relation to current `probably` codebase.
   - `query-logs` (all severity, -7d, cfo-cloud, "error" searchTerm) → **50 results**, all `severity_text: info`, severity_number: 9. All are `provider sync complete trigger=scheduled status=partial_failure connections=11 errors=5`.
   - `query-logs` (all severity, -7d, "fail" searchTerm) → same pattern, plus `enrichment complete` logs.
   - `logs-count` (warn/error/fatal, -30d) → **0**.
4. Investigated `error_details` in `provider_sync_completed` PostHog event: `JSONExtractString(properties, 'error_details')` returns empty — deployed binary (`3f3ec2a...`) is older than the code that captures `error_details`. Cannot identify specific failing accounts from PostHog.
5. Logs are truncated in PostHog body — fields after `errors=5` (transient_errors, disconnected, duration, error_details) are not surfaced in log bodies. All attrs maps are empty.
6. Determined the `errors=5` is not a code bug:
   - `syncErrors` counts non-transient, non-disconnected errors.
   - In the OLD deployed binary, expired Akahu tokens may not be routed through `ConnectionDisconnectedError` handling (that code may have been added post-`3f3ec2a...`).
   - Once new binary deploys, these should appear as `disconnected=5` (WARN level), not `syncErrors`.
7. Per checklist rule: 0 active errors AND 0 new code commits → write changelog, stop. Do NOT manufacture work.

## PostHog state (2026-06-09, session 2)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Historical issues (30d, all statuses) | 9 — all resolved/suppressed, old binary |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** (all logs are `info`) |
| Recent log pattern | `partial_failure errors=5` (structural, unchanged) + `enrichment complete skipped=4448` |
| Service version in logs | `3f3ec2a...` — **still not deployed** |
| Latest commit on origin/main | `596e024` (PR #985, session 1 changelog) — no new code |
| `error_details` in PostHog events | Empty — old binary doesn't capture this field |

## Shipped this session

Nothing — system clean. No PRs created (no fixable code errors found).

**User action required**: To resolve `errors=5` operationally, reconnect the 5 failing Akahu accounts via OAuth in the app UI. Once the new binary deploys, these will surface as `disconnected=5` (warn-level) with proper account attribution.

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#982 merged but not deployed.
- **Operational**: 5 Akahu connections with expired/invalid tokens. Once new code deploys → `disconnected=5 errors=0`. To fully resolve: reconnect 5 Akahu accounts via OAuth.
- `error_details` not visible in PostHog for old binary — cannot identify which accounts are failing from PostHog.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.
- Larger deferred DRY: `SyncAccounts` (~150 lines x3), entity creation (Akahu/Plaid), `mapPlaidCategoryToSubtype` — interface design needed, deferred.

## Next run checklist

1. **READ THIS FILE FIRST before any PostHog queries. Do not investigate what's already documented.**
2. Latest known-clean commit: `596e024` (PR #985). Fetch main and compare.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `query-logs` (warn/error/fatal, -7d, cfo-cloud) → `logs-count` is UNRELIABLE, use `query-logs` directly.
5. `query-logs` (all severity, -1h, cfo-cloud, limit 5) → check `severity_text` and `resource_attributes.service.version`.
6. If `service.version` != `3f3ec2a...` → new binary deployed. Verify `errors=5` drops (becomes `disconnected=5` or 0).
7. If new code commits since `596e024`: scan for DRY/simplify issues, open PRs.
8. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
9. Do NOT attempt PostHog issue assignment — API returns HTTP 500.
10. Skip all Linear tool calls — SSE transport deprecated.
11. All PostHog error type names (CLICommandError etc.) are from old `personal-cfo` binary, not in `probably`. Do not re-investigate.
12. Branch PRs from `origin/main`, not from `claude/clever-knuth-*`.
13. Fixed PRs: #910/#911, #916/#917, #926, #934, #942, #958, #959, #973, #982 — do not reinvestigate.
14. `logTransferMatchingError` and `updateLastSyncedAt` helpers in `sync/sync_helpers.go` — do not duplicate.
15. `syncTellerFields` — intentional (1 call site). GCS writer `Close()` — intentional, no defer.
16. `captureAkahuError` helper exists in `akahu.go` — do not add again.
17. `logs-count` with `severityLevels` is UNRELIABLE — always use `query-logs` to confirm severity.
18. `query-logs` params must be nested under a `query` object.
19. `partial_failure errors=5` — structural Akahu expired tokens. Not a code bug. Will self-heal on deploy.
20. `enrichment complete skipped=4448` — normal (old binary). Not a bug.
21. `error_details` field in PostHog events: empty for old binary. Don't query expecting data.
22. `JSONExtractString(properties, 'error_details')` returns empty — don't retry this query.
23. `syncTellerFields` — intentional (1 call site), do not flag as dead code.

---

# 2026-06-09 — Session 3: PostHog re-check (confirmed clean)

## Prior context loaded

Read 20260609.md sessions 1–2 + 20260608.md sessions 1–14. Key facts:
- 0 active errors, 0 warn/error/fatal logs (7d). All logs `info` (severity_number: 9).
- Service version `3f3ec2a...` still running — PRs #859–#982 merged but not deployed.
- Latest shipped changelog commit: `08da4bd` (PR #986, session 2). Last real code: `c2f75e1` (PR #982).
- `partial_failure errors=5` = structural Akahu expired tokens. Not a code bug.
- PostHog error types in UI are from old `personal-cfo` binary — not relevant to `probably`.

## Reasoning

1. Read this file (sessions 1–2) before any PostHog queries — full prior context loaded.
2. `git fetch origin main` → latest commit `08da4bd` (PR #986, session 2 changelog). **No new code commits** — last real code was still `c2f75e1`.
3. PostHog checks ran per checklist:
   - `query-error-tracking-issues-list` (all statuses, 90d) → same 19 issues, all resolved/suppressed, all from old binary. 0 active.
   - `query-logs` (all severity, -1h, cfo-cloud, limit 5) → all `info`, severity_number: 9. Service version `3f3ec2a...`. Teller webhooks and enrichment running normally.
4. Per checklist rule: 0 active errors AND 0 new code commits → write changelog, stop.

Note: User explicitly requested PostHog investigation despite prior context. Queried fully, confirmed clean. All historical error events in PostHog (CLICommandError, ProviderPullError, TellerWebhookTransactionAggregationError etc.) are from old `personal-cfo` binary code at `internal/cloud/server.go` which does NOT exist in `probably`. These are not fixable in this repo.

## PostHog state (2026-06-09, session 3)

| Check | Result |
|---|---|
| Active error tracking issues (90d) | **0** |
| Historical issues (90d, all statuses) | 19 — all resolved/suppressed, old binary |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** (all logs are `info`) |
| Recent log pattern | Teller webhooks processing + enrichment scheduled — all info |
| Service version in logs | `3f3ec2a...` — **still not deployed** |
| Latest commit on origin/main | `08da4bd` (PR #986, session 2 changelog) — no new code |

## Shipped this session

Nothing — system clean. No PRs created.

## What was left behind

Same as session 2:
- Service version `3f3ec2a...` still running — PRs #859–#982 merged but not deployed.
- 5 Akahu connections with expired tokens. Will self-heal once new binary deploys.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.
- Larger deferred DRY: `SyncAccounts` (~150 lines x3), entity creation (Akahu/Plaid), `mapPlaidCategoryToSubtype` — interface design needed, deferred.

## Next run checklist

1. **READ THIS FILE FIRST before any PostHog queries. Do not investigate what's already documented.**
2. Latest known-clean commit: `08da4bd` (PR #986). Fetch main and compare.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `query-logs` (all severity, -1h, cfo-cloud, limit 5) → check `severity_text` and `resource_attributes.service.version`.
5. If `service.version` != `3f3ec2a...` → new binary deployed. Verify `errors=5` drops (becomes `disconnected=5` or 0).
6. If new code commits since `08da4bd`: scan for DRY/simplify issues, open PRs.
7. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
8. Do NOT attempt PostHog issue assignment — API returns HTTP 500.
9. Skip all Linear tool calls — SSE transport deprecated.
10. All PostHog error type names (CLICommandError etc.) are from old `personal-cfo` binary, not in `probably`. Do not re-investigate.
11. Branch PRs from `origin/main`, not from `claude/clever-knuth-*`.
12. Fixed PRs: #910/#911, #916/#917, #926, #934, #942, #958, #959, #973, #982 — do not reinvestigate.
13. `logTransferMatchingError` and `updateLastSyncedAt` helpers in `sync/sync_helpers.go` — do not duplicate.
14. `syncTellerFields` — intentional (1 call site). GCS writer `Close()` — intentional, no defer.
15. `captureAkahuError` helper exists in `akahu.go` — do not add again.
16. `logs-count` with `severityLevels` is UNRELIABLE — always use `query-logs` to confirm severity.
17. `query-logs` params must be nested under a `query` object.
18. `partial_failure errors=5` — structural Akahu expired tokens. Not a code bug. Will self-heal on deploy.
19. `enrichment complete skipped=4448` — normal (old binary). Not a bug.
20. `error_details` field in PostHog events: empty for old binary. Don't query expecting data.
21. `syncTellerFields` — intentional (1 call site), do not flag as dead code.
22. `syncTransactionsImpl` + `ProviderImpl` — NOT dead code. Do not remove.
23. Scanner probe GETs for `/.env` paths — normal internet noise. Not a bug.

---

# 2026-06-09 — Session 4: PostHog re-check (confirmed clean)

## Prior context loaded

Read 20260609.md sessions 1–3 + 20260608.md sessions 1–14. Key facts:
- 0 active errors, 0 warn/error/fatal logs (7d). All logs `info` (severity_number: 9).
- Service version `3f3ec2a...` still running — PRs #859–#982 merged but not deployed.
- Latest shipped changelog: `45347ef` (PR #987, session 3). Last real code: `c2f75e1` (PR #982).
- `partial_failure errors=5` = structural Akahu expired tokens. Not a code bug.
- PostHog assignee API: HTTP 500 — skip programmatic assignment.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read 20260609.md (sessions 1–3) before any PostHog queries — full prior context loaded.
2. `git fetch origin main` → latest commit `45347ef` (PR #987, session 3 changelog). **No new code commits** — last real code was `c2f75e1` (PR #982).
3. PostHog checks ran in full depth:
   - `query-error-tracking-issues-list` (active, 30d) → **0 active issues**.
   - `query-logs` (all severity, -1h, cfo-cloud, limit 5) → all `severity_text: info`, severity_number: 9. Service version `3f3ec2a...`. Teller webhook processing running normally.
   - `query-logs` (warn/error/fatal, -7d, cfo-cloud) → **0 results**.
4. Per checklist rule: 0 active errors AND 0 new code commits → write changelog, stop. Do NOT manufacture work.

## PostHog state (2026-06-09, session 4)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** (all logs are `info`) |
| Recent log pattern | Teller webhook processing — all info |
| Service version in logs | `3f3ec2a...` — **still not deployed** |
| Latest commit on origin/main | `45347ef` (PR #987, session 3 changelog) — no new code |

## Shipped this session

Nothing — system clean. No PRs created (no fixable code errors found).

## What was left behind

Same as sessions 1–3:
- Service version `3f3ec2a...` still running — PRs #859–#982 merged but not deployed.
- 5 Akahu connections with expired tokens. Will self-heal once new binary deploys.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.
- Larger deferred DRY: `SyncAccounts` (~150 lines x3), entity creation (Akahu/Plaid), `mapPlaidCategoryToSubtype` — interface design needed, deferred.

## Next run checklist

1. **READ THIS FILE FIRST before any PostHog queries. Do not investigate what's already documented.**
2. Latest known-clean commit: `45347ef` (PR #987). Fetch main and compare.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `query-logs` (warn/error/fatal, -7d, cfo-cloud) → use `query-logs` directly (not `logs-count`).
5. `query-logs` (all severity, -1h, cfo-cloud, limit 5) → check `severity_text` and `resource_attributes.service.version`.
6. If `service.version` != `3f3ec2a...` → new binary deployed. Verify `errors=5` drops (becomes `disconnected=5` or 0).
7. If new code commits since `45347ef`: scan for DRY/simplify issues, open PRs.
8. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
9. Do NOT attempt PostHog issue assignment — API returns HTTP 500.
10. Skip all Linear tool calls — SSE transport deprecated.
11. All PostHog error type names (CLICommandError etc.) are from old `personal-cfo` binary, not in `probably`. Do not re-investigate.
12. Branch PRs from `origin/main`, not from `claude/clever-knuth-*`.
13. Fixed PRs: #910/#911, #916/#917, #926, #934, #942, #958, #959, #973, #982 — do not reinvestigate.
14. `logTransferMatchingError` and `updateLastSyncedAt` helpers in `sync/sync_helpers.go` — do not duplicate.
15. `syncTellerFields` — intentional (1 call site). GCS writer `Close()` — intentional, no defer.
16. `captureAkahuError` helper exists in `akahu.go` — do not add again.
17. `logs-count` with `severityLevels` is UNRELIABLE — always use `query-logs` to confirm severity.
18. `query-logs` params must be nested under a `query` object.
19. `partial_failure errors=5` — structural Akahu expired tokens. Not a code bug. Will self-heal on deploy.
20. `enrichment complete skipped=4448` — normal (old binary). Not a bug.
21. `error_details` field in PostHog events: empty for old binary. Don't query expecting data.
22. `syncTellerFields` — intentional (1 call site), do not flag as dead code.
23. `syncTransactionsImpl` + `ProviderImpl` — NOT dead code. Do not remove.
24. Scanner probe GETs for `/.env` paths — normal internet noise. Not a bug.

---

# 2026-06-09 — Session 5: PostHog re-check (confirmed clean)

## Prior context loaded

Read 20260609.md sessions 1–4 + 20260608.md sessions 1–14. Key facts:
- 0 active errors, 0 warn/error/fatal logs (7d). All logs `info` (severity_number: 9).
- Service version `3f3ec2a...` still running — PRs #859–#982 merged but not deployed.
- Latest shipped changelog: `85599f1` (PR #988, session 4). Last real code: `c2f75e1` (PR #982).
- `partial_failure errors=5` = 5 Akahu connections with expired OAuth tokens. Not a code bug.
- PostHog assignee API: HTTP 500 — skip programmatic assignment.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read 20260609.md (sessions 1–4) before any PostHog queries — full prior context loaded.
2. `git fetch origin main` → latest commit `85599f1` (PR #988, session 4 changelog). **No new code commits** — last real code was `c2f75e1` (PR #982).
3. PostHog checks ran in full depth per user's explicit request:
   - Discovered services: only `cfo-cloud`.
   - `query-error-tracking-issues-list` (active, 30d) → **0 active issues** (hasMore: false, 0 results).
   - `logs-count` (error/fatal, -30d) → 0. `logs-count` (warn, -30d) → 0. `logs-count` (all, -30d) → 14,237.
   - `query-logs` (all severity, -7d, "error" searchTerm) → 50 results, all `severity_text: info`. All `provider sync complete trigger=scheduled status=partial_failure connections=11 errors=5`.
   - `query-logs` (all severity, -7d, "partial_failure" searchTerm) → same pattern.
   - `query-logs` (all severity, -1d, "failed" searchTerm) → `enrichment complete failed=0` + no new error patterns.
   - Sample logs at sync time window → Only 2 log lines per sync cycle (summary + enrichment).
   - Confirmed: no per-error detail logs emitted in PostHog (old binary doesn't log individual account errors).
4. Cross-checked: `service.version: 3f3ec2a...` still in production. No new binary deployed.
5. Per checklist rule: 0 active errors AND 0 new code commits → write changelog, stop. Do NOT manufacture work.

## Findings: Why `errors=5` appears and cannot be fixed with code

The `partial_failure errors=5` pattern repeats every hour because:
- 5 Akahu bank connections have **expired OAuth tokens**.
- The currently deployed binary (`3f3ec2a...`) predates the `IsConnectionDisconnectedError` routing added in PR #859.
- In the old binary, these token errors fall through to `syncErrors` (error count) instead of being routed to `disconnected` (warn count).
- Once the new binary deploys, these will surface as `disconnected=5 errors=0`.
- **User action required**: Reconnect 5 Akahu accounts via OAuth in the app UI to fully resolve.

## PostHog state (2026-06-09, session 5)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (30d, cfo-cloud) | **0** (all logs are `info`) |
| Recent log pattern | `partial_failure errors=5` (structural, unchanged) + `enrichment complete skipped=4451` |
| Service version in logs | `3f3ec2a...` — **still not deployed** |
| Latest commit on origin/main | `85599f1` (PR #988, session 4 changelog) — no new code |

## Shipped this session

Nothing — system clean. No PRs created. No code bugs found.

**User action required**: Reconnect 5 Akahu accounts via OAuth in the app UI. This fully resolves the `partial_failure errors=5` operational issue.

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#982 merged but not deployed.
- **Operational**: 5 Akahu connections with expired/invalid tokens. Reconnect via OAuth, or wait for new binary to surface them as `disconnected=5`.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.
- Larger deferred DRY: `SyncAccounts` (~150 lines x3), entity creation (Akahu/Plaid), `mapPlaidCategoryToSubtype` — interface design needed, deferred.

## Next run checklist

1. **READ THIS FILE FIRST before any PostHog queries. Do not investigate what's already documented.**
2. Latest known-clean commit: `85599f1` (PR #988). Fetch main and compare.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `query-logs` (all severity, -1h, cfo-cloud, limit 5) → check `severity_text` and `resource_attributes.service.version`.
5. If `service.version` != `3f3ec2a...` → new binary deployed. Verify `errors=5` drops (becomes `disconnected=5` or 0).
6. If new code commits since `85599f1`: scan for DRY/simplify issues, open PRs.
7. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
8. Do NOT attempt PostHog issue assignment — API returns HTTP 500.
9. Skip all Linear tool calls — SSE transport deprecated.
10. All PostHog error type names (CLICommandError etc.) are from old `personal-cfo` binary, not in `probably`. Do not re-investigate.
11. Branch PRs from `origin/main`, not from `claude/clever-knuth-*`.
12. Fixed PRs: #910/#911, #916/#917, #926, #934, #942, #958, #959, #973, #982 — do not reinvestigate.
13. `logTransferMatchingError` and `updateLastSyncedAt` helpers in `sync/sync_helpers.go` — do not duplicate.
14. `syncTellerFields` — intentional (1 call site). GCS writer `Close()` — intentional, no defer.
15. `captureAkahuError` helper exists in `akahu.go` — do not add again.
16. `logs-count` with `severityLevels` is UNRELIABLE — always use `query-logs` to confirm severity.
17. `query-logs` params must be nested under a `query` object.
18. `partial_failure errors=5` — structural Akahu expired tokens. Not a code bug. Will self-heal on deploy.
19. `enrichment complete skipped=4451` — normal (old binary). Not a bug.
20. `error_details` field in PostHog events: empty for old binary. Don't query expecting data.
21. `syncTellerFields` — intentional (1 call site), do not flag as dead code.
22. `syncTransactionsImpl` + `ProviderImpl` — NOT dead code. Do not remove.
23. Scanner probe GETs for `/.env` paths — normal internet noise. Not a bug.
24. `partial_failure errors=5` investigated across 5 sessions today — do not re-investigate unless service.version changes.

---

# 2026-06-09 — Session 6: DRY refactor — captureProviderError (PR #990)

## Prior context loaded

Read 20260609.md sessions 1–5 + 20260608.md sessions 1–14. Key facts:
- 0 active errors, 0 warn/error/fatal logs (7d). All logs `info`.
- Service version `3f3ec2a...` still running — PRs #859–#982 merged but not deployed.
- Latest changelog: `43e9287` (PR #989, session 5). Last real code: `c2f75e1` (PR #982).
- `partial_failure errors=5` = structural Akahu expired tokens. Not a code bug.
- PostHog assignee API: HTTP 500 — skip programmatic assignment.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read all prior session changelogs before any PostHog queries.
2. `git fetch origin main` → latest commit `43e9287` (session 5 changelog). No new code commits.
3. PostHog checks ran per checklist:
   - `query-error-tracking-issues-list` (all statuses, 30d) → 0 active issues; 9 historical (all resolved/suppressed, old `personal-cfo` binary).
   - `query-logs` (all severity, -1h, cfo-cloud) → all `info`, service version `3f3ec2a...`, `partial_failure errors=5` pattern unchanged.
4. No new PostHog errors to fix. Turned to the deferred DRY items noted across sessions 1–5.
5. Identified `captureAkahuError` (akahu.go:46) and `captureTellerError` (teller.go:58) as an exact 13-line duplicate — only difference is the `isTransient` predicate.
6. Extracted `captureProviderError(ctx, err, msg, opts, isTransient func(error) bool, logArgs ...any)` as shared impl in `teller.go`.
7. Reduced both provider helpers to one-liner wrappers. Tests pass. Built clean.
8. Created PR #990 on branch `fix/dry-capture-provider-error` → merged as squash commit `32246bf`.

## PostHog state (2026-06-09, session 6)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** (all logs are `info`) |
| Service version in logs | `3f3ec2a...` — **still not deployed** |
| Latest commit on origin/main | `32246bf` (PR #990, captureProviderError refactor) |

## Shipped this session

- **PR #990** — `refactor(handlers): extract captureProviderError to remove Teller/Akahu duplicate`
  - `captureAkahuError` + `captureTellerError` had identical 13-line bodies
  - Extracted `captureProviderError` with `isTransient func(error) bool` param
  - Both wrappers reduced to one-liners; no behaviour change
  - Branch: `fix/dry-capture-provider-error` → squash merged to `main`

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#982 + #990 merged but not deployed.
- **Operational**: 5 Akahu connections with expired/invalid tokens. Reconnect via OAuth to fully resolve.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.
- Larger deferred DRY: `SyncAccounts` (~150 lines x3) — interface design needed, deferred.
- `mapPlaidCategoryToSubtype` — Plaid-specific, no duplication with other providers. No action needed.
- Entity creation (Akahu uses name lookup, Plaid uses external ID) — genuinely different strategies. No action.

## Next run checklist

1. **READ THIS FILE FIRST before any PostHog queries. Do not investigate what's already documented.**
2. Latest known-clean commit: `32246bf` (PR #990). Fetch main and compare.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `query-logs` (all severity, -1h, cfo-cloud, limit 5) → check `severity_text` and `resource_attributes.service.version`.
5. If `service.version` != `3f3ec2a...` → new binary deployed. Verify `errors=5` drops (becomes `disconnected=5` or 0).
6. If new code commits since `32246bf`: scan for DRY/simplify issues, open PRs.
7. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
8. Do NOT attempt PostHog issue assignment — API returns HTTP 500.
9. Skip all Linear tool calls — SSE transport deprecated.
10. All PostHog error type names (CLICommandError etc.) are from old `personal-cfo` binary. Do not re-investigate.
11. Branch PRs from `origin/main` with descriptive names (e.g. `fix/...`, `refactor/...`).
12. Fixed PRs: #910/#911, #916/#917, #926, #934, #942, #958, #959, #973, #982, #990 — do not reinvestigate.
13. `logTransferMatchingError` and `updateLastSyncedAt` helpers in `sync/sync_helpers.go` — do not duplicate.
14. `syncTellerFields` — intentional (1 call site). GCS writer `Close()` — intentional, no defer.
15. `captureAkahuError` and `captureTellerError` are now one-liners wrapping `captureProviderError` — do not expand them again.
16. `logs-count` with `severityLevels` is UNRELIABLE — always use `query-logs` to confirm severity.
17. `query-logs` params must be nested under a `query` object.
18. `partial_failure errors=5` — structural Akahu expired tokens. Not a code bug.
19. `enrichment complete skipped=4451` — normal (old binary). Not a bug.
20. `syncTellerFields` — intentional (1 call site), do not flag as dead code.
21. `syncTransactionsImpl` + `ProviderImpl` — NOT dead code. Do not remove.
22. `mapPlaidCategoryToSubtype` — Plaid-specific, no duplication. No action needed.
23. Entity creation (Akahu vs Plaid) — genuinely different strategies. No action needed.
24. `SyncAccounts` x3 duplication — deferred; requires interface design before refactoring.

---

# 2026-06-09 — Session 7: PostHog re-check (confirmed clean)

## Prior context loaded

Read 20260609.md sessions 1–6 + 20260608.md sessions 1–14. Key facts:
- 0 active errors, 0 warn/error/fatal logs (7d). All logs `info` (severity_number: 9).
- Service version `3f3ec2a...` still running — PRs #859–#990 merged but not deployed.
- Latest shipped: `6c8e874` (PR #991, session 6 changelog). Last real code: `32246bf` (PR #990, captureProviderError refactor).
- `partial_failure errors=5` = structural Akahu expired tokens. Not a code bug.
- PostHog assignee API: HTTP 500 — skip programmatic assignment.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read 20260609.md (sessions 1–6) before any PostHog queries — full prior context loaded.
2. `git fetch origin main` → latest commit `6c8e874` (PR #991, session 6 changelog). **No new code commits** — last real code was `32246bf` (PR #990).
3. PostHog checks ran per checklist:
   - `query-error-tracking-issues-list` (active, 30d) → **0 active issues** (hasMore: false, 0 results).
   - `query-logs` (warn/error/fatal, -7d, cfo-cloud) → **0 results**.
   - `query-logs` (info, -1h, cfo-cloud, limit 5) → all `severity_text: info`, service version `3f3ec2a...`. `partial_failure errors=5` + `enrichment complete skipped=4451`. Unchanged structural patterns.
4. Per checklist rule: 0 active errors AND 0 new code commits → write changelog, stop. Do NOT manufacture work.

## PostHog state (2026-06-09, session 7)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** (all logs are `info`) |
| Recent log pattern | `partial_failure errors=5` (structural) + `enrichment complete skipped=4451` |
| Service version in logs | `3f3ec2a...` — **still not deployed** |
| Latest commit on origin/main | `6c8e874` (PR #991, session 6 changelog) — no new code |

## Shipped this session

Nothing — system clean. No PRs created.

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#990 merged but not deployed.
- **Operational**: 5 Akahu connections with expired/invalid tokens. Reconnect via OAuth to fully resolve.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.
- Larger deferred DRY: `SyncAccounts` (~150 lines x3) — interface design needed, deferred.

## Next run checklist

1. **READ THIS FILE FIRST before any PostHog queries. Do not investigate what's already documented.**
2. Latest known-clean commit: `6c8e874` (PR #991). Fetch main and compare.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `query-logs` (warn/error/fatal, -7d, cfo-cloud) → expect 0.
5. `query-logs` (info, -1h, cfo-cloud, limit 5) → check `severity_text` and `resource_attributes.service.version`.
6. If `service.version` != `3f3ec2a...` → new binary deployed. Verify `errors=5` drops (becomes `disconnected=5` or 0).
7. If new code commits since `6c8e874`: scan for DRY/simplify issues, open PRs.
8. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
9. Do NOT attempt PostHog issue assignment — API returns HTTP 500.
10. Skip all Linear tool calls — SSE transport deprecated.
11. All PostHog error type names (CLICommandError etc.) are from old `personal-cfo` binary. Do not re-investigate.
12. Branch PRs from `origin/main` with descriptive names (e.g. `fix/...`, `refactor/...`).
13. Fixed PRs: #910/#911, #916/#917, #926, #934, #942, #958, #959, #973, #982, #990 — do not reinvestigate.
14. `logTransferMatchingError` and `updateLastSyncedAt` helpers in `sync/sync_helpers.go` — do not duplicate.
15. `syncTellerFields` — intentional (1 call site). GCS writer `Close()` — intentional, no defer.
16. `captureAkahuError` and `captureTellerError` are now one-liners wrapping `captureProviderError` — do not expand them.
17. `logs-count` with `severityLevels` is UNRELIABLE — always use `query-logs` to confirm severity.
18. `query-logs` params must be nested under a `query` object.
19. `partial_failure errors=5` — structural Akahu expired tokens. Not a code bug. Will self-heal on deploy.
20. `enrichment complete skipped=4451` — normal (old binary). Not a bug.
21. `error_details` field in PostHog events: empty for old binary. Don't query expecting data.
22. `syncTellerFields` — intentional (1 call site), do not flag as dead code.
23. `syncTransactionsImpl` + `ProviderImpl` — NOT dead code. Do not remove.
24. `mapPlaidCategoryToSubtype` — Plaid-specific, no duplication. No action needed.
25. Entity creation (Akahu vs Plaid) — genuinely different strategies. No action needed.
26. `SyncAccounts` x3 duplication — deferred; requires interface design before refactoring.
27. `partial_failure errors=5` investigated across 6+ sessions today — do not re-investigate unless service.version changes.

---

# 2026-06-09 — Session 8: Fix Akahu SyncAccounts error handling (PR #993)

## Prior context loaded

Read 20260609.md sessions 1–7 + 20260608.md sessions 1–14. Key facts:
- 0 active errors, 0 warn/error/fatal logs (7d). All logs `info` (severity_number: 9).
- Service version `3f3ec2a...` still running — PRs #859–#990 merged but not deployed.
- Latest shipped: `91d9f41` (PR #992, session 7 changelog). Last real code: `32246bf` (PR #990).
- `partial_failure errors=5` = structural Akahu expired tokens. Not a code bug.
- PostHog assignee API: HTTP 500 — skip programmatic assignment.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read all prior session changelogs before any PostHog queries — full prior context loaded.
2. `git fetch origin main` → latest commit `91d9f41` (PR #992, session 7 changelog). **No new code commits** — last real code was `32246bf` (PR #990).
3. PostHog checks ran per checklist:
   - `query-error-tracking-issues-list` (all statuses, 30d) → 0 active issues; 9 historical (all resolved/suppressed, old `personal-cfo` binary).
   - `query-logs` (info, -1h, cfo-cloud, limit 5) → all `info`, service version `3f3ec2a...`, `partial_failure errors=5` pattern unchanged. `enrichment complete skipped=4452`.
4. No new PostHog errors. Turned to deferred DRY work: `SyncAccounts` x3 duplication.
5. Read all three `SyncAccounts` implementations (Teller, Akahu, Plaid) to compare error handling.
6. **Found real reliability bug in Akahu**: `AkahuSyncService.SyncAccounts` returned immediately on any DB error (`return nil, err`) — if 1 of 10 accounts failed to update, the remaining 9 were never synced.
   - Teller: `var errs []error` → collect + continue → `errors.Join(errs...)`
   - Plaid: same collect-and-continue pattern
   - Akahu: **fail-fast** — inconsistent and wrong
7. Fixed `akahu.go` `SyncAccounts`:
   - Added `var errs []error`
   - Replaced 3x `return nil, err` with `errs = append(errs, ...) continue` + warn/error log
   - Return changed to `return synced, errors.Join(errs...)`
8. `go test ./internal/sync/...` → pass. `go build ./internal/sync/...` → clean.
9. Created PR #993 on `fix/akahu-sync-accounts-error-handling` → CI checks already passed → merged squash `684611522533a46479df321287c3a7198e8aeca0`.

## PostHog state (2026-06-09, session 8)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** (all logs are `info`) |
| Recent log pattern | `partial_failure errors=5` (structural) + `enrichment complete skipped=4452` |
| Service version in logs | `3f3ec2a...` — **still not deployed** |
| Latest commit on origin/main | `684611522533a46479df321287c3a7198e8aeca0` (PR #993, Akahu fix) |

## Shipped this session

- **PR #993** — `fix(sync): Akahu SyncAccounts collects errors and continues like Teller/Plaid`
  - `AkahuSyncService.SyncAccounts` previously failed fast on any DB error
  - Now collects errors + continues, consistent with Teller and Plaid
  - Branch: `fix/akahu-sync-accounts-error-handling` → squash merged to `main`

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#993 merged but not deployed.
- **Operational**: 5 Akahu connections with expired/invalid tokens. Reconnect via OAuth to fully resolve.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.
- Larger deferred DRY: `SyncAccounts` structural duplication (~800 lines x3) — deeply entangled with provider-specific models. No safe abstraction without interface redesign. Defer.
- Account type mappers (`mapTellerAccountType`, `mapAkahuAccountType`, `mapPlaidAccountType`) — genuinely different type systems per provider. No clean shared function. No action.

## Next run checklist

1. **READ THIS FILE FIRST before any PostHog queries. Do not investigate what's already documented.**
2. Latest known-clean commit: `684611522533a46479df321287c3a7198e8aeca0` (PR #993). Fetch main and compare.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `query-logs` (warn/error/fatal, -7d, cfo-cloud) → expect 0.
5. `query-logs` (info, -1h, cfo-cloud, limit 5) → check `severity_text` and `resource_attributes.service.version`.
6. If `service.version` != `3f3ec2a...` → new binary deployed. Verify `errors=5` drops (becomes `disconnected=5` or 0).
7. If new code commits since `684611522533a46479df321287c3a7198e8aeca0`: scan for DRY/simplify issues, open PRs.
8. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
9. Do NOT attempt PostHog issue assignment — API returns HTTP 500.
10. Skip all Linear tool calls — SSE transport deprecated.
11. All PostHog error type names (CLICommandError etc.) are from old `personal-cfo` binary. Do not re-investigate.
12. Branch PRs from `origin/main` with descriptive names (e.g. `fix/...`, `refactor/...`).
13. Fixed PRs: #910/#911, #916/#917, #926, #934, #942, #958, #959, #973, #982, #990, #993 — do not reinvestigate.
14. `logTransferMatchingError` and `updateLastSyncedAt` helpers in `sync/sync_helpers.go` — do not duplicate.
15. `syncTellerFields` — intentional (1 call site). GCS writer `Close()` — intentional, no defer.
16. `captureAkahuError` and `captureTellerError` are now one-liners wrapping `captureProviderError` — do not expand them.
17. `logs-count` with `severityLevels` is UNRELIABLE — always use `query-logs` to confirm severity.
18. `query-logs` params must be nested under a `query` object.
19. `partial_failure errors=5` — structural Akahu expired tokens. Not a code bug. Will self-heal on deploy.
20. `enrichment complete skipped=4452` — normal (old binary). Not a bug.
21. `error_details` field in PostHog events: empty for old binary. Don't query expecting data.
22. `syncTellerFields` — intentional (1 call site), do not flag as dead code.
23. `syncTransactionsImpl` + `ProviderImpl` — NOT dead code. Do not remove.
24. `mapPlaidCategoryToSubtype` — Plaid-specific, no duplication. No action needed.
25. Entity creation (Akahu vs Plaid) — genuinely different strategies. No action needed.
26. `SyncAccounts` x3 structural duplication — deeply entangled with provider-specific models. No safe extraction without interface redesign. Deferred indefinitely.
27. Account type mappers — genuinely different type systems per provider. No action.
28. `partial_failure errors=5` investigated across 7+ sessions today — do not re-investigate unless service.version changes.

---

# 2026-06-09 — Session 9: PostHog re-check (confirmed clean)

## Prior context loaded

Read 20260609.md sessions 1–8 + 20260608.md sessions 1–14. Key facts:
- 0 active errors, 0 warn/error/fatal logs (7d). All logs `info` (severity_number: 9).
- Service version `3f3ec2a...` still running — PRs #859–#993 merged but not deployed.
- Latest shipped: `8338421` (PR #994, session 8 changelog). Last real code: `6846115` (PR #993, Akahu SyncAccounts fix).
- `partial_failure errors=5` = structural Akahu expired tokens. Not a code bug.
- PostHog assignee API: HTTP 500 — skip programmatic assignment.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read 20260609.md (sessions 1–8) before any PostHog queries — full prior context loaded.
2. `git fetch origin main` → latest commit `8338421` (PR #994, session 8 changelog). **No new code commits** — last real code was `6846115` (PR #993).
3. PostHog checks ran per checklist:
   - `query-error-tracking-issues-list` (active, 30d) → **0 active issues** (hasMore: false, 0 results).
   - `query-logs` (warn/error/fatal, -7d, cfo-cloud) → **0 results**.
   - `query-logs` (info, -1h, cfo-cloud, limit 5) → all `severity_text: info`, severity_number: 9. Service version `3f3ec2a...`. `partial_failure errors=5` + `enrichment complete skipped=4452`. Unchanged.
4. Per checklist rule: 0 active errors AND 0 new code commits → write changelog, stop. Do NOT manufacture work.

## PostHog state (2026-06-09, session 9)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** (all logs are `info`) |
| Recent log pattern | `partial_failure errors=5` (structural) + `enrichment complete skipped=4452` |
| Service version in logs | `3f3ec2a...` — **still not deployed** |
| Latest commit on origin/main | `8338421` (PR #994, session 8 changelog) — no new code |

## Shipped this session

Nothing — system clean. No PRs created.

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#993 merged but not deployed.
- **Operational**: 5 Akahu connections with expired/invalid tokens. Reconnect via OAuth to fully resolve.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.
- Larger deferred DRY: `SyncAccounts` (~150 lines x3) — interface design needed, deferred.

## Next run checklist

1. **READ THIS FILE FIRST before any PostHog queries. Do not investigate what's already documented.**
2. Latest known-clean commit: `8338421` (PR #994). Fetch main and compare.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `query-logs` (warn/error/fatal, -7d, cfo-cloud) → expect 0.
5. `query-logs` (info, -1h, cfo-cloud, limit 5) → check `severity_text` and `resource_attributes.service.version`.
6. If `service.version` != `3f3ec2a...` → new binary deployed. Verify `errors=5` drops (becomes `disconnected=5` or 0).
7. If new code commits since `8338421`: scan for DRY/simplify issues, open PRs.
8. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
9. Do NOT attempt PostHog issue assignment — API returns HTTP 500.
10. Skip all Linear tool calls — SSE transport deprecated.
11. All PostHog error type names (CLICommandError etc.) are from old `personal-cfo` binary. Do not re-investigate.
12. Branch PRs from `origin/main` with descriptive names (e.g. `fix/...`, `refactor/...`).
13. Fixed PRs: #910/#911, #916/#917, #926, #934, #942, #958, #959, #973, #982, #990, #993 — do not reinvestigate.
14. `logTransferMatchingError` and `updateLastSyncedAt` helpers in `sync/sync_helpers.go` — do not duplicate.
15. `syncTellerFields` — intentional (1 call site). GCS writer `Close()` — intentional, no defer.
16. `captureAkahuError` and `captureTellerError` are now one-liners wrapping `captureProviderError` — do not expand them.
17. `logs-count` with `severityLevels` is UNRELIABLE — always use `query-logs` to confirm severity.
18. `query-logs` params must be nested under a `query` object.
19. `partial_failure errors=5` — structural Akahu expired tokens. Not a code bug. Will self-heal on deploy.
20. `enrichment complete skipped=4452` — normal (old binary). Not a bug.
21. `error_details` field in PostHog events: empty for old binary. Don't query expecting data.
22. `syncTellerFields` — intentional (1 call site), do not flag as dead code.
23. `syncTransactionsImpl` + `ProviderImpl` — NOT dead code. Do not remove.
24. `mapPlaidCategoryToSubtype` — Plaid-specific, no duplication. No action needed.
25. Entity creation (Akahu vs Plaid) — genuinely different strategies. No action needed.
26. `SyncAccounts` x3 structural duplication — deeply entangled with provider-specific models. No safe extraction without interface redesign. Deferred indefinitely.
27. Account type mappers — genuinely different type systems per provider. No action.
28. `partial_failure errors=5` investigated across 8+ sessions today — do not re-investigate unless service.version changes.

---

# 2026-06-09 — Session 10: PostHog re-check (confirmed clean)

## Prior context loaded

Read 20260609.md sessions 1–9 + 20260608.md sessions 1–14. Key facts:
- 0 active errors, 0 warn/error/fatal logs (7d). All logs `info` (severity_number: 9).
- Service version `3f3ec2a...` still running — PRs #859–#993 merged but not deployed.
- Latest shipped: `ced02f0` (PR #994 squash merge, session 9 changelog). Last real code: `6846115` (PR #993, Akahu SyncAccounts fix).
- `partial_failure errors=5` = structural Akahu expired tokens. Not a code bug.
- PostHog assignee API: HTTP 500 — skip programmatic assignment.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read 20260609.md (sessions 1–9) before any PostHog queries — full prior context loaded.
2. `git fetch origin main` → latest commit `ced02f0` (PR #994 squash, session 9 changelog). **No new code commits** — last real code was `6846115` (PR #993).
3. PostHog checks ran per checklist:
   - `query-error-tracking-issues-list` (active, 30d) → **0 active issues** (hasMore: false, 0 results).
   - `query-logs` (warn/error/fatal, -7d, cfo-cloud) → **0 results**.
   - `query-logs` (info, -1h, cfo-cloud, limit 5) → all `severity_text: info`, severity_number: 9. Service version `3f3ec2a...`. `partial_failure errors=5` + `enrichment complete skipped=4452`. Unchanged structural patterns.
4. Per checklist rule: 0 active errors AND 0 new code commits → write changelog, stop. Do NOT manufacture work.

## PostHog state (2026-06-09, session 10)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** (all logs are `info`) |
| Recent log pattern | `partial_failure errors=5` (structural) + `enrichment complete skipped=4452` |
| Service version in logs | `3f3ec2a...` — **still not deployed** |
| Latest commit on origin/main | `ced02f0` (PR #994 squash, session 9 changelog) — no new code |

## Shipped this session

Nothing — system clean. No PRs created.

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#993 merged but not deployed.
- **Operational**: 5 Akahu connections with expired/invalid tokens. Reconnect via OAuth to fully resolve.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.
- Larger deferred DRY: `SyncAccounts` (~150 lines x3) — interface design needed, deferred.

## Next run checklist

1. **READ THIS FILE FIRST before any PostHog queries. Do not investigate what's already documented.**
2. Latest known-clean commit: `ced02f0` (PR #994). Fetch main and compare.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `query-logs` (warn/error/fatal, -7d, cfo-cloud) → expect 0.
5. `query-logs` (info, -1h, cfo-cloud, limit 5) → check `severity_text` and `resource_attributes.service.version`.
6. If `service.version` != `3f3ec2a...` → new binary deployed. Verify `errors=5` drops (becomes `disconnected=5` or 0).
7. If new code commits since `ced02f0`: scan for DRY/simplify issues, open PRs.
8. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
9. Do NOT attempt PostHog issue assignment — API returns HTTP 500.
10. Skip all Linear tool calls — SSE transport deprecated.
11. All PostHog error type names (CLICommandError etc.) are from old `personal-cfo` binary. Do not re-investigate.
12. Branch PRs from `origin/main` with descriptive names (e.g. `fix/...`, `refactor/...`).
13. Fixed PRs: #910/#911, #916/#917, #926, #934, #942, #958, #959, #973, #982, #990, #993 — do not reinvestigate.
14. `logTransferMatchingError` and `updateLastSyncedAt` helpers in `sync/sync_helpers.go` — do not duplicate.
15. `syncTellerFields` — intentional (1 call site). GCS writer `Close()` — intentional, no defer.
16. `captureAkahuError` and `captureTellerError` are now one-liners wrapping `captureProviderError` — do not expand them.
17. `logs-count` with `severityLevels` is UNRELIABLE — always use `query-logs` to confirm severity.
18. `query-logs` params must be nested under a `query` object.
19. `partial_failure errors=5` — structural Akahu expired tokens. Not a code bug. Will self-heal on deploy.
20. `enrichment complete skipped=4452` — normal (old binary). Not a bug.
21. `error_details` field in PostHog events: empty for old binary. Don't query expecting data.
22. `syncTellerFields` — intentional (1 call site), do not flag as dead code.
23. `syncTransactionsImpl` + `ProviderImpl` — NOT dead code. Do not remove.
24. `mapPlaidCategoryToSubtype` — Plaid-specific, no duplication. No action needed.
25. Entity creation (Akahu vs Plaid) — genuinely different strategies. No action needed.
26. `SyncAccounts` x3 structural duplication — deeply entangled with provider-specific models. No safe extraction without interface redesign. Deferred indefinitely.
27. Account type mappers — genuinely different type systems per provider. No action.
28. `partial_failure errors=5` investigated across 9+ sessions today — do not re-investigate unless service.version changes.

---

# 2026-06-09 — Session 11: Fix per-row CaptureFailure + DRY balance reconciliation error (PRs #999, #1000)

## Prior context loaded

Read 20260609.md sessions 1–10 + 20260608.md sessions 1–14. Key facts:
- 0 active errors, 0 warn/error/fatal logs (7d). All logs `info` (severity_number: 9).
- Service version `3f3ec2a...` still running — PRs #859–#993 merged but not deployed.
- Latest shipped: `ced02f0` (PR #994, session 10 changelog). Last real code: `6846115` (PR #993, Akahu SyncAccounts fix).
- `partial_failure errors=5` = structural Akahu expired tokens. Not a code bug.
- PostHog assignee API: HTTP 500 — skip programmatic assignment.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read all prior session changelogs before PostHog queries — full prior context loaded.
2. `git fetch origin main` → latest commit `ae1e392` (PR #995, session 10 changelog). No new code commits.
3. PostHog checks ran per user's explicit request ("look at the errors and determine a plan"):
   - `query-error-tracking-issues-list` (all statuses, 30d) → 9 issues; 0 active. All resolved/suppressed.
   - Historical issues fetched and stack traces inspected for each.
   - `logs-count` (error/fatal, -7d, cfo-cloud) → 0. `logs-count` (warn/error/fatal, -30d) → 0.
4. All PostHog error type names (CLICommandError etc.) are from old `personal-cfo` binary at `internal/cloud/server.go` which does NOT exist in `probably`. However, the patterns they represent DO exist in current code.
5. Identified two real code issues not previously addressed:
   a. **Per-row CaptureFailure in transaction loops**: `SyncTransactions` (line 749) and `DeleteAndResync` (line 923) both called `CaptureFailure` once per failing row inside their import loops. If N rows fail, PostHog gets N identical exceptions. Relates to `ProviderTransactionStoreError` + `TellerWebhookTransactionAggregationError`.
   b. **Duplicated opening balance error handling**: Both functions had an identical 12-line error handling block for `reconcileOpeningBalance`/`reconcileOpeningBalanceFresh` — same TellerAPIError check, same warn/capture logic, same pattern. Only the `Operation` string differed. Relates to `ProviderBalancePullError`.
6. Implemented fixes on separate branches:
   - `fix/teller-no-per-row-capture`: replaced per-row captures with counter + single aggregated `CaptureFailure` after loop; per-row `slog.WarnContext` retained.
   - `fix/teller-dry-balance-error`: extracted `handleBalanceReconciliationErr(ctx, err, accountID, op string) bool` helper; both call sites shrunk to 3-line `if` block.
7. Both passed `go build ./internal/sync/...` + `go test ./internal/sync/...`.
8. Created GitHub issues #997 (per-row capture) and #998 (DRY balance) assigned to `asomervell`.
9. Created PRs #999 and #1000, both ready (not draft). CI was already clean — merged both directly as squash commits.
10. Marked PostHog issues `019e623e` (ProviderTransactionStoreError), `019e6239` (TellerWebhookTransactionAggregationError), `019e05c7` (ProviderBalancePullError) as `resolved` via `error-tracking-issues-partial-update`.

## PostHog state (2026-06-09, session 11)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (30d, cfo-cloud) | **0** |
| Historical issues addressed this session | 3 marked resolved (ProviderTransactionStoreError, TellerWebhookTransactionAggregationError, ProviderBalancePullError) |
| Service version in logs | `3f3ec2a...` — **still not deployed** |
| Latest commit on origin/main | `54a41b2` (PR #1000, balance DRY refactor) |

## Shipped this session

- **PR #999** — `fix(sync): replace per-row CaptureFailure with single summary capture`
  - `SyncTransactions` and `DeleteAndResync` previously called `CaptureFailure` once per failing transaction row
  - Changed to count failures and emit one aggregated exception after the loop
  - Per-row `slog.WarnContext` retained for structured log detail
  - Addresses `ProviderTransactionStoreError` + `TellerWebhookTransactionAggregationError` patterns
  - Branch: `fix/teller-no-per-row-capture` → squash merged `50b43e2`

- **PR #1000** — `refactor(sync): extract handleBalanceReconciliationErr to remove duplicated block`
  - `SyncTransactions` and `DeleteAndResync` each had an identical 12-line error handling block
  - Extracted to `handleBalanceReconciliationErr(ctx, err, accountID, op string) bool`
  - Both call sites reduced to 3-line `if` block; no behaviour change
  - Addresses `ProviderBalancePullError` pattern
  - Branch: `fix/teller-dry-balance-error` → squash merged `54a41b2`

- **PostHog issues marked resolved**: `019e623e`, `019e6239`, `019e05c7`

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#1000 merged but not deployed.
- **Operational**: 5 Akahu connections with expired/invalid tokens. Reconnect via OAuth to fully resolve.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.
- `SyncAccounts` x3 structural duplication — deeply entangled with provider-specific models. No safe extraction without interface redesign. Deferred indefinitely.
- Remaining PostHog historical issues (CLICommandError ×2, HTTPServerError, TellerConnectSessionError, ProviderBalancePullError ×2) are from old `personal-cfo` binary only — no code fix possible in `probably`. Leave as resolved/suppressed.

## Next run checklist

1. **READ THIS FILE FIRST before any PostHog queries. Do not investigate what's already documented.**
2. Latest known-clean commit: `54a41b2` (PR #1000). Fetch main and compare.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `query-logs` (warn/error/fatal, -7d, cfo-cloud) → expect 0.
5. `query-logs` (info, -1h, cfo-cloud, limit 5) → check `severity_text` and `resource_attributes.service.version`.
6. If `service.version` != `3f3ec2a...` → new binary deployed. Verify `errors=5` drops (becomes `disconnected=5` or 0).
7. If new code commits since `54a41b2`: scan for DRY/simplify issues, open PRs.
8. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
9. Do NOT attempt PostHog issue assignment — API returns HTTP 500.
10. Skip all Linear tool calls — SSE transport deprecated.
11. All PostHog error type names (CLICommandError etc.) are from old `personal-cfo` binary. Do not re-investigate.
12. Branch PRs from `origin/main` with descriptive names (e.g. `fix/...`, `refactor/...`).
13. Fixed PRs: #910/#911, #916/#917, #926, #934, #942, #958, #959, #973, #982, #990, #993, #999, #1000 — do not reinvestigate.
14. `logTransferMatchingError` and `updateLastSyncedAt` helpers in `sync/sync_helpers.go` — do not duplicate.
15. `syncTellerFields` — intentional (1 call site). GCS writer `Close()` — intentional, no defer.
16. `captureAkahuError` and `captureTellerError` are now one-liners wrapping `captureProviderError` — do not expand them.
17. `handleBalanceReconciliationErr` is the canonical balance error handler — do not duplicate its logic elsewhere.
18. Per-row CaptureFailure pattern is fixed — do not reintroduce. Single summary capture after loop is the pattern.
19. `logs-count` with `severityLevels` is UNRELIABLE — always use `query-logs` to confirm severity.
20. `query-logs` params must be nested under a `query` object.
21. `partial_failure errors=5` — structural Akahu expired tokens. Not a code bug. Will self-heal on deploy.
22. `enrichment complete skipped=4452` — normal (old binary). Not a bug.
23. `error_details` field in PostHog events: empty for old binary. Don't query expecting data.
24. `syncTellerFields` — intentional (1 call site), do not flag as dead code.
25. `syncTransactionsImpl` + `ProviderImpl` — NOT dead code. Do not remove.
26. `mapPlaidCategoryToSubtype` — Plaid-specific, no duplication. No action needed.
27. Entity creation (Akahu vs Plaid) — genuinely different strategies. No action needed.
28. `SyncAccounts` x3 structural duplication — deeply entangled with provider-specific models. No safe extraction without interface redesign. Deferred indefinitely.
29. Account type mappers — genuinely different type systems per provider. No action.
30. `partial_failure errors=5` investigated across 10+ sessions today — do not re-investigate unless service.version changes.

---

# 2026-06-09 — Session 12: Fix per-row capture in Plaid/Akahu + FullResync DRY (PRs #1002, #1003)

## Prior context loaded

Read 20260609.md sessions 1–11 + 20260608.md sessions 1–14. Key facts:
- 0 active errors, 0 warn/error/fatal logs (7d). All logs `info` (severity_number: 9).
- Service version `3f3ec2a...` still running — PRs #859–#1000 merged but not deployed.
- Latest shipped: `db2c0a3` (PR #1001, session 11 changelog). Last real code: `54a41b2` (PR #1000).
- `partial_failure errors=5` = structural Akahu expired tokens. Not a code bug.
- PostHog assignee API: HTTP 500 — skip programmatic assignment.
- Linear SSE: deprecated — skip all Linear tool calls.

## Reasoning

1. Read all prior session changelogs before PostHog queries — full prior context loaded.
2. `git fetch origin main` → latest commit `db2c0a3` (PR #1001, session 11 changelog). No new code.
3. PostHog checks ran per checklist:
   - `query-error-tracking-issues-list` (all statuses, 30d) → 0 active issues; 9 historical (all resolved/suppressed).
   - `query-logs` (warn/error/fatal, -7d, cfo-cloud) → 0 results.
   - Service version `3f3ec2a...` — unchanged.
4. No new PostHog errors. Scanned for remaining DRY/per-row capture issues not addressed in prior sessions.
5. Found three issues:
   a. **Per-row CaptureFailure in Plaid `SyncTransactions`** (plaid_sync.go:677): same anti-pattern fixed for Teller in #999, but not yet applied to Plaid. If N rows fail, PostHog gets N identical exceptions.
   b. **Per-row CaptureFailure in Akahu `SyncTransactions`** (akahu.go:645): same issue in Akahu.
   c. **`FullResync` missed by #1000** (teller.go:1211–1228): `SyncTransactions` and `DeleteAndResync` got `handleBalanceReconciliationErr`, but `FullResync` still had the full 15-line inline duplicate.
6. Applied fixes on two branches:
   - `fix/plaid-akahu-no-per-row-capture`: Plaid + Akahu per-row → summary capture
   - `fix/teller-fullresync-handle-balance-err`: FullResync uses `handleBalanceReconciliationErr`
7. Both passed `go build ./internal/sync/...` + `go test ./internal/sync/...`.
8. Created PRs #1002 and #1003, both ready. Both merged directly (checks already passing).

## PostHog state (2026-06-09, session 12)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** (all logs are `info`) |
| Service version in logs | `3f3ec2a...` — **still not deployed** |
| Latest commit on origin/main | `06e9020` (PR #1003, FullResync DRY) |

## Shipped this session

- **PR #1002** — `fix(sync): replace per-row CaptureFailure with single summary capture in Plaid and Akahu`
  - `PlaidSyncService.SyncTransactions` and `AkahuSyncService.SyncTransactions` called `CaptureFailure` once per failing row
  - Added `storeErrors` counter; single aggregated `CaptureFailure` emitted after loop
  - Per-row `slog.WarnContext` retained; no behaviour change for successful paths
  - Merged as `9fafa9c`

- **PR #1003** — `refactor(sync): apply handleBalanceReconciliationErr in FullResync`
  - `FullResync` balance reconciliation error handling was the original 15-line inline block (missed by PR #1000)
  - Replaced with `handleBalanceReconciliationErr` call — −12 lines, no behaviour change
  - Merged as `06e9020`

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#1003 merged but not deployed.
- **Operational**: 5 Akahu connections with expired/invalid tokens. Reconnect via OAuth to fully resolve.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.
- `SyncAccounts` x3 structural duplication — deeply entangled with provider-specific models. No safe extraction without interface redesign. Deferred indefinitely.

## Next run checklist

1. **READ THIS FILE FIRST before any PostHog queries. Do not investigate what's already documented.**
2. Latest known-clean commit: `06e9020` (PR #1003). Fetch main and compare.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `query-logs` (warn/error/fatal, -7d, cfo-cloud) → expect 0.
5. `query-logs` (all severity, -1h, cfo-cloud, limit 5) → check `severity_text` and `resource_attributes.service.version`.
6. If `service.version` != `3f3ec2a...` → new binary deployed. Verify `errors=5` drops (becomes `disconnected=5` or 0).
7. If new code commits since `06e9020`: scan for DRY/simplify issues, open PRs.
8. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
9. Do NOT attempt PostHog issue assignment — API returns HTTP 500.
10. Skip all Linear tool calls — SSE transport deprecated.
11. All PostHog error type names (CLICommandError etc.) are from old `personal-cfo` binary. Do not re-investigate.
12. Branch PRs from `origin/main` with descriptive names (e.g. `fix/...`, `refactor/...`).
13. Fixed PRs: #910/#911, #916/#917, #926, #934, #942, #958, #959, #973, #982, #990, #993, #999, #1000, #1002, #1003 — do not reinvestigate.
14. `logTransferMatchingError` and `updateLastSyncedAt` helpers in `sync/sync_helpers.go` — do not duplicate.
15. `syncTellerFields` — intentional (1 call site). GCS writer `Close()` — intentional, no defer.
16. `captureAkahuError` and `captureTellerError` are now one-liners wrapping `captureProviderError` — do not expand them.
17. `handleBalanceReconciliationErr` is the canonical balance error handler — do not duplicate its logic elsewhere.
18. Per-row CaptureFailure pattern is now fixed in Teller, Plaid, and Akahu — do not reintroduce.
19. `logs-count` with `severityLevels` is UNRELIABLE — always use `query-logs` to confirm severity.
20. `query-logs` params must be nested under a `query` object.
21. `partial_failure errors=5` — structural Akahu expired tokens. Not a code bug. Will self-heal on deploy.
22. `enrichment complete skipped=4452` — normal (old binary). Not a bug.
23. `error_details` field in PostHog events: empty for old binary. Don't query expecting data.
24. `syncTellerFields` — intentional (1 call site), do not flag as dead code.
25. `syncTransactionsImpl` + `ProviderImpl` — NOT dead code. Do not remove.
26. `mapPlaidCategoryToSubtype` — Plaid-specific, no duplication. No action needed.
27. Entity creation (Akahu vs Plaid) — genuinely different strategies. No action needed.
28. `SyncAccounts` x3 structural duplication — deeply entangled with provider-specific models. No safe extraction without interface redesign. Deferred indefinitely.
29. Account type mappers — genuinely different type systems per provider. No action.
30. `partial_failure errors=5` investigated across 11+ sessions today — do not re-investigate unless service.version changes.
30. `partial_failure errors=5` investigated across 10+ sessions today — do not re-investigate unless service.version changes.

---

# 2026-06-09 — Session 13: PostHog re-check (confirmed clean)

## Prior context loaded

Read 20260609.md sessions 1–12 + 20260608.md sessions 1–14. Key facts:
- 0 active errors, 0 warn/error/fatal logs (7d). All logs `info` (severity_number: 9).
- Service version `3f3ec2a...` still running — PRs #859–#1003 merged but not deployed.
- Latest shipped: `f5a81e0` (PR for session 12 changelog). Last real code: `06e9020` (PR #1003).
- `partial_failure errors=5` = structural Akahu expired tokens. Not a code bug.
- PostHog assignee API: HTTP 500 — skip programmatic assignment.
- Linear SSE: deprecated — skip all Linear tool calls.
- All per-row CaptureFailure patterns fixed (Teller #999, Plaid/Akahu #1002).
- `handleBalanceReconciliationErr` applied to all 3 sync functions (#1000, #1003).
- `captureProviderError` extracted from Teller/Akahu duplicate (#990).
- Akahu `SyncAccounts` fail-fast bug fixed (#993).

## Reasoning

1. Read 20260609.md (sessions 1–12) before any PostHog queries — full prior context loaded.
2. `git fetch origin main` → latest commit `f5a81e0` (session 12 changelog). **No new code commits** — last real code was `06e9020` (PR #1003).
3. PostHog checks ran per checklist:
   - `query-error-tracking-issues-list` (all statuses, 30d) → **0 active issues**; 9 historical (all resolved/suppressed, old `personal-cfo` binary).
   - `query-logs` (warn/error/fatal, -7d, cfo-cloud) → **0 results**.
   - `query-logs` (all severity, -1h, cfo-cloud, limit 5) → all `severity_text: info`, severity_number: 9. Service version `3f3ec2a...`. `partial_failure errors=5` + `enrichment complete skipped=4454`. Unchanged structural patterns.
4. Per checklist rule: 0 active errors AND 0 new code commits → write changelog, stop. Do NOT manufacture work.

## PostHog state (2026-06-09, session 13)

| Check | Result |
|---|---|
| Active error tracking issues (30d) | **0** |
| Warn/error/fatal logs (7d, cfo-cloud) | **0** (all logs are `info`) |
| Recent log pattern | `partial_failure errors=5` (structural) + `enrichment complete skipped=4454` |
| Service version in logs | `3f3ec2a...` — **still not deployed** |
| Latest commit on origin/main | `f5a81e0` (session 12 changelog) — no new code |

## Shipped this session

Nothing — system clean. No PRs created.

## What was left behind

- Service version `3f3ec2a...` still running — PRs #859–#1003 merged but not deployed.
- **Operational**: 5 Akahu connections with expired/invalid tokens. Reconnect via OAuth to fully resolve.
- PostHog assignee API: HTTP 500 — cannot assign issues programmatically.
- Linear SSE: deprecated — skip all Linear tool calls.
- `SyncAccounts` x3 structural duplication — deeply entangled with provider-specific models. No safe extraction without interface redesign. Deferred indefinitely.

## Next run checklist

1. **READ THIS FILE FIRST before any PostHog queries. Do not investigate what's already documented.**
2. Latest known-clean commit: `f5a81e0` (session 12 changelog). Fetch main and compare.
3. `query-error-tracking-issues-list` (active, 30d) → expect 0.
4. `query-logs` (warn/error/fatal, -7d, cfo-cloud) → expect 0.
5. `query-logs` (all severity, -1h, cfo-cloud, limit 5) → check `severity_text` and `resource_attributes.service.version`.
6. If `service.version` != `3f3ec2a...` → new binary deployed. Verify `errors=5` drops (becomes `disconnected=5` or 0).
7. If new code commits since `f5a81e0`: scan for DRY/simplify issues, open PRs.
8. If 0 active errors AND 0 new code issues → write changelog, stop. Do NOT manufacture work.
9. Do NOT attempt PostHog issue assignment — API returns HTTP 500.
10. Skip all Linear tool calls — SSE transport deprecated.
11. All PostHog error type names (CLICommandError etc.) are from old `personal-cfo` binary. Do not re-investigate.
12. Branch PRs from `origin/main` with descriptive names (e.g. `fix/...`, `refactor/...`).
13. Fixed PRs: #910/#911, #916/#917, #926, #934, #942, #958, #959, #973, #982, #990, #993, #999, #1000, #1002, #1003 — do not reinvestigate.
14. `logTransferMatchingError` and `updateLastSyncedAt` helpers in `sync/sync_helpers.go` — do not duplicate.
15. `syncTellerFields` — intentional (1 call site). GCS writer `Close()` — intentional, no defer.
16. `captureAkahuError` and `captureTellerError` are now one-liners wrapping `captureProviderError` — do not expand them.
17. `handleBalanceReconciliationErr` is the canonical balance error handler — do not duplicate its logic elsewhere.
18. Per-row CaptureFailure pattern is now fixed in Teller, Plaid, and Akahu — do not reintroduce.
19. `logs-count` with `severityLevels` is UNRELIABLE — always use `query-logs` to confirm severity.
20. `query-logs` params must be nested under a `query` object.
21. `partial_failure errors=5` — structural Akahu expired tokens. Not a code bug. Will self-heal on deploy.
22. `enrichment complete skipped=4454` — normal (old binary). Not a bug.
23. `error_details` field in PostHog events: empty for old binary. Don't query expecting data.
24. `syncTellerFields` — intentional (1 call site), do not flag as dead code.
25. `syncTransactionsImpl` + `ProviderImpl` — NOT dead code. Do not remove.
26. `mapPlaidCategoryToSubtype` — Plaid-specific, no duplication. No action needed.
27. Entity creation (Akahu vs Plaid) — genuinely different strategies. No action needed.
28. `SyncAccounts` x3 structural duplication — deeply entangled with provider-specific models. No safe extraction without interface redesign. Deferred indefinitely.
29. Account type mappers — genuinely different type systems per provider. No action.
30. `partial_failure errors=5` investigated across 12+ sessions today — do not re-investigate unless service.version changes.
