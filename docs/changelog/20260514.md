# Session log — 2026-05-14

## What I read on startup

Read `docs/changelog/20260513.md` (Runs 1–6). Prior context summary:

- PostHog: zero active error tracking issues throughout all prior runs. All resolved/suppressed issues originate from `personal-cfo` binary (`cfo-cloud` service, `github.com/asomervell/personal-cfo/internal/cloud/server.go`) — a separate binary sharing this PostHog project. Nothing actionable in `probably`.
- Recent merges since last changelog (post-Run-6):
  - HAM-4867: log swallowed QueryRow error in EntityStore.Upsert ID refetch (#451)
  - HAM-4915: remove dead GetStatementPath and Close from GCSStorage (#454)
  - HAM-4916/4917: remove dead LogoProxy handler and ColdStartDetector (#455)
  - HAM-4920: remove dead SMTP config fields and unused log config fields
  - HAM-4921: remove dead baseURL field from GCSStorage and S3Storage
  - HAM-4919: remove dead ValidateTransaction function and its tests (#459)
  - HAM-4923: DRY null helpers — export from models, remove duplicate in backup (#460)
  - HAM-4924/4925: remove dead ledger methods + DRY rules scan helper (#461)
  - fix: Teller disconnect detection and balance-pull error propagation (#462)
  - HAM-4926: remove dead IsRunning/SyncNow methods and syncing field (#463)
- All packages swept at least once as of Run 6 on May 13.
- Persistent deferred items: POST-based Teller Connect handoff (Teller SDK limitation), `containsLastFour` dynamic regexps (runtime-dependent), `splitTransaction` real accounting (deferred), `cmd/util` hidden debug commands (intentional).

---

## Run 1 — 2026-05-14

### What I reasoned on

**PostHog audit — error tracking (status=all, last 30d):**

All issues found are from `personal-cfo` binary (distinct_id `personal-cfo-service`, module `github.com/asomervell/personal-cfo/internal/cloud/server.go`). Status breakdown:

| Error type | Occurrences | Status | Action |
|---|---|---|---|
| SyncAccountPullError | 106 | resolved | Already resolved in prior sessions |
| ProviderPullError | 100 | resolved | Already resolved in prior sessions |
| ProviderSyncError | 92 | resolved | Already resolved in prior sessions |
| CLICommandError | 39 | resolved | Already resolved in prior sessions |
| TellerWebhookTransactionBackfillRecordError | 12 | resolved | Already resolved |
| ProviderTransactionStoreError | 6 | resolved | Already resolved |
| TellerWebhookTransactionAggregationError | 5 | resolved | Already resolved |
| ProviderBalancePullError | 4 | suppressed | Root cause fixed in PR #462 (Teller disconnect detection) |
| TellerConnectSessionError | 2 | resolved | Already resolved |

**Zero active issues** for `probably`. Log service `cfo-cloud`: zero error/warn/fatal entries in last 7 days ✓.

**Recent commits audit:** 10 commits merged after last changelog, all from other sessions on May 13–14. Notably PR #462 (fix: Teller disconnect detection) was the actual code fix for the recurring `ProviderBalancePullError` pattern — three related changes:
1. `TellerAPIError.IsConnectionDisconnected()` now treats HTTP 401 as disconnect (not just `enrollment.disconnected` code prefix)
2. `SyncTransactions` propagates `ConnectionDisconnectedError` from `reconcileOpeningBalance` instead of silently discarding it
3. `SyncWorker.run()` wraps each `syncAll()` with panic recovery to prevent goroutine death

**Proactive code sweep (Explore agent — `internal/sync/`, `internal/models/rules.go`, `internal/ledger/`, `internal/handlers/teller.go`):**

One genuine finding:

**HAM-4927 — 4 dead exported methods on `AkahuClient` in `internal/sync/akahu.go`:**
- `GetUserToken()` (line 109) — returns `cfg.AkahuUserToken`; personal-app token feature never wired up
- `HasPersonalAppToken()` (line 114) — checks if personal token is set; ditto
- `GetAccountByID()` (lines 303–327) — single-account fetch; sync logic uses `GetAccounts` only
- `RefreshAccessToken()` (lines 443–472) — OAuth token refresh; Akahu uses short-lived tokens via `ExchangeAuthCode`, refresh flow never wired up

All confirmed zero callers via `grep -rn` across all `.go` files. No tests reference them. `AkahuTokenResponse` type retained (used by `ExchangeAuthCode`).

### What I fixed and shipped

**HAM-4927** (Low) → Done ✓. Assigned to Andrew Somervell.

**PR #464** (squash-merged SHA `da571d97`):

| File | Change |
|---|---|
| `internal/sync/akahu.go` | Remove `GetUserToken`, `HasPersonalAppToken`, `GetAccountByID`, `RefreshAccessToken` |

Net: 1 file, -70 lines. Build clean (`go build ./...`). Tests passing (`go test ./internal/sync/...`). Merged directly.

Issue HAM-4927 → closed as Done.

### What was left behind

- **`personal-cfo`** — all PostHog signals from that separate binary; nothing to do in `probably`. All 14 issues in error tracking are from `personal-cfo`; `probably` is clean.
- **POST-based Teller Connect handoff** — access token still passed as GET query param; Teller SDK limitation, deferred.
- **`containsLastFour` in `transfer_matcher.go`** — compiles 4 dynamic regexps per call (runtime-dependent values, cannot pre-compile). Acceptable.
- **`splitTransaction` real accounting** — implementation only adds tags, not separate expense entries. Deferred.
- **`cmd/util` hidden commands** (`check-tables`, `fix-insights-tables`, `list-ledgers`) — not in help, kept for manual debugging/recovery.
- **`_ = e.transactions.LoadEntries/LoadTags`** — best-effort enrichment pattern, intentional.

## Checklist for next run

- [ ] Query PostHog (status=active, last 30d) — zero active; watch for any recurrence
- [ ] `cfo-cloud` logs: zero error/warn/fatal — consistent pattern
- [ ] All packages swept through May 14. Next run: focus on recent commits for new dead code
- [ ] Known false positives to skip: `plaid.go:239`, `akahu.go:547`, `chain.go:21`, `LoadEntries/LoadTags _ =` pattern, MCP/handler `w.Write` discards
