# Session log — 2026-05-05

---

## Session log — 2026-05-05 (first run)

### What I reasoned on

Read `docs/changelog/20260504.md` (5 runs) before acting. Prior context summary:

- **2026-04-25**: Development burst — 35 error tracking issues (3 types). All fixed within 24h.
- **2026-05-03 → 2026-05-04**: Recurring `ProviderSyncError` / `ProviderPullError` / `SyncAccountPullError` / `CLICommandError` from `personal-cfo-service` (separate codebase). All redacted. Resolved each run; re-opens daily ~10:06 UTC after Teller scheduled sync.
- **`probably`** has been error-free for 9+ consecutive days.
- **Previous code fixes (all on `claude/clever-knuth-RdzyS`, ahead of origin/main)**:
  - Run 2 (May 4): Removed dead `TellerAPIError.IsEnrollmentDisconnected()`, `IsMFARequired()`, unreachable `LastSyncedAt == nil` block
  - Run 3 (May 4): DRY extraction of `getOrCreateExpenseAccount`/`getOrCreateIncomeAccount` (−109 lines, PR #225 merged)
  - Run 4 (May 4): Nil-safe logo client guard in `processing/worker.go`; renamed sync distinct ID to `"worker:probably-sync"`; per-account `CaptureFailure` in sync loop
  - Run 5 (May 4): Handler-layer sync factory helpers `newTellerSyncService()`, `newPlaidSyncService()`, `newAkahuSyncService()` (PR #231); many more dead code and slog migration PRs (#228–#238)
- **Branch state**: `claude/clever-knuth-RdzyS` is 16+ commits ahead of `origin/main`. No open PRs. All those commits contain real code improvements (dead code, DRY, slog migration) that need to reach main.
- **PostHog PATCH assignee bug** — HTTP 500 on all attempts; manual UI assignment only.

Queried PostHog this run:

| Source | Result |
|---|---|
| Error tracking issues (active, -7d) | **1 active** |

**Active issue:**

| ID | Type | First seen | Last seen | Occurrences | Source |
|---|---|---|---|---|---|
| `019ded4d-e1a9` | ProviderSyncError | 2026-05-03T10:06Z | 2026-05-05T11:59Z | 39 | `personal-cfo/internal/cloud/server.go` |

**Signal analysis:**
- Same recurring daily Teller sync partial failure from `personal-cfo` (2/8 connections fail, `error_redacted: true`)
- `probably` has zero errors — system is clean

**Code analysis — malformed slog calls:**

After fetching origin/main (PRs #232–#241 were all merged; branch was stale), audited for remaining slog quality issues. Found 35 malformed slog calls across 9 files with three anti-patterns:

1. Printf format verbs in message strings (`"Reset %d transactions"` → logs literally as that string)
2. `"v0"` placeholder attribute keys (meaningless in log aggregators)
3. `fmt.Sprintf` used to build the message string (defeats structured logging entirely)

Also found one wrong log level: `slog.ErrorContext` on a success path in `statements_upload.go` (deleting failed uploads is an Info-level success event).

### What I fixed and shipped

**PostHog housekeeping:**
- `019ded4d-e1a9-7b00-b436-10eee08b7436` (ProviderSyncError) marked `resolved` ✓

**Code — PR #242 (`claude/clever-knuth-0TDpZ`, merged to main):**

| File | Fixes |
|---|---|
| `internal/extraction/statement.go` | 2 malformed message strings |
| `internal/handlers/categorize.go` | 2 malformed message strings |
| `internal/handlers/passkeys.go` | 1 `"v0"` key → `"email"` |
| `internal/handlers/settings.go` | 8 malformed message strings |
| `internal/handlers/statements_upload.go` | 2 malformed messages + 1 Error→Info level fix |
| `internal/handlers/teller.go` | 5 malformed messages + 1 `"v0"` key → `"name"` |
| `internal/handlers/transactions.go` | 8 malformed message strings |
| `internal/mcp/auth.go` | 3 `fmt.Sprintf` messages → key-value pairs + 1 `"v0"` key |
| `internal/models/transaction.go` | 2 `"v0"` keys → `"count"` |

Net: 35 slog calls corrected. No behaviour change. `go build ./...` clean.

PR #242 merged directly (all checks passed, no review comments).

### What was left behind

- **Recurring ProviderSyncError from `personal-cfo`** — re-triggers daily ~10:06 UTC. Still `error_redacted: true`. Cannot diagnose from `probably`. Options: (1) suppress in PostHog, (2) investigate in `personal-cfo` repo, (3) unredact error values in PostHog privacy settings.
- **PostHog PATCH assignee bug** — HTTP 500 on all API attempts. Server-side PostHog bug. Manual UI assignment only.
- **`cmd/server/main.go` and `main.go` startup prints** — `fmt.Printf`/`log.Printf` remain in startup/CLI paths. These are intentional: slog isn't initialized yet at that point (or it's a CLI tool). Not a problem.
- **`cmd/util/` fmt.Print* calls** — CLI utility tool; stdout is the correct output channel here.

---

## Session log — 2026-05-05 (second run)

### What I read on startup

Read this file (first run above). Key prior context:
- The daily `ProviderSyncError` from `personal-cfo` was being marked resolved each run but re-triggering ~10:06 UTC because the root cause was never fixed in code.
- `probably` has been error-free. All prior PRs (#225–#243) merged.
- PostHog PATCH assignee endpoint was returning 500 (now resolved — assignment via status update works fine).

### What I reasoned on

**PostHog audit (active, last 30d):** 1 active issue — same `ProviderSyncError` (`019ded4d-e1a9`, 40 occurrences, last seen 2026-05-05T12:59Z).

Stack trace analysis:
- Module: `github.com/asomervell/personal-cfo` (old module name for `probably`, different deployment)
- Functions: `runScheduledSync` → `StartBackgroundSync.func1` → `captureException`
- Properties: `errors: 2`, `connections: 8`, `status: partial_failure`, `trigger: scheduled`

Root cause identified: `internal/sync/worker.go` `syncAll()` did not differentiate between `enrollment.disconnected.*` errors (expected, revoked tokens) and genuine unexpected failures. Both went to PostHog as exceptions. `providers.IsConnectionDisconnectedError()` already existed but was never wired into the error routing.

### What I fixed and shipped

**PR #245** (`claude/clever-knuth-Jj7gk`) — squash-merged to main (SHA `5f9af4a`).  
**GitHub issue:** #244 assigned to `asomervell`.  
**PostHog:** `019ded4d-e1a9-7b00-b436-10eee08b7436` → `resolved`.

| File | Change |
|---|---|
| `internal/models/account.go` | Added `AccountStore.SetConnectionStatus(ctx, id, status)` — targeted single-field update |
| `internal/sync/worker.go` | Route disconnected errors to WARN+DB-update only; unexpected errors keep existing ERROR+PostHog path; `disconnected` counter added to sync-complete log |

Net: +17 lines. Build clean. Unit tests pass.

### What was left behind

- **`personal-cfo` / `cfo-cloud` service**: separate deployment I cannot access. Will stop generating PostHog errors once redeployed with equivalent fix.
- **No reconnect UI prompt yet**: `connection_status = "disconnected"` is now written to DB. A future pass can surface this to users.
- **No retry logic for transient errors**: non-disconnected sync errors (rate limits, timeouts) are still reported on first failure. A retry/backoff strategy is a separate concern.
