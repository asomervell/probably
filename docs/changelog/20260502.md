# Session log — 2026-05-02

## Context from previous runs

### 2026-04-27 (6 runs)
Fixed all known PostHog errors (PRs #114–#124, #126):
- Error A: Gemini `function_response.name: Name cannot be empty`
- Error B: Gemini `Function call is missing a thought_signature`
- Error C: OTEL `Too many AI spans`
- Error D: Plaid handlers using `log.Printf`, bypassing OTEL
- Error E: Google `API_KEY_INVALID` misclassified as `[parse_error]`

### 2026-04-28 (6 runs)
Defensive hardening (PR #139): `WithMaxQueueSize(75)` on OTEL batcher; `thoughtSignaturesConsistent()` guard for Gemini raw parts. 711k historical error entries confirmed bounded to the April 25 burst, pre-fix.

### 2026-04-29–30 (12 runs total)
All runs: zero new active issues, zero post-April-26 errors or warnings.

### 2026-05-01 (6 runs)
- Run 2: PR #180 merged — `observability: log HTTP 4xx at Info, not Warn` (found 1,244 historical warn-level HTTP 401s exposing the 4xx-at-Warn bug).
- Runs 1, 3–6: zero new signals. System fully steady-state.

---

## What I reasoned on (this run — 2026-05-02)

Read `docs/changelog/20260501.md` (6 runs) before querying. Prior context: 35 resolved error tracking issues all from April 25 burst; PR #180 (HTTP 4xx at Info) merged in the previous session's run 2. Thirteen consecutive clean runs across April 29–May 1.

Queried PostHog across all signals:

| Source | Count |
|---|---|
| Error tracking issues (active, -7d) | 0 |
| Error tracking issues (all, -90d) | 35, all `resolved`, all `first_seen: 2026-04-25` |
| Error/fatal logs since 2026-05-01 | 0 |
| Warn logs since 2026-05-01 | 0 |

The 35 resolved issues break down into 3 error types (unchanged from all prior runs):
- **`function_response.name: Name cannot be empty`** (~20 issues, ~232 occurrences) — confirmed fixed
- **`Function call is missing a thought_signature`** (~2 issues, ~228 occurrences) — confirmed fixed
- **`Too many AI spans: N exceeds limit of 100`** (~13 issues) — confirmed fixed

Services checked: `probably-app`, `probably-worker`, `posthog-browser-logs`.

---

## What I fixed and shipped

Nothing to fix. PostHog is fully clean. No PRs created.

---

## What was left behind

Same deferred items as all prior runs — none are actionable from PostHog signals:
- **Production project** (id 398540) — `ingested_event: false`; no traffic to check.
- **PostHog PATCH assignee bug** — server-side; manual UI assignment still the only path.
- **`syncCtx := context.Background()` in Plaid sync** — accepted low-priority, no PostHog signal.
- **`log.Printf` calls** — 8 remain in `internal/observability/` (intentional bootstrap logging, correct as-is).

---

## Session log — 2026-05-02 (second run)

### What I reasoned on

Read `docs/changelog/20260502.md` (first run) and all prior days before querying. Prior context summary:

- **2026-04-25**: Development burst generated 35 error tracking issues (3 distinct types: `function_response.name empty`, `thought_signature missing`, `Too many AI spans`). All fixed within 24h and resolved in PostHog.
- **2026-04-26 through 2026-05-01**: Zero new signals across 18 consecutive clean runs.
- **2026-05-01 run 2**: PR #180 merged — `observability: log HTTP 4xx at Info, not Warn`.
- **2026-05-02 run 1**: Zero signals, nothing shipped.

Queried PostHog this run:

| Source | Count |
|---|---|
| Error tracking issues (active, -7d) | 0 |
| Error tracking issues (all, -90d) | 35, all `resolved`, all `first_seen: 2026-04-25` |
| Error/fatal/warn logs since 2026-05-02T00:00:00Z | 0 |

### What I fixed and shipped

Nothing to fix. PostHog is fully clean. No PRs created.

### What was left behind

Same deferred items as all prior runs — none are actionable from PostHog signals (second run):
- **Production project** (id 398540) — `ingested_event: false`; no traffic to check.
- **PostHog PATCH assignee bug** — server-side; manual UI assignment still the only path.
- **`syncCtx := context.Background()` in Plaid sync** — accepted low-priority, no PostHog signal.
- **`log.Printf` calls** — 8 remain in `internal/observability/` (intentional bootstrap logging, correct as-is).

---

## Session log — 2026-05-02 (third run)

### What I reasoned on

Read `docs/changelog/20260502.md` (runs 1 & 2) and all prior days before querying.

Prior context summary:
- **2026-04-25**: Development burst generated 35 error tracking issues (3 distinct types: `function_response.name empty`, `thought_signature missing`, `Too many AI spans`). All fixed within 24h.
- **2026-04-26 → 2026-05-02 run 2**: Zero new signals across 20+ consecutive clean runs.

Queried PostHog this run (time-bucketed error log distribution to confirm no post-April-25 tail):

| Source | Result |
|---|---|
| Error tracking issues (all, -90d) | 35, all `resolved`, all `first_seen: 2026-04-25` |
| Error/fatal log count buckets (-7d) | 37,484 on `2026-04-25T00:00→12:00`; 673,791 on `2026-04-25T12:00→2026-04-26T00:00`; **zero since** |

All 711K historical error logs are bounded to the April 25 burst. No errors on any day from April 26 through May 2.

### What I fixed and shipped

Nothing to fix. PostHog is fully clean. No PRs created.

### What was left behind

Same deferred items as all prior runs — none are actionable from PostHog signals (third run):
- **Production project** (id 398540) — `ingested_event: false`; no traffic to check.
- **PostHog PATCH assignee bug** — server-side; manual UI assignment only.
- **`syncCtx := context.Background()` in Plaid sync** — accepted low-priority, no PostHog signal.
- **`log.Printf` calls** — 8 remain in `internal/observability/` (intentional bootstrap logging, correct as-is).

---

## Session log — 2026-05-02 (fourth run)

### What I reasoned on

Read `docs/changelog/20260502.md` (runs 1–3) and all prior days before querying.

Prior context summary:
- **2026-04-25**: Development burst generated 35 error tracking issues (3 distinct types: `function_response.name empty`, `thought_signature missing`, `Too many AI spans`). All fixed within 24h.
- **2026-04-26 → 2026-05-02 run 3**: Zero new signals across 21+ consecutive clean runs.
- **Root causes already fixed**: OTEL `MaxExportBatchSize(50)` + `MaxQueueSize(75)` cap (PR #139); `ThinkingBudget=0` for Vertex AI tool turns; `allowToolCalls=false` guard for Google non-Vertex path; dead fallback returns removed (PR #142); HTTP 4xx at Info (PR #180).

Queried PostHog this run:

| Source | Result |
|---|---|
| Error tracking issues (all, -90d) | 35, all `resolved`, all `first_seen: 2026-04-25` |
| Error/fatal logs (-7d) | 100 entries — all `2026-04-25`, zero since |
| Error/fatal logs (-1d) | **0** |

All 3 distinct error types from the April 25 burst remain fixed and bounded. The 100-entry error log sample is the same historical tail seen in all prior runs.

### What I fixed and shipped

Nothing to fix. PostHog is fully clean. No PRs created.

### What was left behind

Same deferred items as all prior runs — none are actionable from PostHog signals (fourth run):
- **Production project** (id 398540) — `ingested_event: false`; no traffic to check.
- **PostHog PATCH assignee bug** — server-side; manual UI assignment only.
- **`syncCtx := context.Background()` in Plaid sync** — accepted low-priority, no PostHog signal.
- **`log.Printf` calls** — 8 remain in `internal/observability/` (intentional bootstrap logging, correct as-is).

---

## Session log — 2026-05-02 (fifth run)

### What I reasoned on

Read `docs/changelog/20260502.md` (runs 1–4) and all prior days before querying.

Prior context summary:
- **2026-04-25**: Development burst generated 35 error tracking issues (3 distinct types). All fixed and resolved within 24h.
- **2026-04-26 → 2026-05-02 run 4**: Zero new signals across 22+ consecutive clean runs.
- **PR #180** (HTTP 4xx at Info, not Warn) merged in May 1 run 2 — addressed the 1,244 historical HTTP 401 warn logs from April 25.

Queried PostHog this run:

| Source | Result |
|---|---|
| Error tracking issues (active, -30d) | **0** |
| Error tracking issues (all, -90d) | 35, all `resolved`, all `first_seen: 2026-04-25` |
| Error/fatal logs since 2026-05-02T00:00:00Z | **0** |
| Warn logs since 2026-05-02T00:00:00Z | **0** |
| Warn logs 2026-04-26 → 2026-05-02 | **0** |
| Warn logs 2026-04-25 (historical) | 1,244 — already fixed by PR #180 |

Note: the 1,097 warn count visible under a `-7d` window is a subset of the April 25 burst (the window opens mid-April-25). All historical; zero new signals.

### What I fixed and shipped

Nothing to fix. PostHog is fully clean. No PRs created.

### What was left behind

Same deferred items as all prior runs — none are actionable from PostHog signals (fifth run):
- **Production project** (id 398540) — `ingested_event: false`; no traffic to check.
- **PostHog PATCH assignee bug** — server-side; manual UI assignment only.
- **`syncCtx := context.Background()` in Plaid sync** — accepted low-priority, no PostHog signal.
- **`log.Printf` calls** — 8 remain in `internal/observability/` (intentional bootstrap logging, correct as-is).
