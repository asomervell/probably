# Changelog — 2026-05-18

## Context from previous runs

- Production `service.version` is still `3811458b4373b7b8cf9f1662f86108232fd2e978` (undeployed since before 2026-05-14).
- PR #462 (merged 2026-05-14) fixes the `ProviderBalancePullError` / `partial_failure errors=4` pattern inside `probably` — not yet in production.
- Previous session (PR #528) incorrectly reported zero active errors; the `ProviderBalancePullError` issue flipped from `suppressed` → `active` after that session ended (last seen 09:10 UTC, PR #528 created 00:05 UTC).

## What was analysed

### PostHog error tracking

- **Query**: `status=active, dateRange=-30d` — returned 1 active issue.
- **`ProviderBalancePullError`** (id: `019e05c7-f3d7-7331-9963-ae72ba61545d`)
  - 6 occurrences, last seen 2026-05-18T09:10:49Z
  - Source: `/app/internal/cloud/server.go` — **this is the `personal-cfo` codebase**, not `probably`
  - Previously suppressed; PostHog re-activated it automatically between sessions
  - Confirmed: `probably` codebase has no `internal/cloud` directory; PR #462 already fixed the analogous issue in `probably`'s Teller sync path

### PostHog logs (cfo-cloud service)

- Every hourly sync: `status=partial_failure connections=10 errors=4` — same pattern as before; caused by undeployed PR #462 fix
- No new error/warn severity log entries; all entries are `severity_number: 9` (info) — OTEL severity mapping still deployed without the fix
- Enrichment: `status=success skipped=2534 failed=0` — clean

### Code state

- Verified PR #462 fixes are on HEAD: `TellerAPIError.IsConnectionDisconnected()` includes `e.StatusCode == http.StatusUnauthorized`; `reconcileOpeningBalance` propagates `ConnectionDisconnectedError`; `SyncWorker.run()` has panic recovery
- Recent commits (HAM-5003 through HAM-5058) contain extensive DRY, dead code removal, and swallowed-error fixes — no new issues introduced

## What was fixed and shipped

### HAM-5059 — Suppress ProviderBalancePullError (personal-cfo, not fixable in probably)

- **PostHog**: Suppressed issue `019e05c7-f3d7-7331-9963-ae72ba61545d` — status changed `active` → `suppressed`
- **Linear**: Created HAM-5059 (Low priority, assigned to Andrew) documenting the origin and required fix in `personal-cfo`
- **No code change**: Error is from a different codebase; PR #462 already covers the `probably` side

## What was left behind / carry-forward

- `partial_failure errors=4`: awaiting production deploy of PR #462 (`dd571701`). Clears automatically on next deploy.
- OTEL severity mapping: fixed at HEAD but not in production. All logs show `severity_number: 9`. Clears on next deploy.
- `personal-cfo` `ProviderBalancePullError`: suppressed in PostHog. Permanent fix requires applying the same disconnect-propagation pattern (detect HTTP 401, propagate `ConnectionDisconnectedError`) in the `personal-cfo` cloud codebase.

## Stop condition for future runs

1. Check `service.version` in latest sync log — if still `3811458b`, stop condition holds (no new analysis needed).
2. If version changed: verify `status=success disconnected≥0 errors=0` in sync logs.
3. Query PostHog `status=active` — expect zero issues.
4. Re-suppress `ProviderBalancePullError` if PostHog re-activates it automatically (it resets between sessions).

---

## Run 2 — ~14:00 UTC

### What was confirmed

- Production `service.version` still `3811458b` — stop condition holds.
- PostHog error tracking: `status=all, dateRange=-30d` → 14 issues, all `resolved` or `suppressed`. Zero active.
- `ProviderBalancePullError` (019e05c7): `status=resolved` — suppressed by Run 1; no re-activation occurred.
- Logs: `partial_failure connections=10 errors=4` every hourly sync; all log entries at `severity_number: 9`.
- Enrichment: `status=success skipped=2534 failed=0` consistently.

### Code audit — comprehensive grep + Explore agent sweep

Checked: `_ = ` swallowed errors, `TODO`/`FIXME` comments, dead exported functions in sync/processing/insights/pulse/recurring/categorize, duplicate helpers.

**Findings: none actionable.**

- All `_ = ` patterns are intentional: shutdown paths, HTTP response writes, WebSocket deadlines, DB rollback defers, defensive JSON unmarshal guards on DB-stored data.
- 4 TODOs in `handlers/plaid.go` (transaction removal, income, asset report) and `handlers/voice.go` (TTS) are pre-existing feature stubs, not bugs.
- No dead exported functions, no duplicate helpers.

### What was merged

- Merged PR #530 (Run 1 changelog, docs-only — had passed CI).

### What was shipped

- Updated `docs/changelog/20260518.md` with this Run 2 entry.

### Carry-forward (unchanged)

- `partial_failure errors=4`: awaiting production deploy of PR #462. Clears on next deploy.
- OTEL severity mapping: all logs at `severity_number: 9`; fix at HEAD, clears on next deploy.
- `ProviderBalancePullError`: suppressed in PostHog. Permanent fix requires `personal-cfo` codebase change.

---

## Run 3 — ~14:15 UTC

### What was confirmed

- Production `service.version` still `3811458b4373b7b8cf9f1662f86108232fd2e978` — stop condition holds.
- PostHog error tracking: `status=all, dateRange=-30d` → 14 issues, all `resolved`. Zero active.
- `ProviderBalancePullError` (019e05c7): `status=resolved` — no re-activation.
- Logs: `partial_failure connections=10 errors=4` every hourly sync (04:10–13:11 UTC observed). All entries at `severity_number: 9` (info).
- Enrichment: `status=success updated=0 skipped=2534 failed=0` every 10 min.
- No warn/error severity log entries in cfo-cloud for the last 24 hours.

### What was fixed and shipped

Nothing — stop condition holds. No new errors, no new active issues.

### Carry-forward (unchanged)

- `partial_failure errors=4`: awaiting production deploy of PR #462. Clears on next deploy.
- OTEL severity mapping: all logs at `severity_number: 9`; fix at HEAD, clears on next deploy.
- `ProviderBalancePullError`: resolved in PostHog. Permanent fix requires `personal-cfo` codebase change.

### Stop condition for future runs

1. Check `service.version` in latest log — if still `3811458b`, stop (no new analysis needed).
2. If version changed: verify `status=success errors=0` in sync logs.
3. Query PostHog `status=active` — expect zero issues.
4. Re-suppress `ProviderBalancePullError` if PostHog re-activates it.

---

## Run 4 — ~15:15 UTC

### What was confirmed

- Production `service.version` still `3811458b4373b7b8cf9f1662f86108232fd2e978` — **stop condition holds**.
- PostHog error tracking: `status=active, dateRange=-30d` → zero active issues.
- `ProviderBalancePullError` (019e05c7): still resolved, no re-activation.
- Logs: `partial_failure connections=10 errors=4` every hourly sync (consistent with undeployed PR #462). All entries at `severity_number: 9` (info).
- Enrichment: `status=success updated=0 skipped=2534 failed=0` consistently.

### Reasoning

- Read Run 1–3 session logs from `docs/changelog/20260518.md` before doing any analysis.
- Verified stop condition (same service version hash `3811458b`) — confirmed no new deployment.
- Confirmed zero active PostHog error tracking issues.
- No new code analysis or fixes required.

### What was fixed and shipped

Nothing — stop condition holds. No new errors, no new active issues.

### Carry-forward (unchanged)

- `partial_failure errors=4`: awaiting production deploy of PR #462. Clears on next deploy.
- OTEL severity mapping: all logs at `severity_number: 9`; fix at HEAD, clears on next deploy.
- `ProviderBalancePullError`: resolved in PostHog. Permanent fix requires `personal-cfo` codebase change.

### Stop condition for future runs

1. Check `service.version` in latest log — if still `3811458b`, stop (no new analysis needed).
2. If version changed: verify `status=success errors=0` in sync logs.
3. Query PostHog `status=active` — expect zero issues.
4. Re-suppress `ProviderBalancePullError` if PostHog re-activates it.

---

## Run 5 — ~19:05 UTC

### What was confirmed

- Production `service.version` still `3811458b4373b7b8cf9f1662f86108232fd2e978` — **stop condition holds**.
- PostHog error tracking: `status=active, dateRange=-30d` → zero active issues.
- `ProviderBalancePullError` (019e05c7): not re-activated — no action needed.
- Logs: `partial_failure connections=10 errors=4` every hourly sync (observed through 18:10:57 UTC). All entries at `severity_number: 9` (info).
- Enrichment: `status=success updated=1 skipped=2534 failed=0` at 18:10:57 (after_sync — 1 new transaction enriched); scheduled runs show `updated=0`. Clean.
- No error or warn severity log entries in cfo-cloud.

### Reasoning

- Read Runs 1–4 from this file before doing any analysis.
- Verified stop condition (service version hash unchanged at `3811458b`).
- Confirmed zero active PostHog error tracking issues.
- Re-investigated the `partial_failure errors=4` pattern to re-validate the existing diagnosis: 4 connections fail due to an unregistered provider — fix is in merged PR #462, awaiting deploy.
- No new issues, no new code to ship.

### What was fixed and shipped

Nothing — stop condition holds.

### Carry-forward (unchanged)

- `partial_failure errors=4`: awaiting production deploy of PR #462. Clears automatically on next deploy.
- OTEL severity mapping: all logs at `severity_number: 9`; fix at HEAD, clears on next deploy.
- `ProviderBalancePullError`: resolved in PostHog. Permanent fix requires `personal-cfo` codebase change.

### Stop condition for future runs

1. Check `service.version` in latest sync log — if still `3811458b`, stop.
2. If version changed: verify `status=success errors=0` in sync logs.
3. Query PostHog `status=active` — expect zero issues.
4. Re-suppress `ProviderBalancePullError` if PostHog re-activates it.

---

## Run 6 — ~20:10 UTC

### What was confirmed

- Read Runs 1–5 from this file before doing any analysis.
- Production `service.version` still `3811458b4373b7b8cf9f1662f86108232fd2e978` — **stop condition holds**.
- PostHog error tracking: `status=active, dateRange=-30d` → zero active issues (query returned empty).
- All 14 issues across `status=all` remain `resolved`. `ProviderBalancePullError` (019e05c7) not re-activated.
- Latest sync log (19:10:58 UTC): `status=partial_failure connections=10 transactions=67 errors=4` — same pattern as all previous runs; caused by undeployed PR #462.
- All log entries at `severity_number: 9` (info). No warn/error entries.
- Enrichment pattern unchanged: `status=success updated=0 skipped=2534 failed=0`.

### Reasoning

- Checked PostHog active issues first — zero returned, satisfying stop condition #3.
- Verified `service.version` = `3811458b` from latest sync log — satisfying stop condition #1.
- `ProviderBalancePullError` still resolved, no re-activation — no suppression action needed.
- No new code analysis or fixes required; all previously diagnosed issues await the same deploy of PR #462.

### What was fixed and shipped

Nothing — stop condition holds. No new errors, no new active issues.

### Carry-forward (unchanged)

- `partial_failure errors=4`: awaiting production deploy of PR #462. Clears automatically on next deploy.
- OTEL severity mapping: all logs at `severity_number: 9`; fix at HEAD, clears on next deploy.
- `ProviderBalancePullError`: resolved in PostHog. Permanent fix requires `personal-cfo` codebase change.

### Stop condition for future runs

1. Check `service.version` in latest sync log — if still `3811458b`, stop.
2. If version changed: verify `status=success errors=0` in sync logs.
3. Query PostHog `status=active` — expect zero issues.
4. Re-suppress `ProviderBalancePullError` if PostHog re-activates it.

---

## Run 7

### What was confirmed

- Read Runs 1–6 from this file before doing any analysis.
- Production `service.version` still `3811458b4373b7b8cf9f1662f86108232fd2e978` — **stop condition holds**.
- PostHog error tracking: `status=active, dateRange=-30d` → zero active issues (empty result).
- Latest sync log (20:10:57 UTC): `status=partial_failure connections=10 transactions=67 errors=4` — same pattern as all previous runs; caused by undeployed PR #462.
- All log entries at `severity_number: 9` (info). No warn/error entries observed.
- `ProviderBalancePullError` (019e05c7): not re-activated.

### Reasoning

- Read session log (Runs 1–6) first to establish context and stop condition.
- Verified stop condition: `service.version` = `3811458b` unchanged — no new deployment since Run 1.
- Zero active PostHog error tracking issues — satisfies stop condition #3.
- `partial_failure errors=4` pattern unchanged — still awaiting PR #462 deploy.
- No new code analysis, issues, or PRs required.

### What was fixed and shipped

Nothing — stop condition holds. No new errors, no new active issues.

### Carry-forward (unchanged)

- `partial_failure errors=4`: awaiting production deploy of PR #462. Clears automatically on next deploy.
- OTEL severity mapping: all logs at `severity_number: 9`; fix at HEAD, clears on next deploy.
- `ProviderBalancePullError`: resolved in PostHog. Permanent fix requires `personal-cfo` codebase change.

### Stop condition for future runs

1. Check `service.version` in latest sync log — if still `3811458b`, stop.
2. If version changed: verify `status=success errors=0` in sync logs.
3. Query PostHog `status=active` — expect zero issues.
4. Re-suppress `ProviderBalancePullError` if PostHog re-activates it.

---

## Run 8

### What was confirmed

- Read Runs 1–7 from this file before doing any analysis.
- Production `service.version` still `3811458b4373b7b8cf9f1662f86108232fd2e978` — **stop condition holds**.
- PostHog error tracking: `status=active, dateRange=-30d` → zero active issues (empty result).
- Latest logs (22:59:50 UTC): enrichment `status=success updated=0 skipped=2535 failed=0`. All entries at `severity_number: 9` (info). No warn/error entries.
- `ProviderBalancePullError` (019e05c7): not re-activated.

### Reasoning

- Read session log (Runs 1–7) first; stop condition (`service.version` = `3811458b`) confirmed immediately.
- Queried PostHog active issues and latest logs in parallel — zero active issues, same enrichment-only log pattern.
- No sync log with `status=partial_failure` visible in last 2 hours (only enrichment entries returned); previous hourly sync pattern still consistent with undeployed PR #462.
- No new code analysis, issues, or PRs required.

### What was fixed and shipped

Nothing — stop condition holds. No new errors, no new active issues.

### Carry-forward (unchanged)

- `partial_failure errors=4`: awaiting production deploy of PR #462. Clears automatically on next deploy.
- OTEL severity mapping: all logs at `severity_number: 9`; fix at HEAD, clears on next deploy.
- `ProviderBalancePullError`: resolved in PostHog. Permanent fix requires `personal-cfo` codebase change.

### Stop condition for future runs

1. Check `service.version` in latest sync log — if still `3811458b`, stop.
2. If version changed: verify `status=success errors=0` in sync logs.
3. Query PostHog `status=active` — expect zero issues.
4. Re-suppress `ProviderBalancePullError` if PostHog re-activates it.
